---
phase: 06-material-3-ux-remediation
plan: 05
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - src/ui/components/PlanEditorFields.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - src/ui/__tests__/OwnedPlanEditor.test.tsx
autonomous: true
requirements: [UX-02, UX-04]
estimate:
  tokens: 43000
  raw_tokens: 43000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Per D-76, long press on a 48dp handle visibly displaces day and exercise rows before drop, while buttons/accessibility actions remain equivalent.
    - Reorder changes only the existing editor draft; Save Plan Changes remains the only persistence command.
    - Per D-72, the exercise picker uses shared Search and its zero/one/many result cardinals are explicit.
  artifacts:
    - path: src/ui/components/PlanEditorFields.tsx
      provides: continuous accessible reorder row
    - path: src/ui/screens/OwnedPlanEditorScreen.tsx
      provides: draft-only reorder and shared picker Search
  key_links:
    - from: PlanEditorFields
      to: OwnedPlanEditorScreen move callbacks
      via: drag, buttons, and accessibility actions share bounded draft movement
    - from: OwnedPlanEditorScreen
      to: savePlan
      via: explicit Save Plan Changes only
---

<objective>
Replace threshold-based one-step reorder with continuous accessible draft movement on the native gesture seam already proven in Wave 0.

Purpose: make direct manipulation physically understandable without weakening explicit save or non-gesture access.
Output: continuous reorder rows, shared picker Search, and adaptive regressions.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-UI-SPEC.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@.planning/phases/06-material-3-ux-remediation/06-01-SUMMARY.md
@.planning/phases/06-material-3-ux-remediation/06-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Prove continuous reorder through the existing draft operation</name>
  <precondition>06-02-SUMMARY records a passing generated-native emulator gesture smoke with exact package/APK/manifest/report identity; halt if the acceptance gate is absent or red.</precondition>
  <files>src/ui/components/PlanEditorFields.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</files>
  <behavior>
    - Long press activates drag; a held row follows movement and neighbors displace before release; drop invokes the bounded existing draft move.
    - Move buttons and adjustable increment/decrement produce equal order and factual feedback.
    - No gesture or fallback calls savePlan, cancellation preserves order, and reduced motion keeps acknowledgement without positional animation.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Reorder component, editor screen, and tests, specifically PlanEditorFields.tsx:116-224 and OwnedPlanEditorScreen.tsx:850-880 and 1124-1202. Per D-76, replace only release-threshold PanResponder behavior with the Gesture Handler/Reanimated stack accepted by 06-02; preserve canMoveUp/down, 48dp handle, adjustable actions, explicit buttons, bounds, renumbering, impact clearing, and factual feedback. Route drop through the same draft move and reserve persistence for Save Plan Changes. Per D-80/D-81, component-test displacement model, cancellation, fallback parity, keyboard/D-pad, reduced motion, rapid input, and large text; 06-09 owns exact-candidate native continuous-displacement and Samsung backstop. Do not use gesture-only control, persist on drag, install a package, or alter plan/schedule facts.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <done>Day and exercise rows move continuously through one draft operation with accessible equivalence and no pre-save persistence.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver picker Search cardinalities and adaptive reorder hierarchy</name>
  <files>src/ui/components/PlanEditorFields.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</files>
  <behavior>
    - The picker exposes shared M3SearchField zero, one, and many result states, plus clear, IME, busy/error, and focus behavior.
    - Normal scale keeps handle, labels, and fallbacks in one row; at 200% text every control remains reachable and 48dp with no horizontal clipping.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Reorder component, editor screen, and tests plus §M3SearchField, particularly OwnedPlanEditor.test.tsx:551-602 and 744-779 and the Library/Progress input analogs. Per D-72, D-76, D-80, D-81, and UI-SPEC zero-one-many, use 06-01 M3SearchField in the picker while preserving draft selection, factual counts, add-to-plan behavior, focus restore, and UI-SPEC typography/spacing. Assert zero, one, and many result component states; 06-09's native picker path is the evidence backstop. Do not create a second Search, truncate labels, hide fallbacks, or alter explicit-save semantics.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>The picker shares Search cardinality behavior and reorder rows remain complete at every required width and text scale.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Input to editor draft | Touch/keyboard/a11y can reorder uncommitted user-owned plan data. |
| Editor draft to save command | Only explicit Save Plan Changes may persist order. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Reorder draft/persistence | high | mitigate | Route all modes through bounded draft move and reserve persistence for explicit save. |
| T-06-02 | Information Disclosure | Test evidence | medium | mitigate | Use fixture labels and bounded assertions only. |
</threat_model>

<verification>Run OwnedPlanEditor components, typecheck, lint, boundaries, coverage, and git diff --check; confirm the 06-02 gate before starting. Per D-82, do not approve, promote, publish, tag, or run Terminal Seal.</verification>

<success_criteria>UX-04 is continuous and accessibly equivalent, while UX-02 covers the picker without bypassing explicit save.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-05-SUMMARY.md when done.</output>
