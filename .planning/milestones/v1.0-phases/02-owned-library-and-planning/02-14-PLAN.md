---
phase: 02-owned-library-and-planning
plan: 14
type: execute
wave: 9
depends_on: [02-08, 02-12]
files_modified:
  - app/library/starter/[templateId].tsx
  - app/library/starter/[templateId]/activate.tsx
  - maestro/phase2/starter-activation.yaml
  - src/bootstrap/starterPlanRuntime.tsx
  - src/ui/__tests__/StarterPlans.test.tsx
  - src/ui/components/PlanRow.tsx
  - src/ui/screens/StarterActivationScreen.tsx
  - src/ui/screens/StarterPlanDetailScreen.tsx
  - src/ui/screens/TemplateUpdateScreen.tsx
autonomous: true
requirements: [LIB-06, LIB-07]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-18/D-19 starter discovery is deterministic by Goal, Experience, Days per week, Equipment, stable template order, exact `Why this fits`, and one `Clear filters`."
    - "D-20 detail/activation preview shows goal, experience, equipment, duration, all days/exercises, metric types, schedule suggestion, progression summary, and source notes."
    - "D-55 renders `Gym Body-Part Split` as a six-template option with Monday–Friday Chest / Back / Shoulders / Legs / Arms detail, four weighted equipment-first occurrences per day, and no bodyweight occurrence or substitution."
    - "D-01, D-02, D-03, D-04, D-05, and D-40 are visible and wired: explicit copy choice, editable today-default start date/mode/bindings, active-workout block, preserved prior plan/schedule, and commit only after confirmation."
    - "D-54 shows exact `Template update available`, a full diff, and `Create new copy`; existing copies never mutate."
    - "PlanRow is created before any later task reads it, reusing existing PlanActivationRow and ConfirmationSheet exports from src/ui/components/index.ts."
    - "All eight UI truths are covered for starter discovery/detail/activation/update: Empty, Loading, Error, Populated, Partial, Overflow, Zero / one / many, and Long text."
  prohibitions:
    - "Catalog, starter, source, metric, and ownership facts must not be represented as reviewed, approved, personalized, or authoritative when they were inferred, generated, unresolved, or not explicitly accepted."
  artifacts:
    - path: "src/ui/components/PlanRow.tsx"
      provides: "Active/owned/starter plan row with exact lifecycle and recommendation copy"
    - path: "src/ui/screens/StarterActivationScreen.tsx"
      provides: "Complete explicit copy/schedule activation preview"
    - path: "maestro/phase2/starter-activation.yaml"
      provides: "Installed starter discovery, detail, copy choice, activation, block, and template-update journey"
  key_links:
    - from: "src/ui/screens/StarterActivationScreen.tsx"
      to: "src/bootstrap/starterPlanRuntime.tsx"
      via: "accepted preview invokes activateStarterPlan only after confirmation"
      pattern: "activateStarterPlan"
    - from: "app/library/starter/[templateId].tsx"
      to: "src/ui/screens/StarterPlanDetailScreen.tsx"
      via: "thin route adapter"
      pattern: "StarterPlanDetailScreen"
---

<objective>
Deliver accepted starter discovery, complete detail, safe activation, and template-update comparison.

Purpose: Make LIB-06/LIB-07 usable without implicit ranking, copy choice, schedule mutation, or template merging.
Output: PlanRow, starter routes/screens/runtime/tests, and the public installed activation journey.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-08-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-12-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/index.ts
@src/ui/screens/LibraryScreen.tsx
@src/bootstrap/workoutAppRuntime.tsx
</context>

## Artifacts this phase produces

- `src/ui/components/PlanRow.tsx` — `PlanRow`.
- `src/ui/screens/StarterPlanDetailScreen.tsx` — discovery/detail/read-only source notes.
- `app/library/starter/[templateId].tsx` — thin detail route.
- `src/ui/screens/StarterActivationScreen.tsx` — complete preview, copy choice, schedule edit, workout block.
- `src/ui/screens/TemplateUpdateScreen.tsx` — D-54 diff and independent-copy action.
- `src/bootstrap/starterPlanRuntime.tsx` — starter discovery/detail/activation/update runtime adapter over the base app runtime.
- `app/library/starter/[templateId]/activate.tsx` — thin activation/update route.
- `src/ui/__tests__/StarterPlans.test.tsx` — exact copy, eight UI truths, adaptive/focus behavior.
- `maestro/phase2/starter-activation.yaml` — installed owner journey.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Discover one starter, read its full detail, and preserve Library state</name>
  <read_first>src/ui/components/index.ts, src/ui/screens/LibraryScreen.tsx, src/bootstrap/workoutAppRuntime.tsx, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/ui/components/PlanRow.tsx, src/ui/screens/StarterPlanDetailScreen.tsx, app/library/starter/[templateId].tsx, src/ui/__tests__/StarterPlans.test.tsx, src/bootstrap/starterPlanRuntime.tsx</files>
  <action>Start red with one starter displayed through a newly created `PlanRow`, selected from Library, opened through a thin route, and returned to the exact list query/filter/scroll state. Create PlanRow by extending the existing `PlanActivationRow` vocabulary; import shared ConfirmationSheet only from `src/ui/components/index.ts`. Implement D-18 deterministic fit ordering and `Why this fits`, D-19 combinable filters and `Clear filters`, D-20 complete detail with source notes, and D-55 six-template coverage. `Gym Body-Part Split` detail must show Monday–Friday Chest / Back / Shoulders / Legs / Arms, four weighted equipment-first occurrences per day, and no bodyweight occurrence or substitution. Wire accepted starter read models through runtime. Cover all eight UI truths, compact push vs medium/expanded pane semantics, 200% text, 48dp actions, keyboard/D-pad/focus, reduced motion, non-color statuses, and long source notes.</action>
  <acceptance_criteria>All six accepted starters are discoverable and fully inspectable with stable fit order and exact source facts; the D-55 split exposes the exact day/equipment contract and Back restores the exact Library state.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/StarterPlans.test.tsx -t "discovery|detail"</automated>
  </verify>
  <done>The narrowest accepted-starter browse path works and PlanRow exists for all later in-plan reads.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete activation, existing-copy choice, switching block, template update, and Maestro</name>
  <read_first>src/ui/components/PlanRow.tsx, src/ui/screens/StarterPlanDetailScreen.tsx, src/bootstrap/workoutAppRuntime.tsx, src/ui/components/index.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/ui/screens/StarterActivationScreen.tsx, src/ui/screens/TemplateUpdateScreen.tsx, app/library/starter/[templateId]/activate.tsx, src/ui/__tests__/StarterPlans.test.tsx, maestro/phase2/starter-activation.yaml</files>
  <action>Write UI tests before screens. Render the full D-20 preview, start date defaulting to today, preselected suggested Weekday/Rotation and editable bindings per D-05/D-40, and primary `Activate plan`. If copies exist, require exact `Reactivate existing copy` or `Create another copy`; show active/inactive and schedule facts. If a workout is active, show `Finish the current workout before switching plans.` with exact `Resume`, `Finish partial`, `Discard` and no activation control. After committed activation show one active plan while prior plan/schedule remains preserved/inactive. Add D-54 `Template update available` full graph diff and sole adoption `Create new copy`. Author public Maestro assertions for filtering, detail, copy choice, activation, active-workout block, and independent update copy.</action>
  <acceptance_criteria>No activation occurs without explicit confirmation/copy choice; active workouts block switching; template updates never mutate existing copies; installed flow uses only semantic UI controls.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/StarterPlans.test.tsx -t "activation|template update"</automated>
  </verify>
  <done>Starter activation and update UI are complete and publicly testable on the final APK.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted starter read model → UI | UI must not invent or omit source/metric/schedule facts. |
| Owner confirmation → activation command | Copy and schedule choice must be explicit and current. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | starter detail/update | high | mitigate | Consume accepted revision and exact diff only. |
| T-02-03 | Tampering | template/copy authority | high | mitigate | Read-only templates and independent-copy command. |
| T-02-04 | Tampering | activation preview revision | high | mitigate | Confirmation token and active-workout/current-copy revalidation. |
| T-02-05 | Tampering | schedule choice | high | mitigate | Explicit mode/bindings/start date and no silent switching. |
| T-02-07 | Information Disclosure | activation errors | medium | mitigate | Safe codes and factual source notes; no observations. |
</threat_model>

<verification>
Run focused component tests; final exact-APK Maestro execution is owned by Plan 02-21.
</verification>

<success_criteria>
The owner can inspect and activate any accepted starter as an explicit independent copy with safe schedule choice and update comparison.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-14-SUMMARY.md` when done.
</output>
