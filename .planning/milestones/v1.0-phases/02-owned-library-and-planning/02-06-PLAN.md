---
phase: 02-owned-library-and-planning
plan: 06
type: execute
wave: 3
depends_on: [02-04]
files_modified:
  - app/__native-contracts.tsx
  - artifacts/native/phase2-fts/build.json
  - artifacts/native/phase2-fts/gym-tracker-phase2-fts-devtest.apk
  - artifacts/native/phase2-fts/result.json
  - artifacts/native/phase2-fts/zipalign.txt
  - scripts/run-native-sqlite-contracts.mjs
  - src/platform/sqlite/migrations/0005_exercise_search_fts.ts
  - src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts
  - src/testing/contracts/phase2Fts.contract.test.ts
  - src/testing/contracts/phase2Fts.contract.ts
  - tests/migrations/fixtures/v4-content-library.sql
  - tests/sqlite-host/exercise-search-fts.test.ts
autonomous: true
requirements: [LIB-04]
estimate:
  tokens: 35000
  raw_tokens: 35000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "The actual packaged Expo SQLite adapter proves ENABLE_FTS5, trigram creation/query, one/two/three-code-point behavior, punctuation safety, trigger rollback, parity, integrity-check, and rebuild before feature search can close."
    - "Migration 0005 creates an external-content FTS derivative and same-transaction synchronization over relational source search terms; source rows remain authoritative and rebuildable."
    - "E-20 through E-27 are represented in host/native contracts, including page/query bounds, Unicode/FTS encoding, stable ordering inputs, precision-independent search, idempotent rebuild, and interrupted transaction rollback."
  artifacts:
    - path: "src/platform/sqlite/migrations/0005_exercise_search_fts.ts"
      provides: "External-content trigram FTS schema and synchronization triggers"
    - path: "src/testing/contracts/phase2Fts.contract.ts"
      provides: "Shared host/actual-Expo packaged prerequisite contract"
    - path: "src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts"
      provides: "Parity, integrity-check, and deterministic rebuild commands"
  key_links:
    - from: "src/platform/sqlite/migrations/0005_exercise_search_fts.ts"
      to: "exercise_search_terms"
      via: "external-content triggers mirror authoritative normalized source terms"
      pattern: "CREATE VIRTUAL TABLE|CREATE TRIGGER"
    - from: "src/testing/contracts/phase2Fts.contract.ts"
      to: "app/__native-contracts.tsx"
      via: "development-test route exports and runs exact FTS case IDs"
      pattern: "PHASE2_FTS_CASE_IDS"
---

<objective>
Prove packaged FTS5 trigram capability and install the rebuildable source-synchronized index.

Purpose: Open LIB-04 only after actual Expo SQLite, not host assumptions, proves the required search primitive.
Output: Migration 0005, parity/rebuild repository, retained v4 fixture, shared host/native contract, and runnable packaged prerequisite.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-04-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/platform/sqlite/migrations/index.ts
@src/platform/sqlite/serializedWriter.ts
@src/testing/contracts/sqliteKernel.contract.ts
@app/__native-contracts.tsx
@scripts/run-native-sqlite-contracts.mjs
</context>

## Artifacts this phase produces

- `src/platform/sqlite/migrations/0005_exercise_search_fts.ts` — external-content trigram FTS and triggers.
- `tests/migrations/fixtures/v4-content-library.sql` — immutable post-0004 fixture.
- `src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts` — `verifyParity` and `rebuildSearchIndex`.
- `src/testing/contracts/phase2Fts.contract.ts` — packaged FTS case IDs and adapter-neutral runner.
- `src/testing/contracts/phase2Fts.contract.test.ts` — Jest-discovered host wrapper for the shared FTS contract.
- `tests/sqlite-host/exercise-search-fts.test.ts` — migration/trigger/parity/rebuild host proof.
- `artifacts/native/phase2-fts/build.json` — exact prerequisite build manifest.
- `artifacts/native/phase2-fts/gym-tracker-phase2-fts-devtest.apk` — retained prerequisite APK.
- `artifacts/native/phase2-fts/zipalign.txt` — prerequisite 16 KiB alignment proof.
- `artifacts/native/phase2-fts/result.json` — all-pass packaged FTS result.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create/query one trigram term and roll it back with its source row</name>
  <read_first>src/platform/sqlite/migrations/0004_content_library.ts, src/platform/sqlite/migrations/index.ts, src/platform/sqlite/serializedWriter.ts, src/testing/contracts/sqliteKernel.contract.ts, .planning/phases/02-owned-library-and-planning/02-RESEARCH.md</read_first>
  <files>src/platform/sqlite/migrations/0005_exercise_search_fts.ts, tests/migrations/fixtures/v4-content-library.sql, tests/sqlite-host/exercise-search-fts.test.ts</files>
  <action>Start with a host migration test that creates one normalized source term, observes the external-content trigram candidate, updates/deletes it through triggers, and proves a failed source transaction leaves source and FTS unchanged. Export forward migration 0005 for direct host/native contract setup without editing historical migrations; Plan 02-11 owns the final ordered runtime manifest after every Phase 2 migration module exists. Use the researched trigram tokenizer and authoritative `exercise_search_terms`; include deterministic initial rebuild and integrity check. Cover E-20..E-27 host cases for empty catalog, 1/2/3 code points, punctuation/operator words/quotes/diacritics, boundary lengths/page sizes, stable IDs, idempotent rebuild, and interruption.</action>
  <acceptance_criteria>Migration from retained v4 creates a queryable external-content FTS table; trigger updates are transactionally aligned with source rows; rollback and rebuild preserve exact parity.</acceptance_criteria>
  <verify>
    <automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/exercise-search-fts.test.ts</automated>
  </verify>
  <done>The narrowest source-term-to-trigram path works and FTS remains a disposable derivative.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the mandatory packaged Expo SQLite FTS prerequisite</name>
  <read_first>src/platform/sqlite/migrations/0005_exercise_search_fts.ts, tests/sqlite-host/exercise-search-fts.test.ts, src/testing/contracts/sqliteKernel.contract.ts, app/__native-contracts.tsx, scripts/run-native-sqlite-contracts.mjs</read_first>
  <files>src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts, src/testing/contracts/phase2Fts.contract.ts, src/testing/contracts/phase2Fts.contract.test.ts, app/__native-contracts.tsx, scripts/run-native-sqlite-contracts.mjs</files>
  <action>Extract shared FTS cases that assert SQLite version/options, `ENABLE_FTS5`, trigram creation, substring behavior, bounded 1/2-character relational prerequisite result, punctuation-safe bound MATCH data, insert/update/delete rollback, stable-ID parity, `integrity-check`, and `rebuild`. Implement parity/rebuild through the private writer with no source mutation. Extend the existing native route and runner with suite `phase2-fts`, deriving expected count from exported case IDs while preserving Phase 1 suites. This task prepares the runnable packaged contract; Task 3 owns the generated APK/evidence artifacts and executes it.</action>
  <acceptance_criteria>The native route and runner expose every exported FTS case, derive the exact expected total, preserve prior suites, and reject skipped/failed or malformed result payloads.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/testing/contracts/phase2Fts.contract.test.ts</automated>
  </verify>
  <done>The packaged FTS prerequisite runner is complete and ready for exact-APK execution.</done>
</task>

<task type="auto">
  <name>Task 3: Build and execute the exact packaged FTS prerequisite</name>
  <read_first>src/testing/contracts/phase2Fts.contract.ts, app/__native-contracts.tsx, scripts/run-native-sqlite-contracts.mjs, scripts/build-current-native-test-apk.sh, scripts/verify-native-evidence.mjs</read_first>
  <files>artifacts/native/phase2-fts/build.json, artifacts/native/phase2-fts/gym-tracker-phase2-fts-devtest.apk, artifacts/native/phase2-fts/zipalign.txt, artifacts/native/phase2-fts/result.json</files>
  <action>Build one clean development-test APK with suite `phase2-fts`, retain the generated build manifest/APK/alignment log, install the exact retained bytes, run every exported FTS contract, and retain `result.json`. Fail closed unless HEAD/source digest/APK/package/device/installed bytes match and every case passes with zero skipped/failed results. Do not reuse Phase 1 evidence or host proof.</action>
  <acceptance_criteria>The retained prerequisite APK is 16 KiB aligned and byte-identical to the installed package, and `result.json` reports all exported FTS cases passed.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'),c=JSON.parse(fs.readFileSync('artifacts/native/phase2-fts/result.json','utf8')); if(c.status!=='passed'||c.contract?.failed!==0||c.contract?.skipped!==0) process.exit(1)"</automated>
    <automated-native>npm run android:devtest:fresh -- --suite phase2-fts &amp;&amp; npm run test:sqlite:device -- --suite phase2-fts --manifest artifacts/native/phase2-fts/build.json</automated-native>
  </verify>
  <done>The actual packaged Expo SQLite prerequisite is proven and Plan 02-09 may rely on trigram FTS behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Relational source terms → FTS derivative | Index drift must never alter or outrank source truth. |
| Host assumption → packaged Expo SQLite | Native compile options and tokenizer behavior require actual APK proof. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02 | Tampering / Denial | FTS grammar/index | high | mitigate | Fixed bound grammar contract, query bounds, punctuation tables, and packaged proof. |
| T-02-04 | Tampering | migration/rebuild revision | medium | mitigate | Forward-only migration, private writer, and all-or-nothing rebuild. |
| T-02-06 | Tampering | FTS parity | high | mitigate | Same-transaction triggers, stable-ID parity, integrity-check, and deterministic rebuild. |
| T-02-07 | Information Disclosure | native results | medium | mitigate | Emit case IDs/counts/codes only, never raw search text. |
</threat_model>

<verification>
Run focused host/unit samples during implementation and the exact installed native prerequisite before completion.
</verification>

<success_criteria>
FTS5 trigram is proven on the packaged adapter and remains synchronized, verifiable, rebuildable, and non-authoritative.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-06-SUMMARY.md` when done.
</output>
