---
phase: 02-owned-library-and-planning
plan: 30
type: execute
wave: 13
depends_on: [02-21]
files_modified:
  - app.config.ts
  - src/domains/rest/restNotificationPort.ts
  - src/domains/rest/index.ts
  - src/platform/preferences/restAlertPreferenceStore.ts
  - src/platform/preferences/restAlertPreferenceStore.test.ts
  - src/platform/notifications/expoRestNotificationAdapter.ts
  - src/platform/notifications/expoRestNotificationAdapter.test.ts
  - src/platform/notifications/restNotificationReconciler.ts
  - src/platform/notifications/restNotificationReconciler.test.ts
autonomous: true
gap_closure: true
requirements: [LIB-07, LIB-08]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: high }
must_haves:
  truths:
    - "The platform half of D-61/G-02-07 closes with a new versioned Android channel because sound/importance behavior on an existing channel ID is immutable."
    - "Rest alerts default to sound enabled and vibration enabled, and each preference is persisted independently with strict versioned parsing and safe defaults."
    - "Scheduled notification content/channel behavior honors the current preferences; sound-off and vibration-off are distinguishable without creating ambiguous channel mutation."
    - "Permission denial, platform failure, malformed preference data, and notification scheduling failure never mutate or overrule SQLite-authoritative workout/rest state."
    - "No package is installed; existing `expo-notifications`, `expo-sqlite/kv-store`, and `expo-haptics` capabilities are sufficient."
  artifacts:
    - { path: "src/platform/preferences/restAlertPreferenceStore.ts", provides: "Versioned persisted independent sound/vibration preferences" }
    - { path: "src/platform/notifications/expoRestNotificationAdapter.ts", provides: "Versioned Android rest channels and preference-aware scheduling" }
    - { path: "src/platform/notifications/restNotificationReconciler.ts", provides: "Preference-aware, non-authoritative scheduling reconciliation" }
  key_links:
    - { from: "src/platform/preferences/restAlertPreferenceStore.ts", to: "src/platform/notifications/restNotificationReconciler.ts", via: "validated defaults and independent sound/vibration booleans", pattern: "soundEnabled|vibrationEnabled" }
    - { from: "src/platform/notifications/restNotificationReconciler.ts", to: "src/platform/notifications/expoRestNotificationAdapter.ts", via: "preference-derived versioned channel and notification content", pattern: "channel|sound|vibrat" }
---

<objective>
Create the reliable, configurable Android rest-alert platform contract.

Purpose: Fix silent/weak notifications while preserving local workout truth and user control.
Output: Versioned preferences, versioned channel scheduling, reconciliation behavior, and focused tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@app.config.ts
@src/domains/rest/restNotificationPort.ts
@src/platform/preferences/appearancePreferenceStore.ts
@src/platform/notifications/expoRestNotificationAdapter.ts
@src/platform/notifications/restNotificationReconciler.ts
</context>

## Artifacts this phase produces

- A validated v1 rest-alert preference store with independent sound/vibration toggles.
- New immutable Android channel IDs/configurations that can represent enabled combinations safely.
- Preference-aware notification scheduling and recovery tests.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-07, LIB-08 | Reliable rest alerts while running owned/starter plans | COVERED |
| CONTEXT | Platform half D-61 | Versioned channel, defaults, persistence, non-authority | COVERED |
| VERIFICATION | Platform half G-02-07 | LOW/sound-false/no-preference root causes | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add strict persisted rest-alert preferences</name>
  <read_first>src/platform/preferences/appearancePreferenceStore.ts, src/platform/preferences/appearancePreferenceStore.test.ts, src/domains/rest/restNotificationPort.ts, src/domains/rest/index.ts</read_first>
  <files>src/platform/preferences/restAlertPreferenceStore.ts, src/platform/preferences/restAlertPreferenceStore.test.ts, src/domains/rest/restNotificationPort.ts, src/domains/rest/index.ts</files>
  <action>Start with failing tests. Add a versioned preference value with independent `soundEnabled` and `vibrationEnabled` booleans, both defaulting true. Use a distinct SQLite KV key, strict parsing, immutable returned values, and safe fallback to defaults for missing/malformed/unreadable storage. Expose only the narrow types/port needed by notification/runtime consumers. Do not store workout truth or permission state here. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Round-trip, independent toggles, malformed/version-mismatch fallback, storage failure, and default-on behavior are covered.</acceptance_criteria>
  <verify><automated>npm run test:unit -- --runInBand src/platform/preferences/restAlertPreferenceStore.test.ts</automated></verify>
  <done>A bounded, versioned preference contract exists before adapter changes.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Introduce immutable versioned Android rest-alert channels</name>
  <read_first>app.config.ts, src/domains/rest/restNotificationPort.ts, src/platform/notifications/expoRestNotificationAdapter.ts, src/platform/notifications/expoRestNotificationAdapter.test.ts, node_modules/expo-notifications/build/NotificationChannelManager.types.d.ts</read_first>
  <files>app.config.ts, src/domains/rest/restNotificationPort.ts, src/platform/notifications/expoRestNotificationAdapter.ts, src/platform/notifications/expoRestNotificationAdapter.test.ts</files>
  <action>Create new versioned channel identity/configuration rather than mutating `workout-rest-v1`. Use appropriate Android importance, default short system sound when enabled, and a short vibration pattern when enabled; represent independent preference combinations through deterministic channel selection/content options so Android's immutable channel settings cannot retain stale behavior. Configure scheduled content consistently for pre/post Android 8 behavior. Preserve stable `rest:<sessionId>` notification request identity, package permissions, open-settings behavior, and privacy-bounded payload. Add adapter tests for all four preference combinations, channel creation, scheduling, and platform errors. Do not add dependencies. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Default scheduling is audible and vibrates, each toggle can disable only its own modality, and existing v1 channel state cannot suppress the new behavior.</acceptance_criteria>
  <verify><automated>npm run test:unit -- --runInBand src/platform/notifications/expoRestNotificationAdapter.test.ts src/bootstrap/appConfig.test.ts</automated></verify>
  <done>The Android adapter can reliably schedule each supported alert preference.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Reconcile preferences without making notifications authoritative</name>
  <read_first>src/platform/preferences/restAlertPreferenceStore.ts, src/platform/notifications/restNotificationReconciler.ts, src/platform/notifications/restNotificationReconciler.test.ts, src/domains/rest/restNotificationPort.ts</read_first>
  <files>src/platform/notifications/restNotificationReconciler.ts, src/platform/notifications/restNotificationReconciler.test.ts</files>
  <action>Read the validated preference once per reconciliation and pass it through channel/schedule operations. Treat a preference change as reason to replace related scheduled requests while retaining deterministic session/revision/end-time identity. Keep all notification calls fail-soft after SQLite rest transitions: denied/undetermined permission, channel failure, list/cancel/schedule failure, or malformed preferences return bounded outcomes and never mutate source rest state. Cover relaunch repair, preference changes, exact matching, stale duplicates, expiry, denial, and adapter failures. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Reconciliation reflects current preferences and all notification failures leave authoritative rest/session rows unchanged.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:unit -- --runInBand src/platform/preferences/restAlertPreferenceStore.test.ts src/platform/notifications/expoRestNotificationAdapter.test.ts src/platform/notifications/restNotificationReconciler.test.ts</automated></verify>
  <done>The platform alert path is configurable, recoverable, and non-authoritative.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Preference tampering | KV bytes to alert behavior | Strict version/schema parsing and safe default values. |
| Stale-channel spoofing | Existing Android channel to expected alert | New versioned channel identities for immutable behavior. |
| State corruption | Platform notification failure to workout truth | Fail-soft adapter/reconciler with SQLite source facts unchanged. |
| Information disclosure | Notification payload/logs | Session/revision/timestamp only; no exercise observations or owner data. |
| Duplicate alert denial | Reconciliation retries | Stable request identity and related-request cleanup. |
</threat_model>

<verification>Run preference, adapter, reconciler, app-config tests, typecheck, and lint. Physical sound/vibration proof remains in Plan 02-35 after the exact build.</verification>
<success_criteria>The platform portion of D-61/G-02-07 is complete with default tone+haptic and independent persisted controls.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-30-SUMMARY.md` when complete.</output>
