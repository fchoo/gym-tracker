---
phase: 02-owned-library-and-planning
plan: 07
type: execute
wave: 4
depends_on: [02-02, 02-04, 02-06]
files_modified:
  - artifacts/review/phase2/metric-migration-decision.json
  - src/domains/metrics/migrateCustomExerciseMetricProfile.test.ts
  - src/domains/metrics/migrateCustomExerciseMetricProfile.ts
  - src/platform/sqlite/migrations/0006_metric_profiles.ts
  - src/platform/sqlite/repositories/metricRepository.ts
  - tests/integration/metric-profile-migration.test.ts
  - tests/migrations/fixtures/v5-search-fts.sql
  - tests/sqlite-host/metric-profiles.test.ts
autonomous: false
requirements: [LIB-11, LIB-12]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "A blocking checkpoint confirms the already-approved D-34 one-way future-target migration contract before on-disk implementation."
    - "Migration 0006 preserves legacy load_reps V1 and timed_hold V1 seconds while adding explicit profile, contract version, and exercise metric generation to future targets/defaults/snapshots/observations."
    - "D-34 through D-39 are enforced atomically: active workouts block, every affected future target is explicitly replaced, history stays immutable, incompatible recommendations/policies invalidate, manual Hold is valid, and a fresh baseline begins."
    - "E-64 through E-78 persistence cases prove expected-revision rollback, idempotency, boundary values, and no cross-profile reinterpretation."
  artifacts:
    - path: "src/platform/sqlite/migrations/0006_metric_profiles.ts"
      provides: "Forward metric identity and nine-profile persistence schema"
    - path: "src/domains/metrics/migrateCustomExerciseMetricProfile.ts"
      provides: "One-way future-target migration command"
    - path: "src/platform/sqlite/repositories/metricRepository.ts"
      provides: "Metric-aware history/target reads and atomic profile migration"
  key_links:
    - from: "src/domains/metrics/migrateCustomExerciseMetricProfile.ts"
      to: "src/platform/sqlite/repositories/metricRepository.ts"
      via: "validated complete replacement map, expected revisions, and policy decision"
      pattern: "migrateCustomExerciseMetricProfile"
    - from: "src/platform/sqlite/migrations/0006_metric_profiles.ts"
      to: "src/domains/metrics/registry.ts"
      via: "persisted profile/contract/generation values match registered identities"
      pattern: "contract_version|metric_generation"
---

<objective>
Persist complete metric identities and the explicit D-34 future-only custom profile migration.

Purpose: Make all-nine-profile data durable without rewriting legacy observations or guessing replacement targets.
Output: Blocking one-way decision artifact, migration 0006, metric repository, migration command, retained fixture, and rollback/immutability proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-02-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-04-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/domains/metrics/registry.ts
@src/platform/sqlite/migrations/index.ts
@src/platform/sqlite/serializedWriter.ts
</context>

## Artifacts this phase produces

- `artifacts/review/phase2/metric-migration-decision.json` — approved D-34 execution gate.
- `src/platform/sqlite/migrations/0006_metric_profiles.ts` — complete future metric identity schema.
- `tests/migrations/fixtures/v5-search-fts.sql` — immutable post-0005 fixture.
- `tests/sqlite-host/metric-profiles.test.ts` — retained migration/legacy byte proof.
- `src/platform/sqlite/repositories/metricRepository.ts` — metric target/history/profile-migration persistence.
- `src/domains/metrics/migrateCustomExerciseMetricProfile.ts` — explicit command.
- `src/domains/metrics/migrateCustomExerciseMetricProfile.test.ts` — validation/orchestration tests.
- `tests/integration/metric-profile-migration.test.ts` — atomic rollback/history/recommendation/policy proof.

<tasks>

<task type="checkpoint:decision" gate="blocking-human">
  <name>Task 1: Confirm the approved D-34 one-way future-target migration door</name>
  <read_first>.planning/phases/02-owned-library-and-planning/02-CONTEXT.md, .planning/phases/02-owned-library-and-planning/02-RESEARCH.md, src/domains/metrics/registry.ts, src/domains/workout/activeWorkout.ts, src/domains/progression/recommendationCommands.ts</read_first>
  <files>artifacts/review/phase2/metric-migration-decision.json</files>
  <decision>Confirm implementation of the already approved D-34 future-only metric-profile migration as a one-way persisted contract.</decision>
  <context>A committed migration replaces versioned future target/default contracts and cannot reconstruct discarded old future targets without another explicit migration; completed and in-progress history remains immutable.</context>
  <options>
    <option id="approve-d34">
      <name>Approve D-34</name>
      <pros>Implements the locked complete future-only migration contract with explicit replacements, policy decision, and fresh baseline.</pros>
      <cons>Changing back later requires another explicit migration.</cons>
    </option>
    <option id="stop">
      <name>Stop execution</name>
      <pros>Does not cross the one-way on-disk contract.</pros>
      <cons>LIB-05/LIB-11/LIB-12 profile migration and dependent UI remain blocked.</cons>
    </option>
  </options>
  <action>Present the exact approved D-34 contract: profile migration keeps exercise identity and all completed/in-progress snapshots, discards and replaces future target/default contracts only after complete owner entry, blocks during active use, invalidates incompatible recommendations/policies, accepts a valid new policy or manual Hold, and cannot reconstruct discarded old future targets without another explicit migration. Record approval and the current registry digest; do not reopen profile-conversion alternatives or proceed without approval.</action>
  <acceptance_criteria>The decision artifact says `approved: true`, names D-34 through D-39, binds the metric-registry digest, and states the one-way undo cost.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'),c=JSON.parse(fs.readFileSync('artifacts/review/phase2/metric-migration-decision.json','utf8')); if(c.approved!==true||c.decision!=='D-34'||!c.registrySha256) process.exit(1)"</automated>
    <what-built>The exact on-disk and command contract for future-only custom metric-profile migration.</what-built>
    <how-to-verify>Confirm that history and in-progress bytes never change, all future targets require explicit replacement, and undo requires another explicit migration.</how-to-verify>
  </verify>
  <resume-signal>Select `approve-d34` to proceed, or `stop` to halt before implementation.</resume-signal>
  <done>The one-way door is explicitly approved and implementation may create the versioned persistence contract.</done>
</task>

<task type="tracer" tdd="true">
  <name>Task 2: Migrate one legacy timed hold and persist one new-profile target without reinterpretation</name>
  <reversibility rating="one-way">Changing persisted profile/version/generation semantics after use requires another data migration and must retain historical parsers.</reversibility>
  <read_first>artifacts/review/phase2/metric-migration-decision.json, src/domains/metrics/registry.ts, src/domains/workout/activeWorkout.ts, src/platform/sqlite/migrations/index.ts, src/platform/sqlite/serializedWriter.ts</read_first>
  <files>src/platform/sqlite/migrations/0006_metric_profiles.ts, tests/migrations/fixtures/v5-search-fts.sql, tests/sqlite-host/metric-profiles.test.ts</files>
  <action>Start with a retained v5 fixture containing timed_hold V1 seconds, migrate it unchanged, and insert/read one new-profile future target carrying profile, contract version, and exercise metric generation. Export 0006 as the direct successor of 0005 for host/native contract setup; Plan 02-11 owns the final ordered runtime manifest after every Phase 2 migration module exists. Never edit earlier migrations. Widen constrained source tables using forward copy/validation where required; preserve legacy parsers and JSON bytes, add deterministic legacy generation values, and support all registered profiles. Assert invalid identities/values fail before writes and E-64..E-78 boundary/precision/identity cases persist exactly.</action>
  <acceptance_criteria>All retained versions migrate, legacy seconds and load/reps bytes are unchanged, and all nine valid identities persist/round-trip with unknown combinations rejected.</acceptance_criteria>
  <verify>
    <automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/metric-profiles.test.ts</automated>
  </verify>
  <done>The narrowest legacy/new persistence path proves versioned metric storage without reinterpretation.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Implement the atomic D-34 future-target profile migration command</name>
  <reversibility rating="one-way">A committed command replaces future target contracts; another explicit migration is required to change back.</reversibility>
  <read_first>src/domains/metrics/registry.ts, src/platform/sqlite/migrations/0006_metric_profiles.ts, tests/sqlite-host/metric-profiles.test.ts, src/domains/progression/recommendationCommands.ts, src/platform/sqlite/serializedWriter.ts</read_first>
  <files>src/domains/metrics/migrateCustomExerciseMetricProfile.ts, src/domains/metrics/migrateCustomExerciseMetricProfile.test.ts, src/platform/sqlite/repositories/metricRepository.ts, tests/integration/metric-profile-migration.test.ts</files>
  <action>Write command tests first for D-34 through D-39. Require custom origin, expected exercise/target revisions, no active workout using the exercise, a complete replacement target/default map for every affected occurrence, explicit new policy or manual Hold, and acknowledgement that history never changes. In one FIFO `BEGIN IMMEDIATE` transaction update future metric identity/generation and targets, invalidate incompatible recommendations/policies, establish a fresh baseline marker, and return exact invalidations only after commit. Preserve session/snapshot/observation bytes and segment history reads by complete identity. Test stale/repeated calls, missing replacements, commit failure, in-progress block, idempotency, and post-commit result.</action>
  <acceptance_criteria>No inferred conversion exists; all affected future targets migrate or none do; active/completed observations are byte-identical; stale or repeated commands cannot duplicate generations/events.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/metric-profile-migration.test.ts</automated>
  </verify>
  <done>The approved one-way command is atomic, explicit, history-preserving, and revision-safe.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Legacy persisted bytes → widened metric schema | Historical meaning must survive forward migration unchanged. |
| Owner replacement map → future targets | Untrusted/incomplete values could silently reinterpret workouts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | custom-only migration authority | high | mitigate | Origin predicates and tests reject bundled exercises. |
| T-02-04 | Tampering | target/recommendation revisions | high | mitigate | Expected revisions and one serialized aggregate transaction. |
| T-02-05 | Tampering | historical metric meaning | high | mitigate | Legacy parser/byte retention, complete identity, no cross-profile conversion. |
| T-02-07 | Information Disclosure | migration diagnostics | medium | mitigate | Stable codes/counts/IDs only, never observations or replacement values. |
</threat_model>

<verification>
Approve the gate, run focused host migration and integration command suites, and compare historical bytes before/after.
</verification>

<success_criteria>
Nine-profile persistence and the D-34 migration are versioned, explicit, atomic, and incapable of changing history.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-07-SUMMARY.md` when done.
</output>
