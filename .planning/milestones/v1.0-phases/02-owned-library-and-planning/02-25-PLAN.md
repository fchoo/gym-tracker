---
phase: 02-owned-library-and-planning
plan: 25
type: execute
wave: 13
depends_on: [02-21]
files_modified:
  - DESIGN.md
  - src/ui/theme/index.ts
  - src/ui/components/index.ts
  - src/ui/components/PlanRow.tsx
  - src/ui/screens/LibraryScreen.tsx
  - src/ui/__tests__/LibraryScreen.test.tsx
  - src/ui/__tests__/foundation.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-05, LIB-06, LIB-07, LIB-10]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: high }
must_haves:
  truths:
    - "D-56 and G-02-03 establish the app-wide default: neutral grey canvas with black or near-black, high-contrast, flat, non-nested content cards."
    - "Library plan, starter-plan, and exercise items become independently scannable cards without changing search, filter, ownership, attribution, selection, archive, loading, error, or pagination semantics."
    - "Same-row Library actions have equal visible height and a right-edge action cluster, while System/Light/Dark, OLED contrast, visible focus, 200% text, and 48dp targets remain valid."
    - "This plan records the global card token/primitive and explicit remaining-surface inventory; Plan 02-26 completes adoption outside Library."
  artifacts:
    - { path: "src/ui/theme/index.ts", provides: "Global neutral-canvas and near-black content-card tokens across appearance modes" }
    - { path: "src/ui/components/index.ts", provides: "Shared flat content-card primitive and equal-height action cluster" }
    - { path: "src/ui/screens/LibraryScreen.tsx", provides: "Card-based plan, starter, and exercise Library" }
  key_links:
    - { from: "src/ui/theme/index.ts", to: "src/ui/components/index.ts", via: "semantic app canvas and content-card roles", pattern: "canvas|card|surface" }
    - { from: "src/ui/components/index.ts", to: "src/ui/screens/LibraryScreen.tsx", via: "flat non-nested content cards and aligned actions", pattern: "Card|Action" }
---

<objective>
Establish the approved global grey/black card system and apply it fully to Library.

Purpose: Replace the card-light presentation with a consistent, high-contrast content hierarchy while preserving operational behavior.
Output: Updated design contract, semantic tokens/primitives, card-based Library, and focused visual/semantic tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@DESIGN.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@src/ui/theme/index.ts
@src/ui/components/index.ts
@src/ui/components/PlanRow.tsx
@src/ui/screens/LibraryScreen.tsx
</context>

## Artifacts this phase produces

- Global canvas/content-card design tokens and shared flat-card/action primitives.
- Card-based Library across plans, starter plans, and exercises.
- A checked inventory of remaining content-card surfaces for Plan 02-26.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-01..03 | Library navigation/search/filter result presentation | COVERED |
| REQ | LIB-05..07, LIB-10 | Custom, starter, ownership, attribution, archive/unavailable cards | COVERED |
| CONTEXT | D-56 and Library portion of D-58 | Global token/primitive plus full Library adoption | COVERED |
| VERIFICATION | G-02-03 | Grey canvas, near-black flat cards, no nesting | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Define semantic global canvas and content-card roles</name>
  <read_first>DESIGN.md, src/ui/theme/index.ts, src/ui/components/index.ts, src/ui/__tests__/foundation.test.tsx, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md</read_first>
  <files>DESIGN.md, src/ui/theme/index.ts, src/ui/components/index.ts, src/ui/__tests__/foundation.test.tsx</files>
  <action>Start with failing token/primitive tests. Update the persistent design contract to supersede card-light language and define a neutral grey app canvas plus black/near-black content-card role in System, Light, and Dark modes. Implement semantic tokens and one flat ContentCard primitive with high-contrast text, no nested-card styling, visible focus, selected/pressed/disabled/status states, and an equal-height right-aligned ActionCluster. Keep form fields, sheets, navigation chrome, dialogs, and transient notices on their appropriate non-card semantic surfaces. Add contrast-oriented token assertions and a remaining-surface inventory for Root, Today, Active Workout, detail/editor, and completion adoption in Plan 02-26. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The tokens and primitive encode an app-wide default, explicitly distinguish non-card surfaces, and cannot create nested card styling.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/foundation.test.tsx</automated></verify>
  <done>The shared visual contract exists before Library migration begins.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Convert plan and starter items to flat cards</name>
  <read_first>src/ui/components/PlanRow.tsx, src/ui/screens/LibraryScreen.tsx, src/ui/components/index.ts, src/ui/__tests__/LibraryScreen.test.tsx</read_first>
  <files>src/ui/components/PlanRow.tsx, src/ui/screens/LibraryScreen.tsx, src/ui/__tests__/LibraryScreen.test.tsx</files>
  <action>Render Active Plan, My Plans, and Starter Plans as independently scannable ContentCards. Keep names, ownership/source labels, schedule/default state, attribution, update status, disabled reasons, and action semantics intact. Align same-row actions at the right edge with equal visible height and 48dp hit targets. Do not wrap cards inside cards or use accent color as decoration. Cover empty/loading/error/populated/partial/overflow/zero-one-many/long-text states at 200% text. Commit only these files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Every Library plan/starter item is a flat high-contrast card and all behavioral assertions remain unchanged.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/LibraryScreen.test.tsx</automated></verify>
  <done>Plan and starter sections use the approved card model.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Convert exercise results and close Library appearance coverage</name>
  <read_first>src/ui/screens/LibraryScreen.tsx, src/ui/components/index.ts, src/ui/__tests__/LibraryScreen.test.tsx, src/ui/theme/index.ts</read_first>
  <files>src/ui/screens/LibraryScreen.tsx, src/ui/__tests__/LibraryScreen.test.tsx</files>
  <action>Convert default, Favorites, Recent, filtered, archived, unavailable, and paginated exercise rows to flat ContentCards. Preserve matched-alias copy, favorite state, origin/visibility semantics, query/filter/scroll restoration, and deterministic ordering. Add System/Light/Dark token assertions, OLED-safe near-black review hooks, focus/selected/disabled states, long text, 200% text, and no-nesting structural assertions. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>All Library item families share the approved card hierarchy without search/filter/state regressions.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/LibraryScreen.test.tsx src/ui/__tests__/foundation.test.tsx</automated></verify>
  <done>Library is fully card-based and the global contract is ready for app-wide adoption.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Authority spoofing | Visual status to ownership/source facts | Preserve explicit Built-in/Custom/Unavailable/Archived text and source attribution. |
| State tampering | Card refactor changes actions or filters | Keep existing callbacks/IDs and behavior-first component tests. |
| Information hiding | Low contrast or color-only state | Contrast tokens plus text/non-color semantics and focus tests. |
| Denial of use | Nested/dense cards break scaling | Flat primitive, no nesting, long-text and 200% text coverage. |
</threat_model>

<verification>Run Library/foundation suites, typecheck, lint, and inspect the inventory for every non-Library content-card surface. Plan 02-34 owns fresh APK evidence.</verification>
<success_criteria>D-56/G-02-03 is established globally and complete in Library, with remaining app surfaces explicitly delegated to Plan 02-26.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-25-SUMMARY.md` when complete.</output>
