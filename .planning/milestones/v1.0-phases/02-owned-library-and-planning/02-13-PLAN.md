---
phase: 02-owned-library-and-planning
plan: 13
type: execute
wave: 5
depends_on: [02-07, 02-09]
files_modified:
  - src/domains/library/customExerciseCommands.test.ts
  - src/domains/library/customExerciseCommands.ts
  - src/platform/sqlite/repositories/customExerciseRepository.ts
  - tests/integration/custom-exercise.test.ts
autonomous: true
requirements: [LIB-05]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "E-28 through E-33 are executable custom-exercise tests for identity collisions, empty values, Unicode names, stable duplicate order, idempotent requests, and serialized revision conflicts."
    - "D-17 likely duplicates use normalized name plus similar metric/equipment, return existing matches, and require explicit override confirmation."
    - "D-27 bundled rows can be hidden but not edited; Create custom copy uses fresh independent user-owned identity."
    - "D-28/D-29 archive/restore has no deletion, previews affected plans, removes archived rows from new selection, and keeps existing plan references runnable with Archived."
    - "D-33 requires explicit metric choice with no load/reps default; absent progression saves manual Hold."
  artifacts:
    - path: "src/domains/library/customExerciseCommands.ts"
      provides: "Create/edit/hide/custom-copy/archive/restore command contracts"
    - path: "src/platform/sqlite/repositories/customExerciseRepository.ts"
      provides: "Ownership-safe expected-revision custom exercise persistence"
  key_links:
    - from: "src/domains/library/customExerciseCommands.ts"
      to: "src/platform/sqlite/repositories/customExerciseRepository.ts"
      via: "validated explicit metric identity, duplicate decision, and expected revision"
      pattern: "createCustomExercise|archiveCustomExercise"
    - from: "src/platform/sqlite/repositories/customExerciseRepository.ts"
      to: "src/platform/sqlite/repositories/librarySearchRepository.ts"
      via: "source and same-transaction search-term/FTS synchronization"
      pattern: "exercise_search_terms"
---

<objective>
Implement the complete custom exercise lifecycle as domain/repository behavior.

Purpose: Satisfy LIB-05 ownership, duplicate, metric, hide, archive, and custom-copy rules before UI attaches.
Output: Commands, repository, unit/integration tests, and same-transaction search synchronization.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-07-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-09-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@src/domains/metrics/registry.ts
@src/domains/library/search.ts
@src/platform/sqlite/repositories/librarySearchRepository.ts
@src/platform/sqlite/serializedWriter.ts
</context>

## Artifacts this phase produces

- `src/domains/library/customExerciseCommands.ts` — custom lifecycle command/preview types.
- `src/domains/library/customExerciseCommands.test.ts` — D-17/D-27..D-29/D-33 and E-28..E-33 tests.
- `src/platform/sqlite/repositories/customExerciseRepository.ts` — atomic custom preference/source mutation.
- `tests/integration/custom-exercise.test.ts` — duplicate/ownership/search/archive/rollback proof.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create one explicit-profile custom exercise and find it through search</name>
  <read_first>src/domains/metrics/registry.ts, src/domains/library/search.ts, src/platform/sqlite/repositories/librarySearchRepository.ts, src/platform/sqlite/serializedWriter.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/library/customExerciseCommands.ts, src/domains/library/customExerciseCommands.test.ts, src/platform/sqlite/repositories/customExerciseRepository.ts, tests/integration/custom-exercise.test.ts</files>
  <action>Start red with a new Unicode-named custom exercise using an explicitly chosen non-load/reps metric identity and manual Hold, commit it through the private writer, then retrieve it through canonical search in the same transactionally synchronized index. Define strict create/edit inputs, explicit profile requirement per D-33, bounded taxonomy/equipment/aliases, request identity, and expected revisions. Before create, produce D-17 duplicate candidates from normalized name plus similar metric/equipment and require an explicit `create_anyway` confirmation to proceed. Cover E-28..E-33 for empty/single inputs, name collisions, Unicode, stable duplicate order, repeated identity, and concurrent stale edits.</action>
  <acceptance_criteria>A valid explicit-profile custom exercise commits once and is immediately searchable; missing profile, duplicate without confirmation, stale revision, or commit failure writes nothing.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/custom-exercise.test.ts -t "create"</automated>
  </verify>
  <done>The narrowest create-to-search custom exercise path is authoritative, indexed, explicit, and revision-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete bundled hide/custom copy and custom archive/restore lifecycle</name>
  <read_first>src/domains/library/customExerciseCommands.ts, src/domains/library/customExerciseCommands.test.ts, src/platform/sqlite/repositories/customExerciseRepository.ts, tests/integration/custom-exercise.test.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/library/customExerciseCommands.ts, src/domains/library/customExerciseCommands.test.ts, src/platform/sqlite/repositories/customExerciseRepository.ts, tests/integration/custom-exercise.test.ts</files>
  <action>Add D-27 hide/show preference commands for bundled rows while rejecting bundled field edits. Add `createCustomCopy` with fresh custom ID and no mutable upstream link. Add D-28 archive/restore and D-29 affected-plan preview: archived rows leave new selection and default search, existing plan references remain runnable/readable with `Archived`, and no delete SQL exists. Require confirmation against a preview revision, expected exercise revision, and complete rollback. Keep favorite/hide/archive source+search derivative updates in one serialized transaction and return exact invalidations only after commit.</action>
  <acceptance_criteria>Bundled source is immutable; custom copies are independent; archive/restore preserves all references/history and affected-plan previews cannot go stale.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/custom-exercise.test.ts -t "hide|copy|archive|restore"</automated>
  </verify>
  <done>LIB-05 domain/repository behavior is complete without custom editor UI.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Custom form values → source/search rows | User text and metric values require strict validation and atomic indexing. |
| Owner command → bundled/custom authority | Commands must not edit bundled source or delete referenced custom identity. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02 | Tampering / Denial | duplicate/search text | medium | mitigate | Reuse bounded normalization/prepared search contract. |
| T-02-03 | Tampering | origin authority | high | mitigate | Separate bundled preference and custom source predicates; no delete path. |
| T-02-04 | Tampering | exercise/preview revisions | high | mitigate | Expected revisions and preview token in one serialized transaction. |
| T-02-05 | Tampering | metric identity | high | mitigate | Explicit registry identity and no inferred/default profile. |
| T-02-07 | Information Disclosure | custom diagnostics | medium | mitigate | Safe codes/counts/IDs; never raw notes/targets. |
</threat_model>

<verification>
Run focused create and lifecycle integration samples; inspect commands for no delete and no bundled-update path.
</verification>

<success_criteria>
Custom exercises are explicit-profile, searchable, ownership-safe, archiveable/restorable, and never permanently deleted.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-13-SUMMARY.md` when done.
</output>
