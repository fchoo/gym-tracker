---
phase: 02-owned-library-and-planning
plan: 35
type: execute
wave: 21
depends_on: [02-34]
files_modified:
  - artifacts/native/phase2/attended/emulator-supplementary.json
  - artifacts/native/phase2/attended/physical-result.json
  - .planning/phases/02-owned-library-and-planning/02-TERMINAL-SEAL.md
  - .planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
autonomous: false
gap_closure: true
status: superseded
superseded_by: 05-07
superseded_reason: Attended emulator, Samsung, native, human approval, and terminal sealing were consolidated into the single Phase 5 exact-candidate release gate.
requirements: [LIB-01, LIB-02, LIB-03, LIB-04, LIB-05, LIB-06, LIB-07, LIB-08, LIB-09, LIB-10, LIB-11, LIB-12]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "D-67 attended approval uses the unchanged Plan 02-34 APK: emulator supplies deterministic adaptive/input coverage and Samsung SM-S916B supplies physical touch/OLED/motion/tone/haptic coverage."
    - "Every required row for D-56..D-67, G-02-01..G-02-09, amended UI surfaces, all eight UI truth categories, and all three prohibitions receives an explicit human pass before evidence is recorded."
    - "Emulator and Samsung evidence bind the same implementation HEAD, mode-sensitive source digest, APK SHA-256/size, package, and their own installed-byte hash; persisted serial values are SHA-256 hashes only."
    - "The human checkpoint performs no writes or hidden automation: preparation completes before it, and approved evidence/metadata writes occur only in the following ordinary auto task."
    - "Normal GSD plan/phase closeout finishes with `final-verification.json` absent and status `ready_for_terminal_seal`; every metadata commit ends with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`."
    - "The physical-required verifier is outside every PLAN task. Only after `/gsd-execute-phase 2 --gaps-only` fully returns may the top-level TraeX agent read the committed terminal runbook, run its exact command directly as the literal final executable, and make no tool call afterward."
  artifacts:
    - { path: "artifacts/native/phase2/attended/emulator-supplementary.json", provides: "Explicit same-APK emulator adaptive/input approval" }
    - { path: "artifacts/native/phase2/attended/physical-result.json", provides: "Explicit Samsung physical/OLED/tone/haptic approval" }
    - { path: ".planning/phases/02-owned-library-and-planning/02-TERMINAL-SEAL.md", provides: "Non-PLAN ready-for-terminal-seal handoff containing the sole final command" }
    - { path: ".planning/phases/02-owned-library-and-planning/02-35-SUMMARY.md", provides: "Normal GSD closeout record completed before terminal sealing" }
  key_links:
    - { from: "artifacts/native/phase2/build.json", to: "artifacts/native/phase2/attended/physical-result.json", via: "same source/APK/package identity plus Samsung-specific installed-byte proof", pattern: "base_head|source_tree_sha256|apk|installed|samsung-physical" }
    - { from: ".planning/phases/02-owned-library-and-planning/02-35-SUMMARY.md", to: ".planning/phases/02-owned-library-and-planning/02-TERMINAL-SEAL.md", via: "all normal closeout is committed before the external literal-final command", pattern: "ready_for_terminal_seal|final-verification" }
---

> **SUPERSEDED — DO NOT EXECUTE.** This plan is retained as the historical
> Phase 2 attended-gate design. Its complete scope was reassigned to Phase 5
> Plan 05-07 so Phases 2–5 receive one native, device, accessibility,
> performance, design, owner-approval, and terminal-seal pass against the same
> exact signed candidate. No artifact or approval described below has been
> produced by this plan.
>
> Everything below is an archival record of the retired design, not an active
> instruction. The executable replacement is `05-07-PLAN.md`.

<objective>
Complete attended emulator and Samsung approval, finish normal GSD closeout, and hand off the literal final verifier safely.

Purpose: Separate human judgment and ordinary GSD post-processing from the command that must be the last executable.
Output: Approved attended evidence, complete planning metadata, and a committed non-PLAN terminal-seal runbook.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VALIDATION.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-34-SUMMARY.md
@artifacts/native/phase2/build.json
@artifacts/native/phase2/result.json
@artifacts/native/phase2/maestro.json
@artifacts/native/phase2/benchmark.json
@artifacts/native/phase2/artifact-roundtrip.json
@scripts/verify-phase2-native-evidence.mjs
</context>

## Artifacts this phase produces

- Separate same-APK emulator and Samsung attended records.
- Normal Phase 2 summary/state/roadmap/requirements closeout with status `ready_for_terminal_seal`.
- A non-PLAN terminal runbook whose only executable action is the final verifier.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-01..LIB-12 | Attended owner approval and final closeout coverage | COVERED |
| CONTEXT | D-01..D-67 | Exact source-derived ledger, with D-56..D-67 physically reviewed | COVERED |
| VERIFICATION | G-02-01..G-02-09 | Every remediation gap receives emulator/Samsung evidence as applicable | COVERED |
| UI-SPEC | Eight truths by amended surface plus three prohibitions | Explicit checkpoint matrix, never inferred | COVERED |

<tasks>

<task type="tracer">
  <name>Task 1: Prepare exact-byte attended review before the checkpoint</name>
  <read_first>.planning/phases/02-owned-library-and-planning/02-34-SUMMARY.md, artifacts/native/phase2/build.json, artifacts/native/phase2/result.json, artifacts/native/phase2/maestro.json, artifacts/native/phase2/benchmark.json, artifacts/native/phase2/artifact-roundtrip.json, .planning/phases/02-owned-library-and-planning/02-VALIDATION.md, scripts/verify-phase2-native-evidence.mjs</read_first>
  <files>none (read-only preflight and device preparation)</files>
  <precondition>Plan 02-34 automated-only verification passed and its source/APK identities remain unchanged.</precondition>
  <action>Run all machine-verifiable preflight before asking the owner: confirm HEAD/source/APK/package/roundtrip identity and confirm final-verification.json is absent. Connect the API 36 arm64 emulator and Samsung SM-S916B, then run exactly `npm run prepare:attended:phase2 -- --manifest artifacts/native/phase2/build.json --emulator-serial <emulator> --samsung-serial <samsung> --output artifacts/native/phase2/attended/checklist.pending.json`. Never hand-write, copy, patch, or otherwise synthesize the pending checklist. The trusted generator must install the exact retained APK on each device without rebuilding, prove each installed APK hash matches the manifest, and derive the complete pending matrix from canonical source ledgers. Reset only development-test app state as required by that checklist, then navigate both devices to deterministic starting points. Do not mark any human-observation row passed and do not write final attended result files yet. Present the complete emulator and Samsung matrix in the next checkpoint. The pending checklist remains local and ignored; any preparation metadata commit must end with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Both devices run the unchanged candidate, every automated prerequisite passes, all required attended rows are visible in one checklist, and final-verification.json is absent.</acceptance_criteria>
  <verify><automated>npm run verify:native:phase2 -- --manifest artifacts/native/phase2/build.json --automated-only --require-roundtrip</automated></verify>
  <done>The exact candidate and complete attended matrix are ready for human judgment.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 2: Approve the complete emulator and Samsung matrix</name>
  <what-built>One unchanged post-remediation APK with complete automated proof, ready on the emulator and Samsung SM-S916B. The review covers expanded rail geometry; calendar/time/number controls; grey canvas and flat near-black cards; glyph/status/alignment behavior; add/copy persistence and retry/focus; sticky identity and Today's plan; unlimited active-session completed-set editing; collapsed/expanded rest controls; notification defaults/toggles; foreground/background tone and haptic behavior; all appearance/adaptive/accessibility states; eight UI truths; and three prohibitions.</what-built>
  <how-to-verify>On the emulator, complete every source-owned adaptive, compact/medium/expanded, landscape, 200% text, keyboard/D-pad, focus-restoration, semantic-input, card, rail, and long/empty/error/partial-state row. On Samsung SM-S916B, complete every touch-target, OLED/System/Light/Dark, scrolling, add/copy/edit, sticky-header, Today's-plan, rest, default sound+haptic, independent toggle, foreground/background, denied-permission, and reduced-motion row. Approve only if every row passes on installed bytes matching the Plan 02-34 APK; otherwise report every failed row.</how-to-verify>
  <resume-signal>Type `approved` only when every emulator and Samsung row passes on the unchanged APK, or list the failed row IDs and observations.</resume-signal>
</task>

<task type="auto">
  <name>Task 3: Record approval and complete normal GSD closeout as ready for terminal seal</name>
  <read_first>.planning/phases/02-owned-library-and-planning/02-VALIDATION.md, .planning/phases/02-owned-library-and-planning/02-VERIFICATION.md, .planning/phases/02-owned-library-and-planning/02-34-SUMMARY.md, artifacts/native/phase2/build.json, artifacts/native/phase2/result.json, artifacts/native/phase2/maestro.json, artifacts/native/phase2/benchmark.json, artifacts/native/phase2/artifact-roundtrip.json</read_first>
  <files>artifacts/native/phase2/attended/emulator-supplementary.json, artifacts/native/phase2/attended/physical-result.json, .planning/phases/02-owned-library-and-planning/02-TERMINAL-SEAL.md, .planning/phases/02-owned-library-and-planning/02-VERIFICATION.md</files>
  <precondition>The user explicitly approved every row in Task 2; exact HEAD/source/APK/package/installed identities remain unchanged.</precondition>
  <action>After receiving the literal human response `approved`, copy the exact lowercase `checklist_sha256` printed by the trusted preparation command and run exactly `npm run record:attended:phase2 -- --manifest artifacts/native/phase2/build.json --emulator-serial <emulator> --samsung-serial <samsung> --checklist-sha256 <sha256>` to derive and atomically publish emulator-supplementary.json and physical-result.json from those approved immutable checklist bytes. Never hand-write, copy, patch, replace, or otherwise synthesize the pending checklist or either attended role JSON file. Keep all three files local and ignored; do not force-add or commit them. The trusted command must reject a missing or mismatched checklist SHA-256; bind separate roles, timestamps, model/API/ABI, SHA-256 hashed serial values, and exact build/source/APK/package/installed identities without raw serial or owner data; and refuse to run if final-verification.json already exists or appears before publication. Validate both attended files with the non-final physical preflight mode provided by Plan 02-33. Update 02-VERIFICATION.md to record explicit attended approval and status `ready_for_terminal_seal`. Create `02-TERMINAL-SEAL.md` stating: (1) `final-verification.json` must still be absent; (2) wait until `/gsd-execute-phase 2 --gaps-only` has fully returned and every normal GSD summary, state, roadmap, requirements, post-wave, code-review, verification, and commit action is complete; (3) a top-level TraeX agent—not Claude and not a GSD executor subagent—must then run directly the exact sole command `npm run verify:native:phase2 -- --manifest artifacts/native/phase2/build.json --require-physical --require-roundtrip --output artifacts/native/phase2/final-verification.json`; (4) after that command, make no tool call or executable command and return its result directly. Do not run that command in this task, in plan verification, or anywhere inside execute-phase. Assert final-verification.json is absent, then commit only the task-owned planning documents `02-VERIFICATION.md` and `02-TERMINAL-SEAL.md` with a message body ending exactly in `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`. Return to the normal executor so it can create 02-35-SUMMARY.md and perform all ordinary STATE/ROADMAP/REQUIREMENTS/post-wave/code-review/metadata processing before execute-phase exits; every such commit must use the same exact trailer.</action>
  <acceptance_criteria>Both attended records pass non-final validation, verification status is `ready_for_terminal_seal`, the committed runbook contains the exact sole command and no second executable, and final-verification.json remains absent when the task returns to normal GSD processing.</acceptance_criteria>
  <verify><automated>npm run verify:native:phase2 -- --manifest artifacts/native/phase2/build.json --attended-preflight --require-roundtrip</automated></verify>
  <done>Attended evidence and the terminal handoff are committed; normal GSD post-processing can finish with exactly one external command remaining.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Human-evidence spoofing | Observation checklist to attended JSON | Pure checkpoint and explicit approval of every named row. |
| Device-role spoofing | Emulator/Samsung to evidence | Separate roles, model/API/ABI, hashed serial, and per-device installed-byte proof. |
| Artifact tampering | Automated candidate to attended review | Exact HEAD/source/APK/package equality and no rebuild. |
| Sequence tampering | Normal GSD closeout to terminal verifier | Final command omitted from PLAN and isolated in committed non-PLAN runbook. |
| Information disclosure | Physical evidence | Hash serials and store bounded technical observations only. |
</threat_model>

<verification>Task 1 runs automated preflight; Task 2 is human-only; Task 3 validates attended evidence and completes all normal GSD processing. This plan never invokes the physical-required final command.</verification>
<success_criteria>Phase 2 reaches `ready_for_terminal_seal` with complete approved evidence, committed metadata, no final-verification.json, and a safe literal-last-command handoff.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-35-SUMMARY.md` through normal executor post-processing and create `.planning/phases/02-owned-library-and-planning/02-TERMINAL-SEAL.md` in Task 3; do not create final-verification.json. The summary/state/roadmap/requirements metadata commit must end exactly with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</output>
