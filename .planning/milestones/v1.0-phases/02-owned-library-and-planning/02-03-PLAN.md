---
phase: 02-owned-library-and-planning
plan: 03
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domains/scheduling/localDate.test.ts
  - src/domains/scheduling/localDate.ts
  - src/domains/scheduling/scheduleState.test.ts
  - src/domains/scheduling/scheduleState.ts
  - src/domains/scheduling/timeZone.ts
autonomous: true
requirements: [LIB-09]
estimate:
  tokens: 28000
  raw_tokens: 28000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "E-53 through E-58 are executable fake-clock table rows for schedule adjacency, empty state, encoding, order, idempotency, and concurrent/repeated intent."
    - "D-42 through D-49 are pure rules: rotation repeat/skip/advance, overrides, missed weekdays, start LocalDate, DST-safe stored-timezone intent, and prospective timezone choice."
    - "Calendar arithmetic never parses date-only strings as instants and never advances a calendar day by adding 86,400,000 milliseconds."
  prohibitions:
    - "Missed-day, rest-day, schedule, and progression experiences must not shame, pressure, guilt, or punish the owner for resting, skipping, repeating, or changing intent."
  artifacts:
    - path: "src/domains/scheduling/localDate.ts"
      provides: "Validated LocalDate arithmetic independent of instants and DST"
    - path: "src/domains/scheduling/scheduleState.ts"
      provides: "Pure Weekday/Rotation/override/timezone transition state machine"
  key_links:
    - from: "src/domains/scheduling/timeZone.ts"
      to: "src/domains/scheduling/localDate.ts"
      via: "Intl formatToParts resolves an instant in a validated stored IANA zone"
      pattern: "formatToParts"
    - from: "src/domains/scheduling/localDate.ts"
      to: "src/domains/scheduling/scheduleState.ts"
      via: "all opportunity and effective-date transitions use LocalDate values"
      pattern: "LocalDate"
---

<objective>
Freeze calendar-safe schedule meaning before any database or UI work.

Purpose: Satisfy the pure LIB-09 rules without copying Phase 1 instant arithmetic or silently normalizing intent.
Output: LocalDate/timezone utilities, Weekday/Rotation/override transitions, and complete fake-clock edge tables.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/domains/shared/clock.ts
@src/domains/rest/restState.ts
@src/domains/workout/outcomes.ts
</context>

## Artifacts this phase produces

- `src/domains/scheduling/localDate.ts` — parse, compare, add, difference, and weekday operations.
- `src/domains/scheduling/timeZone.ts` — stored-zone validation and instant-to-LocalDate conversion.
- `src/domains/scheduling/localDate.test.ts` — month/year/leap/DST/midnight/timezone tables.
- `src/domains/scheduling/scheduleState.ts` — Weekday, Rotation, override, opportunity, and timezone-choice transitions.
- `src/domains/scheduling/scheduleState.test.ts` — D-42..D-49 and E-53..E-58 transition tables.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Resolve one stored-timezone weekday across midnight and DST</name>
  <read_first>src/domains/shared/clock.ts, src/domains/rest/restState.ts, .planning/phases/02-owned-library-and-planning/02-RESEARCH.md, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/scheduling/localDate.ts, src/domains/scheduling/timeZone.ts, src/domains/scheduling/localDate.test.ts</files>
  <action>Start with a failing test that converts instants immediately before/after midnight and DST changes into the intended LocalDate in a validated stored IANA timezone, then computes weekday and next date from calendar components. Implement strict `YYYY-MM-DD` parsing, leap/month/year boundaries, comparison/add/difference, and `Intl.DateTimeFormat(...).formatToParts` conversion. Cover E-53/E-55/E-56 for touching dates, preserved timezone text, and stable ordering. Per D-47/D-48, retain the session start LocalDate across midnight and allow DST to alter instants only; reject invalid dates/zones with typed errors.</action>
  <acceptance_criteria>The same local calendar intent survives spring/fall DST, month/year/leap boundaries, and midnight without millisecond-day arithmetic or UTC date parsing.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/scheduling/localDate.test.ts</automated>
  </verify>
  <done>The production LocalDate/timezone path proves one calendar-safe opportunity and all boundary tables pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Encode Weekday, Rotation, override, and timezone-choice transitions</name>
  <read_first>src/domains/scheduling/localDate.ts, src/domains/scheduling/timeZone.ts, src/domains/scheduling/localDate.test.ts, src/domains/rest/restState.ts, src/domains/workout/outcomes.ts</read_first>
  <files>src/domains/scheduling/scheduleState.ts, src/domains/scheduling/scheduleState.test.ts</files>
  <action>Write explicit transition tables before implementation. Model pending/consumed opportunities and overrides, Weekday due/skipped/missed, Rotation pointer/repeat/skip/advance/completion, alternate/rest/empty Train anyway intent, and one-time prospective timezone decisions. Implement D-42 through D-49 exactly: only scheduled Rotation completion auto-advances; Repeat holds; Skip records and advances; Advance creates no workout; alternate/rest/empty does not advance absent explicit intent; exactly one pending override may be replaced; consumed facts are immutable; Weekday skip is date-only; missed becomes `Planned but not completed`; timezone choices use the exact labels. Cover E-54/E-57/E-58 with empty schedules, repeated commands, stable typed conflicts, and revision-safe transition inputs.</action>
  <acceptance_criteria>Every valid transition returns one deterministic next state/event set, every invalid or repeated stale transition returns a typed conflict, and no transition rewrites prior facts.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/scheduling/scheduleState.test.ts</automated>
  </verify>
  <done>Pure schedule behavior is exhaustive and ready for persistence without embedding UI or SQLite.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Device instant/zone → schedule calendar | Mutable device time data crosses into stored calendar intent. |
| Command intent → immutable schedule fact | Repeated or stale actions must not rewrite consumed history. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | schedule revision | high | mitigate | Pure expected-revision transitions and immutable consumed states. |
| T-02-05 | Tampering | silent advancement/timezone reinterpretation | high | mitigate | Explicit events, stored timezone, LocalDate arithmetic, and table tests. |
| T-02-07 | Information Disclosure | schedule errors | low | mitigate | Stable codes and dates only; no observation payloads. |
</threat_model>

<verification>
Run both pure suites; confirm D-42..D-49 and E-53..E-58 are named in table cases.
</verification>

<success_criteria>
Schedule meaning is deterministic across empty/repeated/concurrent intent, midnight, DST, and timezone changes before persistence.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-03-SUMMARY.md` when done.
</output>
