---
phase: 02-owned-library-and-planning
plan: 31
type: execute
wave: 17
depends_on: [02-29, 02-30]
files_modified:
  - app/_layout.tsx
  - app/__notification-test-controls.tsx
  - src/bootstrap/workoutLifecycle.ts
  - src/bootstrap/workoutLifecycle.test.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/bootstrap/workoutAppRuntime.test.tsx
  - src/ui/components/RestAlertSettingsSheet.tsx
  - src/ui/screens/TodayScreen.tsx
  - src/ui/__tests__/TodayScreen.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-07, LIB-08]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "The UI/runtime half of D-61/G-02-07 closes with discoverable independent `Rest sound` and `Rest vibration` controls, defaulted on and persisted through Plan 02-30's store."
    - "Each eligible continuously-foreground (or explicit deterministic-test) rest revision is atomically queued with its expiry and durably claimed for at most one platform-feedback attempt. Launch recovers already-pending attempts; a crash or failure after claim can prevent physical delivery; background re-entry or launch overdue reconciliation never synthesizes duplicate manual feedback."
    - "Pause and Resume remain glyph actions through Plan 02-29's RestDock contract; settings copy never implies notifications are workout authority."
    - "Permission denial, notification-handler failure, haptic failure, or sound failure leaves the in-app timer and SQLite workout/rest state correct and usable."
    - "No package is installed; existing Expo Notifications and Haptics adapters are used."
  artifacts:
    - { path: "src/ui/components/RestAlertSettingsSheet.tsx", provides: "Accessible independent sound/vibration preferences" }
    - { path: "src/bootstrap/workoutLifecycle.ts", provides: "Durably claimed at-most-one foreground expiry feedback attempts keyed by session/rest revision" }
    - { path: "app/__notification-test-controls.tsx", provides: "Development-test controls for deterministic native alert evidence" }
  key_links:
    - { from: "src/ui/components/RestAlertSettingsSheet.tsx", to: "src/platform/preferences/restAlertPreferenceStore.ts", via: "validated independent persisted toggles", pattern: "soundEnabled|vibrationEnabled" }
    - { from: "src/bootstrap/workoutLifecycle.ts", to: "src/platform/haptics/expoHapticsAdapter.ts", via: "durably claimed foreground expiry feedback attempt", pattern: "expired|restRevision|haptic" }
---

<objective>
Expose rest-alert preferences and make an eligible foreground rest completion produce a durably claimed, at-most-one enabled feedback attempt.

Purpose: Give the owner control over tone and haptic behavior while fixing the foreground path that Android notifications do not cover reliably.
Output: Settings UI, runtime/lifecycle feedback orchestration, deterministic native test controls, and focused tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-29-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-30-SUMMARY.md
@src/bootstrap/workoutLifecycle.ts
@src/bootstrap/workoutAppRuntime.tsx
@src/ui/screens/TodayScreen.tsx
@app/__notification-test-controls.tsx
</context>

## Artifacts this phase produces

- A discoverable rest-alert settings sheet beside appearance controls.
- Atomically queued, durably claimed at-most-one foreground tone/haptic feedback attempts, with launch recovery of already-pending attempts.
- Native development-test controls for deterministic on/off and foreground/background evidence.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-07, LIB-08 | Alert preferences during starter/user-owned workouts | COVERED |
| CONTEXT | UI/runtime half D-61 | Independent controls and foreground feedback | COVERED |
| VERIFICATION | UI/runtime half G-02-07 | No settings/foreground path finding | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add revision-keyed, durably claimed foreground feedback attempts</name>
  <read_first>src/bootstrap/workoutLifecycle.ts, src/bootstrap/workoutLifecycle.test.ts, src/bootstrap/workoutAppRuntime.tsx, src/platform/haptics/expoHapticsAdapter.ts, src/platform/notifications/expoRestNotificationAdapter.ts, src/platform/preferences/restAlertPreferenceStore.ts</read_first>
  <files>src/bootstrap/workoutLifecycle.ts, src/bootstrap/workoutLifecycle.test.ts, src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutAppRuntime.test.tsx</files>
  <action>Start with failing lifecycle/runtime tests. For each eligible continuously-foreground (or explicit deterministic-test) rest revision, atomically queue the feedback work with the authoritative expiry transaction, including expiry and the `(sessionId, restRevision)` identity. Durably claim queued work before invoking the enabled short-tone and haptic adapters, permitting at most one platform-feedback attempt. Launch recovers already-pending attempts; repeated foreground callbacks, rerenders, and resume cannot create another attempt. Background re-entry or launch overdue reconciliation must not synthesize manual feedback. A crash or adapter failure after claim can prevent physical delivery; return bounded diagnostic outcomes but never roll back or alter rest/workout truth. Do not reuse set-completion haptics as authority. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Tests cover both defaults, each independent toggle, atomically queued expiry, at-most-one durable claim, launch recovery of pending work, duplicate callbacks, pause/resume, foreground re-entry, background/launch overdue no-synthesis, and sound/haptic failures with unchanged SQLite truth.</acceptance_criteria>
  <verify><automated>npm run test:unit -- --runInBand src/bootstrap/workoutLifecycle.test.ts src/bootstrap/workoutAppRuntime.test.tsx</automated></verify>
  <done>An eligible foreground expiry atomically queues enabled feedback and yields at most one durably claimed platform-feedback attempt; physical delivery remains best-effort after claim.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add accessible rest-alert settings to Today</name>
  <read_first>src/ui/screens/TodayScreen.tsx, src/ui/components/index.ts, src/platform/preferences/restAlertPreferenceStore.ts, src/bootstrap/workoutAppRuntime.tsx, src/ui/__tests__/TodayScreen.test.tsx</read_first>
  <files>src/ui/components/RestAlertSettingsSheet.tsx, src/ui/screens/TodayScreen.tsx, src/ui/__tests__/TodayScreen.test.tsx, src/bootstrap/workoutAppRuntime.tsx</files>
  <action>Add a discoverable rest-alert settings entry near the existing appearance affordance. The sheet exposes independent `Rest sound` and `Rest vibration` switches, current on/off state, default-on behavior, concise explanation that the in-app timer remains authoritative, and a link to Android notification settings when permission is denied. Persist changes immediately through the validated store and reconcile active scheduled rest notifications. Preserve unsaved workout input, focus restoration, 200% text, keyboard/D-pad use, visible focus, and 48dp controls. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Each toggle survives remount/relaunch, changes only its own modality, refreshes active scheduling, and remains usable without notification permission.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/TodayScreen.test.tsx &amp;&amp; npm run test:unit -- --runInBand src/bootstrap/workoutAppRuntime.test.tsx</automated></verify>
  <done>The owner can configure sound and vibration independently from an accessible settings surface.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Extend deterministic native notification controls</name>
  <read_first>app/__notification-test-controls.tsx, src/bootstrap/phase1NotificationTestControls.ts, src/bootstrap/phase1NotificationTestControls.test.ts, src/platform/preferences/restAlertPreferenceStore.ts, src/bootstrap/workoutLifecycle.ts</read_first>
  <files>app/__notification-test-controls.tsx, src/bootstrap/workoutAppRuntime.test.tsx</files>
  <action>Extend the development-test-only route so evidence can select each preference combination, trigger eligible foreground and background expiry deterministically, observe atomically-queued and durably-claimed at-most-one-attempt result codes (including `foreground_expiry_attempted_once`), and verify permission-denied/platform-failure behavior. Exercise launch recovery of already-pending attempts without allowing background re-entry or launch overdue reconciliation to synthesize manual feedback. Keep controls unavailable in production builds and never include raw device serial, workout observations, or owner content. Add runtime-side assertions for bounded outputs and preference reset. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Later Maestro/native evidence can prove all four preference combinations, eligible foreground attempt, launch recovery of pending attempts, and background/launch overdue no-synthesis outcomes without sleeps, source mutation, or private data; it does not claim physical delivery after a claim.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:unit -- --runInBand src/bootstrap/workoutLifecycle.test.ts src/bootstrap/workoutAppRuntime.test.tsx src/bootstrap/phase1NotificationTestControls.test.ts &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/TodayScreen.test.tsx</automated></verify>
  <done>D-61/G-02-07 is ready for native and physical evidence.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Duplicate feedback | Lifecycle callbacks to platform effects | Atomically queued revision identity, durable at-most-one claim, launch recovery of pending work, and no background/launch overdue synthesis tests. |
| Preference spoofing | Settings UI to persisted values | Validated store writes and read-back state. |
| State corruption | Feedback failure to rest transition | Expire source fact first; all effects fail soft afterward. |
| Information disclosure | Development-test controls/evidence | Build-profile gate and bounded codes/identities only. |
| Accessibility denial | Toggle/glyph-only controls | Exact names/states, keyboard/D-pad, focus restoration, 48dp targets. |
</threat_model>

<verification>Run lifecycle/runtime/settings/test-control suites, typecheck, and lint. Native sound/vibration behavior is proved only after Plan 02-34's exact build and Plan 02-35's physical review.</verification>
<success_criteria>D-61/G-02-07 closes in code with persisted independent controls and atomically queued, durably claimed at-most-one foreground feedback attempts; physical delivery after claim remains best-effort.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-31-SUMMARY.md` when complete.</output>
