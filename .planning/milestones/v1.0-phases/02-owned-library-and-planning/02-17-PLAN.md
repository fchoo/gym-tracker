---
phase: 02-owned-library-and-planning
plan: 17
type: execute
wave: 9
depends_on: [02-10, 02-12, 02-13]
files_modified:
  - app/library/exercise/create.tsx
  - app/library/exercise/[exerciseId].tsx
  - app/library/exercise/[exerciseId]/edit.tsx
  - maestro/phase2/custom-exercise-lifecycle.yaml
  - src/bootstrap/customExerciseRuntime.tsx
  - src/ui/__tests__/CustomExerciseScreens.test.tsx
  - src/ui/screens/ExerciseDetailScreen.tsx
  - src/ui/screens/ExerciseEditorScreen.tsx
  - src/ui/screens/LibraryScreen.tsx
  - src/ui/screens/MetricMigrationScreen.tsx
autonomous: true
requirements: [LIB-05]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-15 detail shows Built-in/Custom when relevant plus source pack, revision, license, attribution, Unavailable, Archived, and metric-aware Best/Average/Last."
    - "D-17 duplicate warning preserves entered values, displays matches, and requires explicit Create anyway."
    - "D-27/D-28/D-29 UI supports hide/show, Create custom copy, archive/restore, affected-plan preview, no permanent delete, and runnable Archived references."
    - "D-33 makes all nine profiles explicit with examples/comparison behavior and no preselection/default; manual Hold is explicit."
    - "D-34, D-35, D-36, D-37, D-38, and D-39 metric migration UI blocks active workouts, explains future targets/history immutability, collects every replacement/policy, and confirms one-way Save profile change."
    - "Library `Create custom exercise` opens `app/library/exercise/create.tsx` and completes ordinary creation as a distinct path from built-in `Create custom copy`."
    - "The installed custom lifecycle flow proves ordinary create, custom-copy create, edit, archive/restore, and profile migration."
    - "All eight UI truths are covered for detail/editor/migration: Empty, Loading, Error, Populated, Partial, Overflow, Zero / one / many, and Long text."
  artifacts:
    - path: "src/ui/screens/ExerciseDetailScreen.tsx"
      provides: "Attribution/history/visibility/custom-copy/archive exercise detail"
    - path: "src/ui/screens/ExerciseEditorScreen.tsx"
      provides: "Explicit-profile custom create/edit with duplicate confirmation"
    - path: "src/ui/screens/MetricMigrationScreen.tsx"
      provides: "Complete future-target profile migration review and one-way confirmation"
    - path: "maestro/phase2/custom-exercise-lifecycle.yaml"
      provides: "Installed ordinary create/custom-copy/edit/archive/restore/profile-migration journey"
  key_links:
    - from: "src/ui/screens/ExerciseEditorScreen.tsx"
      to: "src/bootstrap/customExerciseRuntime.tsx"
      via: "complete custom draft and duplicate decision submit Save exercise"
      pattern: "createCustomExercise|editCustomExercise"
    - from: "src/ui/screens/MetricMigrationScreen.tsx"
      to: "src/domains/metrics/migrateCustomExerciseMetricProfile.ts"
      via: "complete replacement map/policy and one-way acknowledgement"
      pattern: "migrateCustomExerciseMetricProfile"
---

<objective>
Deliver exercise detail, custom editor/lifecycle, and explicit metric-profile migration UI.

Purpose: Make LIB-05 fully usable while preserving bundled authority, entered values, affected plans, and immutable metric history.
Output: Thin routes, three focused screens, runtime wiring, and complete component coverage.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-12-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-13-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/index.ts
@src/ui/components/MetricProfileOption.tsx
@src/ui/screens/LibraryScreen.tsx
@src/bootstrap/workoutAppRuntime.tsx
</context>

## Artifacts this phase produces

- `src/ui/screens/ExerciseDetailScreen.tsx` — `ExerciseDetailScreen`.
- `src/ui/screens/ExerciseEditorScreen.tsx` — `ExerciseEditorScreen`.
- `src/ui/screens/MetricMigrationScreen.tsx` — `MetricMigrationScreen`.
- `src/bootstrap/customExerciseRuntime.tsx` — custom exercise/detail/profile-migration runtime adapter over the base app runtime.
- `app/library/exercise/create.tsx` — thin ordinary custom-create route.
- `app/library/exercise/[exerciseId].tsx` — thin detail route.
- `app/library/exercise/[exerciseId]/edit.tsx` — thin edit/migration route.
- `src/ui/__tests__/CustomExerciseScreens.test.tsx` — exact lifecycle/migration/eight-state/adaptive tests.
- `maestro/phase2/custom-exercise-lifecycle.yaml` — installed ordinary create/custom-copy/edit/archive/restore/profile-migration flow.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create one ordinary explicit-profile custom exercise, then prove custom copy remains distinct</name>
  <read_first>src/ui/screens/LibraryScreen.tsx, src/ui/components/MetricProfileOption.tsx, src/ui/components/index.ts, src/bootstrap/workoutAppRuntime.tsx, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md</read_first>
  <files>src/bootstrap/customExerciseRuntime.tsx, src/ui/screens/LibraryScreen.tsx, src/ui/screens/ExerciseEditorScreen.tsx, app/library/exercise/create.tsx, src/ui/__tests__/CustomExerciseScreens.test.tsx</files>
  <action>Start red with Library exact action `Create custom exercise` opening the thin ordinary-create route. Create `customExerciseRuntime` in this tracer before the route/screen consumes it, expose ordinary create and custom-copy draft origins separately, require explicit MetricProfileOption with no preselection and explicit manual Hold when no policy is configured, and commit only through `Save exercise`. Implement D-17 duplicate warning with retained values, existing matches, and `Create anyway`; preserve noun-specific Save/dirty behavior, adaptive panes, focus restoration, 48dp targets, and no platform imports in routes/screens. Keep the ordinary-create intent distinct from the custom-copy origin consumed by the next task.
  </action>
  <acceptance_criteria>Library ordinary create opens and commits through `app/library/exercise/create.tsx`; it carries an ordinary-create origin distinct from custom copy, does not default the metric profile, and duplicate warning never loses values.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/CustomExerciseScreens.test.tsx -t "ordinary create|custom copy|duplicate"</automated>
  </verify>
  <done>The narrowest Library-to-ordinary-create path is runtime-backed before use, and custom copy is explicitly distinct and ownership-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete detail/edit/hide/archive/restore and D-34 migration UI</name>
  <read_first>src/ui/screens/ExerciseEditorScreen.tsx, src/ui/__tests__/CustomExerciseScreens.test.tsx, src/ui/components/index.ts, src/bootstrap/customExerciseRuntime.tsx, src/bootstrap/workoutAppRuntime.tsx</read_first>
  <files>src/ui/screens/ExerciseDetailScreen.tsx, src/ui/screens/MetricMigrationScreen.tsx, app/library/exercise/[exerciseId].tsx, app/library/exercise/[exerciseId]/edit.tsx, src/ui/__tests__/CustomExerciseScreens.test.tsx</files>
  <action>Wire D-15 detail attribution/history and custom edit through thin routes, including built-in exact `Create custom copy` prepopulation through the runtime's distinct custom-copy origin and fresh user-owned identity. Add bundled hide/show and custom archive/restore with affected-plan preview and no delete action per D-27..D-29. Existing archived references remain visible/runnable with `Archived`; unavailable built-ins retain attribution. Build D-34..D-39 migration UI: block when an active workout uses the exercise, state that future plan targets migrate while history never changes, enumerate every affected occurrence, require explicit valid replacement defaults and new policy/manual Hold, show recommendation invalidation/fresh baseline, and require a one-way ConfirmationSheet before exact `Save profile change`. Cover all eight UI truths, long notes/many occurrences, 200% stacking, keyboard/D-pad/focus, reduced motion, non-color labels, disabled reasons, and save-error value retention.</action>
  <acceptance_criteria>No lifecycle action deletes identity; metric migration cannot submit incomplete/inferred replacements or active-use changes; committed result updates future facts only.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/CustomExerciseScreens.test.tsx</automated>
  </verify>
  <done>Custom exercise and future-only metric migration UI satisfy every LIB-05 decision and adaptive state.</done>
</task>

<task type="auto">
  <name>Task 3: Author the installed custom create/edit/archive/profile-migration journey</name>
  <read_first>src/ui/__tests__/CustomExerciseScreens.test.tsx, app/library/exercise/create.tsx, app/library/exercise/[exerciseId].tsx, app/library/exercise/[exerciseId]/edit.tsx, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md</read_first>
  <files>maestro/phase2/custom-exercise-lifecycle.yaml</files>
  <action>Author the public semantic Maestro flow for exact `Create custom exercise` ordinary creation, built-in `Create custom copy`, custom edit, archive, restore, active-workout profile-migration block, complete replacement defaults and policy/manual Hold, and confirmed `Save profile change`. Assert ordinary create and custom copy produce distinct owned identities and never use private routes, test IDs, SQL, or direct repository controls.</action>
  <acceptance_criteria>The YAML covers ordinary create, custom copy, edit, archive/restore, and D-34 migration with exact public labels and distinct creation paths.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('maestro/phase2/custom-exercise-lifecycle.yaml','utf8'); for(const x of ['Create custom exercise','Create custom copy','Archived','Save profile change']) if(!s.includes(x)) process.exit(1)"</automated>
  </verify>
  <done>The installed lifecycle flow is complete and ready for unchanged-APK execution in Plan 02-21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Form/preview values → custom/profile commands | Incomplete or stale drafts must not mutate source. |
| Bundled/custom/history facts → detail UI | UI must expose authority and state without allowing forbidden edits. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | bundled/custom actions | high | mitigate | Runtime capability separation and no bundled edit/delete UI. |
| T-02-04 | Tampering | editor/migration revisions | high | mitigate | Complete drafts, preview tokens, expected revisions, post-commit readback. |
| T-02-05 | Tampering | profile/history meaning | high | mitigate | Explicit identity/replacements, active block, immutable history explanation. |
| T-02-07 | Information Disclosure | detail/errors | medium | mitigate | Attribution is intentional; diagnostics omit notes/targets/observations. |
</threat_model>

<verification>
Run focused and full component tests; installed custom lifecycle is covered through the final combined Phase 2 runner.
</verification>

<success_criteria>
The owner can inspect, create, edit, hide, archive, restore, copy, and explicitly migrate custom exercise future targets safely.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-17-SUMMARY.md` when done.
</output>
