---
phase: 02-owned-library-and-planning
plan: 02
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domains/metrics/aggregates.ts
  - src/domains/metrics/comparators.ts
  - src/domains/metrics/contracts.ts
  - src/domains/metrics/exposure.ts
  - src/domains/metrics/metricContracts.test.ts
  - src/domains/metrics/observations.ts
  - src/domains/metrics/registry.ts
autonomous: true
requirements: [LIB-11, LIB-12]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
assumption_delta:
  status: promoted
  primary_identity: "(profile, contractVersion, exerciseMetricGeneration)"
  invariant: "Every supported identity round-trips through target, snapshot, observation, comparator, aggregate, exposure, and presentation without reinterpretation."
must_haves:
  truths:
    - "E-64 through E-78 are mapped to executable metric table tests with requirement/category traceability."
    - "The registry contains exactly nine approved profiles with versioned target and observation contracts, comparator direction, average population, tie order, precision, and comparable-exposure rules."
    - "Legacy load_reps contract 1 remains grams/repetitions and legacy timed_hold contract 1 remains durationSeconds; millisecond timed holds use a later contract version."
    - "Best/Average/Last never crosses profile, contract version, exercise metric generation, or fixed protocol boundaries."
  prohibitions:
    - "Workout and metric feedback must not diagnose fatigue, injury, weakness, health, or worth, and must not use shaming or moral-failure framing."
  artifacts:
    - path: "src/domains/metrics/registry.ts"
      provides: "Nine-profile registry keyed by complete metric identity"
    - path: "src/domains/metrics/metricContracts.test.ts"
      provides: "Invariant, boundary, comparator, aggregate, tie, precision, and exposure table"
  key_links:
    - from: "src/domains/metrics/contracts.ts"
      to: "src/domains/metrics/registry.ts"
      via: "strict identity and target/observation contract registration"
      pattern: "contractVersion|exerciseMetricGeneration"
    - from: "src/domains/metrics/registry.ts"
      to: "src/domains/metrics/comparators.ts"
      via: "registered comparator and aggregate strategy IDs"
      pattern: "comparator|aggregate"
---

<objective>
Define the complete pure metric meaning for all approved starter and workout observations.

Purpose: Make LIB-11/LIB-12 deterministic before persistence or UI can reinterpret historical bytes.
Output: Versioned identities, strict observations/targets, comparator/aggregate/exposure functions, and complete invariant tables.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/domains/progression/loadRepsV1.ts
@src/domains/workout/activeWorkout.ts
</context>

## Artifacts this phase produces

- `src/domains/metrics/contracts.ts` — `MetricIdentity`, nine profile names, versioned targets, and presentation contracts.
- `src/domains/metrics/observations.ts` — strict observation parsing and target/observation compatibility.
- `src/domains/metrics/registry.ts` — complete registry and legacy contract preservation.
- `src/domains/metrics/comparators.ts` — comparator/tie behavior.
- `src/domains/metrics/aggregates.ts` — finite aggregate populations and presentation rounding.
- `src/domains/metrics/exposure.ts` — comparable exposure predicate.
- `src/domains/metrics/metricContracts.test.ts` — all-profile invariant and E-64..E-78 tables.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Round-trip one legacy timed hold and one new profile through the complete metric identity</name>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/progression/loadRepsV1.ts, .planning/phases/02-owned-library-and-planning/02-RESEARCH.md, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/metrics/contracts.ts, src/domains/metrics/observations.ts, src/domains/metrics/registry.ts, src/domains/metrics/metricContracts.test.ts</files>
  <action>Start red with an invariant that parses and serializes timed_hold contract 1 `durationSeconds` unchanged and a new bodyweight_reps identity through target, observation, and registry lookup. Promote the assumption-delta identity `(profile, contractVersion, exerciseMetricGeneration)` as the sole generalized key. Register load_reps, bodyweight_reps, added_load_reps, assisted_reps, timed_hold, fixed_distance, fixed_time, intervals, and unscored. Preserve legacy bytes and assign millisecond timed holds a later contract version chosen explicitly by callers. Cover E-64 through E-67 and E-71 through E-75 with exact min/max/one-step-invalid, empty, identity-adjacency, stable-order, and encoding cases; reject unknown profile/version/generation combinations before any persistence consumer can see them.</action>
  <acceptance_criteria>All nine profiles have strict target/observation contracts; legacy seconds remain seconds; every registered identity round-trips losslessly and unknown combinations return typed boundary errors.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/metrics/metricContracts.test.ts -t "identity"</automated>
  </verify>
  <done>The thinnest legacy/new-profile invariant proves the full identity architecture and every profile is registered explicitly.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete comparator, aggregate, tie, precision, and comparable-exposure contracts</name>
  <read_first>src/domains/metrics/contracts.ts, src/domains/metrics/observations.ts, src/domains/metrics/registry.ts, src/domains/metrics/metricContracts.test.ts, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md</read_first>
  <files>src/domains/metrics/comparators.ts, src/domains/metrics/aggregates.ts, src/domains/metrics/exposure.ts, src/domains/metrics/metricContracts.test.ts</files>
  <action>Write table tests first for every profile's better/worse/equal direction, lexicographic tie order, average population, empty and single populations, fixed-distance/time and interval protocol comparability, and D-37 segmentation. Implement bounded integer atomic values, finite aggregate math, and presentation-only rounding that never becomes an observation or target, covering E-68/E-70/E-76/E-78 as well as the precision and concurrency-relevant pure rejection behavior. Use neutral factual result labels only; do not infer health, fatigue, weakness, or worth.</action>
  <acceptance_criteria>Every profile has deterministic Best/Average/Last behavior; ties and rounding match the research matrix; no cross-identity or cross-protocol exposure is comparable.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/metrics/metricContracts.test.ts -t "comparator|aggregate|exposure"</automated>
  </verify>
  <done>The metric registry completely defines comparison, aggregation, precision, ties, and exposure for LIB-11/LIB-12.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Persisted JSON → metric parser | Historical or malformed values enter strict versioned contracts. |
| Metric result → owner-facing copy | Factual workout values must not become health diagnosis or moral judgment. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | metric identity/revision | high | mitigate | Complete identity keys and typed rejection of unknown/stale contracts. |
| T-02-05 | Tampering | legacy interpretation | high | mitigate | Retain legacy parsers and bytes; new units require a later contract version. |
| T-02-07 | Information Disclosure | metric diagnostics | medium | mitigate | Errors expose stable codes only, never raw observations or targets. |
</threat_model>

<verification>
Run the single pure table suite and inspect that all nine profiles, both legacy contracts, all ties, and E-64..E-78 IDs are enumerated.
</verification>

<success_criteria>
Metric meaning is complete, versioned, deterministic, neutral, and independent of SQLite/UI.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-02-SUMMARY.md` when done.
</output>
