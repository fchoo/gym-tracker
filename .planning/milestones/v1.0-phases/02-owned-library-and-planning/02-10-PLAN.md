---
phase: 02-owned-library-and-planning
plan: 10
type: execute
wave: 7
depends_on: [02-07, 02-11]
files_modified:
  - src/bootstrap/workoutAppRuntime.tsx
  - src/domains/workout/activeWorkout.ts
  - src/domains/workout/index.ts
  - src/domains/workout/sessionDetail.ts
  - src/domains/workout/setCommands.test.ts
  - src/domains/workout/setCommands.ts
  - src/platform/sqlite/repositories/plansWorkoutRepository.ts
  - src/platform/sqlite/repositories/workoutOutcomeRepository.ts
  - src/platform/sqlite/repositories/workoutRepository.ts
  - src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx
  - src/ui/components/MetricProfileOption.tsx
  - src/ui/components/SetRow.tsx
  - src/ui/screens/ActiveWorkoutScreen.tsx
  - tests/integration/cross-profile-workout.test.ts
autonomous: true
requirements: [LIB-11, LIB-12]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "All nine approved profiles have one authoritative workout contract and adapter path across session creation, active read, draft/complete/skip commands, detail/outcome readback, shared SetRow, and ActiveWorkoutScreen."
    - "Every profile keeps inline values, adjacent Complete/Skip, and direct Retry below the set list after a failed save; entered values survive failure."
    - "Fixed distance/time/protocol values remain labelled and non-editable; units are fixed suffixes; warm-ups remain excluded from records/progression."
    - "Complete, Skip, and direct Retry are component/integration tested for every profile with immutable snapshot identity and commit-before-acknowledgement."
    - "ValueEditorSheet and WorkoutActionDock remain retired and absent."
  prohibitions:
    - "Workout and metric feedback must not diagnose fatigue, injury, weakness, health, or worth, and must not use shaming or moral-failure framing."
  artifacts:
    - path: "src/domains/workout/activeWorkout.ts"
      provides: "Authoritative nine-profile active session target/observation and command contracts"
    - path: "src/platform/sqlite/repositories/workoutRepository.ts"
      provides: "Registry-parsed nine-profile active workout read/write adapter"
    - path: "src/ui/components/MetricProfileOption.tsx"
      provides: "Plain-language profile choice with example and comparator behavior"
    - path: "src/ui/components/SetRow.tsx"
      provides: "Nine-profile inline fields and adjacent Complete/Skip"
    - path: "tests/integration/cross-profile-workout.test.ts"
      provides: "All-profile commit/skip/retry and snapshot proof"
  key_links:
    - from: "src/platform/sqlite/repositories/plansWorkoutRepository.ts"
      to: "src/platform/sqlite/repositories/workoutRepository.ts"
      via: "session creation snapshots the same complete metric identity and versioned target parsed by active reads"
      pattern: "contractVersion|metricGeneration"
    - from: "src/ui/components/SetRow.tsx"
      to: "src/domains/workout/activeWorkout.ts"
      via: "profile field descriptor consumes the authoritative versioned SetTarget/SetObservation union"
      pattern: "MetricIdentity|registry"
    - from: "src/ui/screens/ActiveWorkoutScreen.tsx"
      to: "src/bootstrap/workoutAppRuntime.tsx"
      via: "existing update/complete/skip/retry commands over immutable session snapshots"
      pattern: "completeSet|skipWorkingSet|retry"
---

<objective>
Run every approved metric profile through authoritative workout contracts/adapters and the Phase 1 shared inline interaction.

Purpose: Satisfy LIB-11/LIB-12 without reviving retired editors or weakening exactly-once commit behavior.
Output: Nine-profile session creation/read/detail/outcome contracts and adapters, MetricProfileOption, SetRow/ActiveWorkout/runtime integration, and focused tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-07-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-11-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/SetRow.tsx
@src/ui/screens/ActiveWorkoutScreen.tsx
@src/bootstrap/workoutAppRuntime.tsx
@src/domains/metrics/registry.ts
</context>

## Artifacts this phase produces

- `src/ui/components/MetricProfileOption.tsx` — `MetricProfileOption`.
- `src/domains/workout/activeWorkout.ts` — authoritative nine-profile session target, observation, active-read, and command contracts.
- `src/domains/workout/index.ts` — public workout exports and nine-profile Today/start contracts.
- `src/domains/workout/setCommands.ts` — registry-backed draft/complete/skip validation.
- `src/platform/sqlite/repositories/plansWorkoutRepository.ts` — nine-profile session snapshot creation.
- `src/platform/sqlite/repositories/workoutRepository.ts` — nine-profile active session parsing and commands.
- `src/platform/sqlite/repositories/workoutOutcomeRepository.ts` — nine-profile session detail/outcome parsing.
- `src/domains/workout/sessionDetail.ts` — versioned metric-aware detail contracts.
- `src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx` — all-profile fields, Complete/Skip/Retry, layout, semantics, and copy.
- `tests/integration/cross-profile-workout.test.ts` — snapshot/read/write and failure behavior.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Read one non-legacy persisted set through the authoritative contract into shared SetRow</name>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/workout/index.ts, src/platform/sqlite/repositories/workoutRepository.ts, src/ui/components/SetRow.tsx, src/domains/metrics/registry.ts</read_first>
  <files>src/domains/workout/activeWorkout.ts, src/domains/workout/index.ts, src/platform/sqlite/repositories/workoutRepository.ts, src/ui/components/SetRow.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx</files>
  <action>Start red with an added_load_reps target/observation carrying complete profile, contract version, and exercise metric generation from a persisted session row through the repository parser and public workout export into the existing shared SetRow. Replace the two-profile unions and repository branches with registry-backed authoritative target/observation parsing for all nine profiles while preserving legacy load/reps and timed-hold contract bytes. Render the tracer with fixed unit suffixes and labelled non-editable planned dimensions where required; do not create profile-specific screens or permit UI-only shapes that the repository cannot parse.</action>
  <acceptance_criteria>One added-load session set round-trips from SQLite-shaped data to the shared SetRow under the authoritative versioned union, while legacy contracts remain byte-compatible.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx -t "added load"</automated>
  </verify>
  <done>The narrowest persisted non-legacy set proves one contract from repository read to shared UI.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close authoritative session creation, commands, detail, and outcome adapters for all nine profiles</name>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/workout/setCommands.ts, src/platform/sqlite/repositories/plansWorkoutRepository.ts, src/platform/sqlite/repositories/workoutRepository.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts</read_first>
  <files>src/domains/workout/setCommands.ts, src/domains/workout/setCommands.test.ts, src/platform/sqlite/repositories/plansWorkoutRepository.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/cross-profile-workout.test.ts</files>
  <action>Write matched unit and integration tables first. Make session creation snapshot exact versioned targets/observations for load_reps, bodyweight_reps, added_load_reps, assisted_reps, timed_hold legacy/later contracts, fixed_distance, fixed_time, intervals, and unscored. Validate drafts and completed observations through the registry in setCommands, then make active completion/skip and workout outcome/detail reads preserve the same identity and profile-specific values. Cover valid/invalid boundaries, immutable snapshot bytes, expected revisions, already-completed idempotency, commit-before-haptic/invalidation/effects, fixed planned dimensions, warm-up exclusion, and no cross-profile aggregation or reinterpretation.</action>
  <acceptance_criteria>All nine profiles share one authoritative creation/write/outcome path; no adapter accepts a value another adapter cannot read, and no profile bypasses exactly-once source commits.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/workout/setCommands.test.ts &amp;&amp; npm run test:integration -- --runInBand tests/integration/cross-profile-workout.test.ts -t "snapshot|complete|skip|detail|outcome"</automated>
  </verify>
  <done>Session creation, active commands, detail, and outcome adapters are authoritative for every profile before UI expansion.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Close shared inline Complete, Skip, and direct Retry UI for all nine profiles</name>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/workout/sessionDetail.ts, src/ui/components/SetRow.tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/bootstrap/workoutAppRuntime.tsx</read_first>
  <files>src/domains/workout/sessionDetail.ts, src/ui/components/MetricProfileOption.tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx, src/bootstrap/workoutAppRuntime.tsx</files>
  <action>Expand the component table over the authoritative adapters for all nine profiles. Add MetricProfileOption for explicit profile selection elsewhere; keep active rendering descriptor-driven from the versioned contract. Preserve inline entered values, fixed suffixes, labelled non-editable planned distance/time/protocol, adjacent `Complete {set}` and `Skip {set}`, direct Retry below the set list after failure, value-source order, 200% stacking, focus, 48dp targets, warm-up exclusions, and neutral Best/Average/Last/detail formatting. Assert retired editor/dock imports and elements remain absent and no success state appears before committed readback.</action>
  <acceptance_criteria>All profiles pass Complete/Skip/Retry component matrices against authoritative contract shapes; legacy UI remains unchanged and no profile-specific route/modal exists.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx &amp;&amp; npm run test:integration -- --runInBand tests/integration/cross-profile-workout.test.ts</automated>
  </verify>
  <done>LIB-12 is operational across the shared workout surface with authoritative adapters and complete regression protection.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Inline text input → versioned observation | Owner-entered numeric values cross strict metric validation. |
| UI command → committed workout source | Busy state cannot provide duplicate or pre-commit correctness. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | set expected revisions | high | mitigate | Preserve FIFO/expected revision/conditional update exactly-once defense. |
| T-02-05 | Tampering | profile interpretation | high | mitigate | Registry-selected versioned parser and immutable session metric identity. |
| T-02-07 | Information Disclosure | validation/diagnostics | medium | mitigate | Field-safe messages and stable codes; no observation payload logging. |
</threat_model>

<verification>
Run focused component tracer and all-profile integration tests; grep changed UI for retired editor/dock names.
</verification>

<success_criteria>
Every approved metric profile is usable offline through shared inline SetRow behavior with exactly-once Complete/Skip/Retry.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-10-SUMMARY.md` when done.
</output>
