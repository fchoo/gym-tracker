---
phase: 02-owned-library-and-planning
plan: 11
type: execute
wave: 6
depends_on: [02-06, 02-07, 02-08]
files_modified:
  - src/domains/plans/ownedPlanCommands.test.ts
  - src/domains/plans/ownedPlanCommands.ts
  - src/platform/sqlite/migrations/0009_owned_plans.ts
  - src/platform/sqlite/migrations/index.ts
  - src/platform/sqlite/repositories/ownedPlanRepository.ts
  - tests/integration/owned-plan-crud.test.ts
  - tests/migrations/fixtures/v8-schedule-activation.sql
  - tests/sqlite-host/owned-plans.test.ts
autonomous: true
requirements: [LIB-08]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "E-47 through E-52 are executable plan aggregate tests for identity collisions, empty drafts, Unicode names, stable order, idempotent requests, and serialized revision conflicts."
    - "D-21/D-22 create explicit inactive drafts with one named empty day allowed, exact missing requirement, and schedule/activation disabled until one valid targeted exercise exists."
    - "D-23 through D-26 and D-28 are repository/domain facts: noun-specific aggregate Save, dirty state stays process-local, reorder saves only with aggregate Save, duplication copies the full graph into fresh IDs and stays inactive, archive/restore has no deletion."
    - "D-30/D-31 preserve active schedule unless structural impact is explicitly handled later, and immutable in-progress snapshots remain unaffected."
  artifacts:
    - path: "src/platform/sqlite/migrations/0009_owned_plans.ts"
      provides: "Forward owned-plan draft/lifecycle/revision schema"
    - path: "src/domains/plans/ownedPlanCommands.ts"
      provides: "Create/save/duplicate/reorder/archive/restore aggregate commands"
    - path: "src/platform/sqlite/repositories/ownedPlanRepository.ts"
      provides: "Atomic expected-revision owned-plan repository"
  key_links:
    - from: "src/domains/plans/ownedPlanCommands.ts"
      to: "src/platform/sqlite/repositories/ownedPlanRepository.ts"
      via: "complete aggregate with expected revisions and request identity"
      pattern: "saveOwnedPlan|duplicateOwnedPlan"
    - from: "src/platform/sqlite/repositories/ownedPlanRepository.ts"
      to: "src/platform/sqlite/migrations/0009_owned_plans.ts"
      via: "draft/lifecycle/ordinal/revision source tables"
      pattern: "plan_revision|ordinal"
---

<objective>
Implement owned-plan draft, CRUD, duplication, reorder, archive, and restore without UI.

Purpose: Establish the atomic LIB-08 aggregate consumed by the later editor and schedule-impact slices.
Output: Migration 0009, complete plan commands/repository, retained fixture, and host/integration proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-08-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/domains/plans/index.ts
@src/platform/sqlite/migrations/0008_schedule_activation.ts
@src/platform/sqlite/migrations/index.ts
@src/platform/sqlite/serializedWriter.ts
</context>

## Artifacts this phase produces

- `src/platform/sqlite/migrations/0009_owned_plans.ts` — draft/lifecycle/revision/ordinal constraints.
- `src/platform/sqlite/migrations/index.ts` — final ordered runtime manifest through 0009.
- `tests/migrations/fixtures/v8-schedule-activation.sql` — immutable post-0008 fixture.
- `tests/sqlite-host/owned-plans.test.ts` — retained migration/schema/query proof.
- `src/domains/plans/ownedPlanCommands.ts` — `createOwnedPlanDraft`, `saveOwnedPlan`, `duplicateOwnedPlan`, `archiveOwnedPlan`, `restoreOwnedPlan`.
- `src/domains/plans/ownedPlanCommands.test.ts` — aggregate validation and E-47..E-52 tables.
- `src/platform/sqlite/repositories/ownedPlanRepository.ts` — serialized repository.
- `tests/integration/owned-plan-crud.test.ts` — commit/rollback/idempotency/fresh-ID proof.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create and save one inactive plan draft with one named empty day</name>
  <read_first>src/domains/plans/index.ts, src/platform/sqlite/migrations/0008_schedule_activation.ts, src/platform/sqlite/migrations/index.ts, src/platform/sqlite/serializedWriter.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/platform/sqlite/migrations/0009_owned_plans.ts, src/platform/sqlite/migrations/index.ts, tests/migrations/fixtures/v8-schedule-activation.sql, tests/sqlite-host/owned-plans.test.ts</files>
  <action>Start with a retained-v8 host test that creates an inactive user-owned draft with a Unicode plan name and one named empty day, reads exact missing-valid-target reason, then saves a valid graph under expected revision. Export 0009 as the direct successor of 0008 and update the sole runtime manifest after all prerequisite migration modules exist: existing 0001-0003 followed by 0004, 0005, 0006, 0008, and 0009. Add lifecycle/draft/revision/ordinal constraints needed for aggregate edits and archive/restore while sharing schedule source tables from 0008. Enforce no permanent delete and no second active schedule. Cover migration-side E-47..E-52 identity adjacency, empty/single graph, stable order, Unicode, and revision conflict cases.</action>
  <acceptance_criteria>An empty named draft is valid and inactive with a precise scheduling blocker; a complete graph saves atomically; retained source and schedules remain unchanged on failure.</acceptance_criteria>
  <verify>
    <automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/owned-plans.test.ts</automated>
  </verify>
  <done>The narrowest draft-to-valid-plan source path is migrated and revision-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete aggregate CRUD, duplication, reorder, archive, and restore</name>
  <read_first>src/platform/sqlite/migrations/0009_owned_plans.ts, tests/sqlite-host/owned-plans.test.ts, src/domains/plans/index.ts, src/platform/sqlite/serializedWriter.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/plans/ownedPlanCommands.ts, src/domains/plans/ownedPlanCommands.test.ts, src/platform/sqlite/repositories/ownedPlanRepository.ts, tests/integration/owned-plan-crud.test.ts</files>
  <action>Write D-21 through D-31 non-impact command tests before implementation. Commands accept complete plan/day/target drafts with expected revisions and request identity; fields and reorder changes are process-local until Save. Validate schedule eligibility per D-22. Duplicate the entire graph per D-26—days, order, targets, warm-ups, rest, policies, schedule defaults—into fresh IDs and inactive schedule. Archive/restore per D-28 without deletion. Permit active/in-progress plan future edits while preserving schedule and immutable workout snapshots; return `requires_schedule_impact` rather than writing structural changes owned by Plan 02-18. Test stale/repeated/parallel calls, commit failure, fresh IDs, stable ordinals, and exact invalidations.</action>
  <acceptance_criteria>Every ordinary plan mutation is aggregate-atomic; duplicate IDs are fresh and inactive; archive is reversible; structural impact is surfaced, not silently applied.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/owned-plan-crud.test.ts</automated>
  </verify>
  <done>Owned plan persistence fully supports the later editor without containing editor UI or impact/replacement behavior.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| In-memory editor draft → plan aggregate | Partial or stale fields must not persist independently. |
| Owned plan mutation → active schedule/snapshot | Ordinary edits cannot silently restructure schedule or current workout facts. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | owned/bundled authority | high | mitigate | Owned-origin predicates and no template mutation. |
| T-02-04 | Tampering | aggregate revisions | high | mitigate | Complete draft plus expected revisions in one serialized transaction. |
| T-02-05 | Tampering | schedule/snapshot meaning | high | mitigate | Preserve schedules, return impact requirement, never rewrite snapshots. |
| T-02-07 | Information Disclosure | plan diagnostics | medium | mitigate | Stable codes/counts/IDs only; no notes or target payloads. |
</threat_model>

<verification>
Run focused host and integration suites; verify E-47..E-52 and D-21..D-31 command cases.
</verification>

<success_criteria>
Owned plans are complete revisioned aggregates with safe draft, duplication, reorder, and lifecycle behavior ready for UI.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-11-SUMMARY.md` when done.
</output>
