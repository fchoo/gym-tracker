---
phase: 02-owned-library-and-planning
plan: 34
type: execute
wave: 20
depends_on: [02-33]
files_modified:
  - artifacts/native/phase2/build.json
  - artifacts/native/phase2/gym-tracker-phase2-devtest.apk
  - artifacts/native/phase2/zipalign.txt
  - artifacts/native/phase2/result.json
  - artifacts/native/phase2/maestro.json
  - artifacts/native/phase2/benchmark.json
  - artifacts/native/phase2/artifact-roundtrip.json
autonomous: true
gap_closure: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04, LIB-05, LIB-06, LIB-07, LIB-08, LIB-09, LIB-10, LIB-11, LIB-12]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "One clean committed implementation HEAD is frozen after all remediation and harness changes, with every commit ending in `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`."
    - "One development-test release APK is built exactly once from that HEAD, embeds the JS bundle, passes 16KiB alignment, and records full HEAD, mode-sensitive source digest, package, APK hash/size, and automated-device identity."
    - "Host/static/coverage, actual Expo SQLite, every Phase 1 plus Phase 2/remediation Maestro flow, both 100-sample benchmarks, and artifact roundtrip pass against those unchanged bytes with zero failures/skips."
    - "The automated-only verifier derives and reports complete requirement, D-01..D-67, remediation-case, surface/UI-truth, prohibition, native-case, and flow coverage."
    - "`attended/emulator-supplementary.json`, `attended/physical-result.json`, and `final-verification.json` remain absent; no physical result is inferred from automation."
  artifacts:
    - { path: "artifacts/native/phase2/build.json", provides: "Exact committed HEAD/source/APK/package/build-device manifest" }
    - { path: "artifacts/native/phase2/gym-tracker-phase2-devtest.apk", provides: "Single immutable candidate used by every automated and attended gate" }
    - { path: "artifacts/native/phase2/result.json", provides: "Source-derived actual Expo SQLite contract results" }
    - { path: "artifacts/native/phase2/maestro.json", provides: "All prior and remediation installed-flow results" }
    - { path: "artifacts/native/phase2/benchmark.json", provides: "Search and working-set latency evidence" }
    - { path: "artifacts/native/phase2/artifact-roundtrip.json", provides: "Retained/copied/installed APK identity equality" }
  key_links:
    - { from: "artifacts/native/phase2/build.json", to: "artifacts/native/phase2/result.json", via: "exact HEAD/source/APK/package/device identity", pattern: "base_head|source_tree_sha256|apk|package|device" }
    - { from: "artifacts/native/phase2/build.json", to: "artifacts/native/phase2/artifact-roundtrip.json", via: "retained/copied/installed SHA-256 equality", pattern: "matches|sha256" }
---

<objective>
Build one post-remediation exact-HEAD APK and regenerate every automated Phase 2 proof artifact.

Purpose: Replace the stale diagnostic APK with one immutable candidate ready for attended emulator and Samsung review.
Output: Exact build manifest/APK plus complete host, native, Maestro, benchmark, roundtrip, and automated-only evidence.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VALIDATION.md
@.planning/phases/02-owned-library-and-planning/COVERAGE.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-33-SUMMARY.md
@package.json
@scripts/build-current-native-test-apk.sh
@scripts/run-native-sqlite-contracts.mjs
@scripts/run-phase2-maestro.mjs
@scripts/benchmark-phase2.mjs
@scripts/verify-phase2-native-evidence.mjs
</context>

## Artifacts this phase produces

- One immutable exact-HEAD APK and build identity.
- Complete automated native, Maestro, benchmark, and roundtrip evidence.
- An automated-only seal that explicitly leaves attended/final evidence absent.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-01..LIB-12 | Full host/native/installed regression on final candidate | COVERED |
| CONTEXT | D-01..D-67 | Source-derived exact decision coverage | COVERED |
| VERIFICATION | G-02-01..G-02-09 | Automated portions exercised after all remediation | COVERED |
| UI-SPEC | Every amended surface and eight UI truths | Maestro plus host coverage, pending attended judgment | COVERED |

<tasks>

<task type="tracer">
  <name>Task 1: Freeze a clean implementation HEAD after all host gates</name>
  <read_first>package.json, .planning/phases/02-owned-library-and-planning/02-33-SUMMARY.md, scripts/source-tree-digest.mjs, scripts/build-current-native-test-apk.sh</read_first>
  <files>artifacts/native/phase2/build.json</files>
  <precondition>Plans 02-22 through 02-33 have substantive summaries and all their task commits use the exact required co-author trailer.</precondition>
  <action>Run typecheck, lint/boundaries, unit, component, sqlite-host, integration, coverage, and evidence-script fixtures from the current tree. Resolve only remediation-related failures through the owning plan before proceeding; do not patch during this plan. Confirm tracked worktree cleanliness, no generated android/ios paths are tracked, no untracked source affects the source digest, and every remediation commit ends with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`. Remove stale ignored Phase 2 generated evidence only through the existing build script's bounded artifact handling. Record the candidate full HEAD and mode-sensitive source digest for comparison after the build. Do not create attended or final evidence.</action>
  <acceptance_criteria>All host gates pass, the tracked implementation tree is clean and committed, and no source/config/test/flow change remains before the one build.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:unit -- --runInBand &amp;&amp; npm run test:components -- --runInBand &amp;&amp; npm run test:sqlite:host -- --runInBand &amp;&amp; npm run test:integration -- --runInBand &amp;&amp; npm run test:coverage -- --runInBand &amp;&amp; node --test scripts/phase2-evidence-scripts.test.mjs</automated></verify>
  <done>The implementation and evidence harness are frozen at one clean commit.</done>
</task>

<task type="auto">
  <name>Task 2: Build exactly one Phase 2 APK and run all producers</name>
  <read_first>scripts/build-current-native-test-apk.sh, scripts/run-native-sqlite-contracts.mjs, scripts/run-phase2-maestro.mjs, scripts/benchmark-phase2.mjs, scripts/verify-phase2-native-evidence.mjs</read_first>
  <files>artifacts/native/phase2/build.json, artifacts/native/phase2/gym-tracker-phase2-devtest.apk, artifacts/native/phase2/zipalign.txt, artifacts/native/phase2/result.json, artifacts/native/phase2/maestro.json, artifacts/native/phase2/benchmark.json, artifacts/native/phase2/artifact-roundtrip.json</files>
  <precondition>An API 36 test device/emulator is ready; Node/npm/Java/Android/Maestro pins are active; Task 1's HEAD and source modes are unchanged.</precondition>
  <action>Run the existing clean Phase 2 build command exactly once. Require embedded release JS, package `com.fchoo.gymtracker.devtest`, 16KiB alignment, full HEAD, mode-sensitive source digest, APK hash/size, and installed-byte equality. Without rebuilding, changing source/modes, or committing, run aggregate actual Expo SQLite contracts, every source-enumerated Phase 1/Phase 2/remediation Maestro flow, both 100-sample benchmarks at p95 ≤150ms and JS task ≤50ms, and temp-copy artifact roundtrip in that order. Stop on any failure and return to the owning implementation/harness plan rather than rebuilding over a partial chain.</action>
  <acceptance_criteria>All seven artifacts exist, share exact identities, and report zero failed/skipped cases or flows and passing thresholds.</acceptance_criteria>
  <verify><automated-native>npm run android:devtest:fresh -- --suite phase2 &amp;&amp; npm run test:sqlite:device -- --suite phase2 --manifest artifacts/native/phase2/build.json &amp;&amp; npm run test:maestro:phase2 -- --manifest artifacts/native/phase2/build.json &amp;&amp; npm run benchmark:phase2 -- --manifest artifacts/native/phase2/build.json --samples 100 --max-p95-ms 150 --max-js-task-ms 50 &amp;&amp; npm run verify:native:phase2 -- --manifest artifacts/native/phase2/build.json --roundtrip-temp --output artifacts/native/phase2/artifact-roundtrip.json</automated-native></verify>
  <done>One unchanged post-remediation APK has complete automated producer evidence.</done>
</task>

<task type="auto">
  <name>Task 3: Seal automated-only evidence and hand off exact identities</name>
  <read_first>artifacts/native/phase2/build.json, artifacts/native/phase2/result.json, artifacts/native/phase2/maestro.json, artifacts/native/phase2/benchmark.json, artifacts/native/phase2/artifact-roundtrip.json, scripts/verify-phase2-native-evidence.mjs</read_first>
  <files>artifacts/native/phase2/build.json, artifacts/native/phase2/result.json, artifacts/native/phase2/maestro.json, artifacts/native/phase2/benchmark.json, artifacts/native/phase2/artifact-roundtrip.json</files>
  <action>Confirm all artifacts are readable, mutually identity-equal, source-derived, and bound to the frozen HEAD including file modes. Run the automated-only verifier after every producer; require roundtrip, all canonical requirements/decisions/remediation cases/surface truths/prohibitions/native cases/flows, and no failures/skips. Assert `attended/emulator-supplementary.json`, `attended/physical-result.json`, and `final-verification.json` are absent. Record exact HEAD/source/APK/package/build-device identities in 02-34-SUMMARY.md. After this task, permit only normal GSD summary/state/roadmap/commit processing; do not rebuild or mutate evidence. Any metadata commit must end with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Automated-only verification passes from canonical ledgers, final/attended outputs remain absent, and the summary exposes exact immutable identities for Plan 02-35.</acceptance_criteria>
  <verify><automated>npm run verify:native:phase2 -- --manifest artifacts/native/phase2/build.json --automated-only --require-roundtrip</automated></verify>
  <done>The exact APK is sealed for attended review without pretending physical approval exists.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Source/APK tampering | Committed tree to build manifest | Clean-tree gate and mode-sensitive source digest. |
| Artifact substitution | Retained/copied/installed APK | SHA-256 equality and package identity in every artifact. |
| Evidence spoofing | Producer outputs to verifier | Source-derived exact ledgers, zero skips/failures, atomic output. |
| Information disclosure | Build/device evidence | Bounded metadata and hashed serial where persisted. |
| Denial/waste | Failed producer causes repeated builds | Build once; stop and return to owning plan on any source-level failure. |
</threat_model>

<verification>Run host gates before building, build exactly once, run all producers on unchanged bytes, and finish with automated-only verification. Normal GSD metadata commands may follow; the physical terminal seal is not part of this plan.</verification>
<success_criteria>A fresh exact-HEAD candidate has complete automated proof and no attended/final evidence.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-34-SUMMARY.md` with exact identities and source-derived counts.</output>
