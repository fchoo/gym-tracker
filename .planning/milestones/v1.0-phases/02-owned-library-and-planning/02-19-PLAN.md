---
phase: 02-owned-library-and-planning
plan: 19
type: execute
wave: 10
depends_on: [02-14, 02-15, 02-16]
files_modified:
  - app/library/plan/[planId]/schedule.tsx
  - maestro/phase2/schedule-cross-profile.yaml
  - src/bootstrap/scheduleRuntime.tsx
  - src/ui/__tests__/ScheduleEditor.test.tsx
  - src/ui/components/ScheduleBindingEditor.tsx
  - src/ui/screens/ScheduleEditorScreen.tsx
  - src/ui/screens/TodayScreen.tsx
  - tests/integration/today-schedule-workout.test.ts
autonomous: true
requirements: [LIB-01, LIB-07, LIB-08, LIB-09]
estimate:
  tokens: 40000
  raw_tokens: 40000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-40/D-41 schedule UI shows preselected starter suggestion or before/after effective LocalDate defaulting today and commits only with exact `Save schedule`."
    - "D-42/D-43 Today and workout integration expose `Repeat`, `Skip`, `Advance`, overrides/rest/Train anyway, and `Advance rotation after this workout` with exact pointer effects."
    - "D-44/D-45/D-46 pending override replacement is confirmed, consumed override is immutable, Weekday skip is date-only, and missed shows `Planned but not completed`."
    - "D-47/D-48/D-49 start LocalDate, stored-timezone DST intent, and exact `Follow device timezone from today` / `Keep current timezone` are visible and prospective."
    - "The public Maestro flow runs starter/owned schedules and cross-profile workouts without changing the four root destinations or current-session snapshots."
    - "All eight UI truths are covered for schedule editor/Today: Empty, Loading, Error, Populated, Partial, Overflow, Zero / one / many, and Long text."
  prohibitions:
    - "Missed-day, rest-day, schedule, and progression experiences must not shame, pressure, guilt, or punish the owner for resting, skipping, repeating, or changing intent."
  artifacts:
    - path: "src/ui/screens/ScheduleEditorScreen.tsx"
      provides: "Complete Weekday/Rotation/effective-date/override/timezone editor"
    - path: "src/ui/screens/TodayScreen.tsx"
      provides: "Effective schedule opportunity and planned-not-completed presentation"
    - path: "maestro/phase2/schedule-cross-profile.yaml"
      provides: "Installed schedule, Today, timezone, override, rotation, and cross-profile journey"
  key_links:
    - from: "src/ui/screens/ScheduleEditorScreen.tsx"
      to: "src/bootstrap/scheduleRuntime.tsx"
      via: "complete schedule draft/preview token submitted by Save schedule"
      pattern: "saveSchedule"
    - from: "src/ui/screens/TodayScreen.tsx"
      to: "src/platform/sqlite/repositories/scheduleRepository.ts"
      via: "runtime effective opportunity read model"
      pattern: "effectiveOpportunity"
---

<objective>
Integrate complete schedule editing and calendar-safe Today/workout behavior into the public app.

Purpose: Make LIB-09 and relevant LIB-01/LIB-07/LIB-08 schedule actions visible without silent rewrites or punitive copy.
Output: Schedule route/editor/bindings, Today/runtime integration, component/integration tests, and public cross-profile Maestro journey.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-14-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-15-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-16-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/index.ts
@src/ui/screens/TodayScreen.tsx
@src/bootstrap/workoutAppRuntime.tsx
</context>

## Artifacts this phase produces

- `src/ui/components/ScheduleBindingEditor.tsx` — Weekday/Rotation binding editor.
- `src/ui/screens/ScheduleEditorScreen.tsx` — initial/later schedule, overrides, pointer, timezone UI.
- `app/library/plan/[planId]/schedule.tsx` — thin schedule route.
- `src/ui/__tests__/ScheduleEditor.test.tsx` — exact labels/eight UI truths/adaptive/input tests.
- `tests/integration/today-schedule-workout.test.ts` — Today/start/finish/schedule pointer integration.
- `maestro/phase2/schedule-cross-profile.yaml` — public installed schedule and cross-profile flow.
- `src/bootstrap/scheduleRuntime.tsx` — schedule editor/Today runtime adapter over the base app runtime.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Save one prospective schedule and start its effective Today workout</name>
  <read_first>src/ui/screens/TodayScreen.tsx, src/bootstrap/workoutAppRuntime.tsx, src/ui/components/index.ts, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/ui/components/ScheduleBindingEditor.tsx, src/ui/screens/ScheduleEditorScreen.tsx, app/library/plan/[planId]/schedule.tsx, src/ui/__tests__/ScheduleEditor.test.tsx, src/bootstrap/scheduleRuntime.tsx</files>
  <action>Start red with an owned plan opening a thin schedule route, showing the complete current/before-after schedule draft effective today, switching/editing Weekday/Rotation bindings, committing through exact `Save schedule`, then returning to Today with the correct stored-timezone opportunity. Build ScheduleBindingEditor with exact segmented choices, date/binding validation, 48dp controls, keyboard/D-pad/focus restoration, and process-local draft. Reuse existing ConfirmationSheet. Initial activation uses D-40 suggestion; later edit uses D-41 before/after/effective LocalDate. Disable invalid Draft schedules with exact reason and preserve entered values on failure.</action>
  <acceptance_criteria>One complete schedule saves prospectively and Today shows the resulting opportunity; prior dates/facts remain unchanged and no field persists before Save schedule.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/ScheduleEditor.test.tsx -t "save schedule"</automated>
  </verify>
  <done>The narrowest schedule-editor-to-Today path is wired through the authoritative repository and exact Save contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete Today actions, overrides, timezone prompt, cross-profile flow, and Maestro</name>
  <read_first>src/ui/components/ScheduleBindingEditor.tsx, src/ui/screens/ScheduleEditorScreen.tsx, src/ui/__tests__/ScheduleEditor.test.tsx, src/ui/screens/TodayScreen.tsx, src/bootstrap/workoutAppRuntime.tsx</read_first>
  <files>src/ui/screens/ScheduleEditorScreen.tsx, src/ui/screens/TodayScreen.tsx, src/ui/__tests__/ScheduleEditor.test.tsx, tests/integration/today-schedule-workout.test.ts, maestro/phase2/schedule-cross-profile.yaml</files>
  <action>Wire D-42..D-49 through runtime: Repeat/Skip/Advance; scheduled completion auto-advance; plan-day/rest/explicit-skip overrides; Train anyway and explicit rotation advance intent; pending override confirmation; consumed `Used` immutable state; Weekday date-only skip; exact `Planned but not completed`; session start LocalDate across midnight; stored-zone DST; exact timezone prompt choices. Test Today/start/finish/relaunch and cross-profile starter/owned workouts with immutable snapshots. Cover all eight UI truths, compact/medium/expanded/landscape, 200% text, long names/zones, focus/keyboard/D-pad, reduced motion, non-color states, 48dp, and neutral non-shaming copy. Author public Maestro flow for Weekday/Rotation, Repeat/Skip/Advance, override/rest/Train anyway, timezone choice, midnight fixture controls, and multiple metric profiles.</action>
  <acceptance_criteria>Every visible action maps to one explicit source command/result; no implicit carry-forward/advance/rebind exists; exact labels and adaptive states pass; Maestro uses public controls only.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/today-schedule-workout.test.ts</automated>
  </verify>
  <done>Schedule UI and Today/workout integration completely expose LIB-09 and the public installed journey.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Schedule draft/action → repository command | Stale or incomplete UI state must not write source facts. |
| Device timezone/midnight → Today interpretation | Mutable device state cannot rewrite historical LocalDates or schedule intent. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-04 | Tampering | schedule preview/revisions | high | mitigate | Complete draft, preview token, expected revisions, authoritative readback. |
| T-02-05 | Tampering | advancement/timezone semantics | high | mitigate | Exact command mapping, stored zone, explicit prompt, immutable prior facts. |
| T-02-07 | Information Disclosure | Today/editor errors | medium | mitigate | Safe copy/codes; no raw observations or SQL. |
</threat_model>

<verification>
Run focused component and integration suites; exact-APK Maestro is executed and bound in Plan 02-21.
</verification>

<success_criteria>
The owner can edit schedules and act on Today across weekday, rotation, overrides, midnight, DST, and timezone changes without silent mutation.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-19-SUMMARY.md` when done.
</output>
