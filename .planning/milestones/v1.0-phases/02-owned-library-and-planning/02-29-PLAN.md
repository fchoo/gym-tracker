---
phase: 02-owned-library-and-planning
plan: 29
type: execute
wave: 16
depends_on: [02-28]
files_modified:
  - src/ui/components/RestDock.tsx
  - src/ui/screens/ActiveWorkoutScreen.tsx
  - src/ui/__tests__/RestDock.test.tsx
  - src/ui/__tests__/ActiveWorkoutScreen.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-07, LIB-08]
estimate: { tokens: 11000, raw_tokens: 11000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "D-60 and G-02-06 close with a collapsible/expandable in-app rest surface whose remaining time is visible in both states."
    - "Expanded controls appear in the exact order `Skip`, `Pause`/`Resume`, `−15`, `+15`; Skip and Pause/Resume use familiar glyphs with exact accessible names."
    - "Every same-row control has equal visible height, a minimum 48dp target, visible focus, keyboard/D-pad activation, and usable 200% text/landscape behavior."
    - "Skipping rest transitions directly to ready state and never displays `Rest skipped`; timer truth and expiry behavior remain SQLite/timestamp authoritative."
  artifacts:
    - { path: "src/ui/components/RestDock.tsx", provides: "Collapsible ordered rest timer and glyph controls" }
    - { path: "src/ui/screens/ActiveWorkoutScreen.tsx", provides: "Direct skip-to-ready transition without redundant notice" }
    - { path: "src/ui/__tests__/RestDock.test.tsx", provides: "Order, collapse, timer visibility, glyph, and accessibility coverage" }
  key_links:
    - { from: "src/ui/components/RestDock.tsx", to: "src/ui/screens/ActiveWorkoutScreen.tsx", via: "rest commands and direct ready-state rendering", pattern: "onSkip|onPause|onResume|onAdjust" }
---

<objective>
Make the in-app rest timer compact, collapsible, correctly ordered, and free of redundant skipped-state feedback.

Purpose: Preserve timer visibility and one-tap control while reducing screen obstruction during an active workout.
Output: Revised RestDock behavior and focused component tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-28-SUMMARY.md
@src/ui/components/RestDock.tsx
@src/ui/screens/ActiveWorkoutScreen.tsx
</context>

## Artifacts this phase produces

- Collapsed and expanded RestDock presentations with always-visible remaining time.
- Exact action ordering and glyph semantics.
- Direct skip-to-ready behavior without a `Rest skipped` notice.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-07, LIB-08 | Rest behavior while running user-owned plan workouts | COVERED |
| CONTEXT | D-60 | Collapse, order, glyphs, and no skip notice | COVERED |
| VERIFICATION | G-02-06 | Current expanded-only/incorrect-order/redundant-message findings | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add collapsed and expanded RestDock states with exact controls</name>
  <read_first>src/ui/components/RestDock.tsx, src/ui/__tests__/RestDock.test.tsx, src/domains/rest/restState.ts, src/ui/theme/index.ts</read_first>
  <files>src/ui/components/RestDock.tsx, src/ui/__tests__/RestDock.test.tsx</files>
  <action>Start with failing tests. Add an explicit collapsed/expanded local presentation state; both states must show the remaining time and paused/running semantic state. In expanded mode render controls in exact order Skip, Pause/Resume, −15, +15. Use installed lucide glyphs for Skip and Pause/Resume while retaining exact accessible names; provide an accessible expand/collapse control. Enforce equal visible height, 48dp targets, visible focus, Enter/Space and D-pad operation, 200% text reflow, landscape/safe-area compatibility, and reduced motion. Do not alter timestamp-derived rest calculations or authoritative commands. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Tests prove both presentations, always-visible time, paused/running labels, exact order, glyph accessibility, and all interaction targets.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/RestDock.test.tsx</automated></verify>
  <done>The timer can collapse without hiding time or changing rest truth.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Remove skipped-rest notice and prove lifecycle behavior</name>
  <read_first>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/components/RestDock.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx</read_first>
  <files>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx</files>
  <action>Remove `restSkipped` presentation state and all `Rest skipped` copy. After the authoritative skip command commits, render the normal ready/next-set state directly. Preserve busy/conflict handling, current set focus, timer cancellation, and persisted rest revision. Add tests for skip from running and paused states, rapid duplicate activation, collapse/expand around pause/resume/adjust/expiry, and absence of the notice. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Skip commits exactly once, moves directly to ready state, and no visual or accessibility tree contains `Rest skipped`.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/RestDock.test.tsx src/ui/__tests__/ActiveWorkoutScreen.test.tsx</automated></verify>
  <done>D-60/G-02-06 is complete without changing domain timer authority.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Duplicate command tampering | Rapid glyph taps to rest commands | Busy/revision guards and exactly-once command assertions. |
| State spoofing | Collapsed label to persisted rest state | Render directly from authoritative RestStateV1. |
| Denial of use | Collapsed controls hide time or become too small | Always-visible timer, 48dp controls, 200% text and landscape tests. |
| Focus loss | Expand/collapse removes keyboard context | Deterministic focus placement/restoration tests. |
</threat_model>

<verification>Run RestDock and Active Workout component suites, typecheck, and lint. Plan 02-34 owns updated APK evidence.</verification>
<success_criteria>G-02-06 closes with the exact requested control sequence, glyphs, collapse behavior, and no skipped-rest message.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-29-SUMMARY.md` when complete.</output>
