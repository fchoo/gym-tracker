---
phase: 02-owned-library-and-planning
plan: 28
type: execute
wave: 15
depends_on: [02-24, 02-25, 02-26, 02-27]
files_modified:
  - app/workout/[sessionId].tsx
  - app/workout-plan/[sessionId].tsx
  - src/ui/components/SetRow.tsx
  - src/ui/screens/ActiveWorkoutScreen.tsx
  - src/ui/screens/WorkoutPlanOverviewScreen.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - src/ui/__tests__/ActiveWorkoutScreen.test.tsx
  - src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx
  - src/ui/__tests__/OwnedPlanEditor.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-07, LIB-08, LIB-11, LIB-12]
estimate: { tokens: 24000, raw_tokens: 24000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "D-62 and D-65 close through a sticky workout/exercise identity header plus a dedicated `Today's plan` page that lists every exercise in workout order with completed, current, planned, and skipped states."
    - "Opening an earlier, current, or later exercise for review never changes the SQLite-authoritative active pointer; returning to the current exercise is explicit and state-preserving."
    - "D-58/D-59 use accessible 48dp glyph controls for Complete/Skip set and warm-up, Add set/warm-up, Copy warm-up, Move up/down, and Plan default/reset; same-row controls share visible height and align right."
    - "Completed/skipped sets show distinct top-right circle glyphs plus text semantics, and visible `Excluded from records and progression` copy is removed while warm-up exclusion remains true in domain behavior."
    - "D-64 UI closes: add/copy failures retain values and expose inline Retry; success reveals and focuses the committed inserted row."
    - "D-63 UI closes: every completed working set stays editable for the entire `in_progress` workout through Plan 02-27's narrow command; no eight-second countdown/expiry or whole-session Undo is presented."
  artifacts:
    - { path: "src/ui/screens/WorkoutPlanOverviewScreen.tsx", provides: "Dedicated Today's plan page with all exercise states and non-mutating review navigation" }
    - { path: "src/ui/screens/ActiveWorkoutScreen.tsx", provides: "Sticky identity, add/copy retry/focus, and completed-set editing" }
    - { path: "src/ui/components/SetRow.tsx", provides: "Accessible glyph/status actions and editable completed working sets" }
  key_links:
    - { from: "src/ui/screens/WorkoutPlanOverviewScreen.tsx", to: "src/ui/screens/ActiveWorkoutScreen.tsx", via: "review exercise identity is presentation-only; active pointer remains from ActiveWorkoutView", pattern: "review|currentExercise|active" }
    - { from: "src/ui/components/SetRow.tsx", to: "src/bootstrap/workoutAppRuntime.tsx", via: "revision-checked completed-set correction and committed add/copy results", pattern: "reviseCompleted|addWarmup|copyPreviousWarmup|addWorkingSet" }
---

<objective>
Make the active workout fully reviewable and correctable with compact glyph actions and stable context.

Purpose: Keep workout identity visible, expose every exercise, make add/copy failures recoverable, and remove the eight-second correction limit.
Output: Today's-plan route/screen, sticky Active Workout header, status/glyph set rows, and safe completed-set editing UI.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-24-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-26-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-27-SUMMARY.md
@src/ui/screens/ActiveWorkoutScreen.tsx
@src/ui/components/SetRow.tsx
@app/workout/[sessionId].tsx
</context>

## Artifacts this phase produces

- A dedicated Today's-plan page and route for all active-workout exercises.
- Sticky workout/exercise identity and compact accessible action/status controls.
- Unlimited in-progress completed-working-set editing plus reliable add/copy retry/focus behavior.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-07, LIB-08 | Running and reviewing user-owned plan workouts | COVERED |
| REQ | LIB-11, LIB-12 | Metric-aware completed-set display/editing | COVERED |
| CONTEXT | D-58, D-59, D-62, D-63, D-64, D-65 | Complete Active Workout amendment | COVERED |
| VERIFICATION | G-02-04, UI half G-02-05, G-02-08 | Glyph/status, add/copy, page/sticky/correction findings | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add the dedicated Today's-plan page and sticky workout identity</name>
  <read_first>app/workout/[sessionId].tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/domains/workout/activeWorkout.ts, src/ui/__tests__/ActiveWorkoutScreen.test.tsx</read_first>
  <files>app/workout/[sessionId].tsx, app/workout-plan/[sessionId].tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/WorkoutPlanOverviewScreen.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx</files>
  <action>Start with failing component/route tests. Add a dedicated `Today's plan` page sourced from `ActiveWorkoutView.exercises`, preserving workout order and showing explicit completed/current/planned/skipped labels plus distinct non-color state semantics. Let the owner open any exercise for read/review and return, but never call a source mutation or alter `active_session_exercise_id` merely by browsing. Add an explicit return-to-current action. Keep the workout name and viewed/current exercise context sticky above scrolling set content; handle safe areas, keyboard avoidance, compact/medium/expanded, landscape, and 200% text. Preserve scroll position per reviewed exercise where practical. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>All exercises and states are visible; review navigation is reversible and SQL pointer invariance is asserted; the header stays visible throughout set scrolling.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutScreen.test.tsx</automated></verify>
  <done>The owner can understand and review the whole active workout without changing its authoritative progress.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Replace textual set and planning actions with accessible glyph controls</name>
  <read_first>src/ui/components/SetRow.tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</read_first>
  <files>src/ui/components/SetRow.tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</files>
  <action>Use familiar lucide glyphs already installed for Complete/Skip working set, Complete/Skip warm-up, Add warm-up, Copy warm-up, Add working set, Move up/down, and Plan default/reset. Preserve the exact accessible names, add visible focus/pressed/disabled states, enforce 48dp targets, equal visible height, and right-edge alignment for same-row action clusters. Put distinct completed and skipped circle glyphs at each set card's top-right and expose status text to assistive technology; a compact visible pill may reinforce it. Remove visible `Excluded from records and progression` from warm-up rows while preserving exclusion in domain logic and accessible help where useful. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Every named action is a glyph control with exact accessibility semantics, and completed/skipped/warm-up presentation remains understandable without color.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutScreen.test.tsx src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx src/ui/__tests__/OwnedPlanEditor.test.tsx</automated></verify>
  <done>The action-heavy workout and plan rows are compact, aligned, and accessible.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Wire persistent add/copy retry and unlimited active-session correction</name>
  <read_first>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/components/SetRow.tsx, src/bootstrap/workoutAppRuntime.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx</read_first>
  <files>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/components/SetRow.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx</files>
  <action>Consume Plan 02-27's committed inserted-row identity and correction command. For Add warm-up, Copy warm-up, and Add working set, keep user input/state on failure, show one inline error with Retry beside the affected section, prevent duplicate concurrent submissions, and on success reveal, scroll to, and focus the inserted row. Let any completed working set enter edit mode while the session remains `in_progress`; save through expected session/set revisions and preserve later progress/current pointer/rest. Remove the visible eight-second Undo countdown, expiry notice, timer state, and whole-session rollback control. The legacy API may remain internal for compatibility but must not gate editing. Finalized-session editing stays unavailable and directs future work to Phase 3 without exposing a misleading action. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Latest-schema add/copy success appears immediately and focuses the row; retry is visible on failure; earlier completed sets remain editable without time limit only during an active workout.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutScreen.test.tsx src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx</automated></verify>
  <done>G-02-04/G-02-05/G-02-08 are closed at the owner-facing Active Workout surface.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Pointer tampering | Review navigation to authoritative workout state | Read-only review route and explicit SQL pointer invariance test. |
| Duplicate mutation | Rapid glyph taps to add/complete/correct commands | Busy/idempotency guards plus repository revisions. |
| Lost update | Completed-set editor to SQLite | Expected session/set revisions and visible conflict recovery. |
| State spoofing | Color/glyph presentation to actual status | Explicit text semantics backed by authoritative view status. |
| Accessibility denial | Dense icon controls | Exact names, focus, keyboard/D-pad, 48dp, and 200% text tests. |
</threat_model>

<verification>Run Active Workout, metric-profile, and owned-plan component suites plus typecheck/lint. Current APK evidence is invalid after this source change; Plan 02-34 rebuilds once.</verification>
<success_criteria>The owner can see the whole workout, retain context while scrolling, use compact accessible actions, recover add/copy failures, and edit any completed set throughout an active workout.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-28-SUMMARY.md` when complete.</output>
