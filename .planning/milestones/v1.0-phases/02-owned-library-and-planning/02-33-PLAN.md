---
phase: 02-owned-library-and-planning
plan: 33
type: execute
wave: 19
depends_on: [02-32]
files_modified:
  - src/testing/contracts/phase2Plan.contract.ts
  - src/testing/contracts/phase2Shared.contract.test.ts
  - scripts/run-coverage-gate.mjs
  - scripts/run-phase2-maestro.mjs
  - scripts/verify-phase2-native-evidence.mjs
  - scripts/phase2-evidence-scripts.test.mjs
  - maestro/phase2/remediation-workout.yaml
  - maestro/phase2/remediation-inputs-cards-navigation.yaml
  - maestro/phase2/remediation-rest-alerts.yaml
autonomous: true
gap_closure: true
requirements: [LIB-01, LIB-02, LIB-03, LIB-04, LIB-05, LIB-06, LIB-07, LIB-08, LIB-09, LIB-10, LIB-11, LIB-12]
estimate: { tokens: 23000, raw_tokens: 23000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "Every D-56..D-67 and remediation-case ID defined by Plan 02-32 is represented by a source-owned host/native/Maestro/attended evidence mapping before any APK is built."
    - "Native contracts execute latest-schema add/copy/correction and rest-alert integrity cases on actual Expo SQLite with expected counts derived from exported case arrays."
    - "Maestro enumerates all prior public flows plus dedicated remediation flows for rail/input/cards, Active Workout add/copy/edit/sticky/Today's-plan behavior, and rest/settings behavior."
    - "The verifier derives requirements, decisions, remediation cases, UI surfaces/truths, prohibitions, native cases, and flows from canonical sources; stale, missing, duplicate, skipped, failed, cross-APK, or privacy-unsafe evidence fails closed."
    - "Automated-only mode never creates or requires `physical-result.json` or `final-verification.json`; physical mode requires separate emulator/Samsung roles while binding both to the same APK bytes."
  artifacts:
    - { path: "src/testing/contracts/phase2Plan.contract.ts", provides: "Actual Expo SQLite remediation cases for current-schema workout mutations and alert truth" }
    - { path: "scripts/run-phase2-maestro.mjs", provides: "Source-enumerated prior plus remediation installed-flow runner" }
    - { path: "scripts/verify-phase2-native-evidence.mjs", provides: "Manifest-driven fail-closed automated and physical evidence validator" }
  key_links:
    - { from: ".planning/phases/02-owned-library-and-planning/02-VALIDATION.md", to: "scripts/verify-phase2-native-evidence.mjs", via: "source-derived decision/remediation/UI evidence ledgers", pattern: "D-56|G-02|remediation|ui" }
    - { from: "src/testing/contracts/phase2Plan.contract.ts", to: "scripts/run-phase2-maestro.mjs", via: "native source facts are complemented by installed owner journeys", pattern: "add|copy|completed|rest" }
---

<objective>
Upgrade the Phase 2 evidence harness for every post-device-review remediation before rebuilding.

Purpose: Ensure the new implementation can only pass through explicit source-derived host, native, installed-flow, and attended proof.
Output: Remediation native cases, Maestro flows, coverage enumeration, and a fail-closed verifier with no stale hardcoded totals.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VALIDATION.md
@.planning/phases/02-owned-library-and-planning/COVERAGE.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-32-SUMMARY.md
@src/testing/contracts/phase2Plan.contract.ts
@scripts/run-phase2-maestro.mjs
@scripts/verify-phase2-native-evidence.mjs
@scripts/phase2-evidence-scripts.test.mjs
</context>

## Artifacts this phase produces

- Actual Expo SQLite remediation cases and complete integrity-critical coverage enumeration.
- Three installed-app remediation journeys.
- A source-derived verifier schema for distinct automated, emulator, and Samsung evidence roles.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-01..LIB-12 | Aggregate evidence validates the full Phase 2 outcome | COVERED |
| CONTEXT | D-01..D-67 | Source-derived complete decision ledger | COVERED |
| VERIFICATION | G-02-01..G-02-09 | Dedicated remediation cases/flows/attended rows | COVERED |
| UI-SPEC | Eight truth categories by amended surface | Manifest-driven surface matrix | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Add current-schema native remediation contracts and coverage</name>
  <read_first>src/testing/contracts/phase2Plan.contract.ts, src/testing/contracts/phase2Shared.contract.test.ts, app/__native-contracts.tsx, scripts/run-native-sqlite-contracts.mjs, scripts/run-coverage-gate.mjs, .planning/phases/02-owned-library-and-planning/02-VALIDATION.md</read_first>
  <files>src/testing/contracts/phase2Plan.contract.ts, src/testing/contracts/phase2Shared.contract.test.ts, scripts/run-coverage-gate.mjs</files>
  <action>Start with failing contract tests. Extend the existing source-derived Phase 2 aggregate with stable remediation case IDs for latest-manifest Add warm-up, Copy warm-up, Add working set, completed-set revision conflicts/invariance, preference persistence, notification failure non-authority, and exactly-once foreground feedback where actual native adapters are required. Reuse the existing aggregate route/runner rather than adding a parallel suite; their exported arrays must pick up new cases without hardcoded counts. Add every new integrity-critical domain/repository/preference/lifecycle module to the 100% per-file coverage gate while retaining global thresholds. Keep case output bounded and free of raw observations or owner data. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Host tests prove source-derived unique case registration, and native aggregate enumeration includes every required remediation case with no fixed total.</acceptance_criteria>
  <verify><automated>npm run test:unit -- --runInBand src/testing/contracts/phase2Shared.contract.test.ts &amp;&amp; npm run test:coverage -- --runInBand</automated></verify>
  <done>The native/coverage manifests fully represent integrity-critical remediation behavior.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add installed remediation journeys to the existing Maestro runner</name>
  <read_first>scripts/run-phase2-maestro.mjs, scripts/phase2-evidence-scripts.test.mjs, maestro/phase2/owned-plan-editor.yaml, maestro/lifecycle/rest-recovery.yaml, .planning/phases/02-owned-library-and-planning/02-VALIDATION.md</read_first>
  <files>scripts/run-phase2-maestro.mjs, scripts/phase2-evidence-scripts.test.mjs, maestro/phase2/remediation-workout.yaml, maestro/phase2/remediation-inputs-cards-navigation.yaml, maestro/phase2/remediation-rest-alerts.yaml</files>
  <action>Add deterministic public flows that exercise: 839/840/expanded navigation geometry, calendar/time/numeric input, global and Library cards, same-height/right-aligned glyph actions; Add warm-up/Copy warm-up/Add working set persistence, inline retry, focused inserted row, sticky identity, Today's plan states/review pointer invariance, and unlimited active-session completed-set correction; collapsed/expanded rest with exact order/no skipped message plus alert settings/defaults and deterministic foreground/background controls. Preserve all prior flows, source-derived sorted enumeration, exact APK/install checks, no sleep-based success, zero skipped tests, and airplane-mode restoration. Update direct Node fixtures for missing/duplicate/stale flow rejection. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The runner automatically includes all prior and three remediation flows, and fixtures reject omission, duplication, skip, failure, or identity drift.</acceptance_criteria>
  <verify><automated>node --test scripts/phase2-evidence-scripts.test.mjs &amp;&amp; node --check scripts/run-phase2-maestro.mjs</automated></verify>
  <done>The installed flow manifest proves every owner-visible amendment before physical review.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Make verification source-derived and role-aware</name>
  <read_first>scripts/verify-phase2-native-evidence.mjs, scripts/phase2-evidence-scripts.test.mjs, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md, .planning/phases/02-owned-library-and-planning/02-VALIDATION.md, .planning/phases/02-owned-library-and-planning/COVERAGE.md</read_first>
  <files>scripts/verify-phase2-native-evidence.mjs, scripts/phase2-evidence-scripts.test.mjs</files>
  <action>Replace D-01..D-55 and other duplicated fixed arrays with parsers for the canonical requirement/decision/remediation/UI-surface/prohibition/native/flow sources created by Plan 02-32. Validate exact sorted uniqueness, complete surface-to-eight-truth mappings, all automated artifacts, source/mode/APK/package identity, 16KiB alignment, benchmark thresholds, zero skipped/failed entries, and privacy constraints. In automated-only mode reject physical/final output creation. In physical mode require separate `emulator-supplementary` and `samsung-physical` role records with their own model/API/ABI/hashed serial and installed-APK hash; do not require either device identity to equal the build device, but require all APK/source/package identities to match. Write final output atomically only when explicitly requested. Add fixtures for stale decisions, missing remediation rows, generic truth-only claims, wrong device role/model, raw serial, cross-APK evidence, duplicates, and partial output cleanup. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>No stale hardcoded count can pass, automated-only remains physical-independent, and physical mode requires both correctly scoped devices on exact APK bytes.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; node --test scripts/phase2-evidence-scripts.test.mjs &amp;&amp; node --check scripts/verify-phase2-native-evidence.mjs</automated></verify>
  <done>The evidence harness is ready for one exact-HEAD build and complete regeneration.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Evidence spoofing | Source ledgers to result JSON | Source-derived exact IDs and duplicate/missing rejection. |
| Device-role spoofing | Emulator/Samsung evidence to final verifier | Separate roles, expected physical model, hashed serial, own installed-byte proof. |
| Artifact tampering | Build/APK to producers | Exact HEAD, mode-sensitive digest, APK/package/installed hash equality. |
| Information disclosure | Native/Maestro/physical output | Bounded IDs/counts/timings and hashed serial only. |
| Denial by stale totals | New cases to hardcoded validator | Derive totals from canonical source exports/doc ledgers. |
</threat_model>

<verification>Run direct script fixtures, source-derived contract tests, coverage, typecheck, and lint. Do not build an APK in this plan.</verification>
<success_criteria>The complete amendment is represented in deterministic, fail-closed evidence producers before the exact candidate is built.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-33-SUMMARY.md` when complete.</output>
