---
phase: 01-trustworthy-workout-loop
plan: 02
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - DESIGN.md
  - app/_layout.tsx
  - app/(tabs)/_layout.tsx
  - app/(tabs)/index.tsx
  - app/(tabs)/calendar.tsx
  - app/(tabs)/library.tsx
  - app/(tabs)/progress.tsx
  - app/workout/[sessionId].tsx
  - src/ui/theme/index.ts
  - src/ui/layout/AdaptiveScreen.tsx
  - src/ui/components/index.ts
  - src/ui/screens/RootScreens.tsx
  - src/ui/__tests__/foundation.test.tsx
autonomous: true
requirements:
  - FOUND-02
  - FOUND-07
  - FOUND-08
  - FOUND-09
must_haves:
  truths:
    - "Owner can navigate Today, Calendar, Library, and Progress in that order, while Active Workout hides root navigation per D-23."
    - "System, Light, and Dark use the exact approved precision-instrument tokens and preserve route/focus state per D-22."
    - "Compact, medium, expanded, 200% text, keyboard/D-pad, logical-focus, reduced-motion, non-color, safe-area, and 48dp contracts work from the first screen per D-24 and D-25."
    - "Calendar, Library, and Progress expose only their exact Phase 1 intentional empty states."
  artifacts:
    - path: "DESIGN.md"
      provides: "Repository design-system source extracted from 01-UI-SPEC.md"
    - path: "app/(tabs)/_layout.tsx"
      provides: "Four-destination root shell"
    - path: "src/ui/theme/index.ts"
      provides: "Approved color, typography, spacing, radius, and appearance tokens"
    - path: "src/ui/layout/AdaptiveScreen.tsx"
      provides: "Compact, medium, and expanded layout primitive"
    - path: "src/ui/__tests__/foundation.test.tsx"
      provides: "Component, semantics, large-text, and adaptive contract proof"
  key_links:
    - from: "app/(tabs)/_layout.tsx"
      to: "src/ui/theme/index.ts"
      via: "theme and appearance provider"
      pattern: "Theme|Appearance"
    - from: "app/(tabs)/calendar.tsx"
      to: "src/ui/screens/RootScreens.tsx"
      via: "thin route imports intentional empty screen"
      pattern: "Calendar"
---

<objective>
Deliver reviewed slice 01-02: the accessible precision-instrument UI foundation and four-root shell.

Purpose: Establish one reusable visual, adaptive, and accessibility vocabulary before feature screens can diverge from the approved contract.
Output: `DESIGN.md`, route-only shell, theme/appearance system, adaptive primitives, shared components, exact Phase 1 empty destinations, and RNTL proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trustworthy-workout-loop/01-CONTEXT.md
@.planning/phases/01-trustworthy-workout-loop/01-UI-SPEC.md
@.planning/phases/01-trustworthy-workout-loop/01-PATTERNS.md
@.planning/phases/01-trustworthy-workout-loop/01-01-SUMMARY.md
@~/.gstack/projects/gym_tracker/designs/approved-20260815/core-workout-flow.png
@~/.gstack/projects/gym_tracker/designs/system-review-20260815/app-system-reference.png
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Encode the design system and adaptive accessibility primitives</name>
  <files>DESIGN.md, src/ui/theme/index.ts, src/ui/layout/AdaptiveScreen.tsx, src/ui/components/index.ts, src/ui/__tests__/foundation.test.tsx</files>
  <behavior>
    - System is default; explicit Light/Dark persists and selected semantics are exposed.
    - Width classes select compact, medium, or expanded without separating active work from its dock.
    - Controls enforce 48dp minimum targets and primary actions enforce 56dp minimum height.
    - 200% text wraps required labels, numeric groups stack, and reduced motion removes position/scale movement.
  </behavior>
  <action>Extract the complete token tables, component vocabulary, voice, motion, width, safe-area, focus, and accessibility rules from `01-UI-SPEC.md` into `DESIGN.md`. Implement Source Sans 3 and IBM Plex Mono weights 400/600, the fixed spacing/type/color/radius tokens, appearance persistence, width-class selection, focus restoration, non-color cues, safe areas, reduced motion, and reusable `AppTabs`, headers, actions, notices, empty/failure states, sheets, skeletons, and focus-ring semantics per D-22, D-24, and D-25. Use Lucide outlined 2dp icons only; do not add a component framework, gradients, decorative pills, nested cards, fake metrics, or later-phase controls.</action>
  <verify>
    <automated>npm run test:components -- foundation --runInBand &amp;&amp; npm run typecheck</automated>
  </verify>
  <done>The design source and primitives match the approved tokens and pass component tests for appearance, dimensions, large text, semantics, focus, reduced motion, and every width class.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build the route-only shell and intentional Phase 1 destinations</name>
  <files>app/_layout.tsx, app/(tabs)/_layout.tsx, app/(tabs)/index.tsx, app/(tabs)/calendar.tsx, app/(tabs)/library.tsx, app/(tabs)/progress.tsx, app/workout/[sessionId].tsx, src/ui/screens/RootScreens.tsx, src/ui/__tests__/foundation.test.tsx</files>
  <behavior>
    - Root destinations appear as Today, Calendar, Library, Progress with visible labels in locked order.
    - Root navigation is disabled during boot and hidden on Active Workout at every width.
    - Empty roots show the exact approved heading/body/Go to Today copy and no future controls.
    - Android Back dismisses a sheet, then focused route, then follows root history/exit behavior.
  </behavior>
  <action>Compose providers in `app/_layout.tsx`, keep route files thin, and implement the four root destinations plus a focused workout placeholder outside the tab group per D-23 and D-27. Shell renders immediately with structured Today placeholders but keeps roots disabled until launch state becomes trusted. Use the exact Calendar/Library/Progress empty-state copy from `01-UI-SPEC.md`; Today remains a shell-ready placeholder for slice 01-07. Ensure headings, selected/disabled semantics, visible labels at 200% text, focus order, Back behavior, bottom navigation on compact/medium, optional rail on expanded, and absence of root navigation on focused workout.</action>
  <verify>
    <automated>npm run test:components -- foundation --runInBand &amp;&amp; npm run lint</automated>
  </verify>
  <done>All roots are real and accessible, empty destinations are intentionally scoped, the focused workout route hides tabs, and no route owns SQL, platform adapters, or feature logic.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| Android input methods → UI commands | Touch, keyboard, and D-pad input must resolve to semantic actions. |
| persisted appearance → rendered theme | Malformed/stale preferences must not create unreadable or mixed state. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-02-01 | Spoofing | semantic action labels | mitigate | Visible labels, explicit accessibility labels, one shared action path, and RNTL role/name assertions. |
| T-01-02-02 | Denial of Service | 200% text/adaptive layout | mitigate | Wrapping/growing controls, bounded width primitives, keyboard/safe-area tests, and no hidden required action. |
| T-01-02-03 | Tampering | appearance preference | mitigate | Validate allowed System/Light/Dark values and fall back to System without altering workout state. |
| T-01-02-SC | Tampering | npm installs | mitigate | No new packages beyond the legitimacy-approved locked set from 01-01. |
</threat_model>

<verification>
- `npm run test:components -- foundation --runInBand`
- `npm run typecheck && npm run lint`
- Assert exact empty-state strings and root order.
</verification>

<success_criteria>
- FOUND-02 and FOUND-07..09 have executable component proof.
- D-22..25 are represented by shared primitives rather than screen-local variants.
- No later-phase Calendar, Library, or Progress functionality appears.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-workout-loop/01-02-SUMMARY.md` when done.
</output>
