---
phase: 03-calendar-and-history-integrity
plan: 02
type: execute
wave: 2
depends_on: [03-01]
files_modified:
  - src/domains/history/metricHistory.ts
  - src/platform/sqlite/repositories/historyRepository.ts
  - src/platform/sqlite/repositories/metricRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/ui/screens/ExerciseHistoryScreen.tsx
  - src/ui/screens/ExerciseDetailScreen.tsx
  - app/exercise-history/[exerciseId].tsx
  - src/ui/__tests__/ExerciseHistoryScreen.test.tsx
  - src/platform/sqlite/repositories/historyRepository.test.ts
autonomous: true
requirements: [HIST-04]
must_haves:
  truths:
    - Best, Average, and Last use comparable working sets under complete metric identity and comparator boundaries.
    - Warm-ups remain visible separately and never enter records, aggregates, comparable exposure, or progression evidence.
    - A partial-session exposure is eligible only when every planned working set for that exercise completed.
  artifacts:
    - { path: src/domains/history/metricHistory.ts, provides: effective-history comparable selection and metric summary contracts }
    - { path: src/ui/screens/ExerciseHistoryScreen.tsx, provides: source-backed metric-aware Best/Average/Last and separate warm-ups }
  key_links:
    - { from: src/platform/sqlite/repositories/historyRepository.ts, to: src/domains/metrics/exposure.ts, via: effective working-set exposures, pattern: areMetricExposuresComparable }
---

<objective>
Route exercise history through effective facts and expose metric-aware Best/Average/Last without leaking warm-ups into evidence.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Build effective comparable-history reads</name>
<read_first>src/domains/metrics/exposure.ts, src/domains/metrics/aggregates.ts, src/domains/metrics/comparators.ts, src/platform/sqlite/repositories/metricRepository.ts, src/bootstrap/workoutAppRuntime.tsx</read_first>
<files>src/domains/history/metricHistory.ts, src/platform/sqlite/repositories/historyRepository.ts, src/platform/sqlite/repositories/metricRepository.ts, src/platform/sqlite/repositories/historyRepository.test.ts</files>
<action>Write failing fixtures for every profile/comparator boundary, then consume effective snapshots rather than direct session rows for comparable history. Preserve complete identity segmentation, protocol/target boundaries, canonical date/session/set ordering, partial eligibility, and void exclusion. Use existing aggregate/best-candidate functions instead of hand-written profile comparisons. Return separate warm-up visit rows with explicit exclusion semantics.</action>
<acceptance_criteria>Metric output is stable across original/overlay reads, never compares incompatible observations, and never uses warm-ups as evidence.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/metrics src/domains/history &amp;&amp; npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add metric-aware exercise history presentation and navigation</name>
<read_first>03-UI-SPEC.md, src/ui/screens/ExerciseDetailScreen.tsx, src/ui/screens/SessionDetailScreen.tsx, src/bootstrap/workoutAppRuntime.tsx</read_first>
<files>src/bootstrap/workoutAppRuntime.tsx, src/ui/screens/ExerciseHistoryScreen.tsx, src/ui/screens/ExerciseDetailScreen.tsx, app/exercise-history/[exerciseId].tsx, src/ui/__tests__/ExerciseHistoryScreen.test.tsx</files>
<action>Implement loading/error/empty/populated metric history views using the UI-SPEC. Show segment identity in text, Best/Average/Last for comparable working sets, visit order, and separately disclosed warm-ups. Wire detail links from Calendar/session and preserve originating Back destination. Do not add charts, global volume, fake zero data, or direct SQL from UI.</action>
<acceptance_criteria>Every metric segment is understandable, warm-ups stay separate, and no-comparable history explains absence without fabricating zero values.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ExerciseHistoryScreen.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run metric domain/repository/component tests for every profile plus empty/partial/warm-up state.</verification>
<success_criteria>HIST-04 has one effective-history source with correct metric semantics and accessible presentation.</success_criteria>
