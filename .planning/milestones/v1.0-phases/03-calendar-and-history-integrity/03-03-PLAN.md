---
phase: 03-calendar-and-history-integrity
plan: 03
type: execute
wave: 3
depends_on: [03-01, 03-02]
files_modified:
  - src/domains/history/projectionReducer.ts
  - src/domains/history/historySubjects.ts
  - src/platform/sqlite/effects/effectStore.ts
  - src/platform/sqlite/effects/historyProjectionEffects.ts
  - src/platform/sqlite/repositories/historyRepository.ts
  - src/platform/sqlite/repositories/historyProjectionRepository.ts
  - src/bootstrap/workoutLifecycle.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/platform/sqlite/repositories/historyProjectionRepository.test.ts
  - src/domains/history/projectionReducer.test.ts
autonomous: true
requirements: [HIST-08, HIST-09]
must_haves:
  truths:
    - Every correction-class source mutation can advance all old and new effective-history subjects in one transaction and invalidate matching pending recommendations before acknowledgement.
    - Durable targeted rebuild effects carry their source revision and stale handlers cannot write derived rows.
    - A canonical reducer makes targeted and full rebuild results identical for records, comparable exposures, exercise metrics, affected period inputs, and recommendation invalidation scopes.
  artifacts:
    - { path: src/domains/history/historySubjects.ts, provides: canonical old/new snapshot subject fan-out }
    - { path: src/domains/history/projectionReducer.ts, provides: one deterministic effective-history reducer for target and full rebuilds }
    - { path: src/platform/sqlite/effects/historyProjectionEffects.ts, provides: durable revision-checked history rebuild handler }
  key_links:
    - { from: src/platform/sqlite/repositories/historyRepository.ts, to: history_rebuild_effects, via: enqueue revision-checked history rebuild after source commit, pattern: rebuild_history_subject }
    - { from: src/bootstrap/workoutLifecycle.ts, to: src/platform/sqlite/effects/historyProjectionEffects.ts, via: post-commit and launch effect draining, pattern: rebuild_history_subject }
---

<objective>
Make all history-derived state deterministic and rebuildable before the mutable commands exist.

Purpose: establish a single reducer and a revision-fenced effect path so corrections cannot leave records, recommendation evidence, or future Progress inputs stale.
Output: subject graph, persistent freshness state, targeted/full rebuild workers, and byte-for-byte equivalence tests.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Define canonical history subjects and the pure projection reducer</name>
<read_first>03-CONTEXT.md, 03-RESEARCH.md, src/domains/metrics/exposure.ts, src/domains/metrics/aggregates.ts, src/domains/metrics/comparators.ts, src/domains/progression/recommendationCommands.ts</read_first>
<files>src/domains/history/historySubjects.ts, src/domains/history/projectionReducer.ts, src/domains/history/projectionReducer.test.ts</files>
<action>Begin with table-driven pure-domain tests. Derive a sorted de-duplicated union of subjects from the old and new complete effective snapshots: session, date, exercise metric identity and comparator, target/reference, and period. Keep void lifecycle in the subject graph so remove and restore cover both the former and restored scope. Implement one deterministic reducer over active effective source facts. Its output must include record candidates, comparable exposures, metric aggregates, period-input rows, and recommendation invalidation scopes. Normalize ordering and JSON representation before assertions. Reuse approved metric functions; never create a generic load-times-reps metric or admit warm-ups.</action>
<acceptance_criteria>Every mutation class yields the complete old/new subject union, voided sessions produce no ordinary evidence, and canonical reducer output is stable under input order changes.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/history/projectionReducer.test.ts src/domains/history/historySubjects.test.ts</automated></verify>
</task>

<task type="tracer" tdd="true">
<name>Task 2: Add revision-fenced durable targeted and full rebuild infrastructure</name>
<read_first>src/platform/sqlite/effects/effectStore.ts, src/platform/sqlite/effects/effectRunner.ts, src/bootstrap/workoutLifecycle.ts, src/platform/sqlite/repositories/metricRepository.ts, src/platform/sqlite/migrations/0013_history_integrity.ts</read_first>
<files>src/platform/sqlite/effects/effectStore.ts, src/platform/sqlite/effects/historyProjectionEffects.ts, src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.test.ts, src/bootstrap/workoutLifecycle.ts, src/bootstrap/workoutAppRuntime.tsx</files>
<action>Implement the dedicated `history_rebuild_effects` queue's versioned rebuild_history_subject payload and handler only after source-backed tests. Do not widen the existing constrained `pending_effects` contract. Implement monotonic subject revisions, a projection watermark/freshness read model, and transactional enqueue/coalescing keyed by subject and expected revision. The handler must re-check its expected revision immediately before projection replacement inside the writer transaction, so a source mutation between claim and write cannot publish stale output. Targeted and full rebuilds must invoke the same reducer and replace deterministic rows only. Lifecycle drains the dedicated queue on launch, foreground, and successful history source commits; derivative failures are diagnosable and never revoke an acknowledged source write.</action>
<acceptance_criteria>Old effects are superseded, live effects are idempotent, a crash/retry converges, and stale workers cannot write data or freshness watermarks for a newer source revision.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyProjectionRepository.test.ts &amp;&amp; npm run test:unit -- --runInBand src/platform/sqlite/effects</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 3: Prove target/full equivalence and expose conservative freshness reads</name>
<read_first>src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/repositories/historyRepository.ts, src/bootstrap/workoutAppRuntime.tsx, src/ui/screens/ExerciseHistoryScreen.tsx</read_first>
<files>src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.test.ts, src/bootstrap/workoutAppRuntime.tsx</files>
<action>Seed retained source fixtures for value correction, set kind/add/remove, exercise replacement, date move, effort/note/association change, void, restore, same-subject repeated writes, retry, and stale claim. For each fixture capture canonical projection rows after targeted draining, force a full rebuild from the same effective facts, and assert exact equality. Surface only a bounded freshness state through runtime reads: current, updating, or unavailable. Calendar/session detail remains factual even while projections update; metric/progress consumers show Updating rather than stale values or fabricated zeros.</action>
<acceptance_criteria>HIST-09 is proven for every mutation class, runtime consumers can distinguish current versus rebuilding state, and diagnostics contain IDs/revisions/counts but no workout payload.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyProjectionRepository.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run pure reducer/subject tests, host SQLite retained-schema integration, effect-runner stale/retry coverage, target/full equality fixtures, typecheck, and boundary lint. Do not create native or attended evidence until all source-changing Phase 3 plans are complete.</verification>
<success_criteria>HIST-08 and HIST-09 have one atomic source-to-effect contract and one canonical reducer whose targeted output is equivalent to full rebuilding.</success_criteria>
