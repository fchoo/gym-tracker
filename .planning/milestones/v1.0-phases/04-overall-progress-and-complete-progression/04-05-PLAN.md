---
phase: 04-overall-progress-and-complete-progression
plan: 05
type: execute
wave: 3
depends_on: [04-03, 04-04]
files_modified:
  - src/domains/progression/recommendationContracts.ts
  - src/domains/progression/recommendationCommands.ts
  - src/domains/progression/index.ts
  - src/platform/sqlite/migrations/0015_progression_evidence.ts
  - src/platform/sqlite/migrations/index.ts
  - src/platform/sqlite/repositories/workoutOutcomeRepository.ts
  - src/platform/sqlite/repositories/historyProjectionRepository.ts
  - src/domains/progression/recommendationContracts.test.ts
  - tests/sqlite-host/progressionEvidenceRepository.test.ts
  - tests/integration/load-reps.test.ts
autonomous: true
requirements: [PROG-09, PROG-10]
must_haves:
  truths:
    - A stored recommendation includes a named versioned rule, exact source evidence, current/proposed target, confidence/reason, source/target revision, and lifecycle.
    - Acceptance changes a target only after a same-transaction target revision compare-and-swap; manual edits win and stale recommendations become superseded.
    - Rejected, invalidated, superseded, or stale recommendations remain historical evidence but cannot change future targets.
  artifacts:
    - { path: src/domains/progression/recommendationContracts.ts, provides: profile-generic stored recommendation contract }
    - { path: src/platform/sqlite/migrations/0015_progression_evidence.ts, provides: additive integrity constraints/indexes for complete stored evidence }
    - { path: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, provides: atomic generation and decision lifecycle }
  key_links:
    - { from: src/platform/sqlite/repositories/historyProjectionRepository.ts, to: progression recommendation tables, via: correction/lifecycle invalidation, pattern: invalidated }
    - { from: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, to: target rows, via: revision-guarded acceptance, pattern: revision }
---

<objective>
Make recommendation evidence and decisions profile-generic, versioned, and resistant to stale/manual-edit races.

Purpose: allow the owner to inspect and decide a suggestion while ensuring no derivative calculation silently changes a future plan.
Output: normalized contracts/schema, atomic lifecycle repository behavior, and race/invalidation proof.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Harden the versioned stored-evidence contract and additive schema</name>
<read_first>04-CONTEXT.md, src/platform/sqlite/migrations/0006_metric_profiles.ts, src/platform/sqlite/migrations/0010_owned_recommendations.ts, src/domains/progression/recommendationCommands.ts</read_first>
<files>src/domains/progression/recommendationContracts.ts, src/domains/progression/recommendationCommands.ts, src/domains/progression/index.ts, src/platform/sqlite/migrations/0015_progression_evidence.ts, src/platform/sqlite/migrations/index.ts, src/domains/progression/recommendationContracts.test.ts, tests/sqlite-host/progressionEvidenceRepository.test.ts</files>
<action>Start with migration fixtures from version 14 and retained legacy recommendation rows. Define validated evidence envelopes that include rule ID/version, metric identity, source session/exercise/set identity, source and target revision, current/proposed targets, decision/reason/confidence, and lifecycle timestamps. Add only forward-compatible constraints/indexes/backfills required to distinguish legacy content from complete envelopes; do not rewrite historical source facts. Reject malformed/identity-mismatched actionable evidence while retaining safely readable legacy results. Prove both recommendation graphs enforce one current pending suggestion per target and all rows remain audit-readable after decision.</action>
<acceptance_criteria>Schema and contracts fail closed on incomplete actionable evidence while preserving accepted prior recommendation records and current migration compatibility.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressionEvidenceRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Generalize generation and atomic decision lifecycle</name>
<read_first>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.ts, tests/integration/load-reps.test.ts, src/domains/progression/policyRegistry.ts</read_first>
<files>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.ts, tests/integration/load-reps.test.ts, tests/sqlite-host/progressionEvidenceRepository.test.ts</files>
<action>Refactor repository serialization and detail reads to the validated profile-generic envelope while preserving existing load/reps user wording. Generation must be idempotent per source revision, supersede only a prior pending row for the same target, and keep every decision state inspectable. Acceptance must validate the recommendation is pending/actionable, its source and target revision still match all target rows, then update target and decision in one transaction. A concurrent manual edit, history correction/void/restore, policy/identity change, or stale effect supersedes/invalidates the suggestion and leaves the manual target intact. Reject only moves a pending item to rejected. Add rollback and two-call race fixtures for legacy and owned graphs.</action>
<acceptance_criteria>No unaccepted/stale decision can mutate a target, all invalidation causes are explicit, and stored evidence continues to reproduce the displayed recommendation after a later plan edit.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressionEvidenceRepository.test.ts &amp;&amp; npm run test:integration -- --runInBand tests/integration/load-reps.test.ts</automated></verify>
</task>
</tasks>

<verification>Run migration, host SQLite, integration, boundary, and integrity coverage checks. Validate legacy and owned target graphs independently.</verification>
<success_criteria>PROG-09 and PROG-10 have a complete, auditable recommendation lifecycle whose only target mutation is explicit committed acceptance.</success_criteria>
