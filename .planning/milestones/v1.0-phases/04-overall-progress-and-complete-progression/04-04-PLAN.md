---
phase: 04-overall-progress-and-complete-progression
plan: 04
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/domains/progression/planAuthoredV1.ts
  - src/domains/progression/policyRegistry.ts
  - src/domains/progression/index.ts
  - src/domains/progression/planAuthoredV1.test.ts
  - src/platform/sqlite/repositories/workoutOutcomeRepository.ts
  - tests/integration/cross-profile-workout.test.ts
  - tests/integration/load-reps.test.ts
autonomous: true
requirements: [PROG-08]
must_haves:
  truths:
    - Assistance, duration, variation, fixed-distance/time, and interval behavior is accepted only from a named versioned policy owned by the copied plan.
    - Unknown policy/version and unscored work fail closed to a manual state with no automatic target mutation.
    - Fixed-distance/time and intervals preserve their immutable target/protocol comparator boundary in stored evidence.
  artifacts:
    - { path: src/domains/progression/policyRegistry.ts, provides: fail-closed named policy dispatch }
    - { path: src/domains/progression/planAuthoredV1.ts, provides: deterministic reviewed non-load outcomes }
  key_links:
    - { from: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, to: src/domains/progression/policyRegistry.ts, via: copied policy ID/version lookup, pattern: evaluateProgressionPolicy }
---

<objective>
Implement only the reviewed plan-owned non-load policy surface.

Purpose: keep valid profile-specific evidence useful without turning training records into generic coaching or silently changing targets.
Output: policy registry, explicit manual/plan-authored outcomes, and fixture-backed persistence.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Define fail-closed registry and approved non-load policies</name>
<read_first>04-CONTEXT.md, 04-RESEARCH.md, assets/content/starter-plans.v2.json, src/domains/metrics/contracts.ts, src/domains/history/historySubjects.ts</read_first>
<files>src/domains/progression/planAuthoredV1.ts, src/domains/progression/policyRegistry.ts, src/domains/progression/index.ts, src/domains/progression/planAuthoredV1.test.ts</files>
<action>Create failing fixtures for each exact copied-plan policy: `manual_hold` for assisted/bodyweight/timed/variation work, `fixed_distance.plan_authored.v1`, `fixed_time.plan_authored.v1`, and `intervals.plan_authored.v1`. Return a versioned evidence outcome that names profile, policy ID/version, immutable comparator dimensions, comparable source facts, current target, and whether a review is actionable. Manual hold and unscored work return a manual non-actionable state. Plan-authored fixed targets return a factual reviewed outcome without a generic proposed change unless a future explicit rule defines one. Unknown policy IDs, versions, metric identities, or protocol mismatch must resolve to fail-closed manual, never guessed behavior.</action>
<acceptance_criteria>Each listed policy is reproducible from explicit rule input, preserves its comparator boundary, and cannot become a generic automatic cardio/assistance/variation policy.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/progression/planAuthoredV1.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Integrate copied-plan policy lookup and stored outcome evidence</name>
<read_first>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/platform/sqlite/repositories/ownedPlanRepository.ts, src/platform/sqlite/migrations/0009_owned_plans.ts, src/platform/sqlite/migrations/0010_owned_recommendations.ts</read_first>
<files>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/cross-profile-workout.test.ts, tests/integration/load-reps.test.ts</files>
<action>Write full-manifest SQLite fixtures for copied policy rows. Read policy identity/version/rule and metric identity from the owner’s plan graph, pass immutable session evidence to the registry, and persist a recommendation record only when the result explicitly calls for owner review. Preserve manual outcomes as facts available to Progress/history without creating a bogus pending decision. Assert assisted, variation, duration, fixed-distance, fixed-time, and interval examples remain profile-safe; interval protocol mismatch and unscored work must never generate actionable target writes.</action>
<acceptance_criteria>Only reviewed copied-plan policies produce stored outcomes, and every non-load result preserves profile target semantics and owner control.</acceptance_criteria>
<verify><automated>npm run test:integration -- --runInBand tests/integration/cross-profile-workout.test.ts tests/integration/load-reps.test.ts</automated></verify>
</task>
</tasks>

<verification>Run policy unit and cross-profile integration coverage with the latest migration manifest. Verify unknown-policy and protocol-mismatch paths fail closed.</verification>
<success_criteria>PROG-08 remains explicit, plan-authored, metric-safe, and manual where it must be manual.</success_criteria>
