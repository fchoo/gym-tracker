---
phase: 04-overall-progress-and-complete-progression
plan: 08
type: execute
wave: 5
depends_on: [04-07]
gap_closure: true
autonomous: true
requirements: [PROG-08]
files_modified:
  - src/domains/workout/sessionDetail.ts
  - src/platform/sqlite/repositories/workoutOutcomeRepository.ts
  - src/ui/screens/SessionDetailScreen.tsx
  - src/ui/__tests__/WorkoutCompletionScreen.test.tsx
  - tests/integration/starter-activation-repository.test.ts
must_haves:
  truths:
    - Completed non-load work exposes one deterministic versioned policy outcome derived from effective session facts and the immutable raw session-set rule type/version matched through source target identity.
    - Manual-hold and unscored outcomes remain explicit owner-controlled manual states; fixed-distance, fixed-time, and interval outcomes remain factual holds with their immutable comparator dimensions.
    - Non-load outcomes are readable from Workout details, link to the source exercise history, and never create a pending recommendation or mutate a future target.
    - Corrected, replaced, added-set, voided, or malformed effective histories either resolve an exact immutable source-policy identity or fail closed without presenting stale or invented outcomes.
    - Each outcome carries deterministic source session/exercise/set IDs and the effective history revision in stable order.
  artifacts:
    - { path: src/domains/workout/sessionDetail.ts, provides: typed non-actionable policy outcome on historical session details }
    - { path: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, provides: deterministic outcome projection from immutable/effective session facts }
    - { path: src/ui/screens/SessionDetailScreen.tsx, provides: accessible manual/factual outcome presentation and exercise-history link }
  key_links:
    - { from: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, to: src/domains/progression/policyRegistry.ts, via: snapshotted rule type/version and metric identity evaluation, pattern: evaluateProgressionPolicy }
    - { from: src/ui/screens/SessionDetailScreen.tsx, to: app/session/[sessionId].tsx, via: existing onOpenExerciseHistory callback, pattern: onOpenExerciseHistory }
---

<objective>
Close PROG-08 by carrying deterministic non-load policy outcomes from completed source facts into a readable history surface.

Purpose: reviewed non-load policies must produce evidence the owner can inspect, even though they deliberately never create an automatic target change.
Output: typed source-backed policy outcomes on Session Detail, repository projection, accessible UI, and regression coverage.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Project versioned non-load outcomes from immutable/effective session facts</name>
<read_first>04-VERIFICATION.md, 04-CONTEXT.md, src/domains/progression/policyRegistry.ts, src/domains/progression/planAuthoredV1.ts, src/domains/workout/sessionDetail.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/starter-activation-repository.test.ts</read_first>
<files>src/domains/workout/sessionDetail.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/starter-activation-repository.test.ts</files>
<action>RED first: replace the existing weak “zero pending rows” assertion with value-level assertions for fixed-distance, fixed-time, interval, assisted/bodyweight/timed/manual, and unscored outcomes. Add a typed non-actionable outcome collection to SessionDetail containing the versioned result plus stable source session/exercise/set IDs and effective revision. For raw history, derive the canonical policy kind/ID/version from the immutable `session_sets.rule_type`/`rule_version` snapshot plus complete metric identity. For an effective correction overlay, match each retained or added effective set through its source legacy/owned target identity to the original raw set snapshot; accept a consistent identity, and fail closed if added/replaced/malformed data cannot resolve one exact policy identity. Explicitly gate `history_session_overlays.lifecycle = 'voided'`, recalculate restored/corrected outcomes from the effective snapshot, evaluate completed working-set facts through `evaluateProgressionPolicy`, and sort source IDs deterministically. Remove the dead evaluate-then-discard branch from recommendation generation. Do not add a migration or a new mutable authority: outcomes are deterministic projections of immutable/effective history facts.</action>
<acceptance_criteria>Repeated reads return byte-equivalent versioned outcome values and source identities for the same effective facts; correction and restore recompute from the effective snapshot, void suppresses outcomes, unresolved added/replaced/malformed inputs fail closed, and no recommendation/target row is created or changed.</acceptance_criteria>
<verify><automated>npm run test:integration -- --runInBand tests/integration/starter-activation-repository.test.ts</automated></verify>
<done>Raw and effective history return deterministic non-load outcomes with stable source identity, while voided or unresolved history returns none and target/recommendation rows remain unchanged.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Present manual and factual outcomes in Workout details</name>
<read_first>04-UI-SPEC.md, src/ui/screens/SessionDetailScreen.tsx, src/ui/components/RecommendationSurface.tsx, src/ui/__tests__/WorkoutCompletionScreen.test.tsx</read_first>
<files>src/ui/screens/SessionDetailScreen.tsx, src/ui/__tests__/WorkoutCompletionScreen.test.tsx</files>
<action>RED first: assert that Workout details renders the exact manual/factual state, named rule/version, reason, current target context, and source-evidence count for each outcome, offers an accessible exercise-history drill-down, and renders no accept/reject target action. Implement the compact read-only section with the accepted `Manual review` and `This target has no automatic change.` language and existing card/focus primitives.</action>
<acceptance_criteria>The owner can inspect each non-load outcome from history, trace it to the exercise, and cannot mistake it for an actionable recommendation.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/WorkoutCompletionScreen.test.tsx</automated></verify>
<done>Workout details presents every returned manual/factual outcome as read-only evidence with an exercise-history link and no decision controls.</done>
</task>
</tasks>

<verification>Run focused policy/integration/component tests, typecheck, lint, boundaries, and integrity coverage. Verify no new recommendation row or target mutation occurs for non-load outcomes. Native/device/attended checks remain exclusively in Phase 5 Plan 05-07.</verification>
<success_criteria>PROG-08 is closed by a reproducible, source-backed, readable non-load outcome path that remains strictly non-actionable.</success_criteria>
