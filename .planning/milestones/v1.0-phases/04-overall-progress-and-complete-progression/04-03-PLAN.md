---
phase: 04-overall-progress-and-complete-progression
plan: 03
type: execute
wave: 2
depends_on: [04-01]
files_modified:
  - src/domains/progression/loadRepsV1.ts
  - src/domains/progression/loadRepsV1.test.ts
  - src/platform/sqlite/repositories/workoutOutcomeRepository.ts
  - tests/integration/load-reps.test.ts
autonomous: true
requirements: [PROG-06, PROG-07]
must_haves:
  truths:
    - Complete weighted double progression has explicit baseline, hold, increase, retry, regression, missing/hard/failed effort, incomplete exposure, and noncomparable outcomes.
    - Warm-ups and mismatched/unknown comparable sets cannot contribute to a progression increase.
    - Equipment increments are explicit evidence and an unusable increment resolves to a manual outcome rather than an invented target.
  artifacts:
    - { path: src/domains/progression/loadRepsV1.ts, provides: complete versioned weighted double-progression evaluation }
    - { path: src/domains/progression/loadRepsV1.test.ts, provides: branch-complete fixture matrix }
  key_links:
    - { from: src/platform/sqlite/repositories/workoutOutcomeRepository.ts, to: src/domains/progression/loadRepsV1.ts, via: stored session evidence evaluation, pattern: evaluateLoadRepsV1 }
---

<objective>
Finish the approved weighted double-progression policy with source-explainable outcomes.

Purpose: make every resistance recommendation reproducible and avoid silently treating difficult, incomplete, regressed, or incompatible training as an increase.
Output: complete policy result contract, persisted generation integration, and exhaustive fixtures.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Expand the versioned weighted double-progression evidence/result contract</name>
<read_first>04-CONTEXT.md, src/domains/progression/loadRepsV1.ts, src/domains/metrics/contracts.ts, src/domains/metrics/comparators.ts</read_first>
<files>src/domains/progression/loadRepsV1.ts, src/domains/progression/loadRepsV1.test.ts</files>
<action>Write table-driven fixtures before changing the evaluator. Cover no history, every planned comparable working set at upper bound with easy/on-target effort, one set below bound, missing effort, hard effort, failed effort, incomplete exposure, skipped/draft set, different load/profile/version, repeated below-range regression, warm-up exclusion, zero/invalid increment, and an explicit available equipment increment differing from the target increment. Extend versioned evidence with enough counted/reason-coded facts to explain each outcome. A regression may yield an explicit retry/manual/deload-review outcome only when a named rule says so; never imply injury, readiness, or medical advice. Keep legacy v1 valid behavior stable through a compatible rule-versioned addition where required.</action>
<acceptance_criteria>Every policy decision has a stable reason, evidence, current/proposed target, and confidence; only all-qualified complete working sets with an available increment can increase load.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/progression/loadRepsV1.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Persist complete source evidence through recommendation generation</name>
<read_first>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/load-reps.test.ts, src/platform/sqlite/migrations/0006_metric_profiles.ts</read_first>
<files>src/platform/sqlite/repositories/workoutOutcomeRepository.ts, tests/integration/load-reps.test.ts</files>
<action>Use integration fixtures that run the full current migration manifest. Map only persisted working-set facts, target values, effort, and configured equipment increment into the policy input. Store the resulting named rule/version/evidence without substituting warm-up values or mutable plan copies. Preserve idempotent same-revision generation and supersede only the relevant pending target recommendation. Verify incomplete, hard/failed/missing effort, regression, and invalid equipment outcomes do not mutate a future target merely by being generated.</action>
<acceptance_criteria>Stored evidence can reproduce the evaluator result and all decision branches remain safe across legacy and owned target graphs.</acceptance_criteria>
<verify><automated>npm run test:integration -- --runInBand tests/integration/load-reps.test.ts</automated></verify>
</task>
</tasks>

<verification>Run domain and real-SQLite integration tests, then the integrity coverage gate for the policy module.</verification>
<success_criteria>PROG-06 and PROG-07 produce complete, explainable resistance outcomes.</success_criteria>
