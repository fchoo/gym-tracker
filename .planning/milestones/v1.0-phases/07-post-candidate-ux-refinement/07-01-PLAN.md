---
phase: 07-post-candidate-ux-refinement
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/sqlite/migrations/0017_workout_remove_receipts.ts
  - src/platform/sqlite/migrations/index.ts
  - src/domains/portability/restoreCommands.ts
  - src/domains/portability/restoreCommands.test.ts
  - tests/sqlite-host/migrations-effects.test.ts
autonomous: true
requirements: [UX-18]
estimate: {tokens: 26000, raw_tokens: 26000, tasks: 2, confidence: low}
ui_considerations:
  - {id: UI-E03-REMOVE-FOUNDATION, category: error, element: replay receipt schema rejects malformed or conflicting state, verification: explicit, owner: 07-01 migration and restore tests}
must_haves:
  truths:
    - Per D-01, durable request receipts support hard deletion without becoming workout or backup source facts.
    - Existing schema 15 and 16 logical backups remain restorable and schema 17 is explicitly accepted.
    - The released migration prefix remains immutable and malformed receipt schemas fail closed.
  artifacts:
    - {path: src/platform/sqlite/migrations/0017_workout_remove_receipts.ts, provides: additive STRICT removal-receipt schema}
    - {path: src/domains/portability/restoreCommands.ts, provides: explicit schema 15 through 17 restore compatibility}
  key_links:
    - {from: migrations/index.ts, to: 0017_workout_remove_receipts.ts, via: append-only migration registration}
    - {from: restoreCommands.ts, to: logical backup versions, via: explicit producer-version allowlist excluding retry receipts}
---

<objective>Establish the durable receipt and restore-compatible schema foundation for D-01 hard deletion.

Purpose: let later removal commands replay committed results without treating operational receipts as user-owned workout facts.
Output: migration 17, manifest registration, restore compatibility, and schema tests.</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-post-candidate-ux-refinement/07-CONTEXT.md
@.planning/phases/07-post-candidate-ux-refinement/07-UI-SPEC.md
@.planning/phases/07-post-candidate-ux-refinement/07-RESEARCH.md
@.planning/phases/07-post-candidate-ux-refinement/07-VALIDATION.md
</context>

<tasks>
<task type="tracer" tdd="true">
  <name>Task 1: Add the durable removal-receipt migration</name>
  <files>src/platform/sqlite/migrations/0017_workout_remove_receipts.ts, src/platform/sqlite/migrations/index.ts, tests/sqlite-host/migrations-effects.test.ts</files>
  <read_first>src/platform/sqlite/migrations/0016_portability_restore_state.ts, src/platform/sqlite/migrations/index.ts, tests/sqlite-host/migrations-effects.test.ts</read_first>
  <behavior>
    - Migration 17 creates a STRICT receipt table keyed by bounded request ID with canonical hash, operation, session/set identity, committed result/revision, and commit time.
    - The table has no foreign key to a deletable set row and the immutable migration prefix is unchanged.
    - Schema verification rejects missing columns, loosened constraints, or reordered historical migrations.
  </behavior>
  <action>RED first. Per D-01, append migration 17 and register it after v16. Store only stable identity/hash/result metadata needed to replay a committed deletion; never add UI state, set observations, or a foreign key to the row that will be deleted. Extend the host migration contract for clean and upgraded databases.</action>
  <verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/migrations-effects.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Migration 17 is additive, STRICT, append-only, verifiable, and can preserve a deletion receipt after its target row is gone.</acceptance_criteria>
  <done>The database can durably identify a committed removal request.</done>
  <reversibility rating="costly">Once databases migrate, rollback requires a later forward migration rather than changing released history.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve logical restore compatibility across schema 17</name>
  <files>src/domains/portability/restoreCommands.ts, src/domains/portability/restoreCommands.test.ts, tests/sqlite-host/migrations-effects.test.ts</files>
  <read_first>src/domains/portability/restoreCommands.ts, src/domains/portability/restoreCommands.test.ts, src/domains/portability/backupContracts.ts</read_first>
  <behavior>
    - Logical backups produced by schemas 15 and 16 remain accepted and schema 17 is explicitly accepted.
    - Removal receipts stay outside logical user-data backup and restore.
    - Unsupported future versions and malformed payloads still fail before mutation.
  </behavior>
  <action>RED first. Extend the explicit restore producer-version contract to schema 17 while preserving validation and all 15/16 cases. Keep the receipt table outside LOGICAL_BACKUP_TABLES because it is retry metadata, not authoritative user data. Do not weaken fail-closed future-version handling.</action>
  <verify><automated>npm run test:unit -- --runInBand src/domains/portability/restoreCommands.test.ts &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/migrations-effects.test.ts</automated></verify>
  <acceptance_criteria>Schemas 15–17 restore through explicit tested compatibility while receipts never enter logical backup content.</acceptance_criteria>
  <done>The removal foundation preserves portability and source-data boundaries.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
| Artifact | Contract |
|---|---|
| `workout_remove_receipts` | Durable request/hash/result replay metadata |
| migration 17 restore support | Explicit schemas 15–17 compatibility |
</artifacts_this_phase_produces>

<commit_protocol>After each task passes, create one atomic commit with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit`; its final non-empty line must be `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`, verified by `git log -1 --format=%B`.</commit_protocol>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Migration/restore input to SQLite | Schema and archive versions cross into persistent local state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-01-SCHEMA | Tampering | migration manifest | high | mitigate | Append-only index and exact schema assertions. |
| T-07-01-RESTORE | Tampering | restore compatibility | high | mitigate | Explicit versions, pre-mutation validation, receipts excluded from backup. |
</threat_model>

<verification>Run focused restore and host-migration suites, typecheck, and git diff --check.</verification>
<success_criteria>UX-18 has a bounded durable-replay foundation without changing workout facts or backup ownership.</success_criteria>
<output>Create `.planning/phases/07-post-candidate-ux-refinement/07-01-SUMMARY.md` when done.</output>
