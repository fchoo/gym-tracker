---
phase: 07-post-candidate-ux-refinement
plan: 02
type: execute
wave: 2
depends_on: [07-01]
files_modified:
  - src/domains/workout/activeWorkout.ts
  - src/domains/workout/setCommands.ts
  - src/domains/workout/setCommands.test.ts
  - src/domains/workout/index.ts
  - src/platform/sqlite/repositories/workoutRepository.ts
  - tests/integration/complete-set.test.ts
  - tests/sqlite-host/historyCommandRepository.test.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/bootstrap/workoutAppRuntime.test.tsx
  - app/workout/[sessionId].tsx
autonomous: true
requirements: [UX-18]
estimate: {tokens: 48000, raw_tokens: 48000, tasks: 3, confidence: low}
ui_considerations:
  - {id: UI-E03-REMOVE, category: error, element: failed removal preserves active facts and supports retry, verification: explicit, owner: 07-02 command repository and runtime tests}
  - {id: UI-E05-REMOVE, category: partial, element: incomplete rows removable and completed rows immutable, verification: explicit, owner: 07-02 transaction tests}
must_haves:
  truths:
    - Per D-01, the command hard-deletes exactly one eligible row in the existing serialized BEGIN IMMEDIATE writer and records its receipt in the same transaction.
    - Per D-02, completed rows and completed-history facts cannot be deleted; stale, mismatched, or replay-conflicting inputs leave all source facts unchanged.
    - Commit deletes and repairs active-session facts; the refreshed active view derives totals immediately from remaining rows.
    - `workoutOutcomeRepository` and `historyRepository` finalization later build partial/completed history snapshots from those remaining rows; removal creates no in-progress effective-history projection.
    - Runtime and route return only a trusted post-commit refresh and expose distinct remove commands rather than reinterpreting skip.
  artifacts:
    - {path: src/domains/workout/activeWorkout.ts, provides: typed remove inputs results and repository ports}
    - {path: src/platform/sqlite/repositories/workoutRepository.ts, provides: incomplete-only receipt-backed hard delete and active-state repair}
    - {path: src/bootstrap/workoutAppRuntime.tsx, provides: committed remove adapters and trusted refresh}
  key_links:
    - {from: remove command, to: workoutRepository, via: canonical request hash and expected session/set revisions}
    - {from: removal transaction, to: refreshed ActiveWorkoutView, via: commit then trusted read of remaining rows}
    - {from: remaining session_sets, to: workoutOutcomeRepository/historyRepository, via: existing finalization path tested after removal}
---

<objective>Implement D-01/D-02 removal below the UI, from typed commands through authoritative SQLite and committed runtime refresh.

Purpose: provide an integrity-complete removal port for the later confirmation UI.
Output: domain contracts, atomic repository mutation, finalization regressions, runtime adapters, and route wiring.</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-post-candidate-ux-refinement/07-CONTEXT.md
@.planning/phases/07-post-candidate-ux-refinement/07-UI-SPEC.md
@.planning/phases/07-post-candidate-ux-refinement/07-RESEARCH.md
@.planning/phases/07-post-candidate-ux-refinement/07-01-SUMMARY.md
</context>

<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Define and validate distinct hard-remove commands</name>
  <files>src/domains/workout/activeWorkout.ts, src/domains/workout/setCommands.ts, src/domains/workout/setCommands.test.ts, src/domains/workout/index.ts</files>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/workout/setCommands.ts, src/domains/workout/setCommands.test.ts, src/domains/workout/index.ts</read_first>
  <behavior>
    - removeWarmup/removeWorkingSet require request identity/hash, session/set identity, and expected session/set revisions.
    - Inputs are kind-specific; invalid, empty, or mismatched data fails before repository invocation.
    - Commands expose committed/already_committed results and never call a skip method.
  </behavior>
  <action>RED first. Add exported RemoveWarmupInput, RemoveWorkingSetInput, RemoveSetResult, repository methods, and thin command wrappers per D-01/D-02. Preserve request/revision validation and post-commit-effect ordering. Delete the orphaned copy-previous-warmup domain export while touching this surface; keep skip APIs only for historical/internal compatibility, never as remove implementation.</action>
  <verify><automated>npm run test:unit -- --runInBand src/domains/workout/setCommands.test.ts &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Typed removal rejects invalid/stale-shaped inputs before persistence and cannot resolve to skip semantics.</acceptance_criteria>
  <done>The domain exposes distinct, revision-bound hard-removal commands.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Delete and repair active-session facts atomically</name>
  <files>src/platform/sqlite/repositories/workoutRepository.ts, tests/integration/complete-set.test.ts, tests/sqlite-host/historyCommandRepository.test.ts</files>
  <read_first>src/platform/sqlite/repositories/workoutRepository.ts, src/platform/sqlite/serializedWriter.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/platform/sqlite/repositories/historyRepository.ts, tests/integration/complete-set.test.ts, tests/sqlite-host/historyCommandRepository.test.ts</read_first>
  <behavior>
    - The writer validates in-progress session, ownership, revisions, set kind, incomplete status including legacy skipped, and receipt hash before deleting.
    - Delete, ordinal normalization, active pointer/rest repair, session revision, and receipt commit atomically; injected failure rolls all back.
    - Replayed same ID/hash returns the recorded result; a changed payload fails; completed/snapshot-referenced rows cannot be removed.
    - Refreshed active totals immediately derive from remaining rows.
    - Finalizing partial or completed workouts through workoutOutcomeRepository/historyRepository creates history from remaining rows only; no removal-time effective-history projection is written.
  </behavior>
  <action>RED first. Implement both repository methods in the existing serialized writer and use a collision-safe two-stage ordinal normalization. Repair active_set_id and rest only when the removed row owns that state. Test warm-up, active/non-active/last working row, legacy skipped row, replay/conflict, ownership, stale revisions, completed/snapshot references, FK and rollback. Add explicit finalization regressions that remove a row, then call the existing workoutOutcomeRepository/historyRepository partial and completed finalization paths and assert their snapshots/totals contain only remaining rows. Do not create or rebuild an effective-history projection while the session is active.</action>
  <verify><automated>npm run test:integration -- --runInBand tests/integration/complete-set.test.ts &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/historyCommandRepository.test.ts &amp;&amp; npm run test:coverage -- --runInBand</automated></verify>
  <acceptance_criteria>Commit atomically deletes/repairs active facts, active totals refresh from remaining rows, and later partial/completed finalization snapshots those same rows through both named repositories.</acceptance_criteria>
  <done>Removal and finalization semantics exactly match the clarified CONTEXT/UI-SPEC contract.</done>
  <reversibility rating="costly">A confirmed incomplete-row delete cannot be undone, so receipt, revision, status, and transaction guards are mandatory.</reversibility>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose committed removal through runtime and route</name>
  <files>src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutAppRuntime.test.tsx, app/workout/[sessionId].tsx</files>
  <read_first>src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutAppRuntime.test.tsx, app/workout/[sessionId].tsx, src/domains/workout/activeWorkout.ts</read_first>
  <behavior>
    - Runtime forwards exact input, awaits repository commit, then trusted-reads the active view before resolving.
    - Rejection returns no success acknowledgement/effect and leaves the prior view available.
    - Route exposes removeWarmup/removeWorkingSet to the screen and removes obsolete copy-warmup wiring.
  </behavior>
  <action>RED first. Add both remove adapters to the existing runWorkoutMutation family, preserve conflict/error mapping, and refresh only after commit. Pass typed ports through the workout route for 07-04 consumption and remove the unused copy-warmup route/runtime seam. Do not splice view rows or drain success effects before repository resolution.</action>
  <verify><automated>npm run test:unit -- --runInBand src/bootstrap/workoutAppRuntime.test.tsx &amp;&amp; npm run test:integration -- --runInBand tests/integration/complete-set.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <acceptance_criteria>The screen-facing port receives only committed refreshed state and no obsolete copy or skip-backed remove route remains.</acceptance_criteria>
  <done>The integrity-complete removal capability is ready for the dependent UI plan.</done>
</task>
</tasks>

<artifacts_this_phase_produces>
| Artifact | Contract |
|---|---|
| Remove domain inputs/results | Revision-bound distinct commands |
| workoutRepository remove methods | Atomic incomplete delete and active-state repair |
| runtime/route remove ports | Trusted post-commit refresh |
| finalization regressions | Remaining-row history through workoutOutcomeRepository/historyRepository |
</artifacts_this_phase_produces>

<commit_protocol>After each task passes, create one atomic commit with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit`; end with exactly `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;` and verify via `git log -1 --format=%B`.</commit_protocol>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Runtime request to SQLite | Destructive stale/replayed input crosses into authoritative facts. |
| Active rows to later history | Finalization must snapshot only committed remaining rows. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-REMOVE | Tampering | remove transaction | high | mitigate | Ownership/status/revision/hash checks, durable receipt, one exclusive transaction. |
| T-07-REMOVE-REPLAY | Repudiation | retry identity | high | mitigate | Persist result; reject same ID with changed hash. |
| T-07-HISTORY | Tampering | finalization snapshot | high | mitigate | Explicit outcome/history repository tests after removal; no active-session projection. |
</threat_model>

<verification>Run focused command/runtime/integration/host/coverage gates, typecheck, boundaries, and git diff --check.</verification>
<success_criteria>UX-18 is authoritative below the UI: exact incomplete deletion, source-backed active totals, and later remaining-row finalization history.</success_criteria>
<output>Create `.planning/phases/07-post-candidate-ux-refinement/07-02-SUMMARY.md` when done.</output>
