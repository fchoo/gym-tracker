---
phase: 07-post-candidate-ux-refinement
plan: 07
type: execute
wave: 1
depends_on: []
files_modified:
  - package.json
  - package-lock.json
  - src/domains/rest/restCountdownCuePort.ts
  - src/platform/audio/expoRestCountdownCueAdapter.ts
  - src/platform/audio/expoRestCountdownCueAdapter.test.ts
  - assets/audio/rest-cue-short.wav
  - assets/audio/rest-cue-long.wav
autonomous: false
requirements: [UX-21]
estimate: {tokens: 37000, raw_tokens: 37000, tasks: 3, confidence: low}
ui_considerations:
  - {id: UI-B01-AUDIO, category: loading, element: audio initialization never blocks rest, verification: backstop, owner: 07-07 adapter tests and 07-10 native evidence}
must_haves:
  truths:
    - Per D-03, official `expo-audio@~57.0.4` is installed only after a blocking legitimacy checkpoint.
    - Hook-shaped local playback exposes short/long best-effort cues and no rest mutation authority.
    - Deterministic bundled WAV assets require no network and playback failure is bounded.
  artifacts:
    - {path: src/domains/rest/restCountdownCuePort.ts, provides: narrow short/long cue contract}
    - {path: src/platform/audio/expoRestCountdownCueAdapter.ts, provides: hook-safe Expo local players}
    - {path: assets/audio/rest-cue-short.wav, provides: deterministic short cue}
    - {path: assets/audio/rest-cue-long.wav, provides: deterministic terminal cue}
  key_links:
    - {from: expo audio hook adapter, to: bundled WAVs, via: documented local require sources}
---
<objective>Approve and implement the native local-audio core for UX-21 without touching ActiveWorkout files.</objective>
<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-post-candidate-ux-refinement/07-CONTEXT.md
@.planning/phases/07-post-candidate-ux-refinement/07-RESEARCH.md
@.planning/phases/07-post-candidate-ux-refinement/07-PATTERNS.md
</context>
<tasks>
<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify `expo-audio@~57.0.4` legitimacy</name>
  <files>package.json, package-lock.json</files>
  <read_first>.planning/phases/07-post-candidate-ux-refinement/07-RESEARCH.md, package.json</read_first>
  <action>Before install, record official Expo SDK docs, `expo/expo` repository, bundled `~57.0.4` range, approximately 2.04M weekly downloads, and absent postinstall. SUS is solely publication recency. D-03 already approves a native audio dependency; verify identity only and make no dependency change before confirmation.</action>
  <verify><automated>node -e "const p=require('./package.json');if(p.dependencies?.['expo-audio'] || p.devDependencies?.['expo-audio'])process.exit(1)"</automated></verify>
  <acceptance_criteria>Human verifies all named supply-chain facts.</acceptance_criteria>
  <done>The blocking legitimacy record permits the exact package.</done>
  <what-built>Package identity evidence for the owner-approved dependency.</what-built>
  <how-to-verify>Confirm official Expo docs/repository, range, downloads, no postinstall, and recency-only SUS.</how-to-verify>
  <resume-signal>Reply `approved expo-audio legitimacy` or report mismatch.</resume-signal>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Install Expo Audio and define the hook-shaped port</name>
  <precondition>The blocking expo-audio legitimacy checkpoint is approved.</precondition>
  <files>package.json, package-lock.json, src/domains/rest/restCountdownCuePort.ts, src/platform/audio/expoRestCountdownCueAdapter.ts, src/platform/audio/expoRestCountdownCueAdapter.test.ts</files>
  <read_first>src/domains/rest/restNotificationPort.ts, src/platform/notifications/expoForegroundRestFeedbackAdapter.ts, src/platform/notifications/expoForegroundRestFeedbackAdapter.test.ts</read_first>
  <behavior>
    - Direct dependency is exact compatible range and lockfile has no unexpected scripts.
    - Hook-shaped adapter owns player lifecycle and exposes short/long best-effort calls only.
    - Failure cannot throw into or command rest state.
  </behavior>
  <action>RED first. Install exactly `expo-audio@~57.0.4` using the pinned lockfile toolchain and review dependency diff. Define a hook-shaped port so useAudioPlayer remains in React lifecycle. Exclude recording, remote URLs, notification-channel reuse, custom native bridges, and rest command methods.</action>
  <verify><automated>npm run test:unit -- --runInBand src/platform/audio/expoRestCountdownCueAdapter.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm ls expo-audio</automated></verify>
  <acceptance_criteria>Approved dependency and narrow lifecycle-safe non-authoritative adapter pass.</acceptance_criteria>
  <done>The native audio boundary is ready for 07-04 integration.</done>
  <reversibility rating="costly">Removing a native module requires dependency, CNG, build, and consumer changes but no data migration.</reversibility>
</task>
<task type="auto" tdd="true">
  <name>Task 3: Generate deterministic local short and long tones</name>
  <files>assets/audio/rest-cue-short.wav, assets/audio/rest-cue-long.wav, src/platform/audio/expoRestCountdownCueAdapter.test.ts</files>
  <read_first>src/platform/audio/expoRestCountdownCueAdapter.ts, .planning/phases/07-post-candidate-ux-refinement/07-UI-SPEC.md</read_first>
  <behavior>
    - PCM assets are deterministic, source-controlled, license-safe, audibly distinct, and locally loadable.
    - Short cue is used for 3/2/1 and longer distinct cue for zero.
  </behavior>
  <action>Generate both WAVs from documented frequency/duration/sample constants and test headers/duration/hash plus adapter mapping. Do not fetch media or synthesize dynamically at runtime.</action>
  <verify><automated>npm run test:unit -- --runInBand src/platform/audio/expoRestCountdownCueAdapter.test.ts &amp;&amp; git diff --check</automated></verify>
  <acceptance_criteria>Assets regenerate deterministically and map to distinct short/long operations.</acceptance_criteria>
  <done>UX-21 audio core is complete.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
| Artifact | Contract |
|---|---|
| RestCountdownCuePort | Playback only, no authority |
| Expo adapter | Hook-safe bundled asset players |
| WAV pair | Deterministic distinct cues |
</artifacts_this_phase_produces>
<commit_protocol>After implementation tasks pass, make atomic commits ending exactly `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`; checkpoint makes no commit.</commit_protocol>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| npm to native runtime | New native code enters build. |
| playback to rest | Disposable feedback must not own state. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-SC | Tampering | dependency | high | mitigate | Blocking identity checkpoint, exact range, lockfile review, no postinstall. |
| T-07-AUDIO-CORE | Tampering | adapter | high | mitigate | Narrow no-command port, caught failures, local assets. |
</threat_model>
<verification>Run adapter tests, typecheck, dependency inspection, boundaries, and git diff --check.</verification>
<success_criteria>UX-21 has an approved deterministic native playback core ready for UI integration.</success_criteria>
<output>Create `.planning/phases/07-post-candidate-ux-refinement/07-07-SUMMARY.md` when done.</output>
