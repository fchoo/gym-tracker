---
phase: 05-recovery-distribution-and-release
plan: 02
type: execute
wave: 1
depends_on: [05-01]
files_modified:
  - .github/workflows/nightly.yml
  - .github/workflows/release-candidate.yml
  - .github/workflows/release-promotion.yml
  - scripts/create-release-candidate-manifest.mjs
  - scripts/verify-release-candidate-manifest.mjs
  - scripts/verify-release-promotion.mjs
  - scripts/release-candidate-contract.test.mjs
  - package.json
autonomous: true
requirements: [REL-03, REL-04, REL-05]
must_haves:
  truths:
    - A private candidate workflow creates signed APK/AAB bytes exactly once, records source/build/raw/inner-file hashes, and retains one immutable bundle.
    - The nightly/release matrix is explicit and cannot quietly omit migration, lifecycle, backup/restore, adaptive/accessibility, performance, or mutation contracts.
    - Promotion can only publish downloaded validated candidate bytes and contains no source build step.
  artifacts:
    - { path: .github/workflows/release-candidate.yml, provides: private build-once signed candidate workflow }
    - { path: .github/workflows/release-promotion.yml, provides: digest-verified no-rebuild public promotion }
    - { path: scripts/verify-release-promotion.mjs, provides: candidate/approval/digest validation contract }
  key_links:
    - { from: .github/workflows/release-candidate.yml, to: scripts/create-release-candidate-manifest.mjs, via: exact APK/AAB source/config and SHA-256 manifest, pattern: sha256 }
    - { from: .github/workflows/release-promotion.yml, to: scripts/verify-release-promotion.mjs, via: downloaded artifact validation before release upload, pattern: no rebuild }
---

<objective>
Rehearse the private candidate and public-promotion contracts before Phase 5 product data work depends on them.

Purpose: ensure release identity is a property of retained bytes, not a future rebuild.
Output: workflow and script contracts for nightly coverage, private candidate creation, digest-bound physical evidence, and no-rebuild promotion.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Define canonical candidate/approval manifests and local verification scripts</name>
<read_first>.github/workflows/pr.yml, scripts/verify-phase4-native-evidence.mjs, scripts/hash-apk.sh, app.config.ts, 05-CONTEXT.md</read_first>
<files>scripts/create-release-candidate-manifest.mjs, scripts/verify-release-candidate-manifest.mjs, scripts/verify-release-promotion.mjs, scripts/release-candidate-contract.test.mjs, package.json</files>
<action>Start with Node tests using synthetic artifacts. Define strict canonical manifests containing candidate schema version, commit/source-tree/config digest, build profile/toolchain metadata, APK/AAB raw SHA-256/size, relevant inner-file hashes, and retained artifact identity. Define a separately supplied physical-review record that must match every candidate digest and only becomes eligible when its owner token is the literal lowercase `approved`; do not generate that record in this plan. Reject missing/extra fields, digest mismatch, source mismatch, path traversal, stale/malformed record, wrong token, and candidate substitutions. Add named scripts for candidate verification, promotion verification, and the future release matrix without invoking a build locally.</action>
<acceptance_criteria>Contract scripts demonstrate that a promotion verifier accepts only an unchanged validated candidate bundle and cannot mistake a hand-written manifest or altered nested artifact for the reviewed candidate.</acceptance_criteria>
<verify><automated>node --test scripts/release-candidate-contract.test.mjs</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add private candidate, no-rebuild promotion, and nightly matrix workflows</name>
<read_first>.github/workflows/pr.yml, package.json, scripts/release-candidate-contract.test.mjs, 05-CONTEXT.md</read_first>
<files>.github/workflows/nightly.yml, .github/workflows/release-candidate.yml, .github/workflows/release-promotion.yml, package.json, scripts/release-candidate-contract.test.mjs</files>
<action>Implement pinned-action workflows with least-privilege permissions, signing secrets confined to the private candidate job, deterministic toolchain setup, and no secret-bearing logs. Candidate builds APK/AAB once in one job, runs the applicable automated suite against those exact outputs, writes/verifies the manifest, and retains a private artifact bundle. Nightly schedules the complete source/native automated matrix, including migration/lifecycle/permission/backup-restore/mutation/adaptive/accessibility/performance contracts as they land. Promotion takes a candidate identifier and release tag, downloads the retained bundle, validates candidate plus reviewed physical record in a protected environment, uploads only those exact bytes, and contains no `expo`, Gradle build, or build script invocation. Add workflow-content tests that fail if a promotion build step or missing required suite appears.</action>
<acceptance_criteria>REL-03 through REL-05 are executable release contracts; public promotion has an auditable digest boundary and cannot rebuild, while physical approval remains intentionally unavailable until the final gate.</acceptance_criteria>
<verify><automated>node --test scripts/release-candidate-contract.test.mjs &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run script/workflow contract tests, `node --check` for all new scripts, typecheck, lint, boundary checks, and diff hygiene. Do not trigger a signing, release, or publication workflow.</verification>
<success_criteria>Release contracts retain the exact candidate identity needed by the final physical gate without creating release evidence or public artifacts early.</success_criteria>
