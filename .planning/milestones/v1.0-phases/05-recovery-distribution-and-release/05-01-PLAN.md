---
phase: 05-recovery-distribution-and-release
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domains/portability/backupContracts.ts
  - src/domains/portability/backupFormat.ts
  - src/domains/portability/backupErrors.ts
  - src/domains/portability/index.ts
  - src/platform/crypto/aesGcmArchivePort.ts
  - src/platform/crypto/passwordKdf.ts
  - src/domains/portability/backupContracts.test.ts
  - src/domains/portability/backupFormat.test.ts
  - src/platform/crypto/aesGcmArchivePort.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - Backup v1 has an exact versioned logical manifest/envelope contract and never names raw SQLite as its product payload.
    - Header metadata is canonical AES-GCM AAD and KDF/cipher/nonce/tag/length fields fail closed on drift or tampering.
    - Archive bounds, ownership/wiping, safe errors, and frozen crypto-format vectors are executable before data collection exists.
  artifacts:
    - { path: src/domains/portability/backupContracts.ts, provides: versioned logical snapshot, limits, and table-manifest contract }
    - { path: src/domains/portability/backupFormat.ts, provides: bounded GTBK v1 binary parser/encoder and canonical AAD header }
    - { path: src/platform/crypto/aesGcmArchivePort.ts, provides: narrow Expo AES-256-GCM adapter with owned-buffer cleanup }
  key_links:
    - { from: src/domains/portability/backupFormat.ts, to: src/platform/crypto/passwordKdf.ts, via: exact Argon2id descriptor and owned bytes, pattern: argon2id }
    - { from: src/platform/crypto/aesGcmArchivePort.ts, to: src/domains/portability/backupFormat.ts, via: canonical header AAD, pattern: additionalData }
---

<objective>
Freeze the logical backup v1 and cryptographic boundary before any SQLite collection or UI code can emit an archive.

Purpose: make format compatibility, authentication, limits, and secret lifetime auditable and testable in isolation.
Output: a canonical GTBK v1 contract, bounded parser/encoder, reviewed AES-GCM adapter, and known-answer/failure fixtures.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Define the fail-closed logical snapshot, table ownership, and archive limits</name>
<read_first>05-CONTEXT.md, 05-RESEARCH.md, src/platform/crypto/passwordKdf.ts, src/domains/shared/errors.ts, src/platform/sqlite/migrations/0001_initial.ts, src/platform/sqlite/migrations/0015_progression_evidence.ts</read_first>
<files>src/domains/portability/backupContracts.ts, src/domains/portability/backupErrors.ts, src/domains/portability/index.ts, src/domains/portability/backupContracts.test.ts</files>
<action>Write tests first for a version-1 logical snapshot manifest and explicit allowlisted table ownership registry. Encode the locked 16 KiB/32 MiB/24 MiB/100,000/25,000/64 KiB/depth-16 ceilings, exact logical schema version, stable row ordering requirements, source-reference versus bundled/derivative exclusions, safe error taxonomy, and zeroing helpers. Model only non-sensitive metadata in the header. Reject unknown tables/fields, unsafe numbers, duplicate IDs, invalid references, too-large values, and inconsistent per-table/total counts. Do not enumerate SQLite dynamically or reuse migration raw-file recovery.</action>
<acceptance_criteria>There is one test-owned source of truth for what a user backup may contain, every v1 bound fails closed at its boundary plus one, and error values cannot include passwords, keys, archive bytes, session payloads, or file paths.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/portability/backupContracts.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement canonical authenticated envelope and reviewed AES-GCM bridge</name>
<read_first>src/platform/crypto/passwordKdf.ts, node_modules/expo-crypto/src/aes/, 05-CONTEXT.md, src/domains/shared/diagnostics.ts</read_first>
<files>src/domains/portability/backupFormat.ts, src/platform/crypto/aesGcmArchivePort.ts, src/platform/crypto/passwordKdf.ts, src/domains/portability/backupFormat.test.ts, src/platform/crypto/aesGcmArchivePort.test.ts</files>
<action>Test first with fixed byte fixtures. Implement GTBK magic/version/header-length encoding, canonical UTF-8 header serialization, exact header bytes as AES-GCM AAD, fixed Argon2id release descriptor validation, 16-byte salt/12-byte nonce/32-byte key constraints, authenticated decrypt-before-parse, and owned-buffer `finally` wiping. Probe the installed Expo Crypto API behind one adapter and prove detached/combined tag behavior by test rather than assumption. Reject altered header/ciphertext/tag/nonce/length, bad base64, noncanonical JSON, unsupported compression/version, repeated/invalid field names, and wrong passwords with the same safe unopened result. Preserve the existing KDF behavior and add only caller-boundary cleanup/tests needed for archive use.</action>
<acceptance_criteria>Frozen known-answer archives decrypt only with the exact descriptor/header/ciphertext, every authenticated-field mutation fails before payload parsing, and owned secret/plaintext buffers are wiped on success, failure, and cancellation paths.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/portability/backupFormat.test.ts src/platform/crypto/aesGcmArchivePort.test.ts src/platform/crypto/passwordKdf.test.ts</automated></verify>
</task>
</tasks>

<verification>Run the focused unit suite, typecheck, lint/boundary checks, and coverage gate. Inspect test vectors to ensure no real user data or passwords are embedded.</verification>
<success_criteria>DATA-01 through DATA-03 have an audited v1 contract whose only later input is validated logical data, never a raw database copy.</success_criteria>
