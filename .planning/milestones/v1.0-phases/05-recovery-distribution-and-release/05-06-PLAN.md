---
phase: 05-recovery-distribution-and-release
plan: 06
type: execute
wave: 2
depends_on: [05-03, 05-05]
files_modified:
  - src/domains/portability/csvExport.ts
  - src/platform/sqlite/repositories/csvExportRepository.ts
  - src/platform/files/expoCsvFilePort.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - app/more/data-and-recovery.tsx
  - src/domains/portability/csvExport.test.ts
  - tests/sqlite-host/csvExportRepository.test.ts
  - app/more/__tests__/data-and-recovery.test.tsx
autonomous: true
requirements: [DATA-07]
must_haves:
  truths:
    - CSV has a fixed versioned schema/order with explicit units and locale-independent numeric/timestamp output.
    - Rows include set kind, correction/void state, recommendation/decision context, and safe spreadsheet escaping.
    - The user explicitly shares a readable export through the same bounded file lifecycle discipline.
  artifacts:
    - { path: src/domains/portability/csvExport.ts, provides: versioned CSV schema/serializer and injection-safe escaping }
    - { path: src/platform/sqlite/repositories/csvExportRepository.ts, provides: stable factual export rows }
    - { path: src/platform/files/expoCsvFilePort.ts, provides: deterministic readable-file and share lifecycle }
  key_links:
    - { from: src/platform/sqlite/repositories/csvExportRepository.ts, to: src/domains/portability/csvExport.ts, via: ordered factual rows to stable serializer, pattern: CSV }
    - { from: src/bootstrap/workoutAppRuntime.tsx, to: src/platform/files/expoCsvFilePort.ts, via: typed explicit export/share command, pattern: share }
---

<objective>
Provide a separate readable, safely escaped, versioned CSV export of the owner’s historical and decision data.

Purpose: support owner-controlled analysis without confusing a CSV with encrypted backup/restore.
Output: stable SQL read model, RFC-compatible serializer, bounded file/share adapter, and accessible export flow.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Freeze CSV schema and deterministic factual row read model</name>
<read_first>05-CONTEXT.md, src/platform/sqlite/repositories/historyRepository.ts, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/domains/portability/backupContracts.ts</read_first>
<files>src/domains/portability/csvExport.ts, src/platform/sqlite/repositories/csvExportRepository.ts, src/domains/portability/csvExport.test.ts, tests/sqlite-host/csvExportRepository.test.ts</files>
<action>Write golden fixtures first for empty, one/many, corrected, voided/restored, warmup/working, pending/decided recommendation, commas/quotes/newlines, formula-leading text, Unicode, decimal, and timestamp values. Define a v1 fixed header/column order with explicit unit columns, ISO/UTC and local-context fields as required, invariant decimal serialization, set kind, correction/void lifecycle, recommendation rule/status/decision data, and source IDs. Query only factual source/audit tables in deterministic primary/time order; do not query FTS/projections or invent localized display strings. Implement RFC 4180 quoting plus safe leading-character neutralization for spreadsheet formulas.</action>
<acceptance_criteria>Repeated exports of the same logical state produce byte-stable rows/header ordering, preserve required audit/decision facts, and prevent formula interpretation while retaining readable values.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/portability/csvExport.test.ts &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/csvExportRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Write/share CSV and complete the Data and recovery export path</name>
<read_first>src/platform/files/expoBackupFilePort.ts, src/bootstrap/workoutAppRuntime.tsx, app/more/data-and-recovery.tsx, 05-UI-SPEC.md</read_first>
<files>src/platform/files/expoCsvFilePort.ts, src/bootstrap/workoutAppRuntime.tsx, app/more/data-and-recovery.tsx, app/more/__tests__/data-and-recovery.test.tsx</files>
<action>Implement a cache-file adapter with deterministic safe filenames, UTF-8 bytes, bounded write/cleanup, and explicit OS share. Add a runtime capability and UI section using the exact CSV warning, busy/error/success/share states, semantic labels, and no optimistic claim. Reuse file/share abstractions where appropriate without treating CSV as encrypted, restoreable, or secret. Test share-unavailable/cancel/error cleanup and verify route code still reaches only the runtime surface.</action>
<acceptance_criteria>The owner can explicitly create and share a deterministic readable CSV, with an unambiguous privacy warning and no source mutation on any export failure.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand app/more/__tests__/data-and-recovery.test.tsx &amp;&amp; npm run test:unit -- --runInBand src/domains/portability/csvExport.test.ts</automated></verify>
</task>
</tasks>

<verification>Run golden serializer, host SQLite, component, typecheck, lint/boundary, and integrity coverage checks. Inspect fixtures for synthetic-only data and invariant decimal/timestamp output.</verification>
<success_criteria>DATA-07 is delivered as an explicit, deterministic, readable export with complete audit and safe-escaping coverage.</success_criteria>
