---
phase: 06-material-3-ux-remediation
plan: 04
type: execute
wave: 1
depends_on: [06-01, 06-02]
files_modified:
  - src/ui/screens/CalendarScreen.tsx
  - src/ui/__tests__/CalendarScreen.test.tsx
  - src/ui/components/CalendarField.tsx
  - src/ui/components/CalendarField.test.tsx
  - src/ui/__tests__/ScheduleEditor.test.tsx
  - src/ui/__tests__/SessionCorrectionScreen.test.tsx
  - src/ui/__tests__/StarterPlans.test.tsx
autonomous: true
requirements: [UX-03, UX-05]
estimate:
  tokens: 52000
  raw_tokens: 52000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Per D-73 and D-74, Calendar has 42 civil-date cells, selectable adjacent dates, and equal swipe/button/keyboard month transitions.
    - Per D-75, all CalendarField consumers share a private bounded draft that commits only by explicit Apply Date.
    - Per D-80 and D-81, component contracts and the passed 06-02 native gesture gate establish accessible date behavior on exact LocalDate values.
  artifacts:
    - path: src/ui/screens/CalendarScreen.tsx
      provides: six-row root Calendar with adjacent dates and dual month controls
    - path: src/ui/components/CalendarField.tsx
      provides: shared bounded Material 3 civil-date dialog
  key_links:
    - from: CalendarScreen
      to: loadCalendarMonth
      via: selected LocalDate plus visible-month inputs
    - from: CalendarField
      to: consumer onChange
      via: valid private draft only after Apply Date
---

<objective>
Deliver complete civil-date Calendar and date-dialog interaction on the native gesture configuration proven in Wave 0.

Purpose: replace variable/blank date grids without changing civil-date truth, bounds, or explicit commit behavior.
Output: 42-cell root Calendar, shared CalendarField dialog, and consumer regressions.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-UI-SPEC.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@.planning/phases/06-material-3-ux-remediation/06-01-SUMMARY.md
@.planning/phases/06-material-3-ux-remediation/06-02-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make root Calendar a complete LocalDate grid with equal month controls</name>
  <precondition>06-02-SUMMARY records a passing generated-native emulator gesture smoke with exact package/APK/manifest/report identity; halt if the acceptance gate is absent or red.</precondition>
  <files>src/ui/screens/CalendarScreen.tsx, src/ui/__tests__/CalendarScreen.test.tsx</files>
  <behavior>
    - Every visible month has 42 cells; adjacent dates are subdued, labelled, selectable, and consistently move visible month and selected LocalDate.
    - Left swipe equals Next and right swipe equals Previous, with labelled buttons plus keyboard/D-pad equivalents.
    - Loading preserves geometry, retry is recoverable, and date state has non-colour semantics.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Root Calendar and CalendarScreen.test.tsx, specifically CalendarScreen.tsx:276-386 and CalendarScreen.test.tsx:87-183. Use parseLocalDate/addLocalDays rather than JavaScript Date; retain selected-day clamping and typed loadCalendarMonth strings. Consume only the native seam proven by 06-02 to add the narrow horizontal gesture, with left=next and right=previous, while retaining labelled IconAction alternatives per D-73 and D-74. Per D-80 and D-81, test cells, adjacency, parity, focus, D-pad, reduced motion, System/Light/Dark, compact/medium/expanded, and 200% text; 06-09 owns exact-candidate native/attended backstop. Do not alter history facts, timezone ownership, routes, or add required horizontal scrolling.</action>
  <verify><automated>npm run test:unit -- --runInBand src/domains/scheduling/localDate.test.ts &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/CalendarScreen.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <done>Root Calendar always renders a stable 42-cell month and all supported controls yield the same selected civil date.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Unify all date fields on one explicit bounded dialog</name>
  <reversibility rating="costly">All in-app date fields share the dialog interaction contract, while stored-date representation remains unchanged.</reversibility>
  <files>src/ui/components/CalendarField.tsx, src/ui/components/CalendarField.test.tsx, src/ui/__tests__/ScheduleEditor.test.tsx, src/ui/__tests__/SessionCorrectionScreen.test.tsx, src/ui/__tests__/StarterPlans.test.tsx</files>
  <behavior>
    - Open creates a private draft; Keep Original Date, Back, and dismiss write nothing; Apply Date writes one in-bounds LocalDate.
    - The dialog exposes 42 cells, disabled out-of-bounds dates, valid adjacent selection, and swipe/button/arrow-key parity.
    - Leap days, year range, min/max, short height, 200% text, modal containment, and invoker focus restoration remain correct.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §CalendarField and CalendarField.test.tsx, specifically CalendarField.tsx:199-594, date helpers at 76-181, focus behavior at 240-293 and 394-430, and test analogs at 63-169. Per D-75, build the UI-SPEC hierarchy, complete bounded adjacent-month grid, Keep Original Date and Apply Date around the existing private LocalDate draft. Consume the native gesture seam accepted by 06-02 but retain buttons and arrow keys. Per D-80/D-81, test no-write paths, LocalDate bounds, keyboard/D-pad, reduced motion, 48dp, appearance, 200% text, and representative consumer coverage; 06-09 owns native/attended evidence. Do not parse date-only strings as instants, persist drafts, add per-screen pickers, or make a gesture essential.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/components/CalendarField.test.tsx src/ui/__tests__/ScheduleEditor.test.tsx src/ui/__tests__/SessionCorrectionScreen.test.tsx src/ui/__tests__/StarterPlans.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>Every date field inherits one bounded explicit-commit dialog with unchanged LocalDate and focus semantics.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Touch/keyboard input to date draft | Input changes bounded presentation draft only. |
| Date draft to typed consumer | A date crosses to source operations only after explicit Apply Date. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | LocalDate grid/bounds | high | mitigate | Use existing helpers, private drafts, bounds validation, and explicit apply. |
| T-06-02 | Tampering | Gesture input | high | mitigate | Keep gestures presentation-only and preserve equal labelled controls. |
</threat_model>

<verification>Run focused LocalDate/Calendar/CalendarField/consumer tests, typecheck, lint, boundaries, coverage, and git diff --check. Confirm 06-02's gate before starting. Per D-82, do not approve, promote, publish, tag, or run Terminal Seal.</verification>

<success_criteria>UX-03 and UX-05 are complete on the existing LocalDate model, and their gesture paths are gated by native evidence.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-04-SUMMARY.md when done.</output>
