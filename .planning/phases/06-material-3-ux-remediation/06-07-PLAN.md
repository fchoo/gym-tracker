---
phase: 06-material-3-ux-remediation
plan: 07
type: execute
wave: 2
depends_on: [06-01, 06-03]
files_modified:
  - tests/sqlite-host/progressRepository.test.ts
  - src/platform/sqlite/repositories/progressRepository.ts # conditional: only when named by 06-PROGRESS-DIAGNOSIS.md
  - src/bootstrap/workoutAppRuntime.tsx # conditional: only when named by 06-PROGRESS-DIAGNOSIS.md
  - src/bootstrap/workoutLifecycle.ts # conditional: only when named by 06-PROGRESS-DIAGNOSIS.md
  - src/platform/sqlite/effects/historyProjectionEffects.ts # conditional: only when named by 06-PROGRESS-DIAGNOSIS.md
  - src/ui/screens/ProgressScreen.tsx # conditional: only when named by 06-PROGRESS-DIAGNOSIS.md
  - src/ui/__tests__/ProgressScreen.test.tsx
autonomous: true
requirements: [UX-02, UX-10]
estimate:
  tokens: 48000
  raw_tokens: 48000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Per D-78, Progress returns its factual normal empty/populated/updating state or a bounded recoverable error; Retry reaches a later current result when the diagnosed condition is recoverable.
    - Per D-72, Progress uses the same labelled M3SearchField contract as the other three consumers while retaining screen-owned query filtering and source-backed cardinality states.
    - SQLite source facts, projection rebuildability, period semantics, parameterized reads, and safe freshness outcomes remain authoritative.
    - Production edits are limited to the exact diagnosis-authorized subset of the conditional paths below; an unlisted or unjustified production path blocks execution.
  artifacts:
    - path: .planning/phases/06-material-3-ux-remediation/06-PROGRESS-DIAGNOSIS.md
      provides: blocking causal branch, exact fixture, recovery predicate, and diagnosis-authorized production-path subset
    - path: src/ui/screens/ProgressScreen.tsx
      provides: shared Search and causal-repair-aware typed Progress retry presentation only when authorized
    - path: tests/sqlite-host/progressRepository.test.ts
      provides: full-migration causal regression and recovery proof
  key_links:
    - from: .planning/phases/06-material-3-ux-remediation/06-PROGRESS-DIAGNOSIS.md
      to: conditional production paths
      via: explicit allowed-production-paths subset and causal fixture requirement
    - from: src/ui/screens/ProgressScreen.tsx
      to: workoutAppRuntime loadProgress
      via: the established request-generation retry path and typed source-backed result
    - from: src/ui/components/M3SearchField.tsx
      to: src/ui/screens/ProgressScreen.tsx
      via: controlled Progress query and owner-supplied busy/empty/error/result slots
---

<objective>
Repair the evidenced Progress cause, then attach the shared Search and truthful retry presentation through the existing typed runtime seam.

Purpose: remove the audited Progress blocker without guessing its layer, bypassing SQLite/projections, or treating generic UI copy as a causal diagnosis.
Output: diagnosis-gated causal regression, minimal authorized repair, and Progress Search/retry coverage.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-UI-SPEC.md
@.planning/phases/06-material-3-ux-remediation/06-RESEARCH.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@.planning/phases/06-material-3-ux-remediation/06-PROGRESS-DIAGNOSIS.md
@.planning/phases/06-material-3-ux-remediation/06-01-SUMMARY.md
@.planning/phases/06-material-3-ux-remediation/06-03-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Repair only the diagnosis-authorized Progress source/projection seam</name>
  <precondition>06-PROGRESS-DIAGNOSIS.md exists, is candidate-bound, and explicitly records the exact rejecting branch, full-migration fixture, recovery predicate, and a non-empty allowed-production-paths subset. The subset may contain only src/platform/sqlite/repositories/progressRepository.ts, src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutLifecycle.ts, and src/platform/sqlite/effects/historyProjectionEffects.ts; halt before production edits if the artifact is missing, redacted inadequately, names no path, or names any other path.</precondition>
  <files>tests/sqlite-host/progressRepository.test.ts, src/platform/sqlite/repositories/progressRepository.ts (conditional), src/bootstrap/workoutAppRuntime.tsx (conditional), src/bootstrap/workoutLifecycle.ts (conditional), src/platform/sqlite/effects/historyProjectionEffects.ts (conditional)</files>
  <behavior>
    - The exact full-migration fixture first fails or returns the diagnosed bounded safe state, then passes only through the diagnosis-authorized causal repair.
    - Parameterized reads, typed period inputs, source authority, freshness/unavailable/updating semantics, and projection replay/rebuild behavior remain intact.
    - A recoverable diagnosed condition produces a current result after the same typed retry/load operation; an unrecoverable condition remains bounded and factual.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Progress UI and conditional storage lane, especially progressRepository.ts:283-438, workoutAppRuntime.tsx:2564-2567, workoutLifecycle.ts:131-160, historyProjectionEffects.ts:119-176 and 220-321, plus the host-test analog at progressRepository.test.ts:107-132 and 170-307. Per D-78 and D-80, implement only the artifact-named subset of the four conditional production paths and only the repair invariant the diagnosis proves. Preserve parameterized SQL, projectProgressPeriod inputs, source-backed freshness states, claim/retry/replay bounds, and migration cleanup. Do not add a path outside the stated subset, fabricate progress, suppress a rejecting branch, print source rows/IDs/JSON/paths, or use UI state as the data repair.</action>
  <verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressRepository.test.ts &amp;&amp; npm run test:integration -- --runInBand &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>The diagnosis-selected source/projection cause is repaired with a full-migration regression and recovery proof, and no unapproved production path changed.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Use shared Search and the typed recoverable Retry path in Progress</name>
  <precondition>06-PROGRESS-DIAGNOSIS.md explicitly authorizes src/ui/screens/ProgressScreen.tsx after recording the resolved causal branch and recovery predicate; halt rather than changing the Progress screen if that authorization is absent.</precondition>
  <files>src/ui/screens/ProgressScreen.tsx, src/ui/__tests__/ProgressScreen.test.tsx</files>
  <behavior>
    - Progress uses M3SearchField with its leading icon, controlled clear/IME/focus behavior, and source-backed busy, empty, error, zero, one, and many result states.
    - Retry loading progress repeats the existing typed request-generation operation and transitions to the actual recovered current result when the fixture makes the condition recoverable.
    - Empty, updating, unavailable, and populated regions remain factual; Search and retry never execute SQL or create synthetic analytics.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Progress UI and conditional storage lane, specifically ProgressScreen.tsx:528-557 and 594-735, ProgressScreen.test.tsx:226-316, and §Shared Search and filter primitives. Per D-72, D-78, D-80, and D-81, consume 06-01 M3SearchField with the Progress-owned query/filter and result slots, then wire Retry through the established loadProgress request-generation path authorized by the diagnosis. Test first rejection then recovered retry, current empty/populated data, keyboard/D-pad, focus after clear, non-colour error/state cues, System/Light/Dark, compact/medium/expanded, and 200% text. Do not create a local Search, bypass the typed runtime, replace source-backed states with placeholders, or edit any production file outside the diagnostic authorization.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ProgressScreen.test.tsx &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressRepository.test.ts &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>Progress Search and Retry expose only the causal repair's factual typed state, and a recoverable retry demonstrably reaches a current result.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Candidate diagnosis to source repair | A generic runtime failure must not authorize speculative storage/runtime changes. |
| Progress screen to typed runtime | Search and Retry must consume typed source-backed results rather than bypassing repositories. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Information Disclosure | Progress diagnosis and tests | high | mitigate | Consume only the redacted branch/class/code artifact and bounded fixture assertions; never emit local data. |
| T-06-02 | Tampering | Conditional production repair | high | mitigate | Require the diagnosis allowlist precondition and retain source/projection/replay contracts. |
| T-06-03 | Tampering | Search/Retry presentation | high | mitigate | Keep Progress on the typed request-generation path with factual safe states and no SQL in UI. |
</threat_model>

<verification>Run the diagnosis-selected focused host/integration suites, Progress component suite, typecheck, lint, boundaries, integrity coverage where applicable, and git diff --check. Per D-82, do not approve, promote, publish, tag, or run Terminal Seal.</verification>

<success_criteria>UX-02 and UX-10 are complete only after the diagnostic artifact authorizes the exact repair path and the Progress screen proves factual Search/retry recovery.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-07-SUMMARY.md when done.</output>
