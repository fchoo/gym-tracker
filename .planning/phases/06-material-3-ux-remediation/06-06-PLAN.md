---
phase: 06-material-3-ux-remediation
plan: 06
type: execute
wave: 1
depends_on: [06-01]
files_modified:
  - src/ui/screens/LibraryScreen.tsx
  - src/ui/__tests__/LibraryScreen.test.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-06, UX-07, UX-08]
estimate:
  tokens: 46000
  raw_tokens: 46000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Per D-68, D-69, and D-72, Plans and Exercises use shared Search and selected M3 chips with no permanent inactive-filter status label.
    - Per D-70, refresh preserves current state and Retry Library refresh appears only after the same operation fails.
    - Per D-71, Favorite is commit-bound and browse rows omit provenance while Detail retains it.
  artifacts:
    - path: src/ui/screens/LibraryScreen.tsx
      provides: Library Search, chips, Favorite, refresh, and browse/detail hierarchy
    - path: src/ui/__tests__/LibraryScreen.test.tsx
      provides: cardinality, state-preservation, commit, provenance, and adaptive regressions
  key_links:
    - from: LibraryScreen
      to: M3SearchField and M3FilterChip
      via: typed Plans/Exercises adapters and controlled selection
    - from: LibraryScreen
      to: requestLibraryRefresh and setExerciseFavorite
      via: existing typed refresh and committed exact-item results
---

<objective>
Deliver the owner-visible Library remediation on the established shared contracts.

Purpose: make discovery compact, source-safe, accessible, and free of browse provenance noise.
Output: shared Search/chips, state-preserving refresh, commit-bound Favorite, and concise browse rows.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-UI-SPEC.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@.planning/phases/06-material-3-ux-remediation/06-01-SUMMARY.md
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Replace Library discovery controls with shared Search and chips</name>
  <files>src/ui/screens/LibraryScreen.tsx, src/ui/__tests__/LibraryScreen.test.tsx</files>
  <behavior>
    - Plans and Exercises use one Search anatomy/clear/IME/state contract while retaining independent typed query meaning and focus restoration.
    - Component assertions cover zero, one, and many plans/exercises result sets, each with factual result state and correct clear behavior.
    - Favorite stays one tap away; active taxonomy chips have text/icon/shape plus selected semantics, and no inactive filter status consumes row space.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §LibraryScreen and LibraryScreen.test.tsx, particularly LibraryScreen.tsx:1672-1725, filter semantics at 219-305, and tests 818-861. Per D-68, D-69, D-72, D-80, D-81, and UI-SPEC zero-one-many, replace both local Search inputs and the filter action/summary with 06-01 shared Search/chips. Preserve independent queries, OR-within/AND-across taxonomy, selection, modal containment, and focus restoration. Assert zero, one, and many plans/exercises component results; 06-09 owns native keyboard/TalkBack evidence. Do not change repository queries, source fields, or create local component clones.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/LibraryScreen.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <done>Library provides shared Search and compact reversible non-colour-dependent chips for zero, one, and many result sets.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Preserve Library state through refresh and simplify browse rows</name>
  <files>src/ui/screens/LibraryScreen.tsx, src/ui/__tests__/LibraryScreen.test.tsx</files>
  <behavior>
    - Pull-to-refresh retains rows, query, filters, active section, selected plan, and scroll host while pending and after failure.
    - Retry Library refresh appears only after failure, repeats the same guarded operation, and disappears on success.
    - Browse/Favorite/Recent rows omit provenance, Detail retains it, and Favorite applies only returned committed exact-item state.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §LibraryScreen and LibraryScreen.test.tsx:1446-1522, 490-574, 781-816, and 983-1049, plus §AdaptiveScreen. Per D-70 and D-71, attach requestLibraryRefresh to the 06-01 owning-scroll contract, retain snapshot/display preferences until success, remove the permanent action, and expose only failure retry. Preserve per-item busy guard and apply Favorite from the returned committed item; confine provenance to Detail. Per D-80/D-81, test pending/failure/success, focus, target sizes, appearance, and 200% text; 06-09 owns native pull/appearance/TalkBack backstops. Do not clear state optimistically, rewrite catalog facts, or remove Detail provenance.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/LibraryScreen.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>Library refresh preserves state, Favorite is commit-bound, and browse/detail provenance is separated exactly as D-71 requires.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| User input to Library state | Query/filter/refresh intent must remain bounded and reversible. |
| Library screen to catalog runtime | Refresh/Favorite cross bundled and user-owned authority boundaries. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Refresh and Favorite | high | mitigate | Preserve snapshots and accept only committed exact-item results. |
| T-06-02 | Information Disclosure | Browse rows/tests | high | mitigate | Omit internal provenance from browse/spoken rows while retaining Detail evidence. |
</threat_model>

<verification>Run Library and shared primitive tests, typecheck, lint, boundaries, coverage, and git diff --check. 06-09 runs the native Library flow. Per D-82, do not approve, promote, publish, tag, or run Terminal Seal.</verification>

<success_criteria>UX-01, UX-02, UX-06, UX-07, and UX-08 are coherent in Library without changing catalogue authority or taxonomy semantics.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-06-SUMMARY.md when done.</output>
