---
phase: 06-material-3-ux-remediation
plan: 01
type: execute
wave: 0
depends_on: []
files_modified:
  - src/ui/components/M3SearchField.tsx
  - src/ui/components/M3SearchField.test.tsx
  - src/ui/components/M3FilterChip.tsx
  - src/ui/components/M3FilterChip.test.tsx
  - src/ui/components/index.ts
  - src/ui/layout/AdaptiveScreen.tsx
  - src/ui/__tests__/foundation.test.tsx
autonomous: true
requirements: [UX-01, UX-02, UX-06, UX-07]
estimate:
  tokens: 50000
  raw_tokens: 50000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - Per D-72, all affected consumers can use one labelled Search contract with a leading icon, integrated clear, IME handling, and explicit busy, empty, error, and result states.
    - Per D-68 and D-71, filters and Favorite chips communicate selection with accessibility semantics and non-colour cues.
    - Per D-70, the one owning scroll surface can run a controlled refresh without replacing visible Library state or nesting scroll views.
  artifacts:
    - path: src/ui/components/M3SearchField.tsx
      provides: repository-owned Material 3 Search primitive
    - path: src/ui/components/M3FilterChip.tsx
      provides: repository-owned accessible filter and Favorite chip
    - path: src/ui/layout/AdaptiveScreen.tsx
      provides: controlled RefreshControl seam
  key_links:
    - from: src/ui/components/index.ts
      to: M3SearchField and M3FilterChip
      via: shared exports used by typed screen adapters
    - from: src/ui/layout/AdaptiveScreen.tsx
      to: react-native RefreshControl
      via: the existing single ScrollView
---

<objective>
Establish the shared Material 3 interaction contracts that the screen remediation plans consume.

Purpose: centralize presentation and accessibility behavior without moving query, mutation, or SQLite ownership out of typed screens.
Output: tested Search, chip, and controlled-refresh primitives.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-UI-SPEC.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@src/ui/components/index.ts
@src/ui/layout/AdaptiveScreen.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Extract the shared Material 3 Search contract</name>
  <files>src/ui/components/M3SearchField.tsx, src/ui/components/M3SearchField.test.tsx, src/ui/components/index.ts</files>
  <behavior>
    - A labelled field renders the leading Search icon, IME Search handling, and a 48dp integrated clear control only when its controlled query is non-empty.
    - Busy, empty, error, and result state slots expose explicit accessible semantics while clear retains input focus and the keyboard.
    - The primitive remains presentation-only; owners provide typed query, count/state, and filtering behavior.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Shared Search and filter primitives, specifically the LibraryScreen.tsx:382-435 and ProgressScreen.tsx:528-557 controlled-input analogs, to create and export M3SearchField per D-72. Reuse the shared theme, FocusablePressable/IconAction, Lucide, focus-ring, reduced-motion, and 48dp conventions. Per D-80 and D-81, cover label, icon, clear, IME, focus, busy, empty, error, result, System/Light/Dark, width classes, and 200% text; do not move filtering, remote search, debounce copy, persistence, or SQL into the primitive.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/components/M3SearchField.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <done>M3SearchField is the sole reusable Search implementation available to the four D-72 consumers.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add accessible filter and Favorite chip contracts</name>
  <files>src/ui/components/M3FilterChip.tsx, src/ui/components/M3FilterChip.test.tsx, src/ui/components/index.ts</files>
  <behavior>
    - Selected and unselected chips are distinguishable without colour and expose accessibilityState.selected through touch, Enter, Space, and D-pad activation.
    - Favorite is outlined when inactive and filled approved-green with a star-shape and selected companion cue when active.
    - Every chip retains its 48dp target and typed callback ownership.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §M3FilterChip and the components/index.ts:333-376 and 393-460 selected-control analogs to implement and export M3FilterChip per D-68 and D-71. Keep taxonomy and Favorite writes in consumers; this primitive only renders the controlled state. Per D-80 and D-81, test selected icon/shape/outline, Favorite state, disabled/busy behavior, focus, keyboard, System/Light/Dark, 200% text, and 48dp targets; do not introduce a package, screen-local chip styling, or a catalogue mutation path.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/components/M3FilterChip.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <done>All D-68/D-71 consumers can share one non-colour-dependent chip contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose one controlled owning-scroll refresh seam</name>
  <files>src/ui/layout/AdaptiveScreen.tsx, src/ui/__tests__/foundation.test.tsx</files>
  <behavior>
    - Optional refreshing/onRefresh props attach one controlled RefreshControl to AdaptiveScreen's existing ScrollView.
    - No second scroll host is rendered, and existing keyboard, scroll restoration, pane, inset, and dock contracts remain intact.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §AdaptiveScreen, especially AdaptiveScreen.tsx:43-215 and foundation.test.tsx:347-430, to add the narrow controlled refresh contract per D-70. Preserve keyboardShouldPersistTaps, scrollEventThrottle, restoration, dock padding, and layout widths. Per D-80 and D-81, assert refresh present/absent, busy state, one ScrollView host, focus order, and compact/medium/expanded rendering; do not add nested scrolling, an optimistic source acknowledgement, or Library-specific data logic.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/foundation.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <done>Library can later own pull-to-refresh through the existing AdaptiveScreen without loss of state or scroll semantics.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Input to shared controls | Search, chip, and refresh input must remain controlled presentation state. |
| Screens to typed runtime | Shared controls cannot bypass source authority. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Shared controls | high | mitigate | Keep primitives controlled and mutation-free; screens retain typed runtime operations. |
| T-06-02 | Information Disclosure | UI test evidence | medium | mitigate | Use fixture labels and bounded assertions only. |
</threat_model>

<verification>Run the three focused component suites, typecheck, boundaries, lint, integrity coverage where applicable, and git diff --check. Per D-82, do not approve, promote, publish, tag, or run Terminal Seal.</verification>

<success_criteria>Shared Search, chip, and refresh seams meet D-68 through D-72 and D-80/D-81 before screen consumers adopt them.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-01-SUMMARY.md when done.</output>
