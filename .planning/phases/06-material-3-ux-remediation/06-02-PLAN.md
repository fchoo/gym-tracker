---
phase: 06-material-3-ux-remediation
plan: 02
type: execute
wave: 0
depends_on: []
files_modified:
  - package.json
  - scripts/run-phase6-maestro.mjs
  - scripts/phase6-evidence-scripts.test.mjs
  - maestro/phase6/gesture-smoke.yaml
  - app/_layout.tsx
autonomous: true
requirements: [UX-03, UX-04, UX-05, UX-09]
estimate:
  tokens: 44000
  raw_tokens: 44000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - Per D-80 and D-81, a generated Android emulator executes a real horizontal swipe and long-press displacement smoke before Calendar or reorder implementation begins.
    - Per the resolved research dispositions, the runner uses ADB from PATH or the pinned fallback and rejects wrong package, candidate manifest, report, installed bytes, or cleanup state.
    - The exact npm entry point exists before any verification invokes it.
  artifacts:
    - path: package.json
      provides: exact Phase 6 Maestro command
    - path: scripts/run-phase6-maestro.mjs
      provides: fail-closed candidate/install/gesture runner
    - path: maestro/phase6/gesture-smoke.yaml
      provides: native generated-Android gesture smoke flow
  key_links:
    - from: package.json
      to: scripts/run-phase6-maestro.mjs
      via: test:maestro:phase6
    - from: scripts/run-phase6-maestro.mjs
      to: installed emulator APK
      via: package and APK identity validation before Maestro flows
---

<objective>
Create and execute the Wave 0 generated-native gesture gate that Calendar and continuous reorder must pass before consuming the installed stack.

Purpose: resolve the native root/configuration question with device evidence rather than component-test inference.
Output: exact npm runner, fail-closed evidence contract, gesture smoke flow, and real emulator smoke result.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/06-material-3-ux-remediation/06-CONTEXT.md
@.planning/phases/06-material-3-ux-remediation/06-RESEARCH.md
@.planning/phases/06-material-3-ux-remediation/06-PATTERNS.md
@scripts/run-phase5-maestro.mjs
@app/_layout.tsx
</context>

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create the exact Phase 6 native runner contract and npm command</name>
  <files>package.json, scripts/run-phase6-maestro.mjs, scripts/phase6-evidence-scripts.test.mjs, maestro/phase6/gesture-smoke.yaml</files>
  <behavior>
    - package.json contains exactly `"test:maestro:phase6": "node scripts/run-phase6-maestro.mjs"`.
    - The runner requires bundle directory, manifest SHA-256, package, serial, output, and report-directory arguments, resolves ADB from PATH then `/opt/homebrew/share/android-commandlinetools/platform-tools/adb`, and rejects every missing or malformed identity input.
    - The runner validates candidate manifest/APK hash, installed package/APK identity, report existence, and font-scale restoration without writing private rows, paths, or serials to evidence.
  </behavior>
  <action>RED first. Follow 06-PATTERNS.md §Native runner and flows and scripts/run-phase5-maestro.mjs:20-48 and 172-225 as the exact structural analog; for the new Phase 6 runner use 06-RESEARCH.md §Open Questions (RESOLVED FOR PLANNING), items 2 and 3, as the mandatory native-smoke and pinned-ADB contract. Add exactly the required test:maestro:phase6 package script before any verification invokes it. Derive the flow from maestro/phase5/adaptive-accessibility.yaml, require exact command arguments, validate manifest/package/APK/device/report identity, and restore font scale in finally. Per D-80, use contract tests before implementation; do not install packages, alter lockfiles, create approval/promotion evidence, or disclose private runtime data.</action>
  <verify><automated>node --test scripts/phase6-evidence-scripts.test.mjs &amp;&amp; node -e "const p=require('./package.json'); if(p.scripts['test:maestro:phase6']!=='node scripts/run-phase6-maestro.mjs') process.exit(1)"</automated></verify>
  <done>The exact runner command exists, its identity/cleanup contract is tested, and no native consumer has been unblocked by a stub.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Run the generated-native emulator gesture smoke and conditionally repair the existing root seam</name>
  <precondition>An Android emulator is booted at serial emulator-5554 and the project-pinned Node 24.19.0/npm 11.17.0, Maestro 2.8.0, and ADB from PATH or `/opt/homebrew/share/android-commandlinetools/platform-tools/adb` pass version checks.</precondition>
  <files>app/_layout.tsx</files>
  <behavior>
    - On generated Android, the smoke proves both a horizontal swipe and long-press held-row displacement on the installed Gesture Handler/Reanimated stack.
    - The exact runner installs the manifest APK, verifies the requested installed package and pulled APK SHA-256, runs Maestro with the requested serial, and writes the required output and report directory.
    - app/_layout.tsx changes only if the red native smoke proves existing-stack root integration is missing.
  </behavior>
  <action>Run this task before the Calendar and reorder consumers may begin. Follow 06-PATTERNS.md §Native runner and flows and 06-RESEARCH.md §Open Questions item 2: build clean generated development-test bytes, inspect generated configuration/root, then execute the actual emulator smoke with the Task 1 runner. Use the installed stack only; change app/_layout.tsx using the existing root pattern only when the smoke proves it is necessary. Per D-74, D-76, D-80, and D-81, retain labelled non-gesture alternatives in later consumers; this smoke is the acceptance gate for their native implementation, not merely a contract-test substitute.</action>
  <verify><automated>npm run android:devtest:fresh -- --suite phase6-gesture-smoke &amp;&amp; PHASE6_MANIFEST_SHA256=$(node --input-type=module -e "import { sha256File } from './scripts/run-phase6-maestro.mjs'; process.stdout.write(sha256File('./artifacts/native/phase6-gesture-smoke/build.json'))") &amp;&amp; npm run test:maestro:phase6 -- --bundle-dir artifacts/native/phase6-gesture-smoke --manifest-sha256 "$PHASE6_MANIFEST_SHA256" --package com.fchoo.gymtracker.devtest --serial emulator-5554 --output artifacts/native/phase6-gesture-smoke/evidence/gesture-smoke.json --report-dir artifacts/native/phase6-gesture-smoke/evidence/maestro --flow gesture-smoke</automated></verify>
  <done>A real generated-native emulator run has passed horizontal-swipe and long-press-displacement evidence with exact package/APK/manifest/report identity, or execution halts before Calendar and reorder work.</done>
</task>

</tasks>

<commit_protocol>
After each task's verification passes, create one atomic task commit. Invoke it with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit` and supply exactly one final trailer line, `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`. After every new commit, inspect `git log -1 --format=%B`; assert exactly one matching trailer line exists and its final non-empty line is that trailer.
</commit_protocol>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Candidate bundle to emulator | Unchanged package/APK/manifest bytes must identify the installed test target. |
| Device/runtime to evidence | Device and runtime output can disclose local information. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-06-01 | Tampering | Native runner | high | mitigate | Fail closed on manifest, package, APK, serial, report, and font-scale mismatches. |
| T-06-02 | Information Disclosure | Native evidence | high | mitigate | Emit only hashes and bounded flow state; reject raw rows, IDs, paths, JSON, and serials. |
</threat_model>

<verification>Run contract tests, generated-native inspection, the exact emulator command above, typecheck, lint, boundaries, and git diff --check. Per D-82, create no approval, promotion, public tag, publication, or Terminal Seal state.</verification>

<success_criteria>The resolved gesture and ADB research dispositions are proven by a real Wave 0 emulator run before gesture-dependent plans proceed.</success_criteria>

<output>Create .planning/phases/06-material-3-ux-remediation/06-02-SUMMARY.md when done.</output>
