---
phase: 04-overall-progress-and-complete-progression
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/domains/progress/contracts.ts
  - src/domains/progress/periodProjection.ts
  - src/domains/progress/index.ts
  - src/platform/sqlite/repositories/progressRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/domains/progress/periodProjection.test.ts
  - tests/sqlite-host/progressRepository.test.ts
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-05]
must_haves:
  truths:
    - The same current effective-history facts produce reproducible 4-week, 12-week, and all-time factual period summaries.
    - Summaries expose source session/exercise IDs and explicit Baseline, Hold, or Updating states instead of implicit zero analytics.
    - Selected windows count persisted scheduled opportunities, completed working sets, profile-aware records, exercise statuses, and one consistency trend without a global score or aggregate kilogram volume.
  artifacts:
    - { path: src/domains/progress/periodProjection.ts, provides: pure versioned period projection reducer and source drill-downs }
    - { path: src/platform/sqlite/repositories/progressRepository.ts, provides: current/revision-aware projection reads }
    - { path: src/bootstrap/workoutAppRuntime.tsx, provides: typed Progress read capability }
  key_links:
    - { from: src/platform/sqlite/repositories/progressRepository.ts, to: src/platform/sqlite/repositories/historyProjectionRepository.ts, via: freshness from history subject revisions, pattern: loadFreshness }
    - { from: src/domains/progress/periodProjection.ts, to: src/domains/metrics/comparators.ts, via: profile-specific comparable evidence, pattern: compareMetricObservations }
---

<objective>
Create the revision-aware Progress read model over Phase 3 effective-history projections and persisted schedule opportunities.

Purpose: deliver one factual source for all Progress text, table, chart, and drill-down views without a competing analytics store.
Output: pure period reducer, SQLite repository, runtime capability, and deterministic proof.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Define period-view contracts and reducer from current effective inputs</name>
<read_first>04-CONTEXT.md, 04-RESEARCH.md, src/domains/history/projectionReducer.ts, src/domains/history/historySubjects.ts, src/domains/metrics/comparators.ts</read_first>
<files>src/domains/progress/contracts.ts, src/domains/progress/periodProjection.ts, src/domains/progress/index.ts, src/domains/progress/periodProjection.test.ts</files>
<action>Begin with fixtures for empty, one-source-row, sparse, current, stale, corrected, and voided histories. Define `4_weeks`, `12_weeks`, and `all_time` windows from civil LocalDate values. Reduce only provided factual inputs into completed scheduled opportunities, completed/planned working sets, profile-aware record rows, one date-ordered consistency series, exercise evidence/status rows, source ID drill-downs, and explicit `baseline`, `hold`, or `updating` states. Determine improvement only by the registered metric comparator within a matching identity/comparator boundary; ties hold and no previous comparable observation is baseline. Never emit a universal volume, fitness/readiness, medical, or synthetic numeric value. Make sort order stable.</action>
<acceptance_criteria>Every output is deterministic from canonical inputs, preserves source IDs, and cannot present a stale, sparse, incompatible, warm-up, or voided observation as a current improvement.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/progress/periodProjection.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement current projection and schedule-backed Progress repository reads</name>
<read_first>src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/repositories/historyRepository.ts, src/platform/sqlite/repositories/scheduleRepository.ts, src/bootstrap/workoutAppRuntime.tsx</read_first>
<files>src/platform/sqlite/repositories/progressRepository.ts, src/bootstrap/workoutAppRuntime.tsx, tests/sqlite-host/progressRepository.test.ts</files>
<action>Write failing real-SQLite fixtures first. Read current `period/all`, exercise-metric, and recommendation-target projection rows with their revision freshness; query persisted schedule opportunities only for the selected civil range. Pass normalized records to the pure reducer. If any required subject is unavailable or behind, return `updating` and do not return a factual aggregate as current. Read source IDs directly from projection/source rows so every UI value can drill to existing routes. Include intentionally empty/sparse result shapes. Add only typed runtime methods and preserve the no-repository-import UI boundary.</action>
<acceptance_criteria>Targeted and full history rebuilds produce equivalent Progress reads; stale/unavailable queries visibly distinguish from zero activity and scheduled opportunities are never guessed.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressRepository.test.ts &amp;&amp; npm run typecheck</automated></verify>
</task>
</tasks>

<verification>Run unit reducer and SQLite host tests, then typecheck. Keep actual source facts and projection freshness as independent test fixtures.</verification>
<success_criteria>PROG-01/02/03/05 have one typed factual query path ready for presentation.</success_criteria>
