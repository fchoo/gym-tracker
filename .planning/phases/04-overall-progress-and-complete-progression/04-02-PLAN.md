---
phase: 04-overall-progress-and-complete-progression
plan: 02
type: execute
wave: 1
depends_on: [04-01]
files_modified:
  - src/ui/screens/ProgressScreen.tsx
  - src/ui/components/ProgressTrend.tsx
  - app/(tabs)/progress.tsx
  - src/ui/screens/RootScreens.tsx
  - src/ui/__tests__/ProgressScreen.test.tsx
  - app/__tests__/progress-route.test.tsx
autonomous: true
requirements: [PROG-01, PROG-02, PROG-03, PROG-04, PROG-05]
must_haves:
  truths:
    - Progress is a factual root screen with 4-week, 12-week, and all-time controls.
    - Needs attention, Recent improvements, and searchable exercise progress use calm evidence-first wording and source-backed drill-downs.
    - The consistency visualization and all status rows have equivalent plain-language and accessible tabular output from the same view model.
  artifacts:
    - { path: src/ui/screens/ProgressScreen.tsx, provides: source-backed Progress root UI }
    - { path: src/ui/components/ProgressTrend.tsx, provides: visual/text/table-equivalent consistency presentation }
    - { path: app/(tabs)/progress.tsx, provides: runtime-bound Progress route }
  key_links:
    - { from: app/(tabs)/progress.tsx, to: src/bootstrap/workoutAppRuntime.tsx, via: typed loadProgress capability, pattern: loadProgress }
    - { from: src/ui/screens/ProgressScreen.tsx, to: app/session/[sessionId].tsx, via: source session drill-down callback, pattern: sessionId }
---

<objective>
Replace the Progress placeholder with the accessible factual Progress landing page.

Purpose: let the owner understand selected-period training evidence and act on a specific source, not a dashboard score.
Output: Progress screen, equivalent trend outputs, source drill-down interactions, and UI coverage.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Build period controls and Overall Progress from one view model</name>
<read_first>04-CONTEXT.md, 04-01-PLAN.md, src/ui/screens/CalendarScreen.tsx, src/ui/screens/ExerciseHistoryScreen.tsx, src/ui/theme, src/ui/components/index.ts</read_first>
<files>src/ui/screens/ProgressScreen.tsx, src/ui/components/ProgressTrend.tsx, src/ui/__tests__/ProgressScreen.test.tsx</files>
<action>Start with component tests. Render a headed Progress root with `4 weeks`, `12 weeks`, and `All time` controls, a single plain-language Overall summary, source-backed opportunity/working-set/status/record rows, and a full-width consistency trend. The trend consumes the exact same row sequence for visual labels, spoken text, and accessible data table. Keep all counts and state labels textual; no color-only status, global fitness/readiness claim, aggregate kilogram-volume headline, or independent stat-card grid. Use explicit Baseline, Hold, and Updating visuals and retain selected period locally as a reversible preference.</action>
<acceptance_criteria>Changing the period refreshes one coherent view; every visible metric can be read without the chart and the same source IDs remain available to the table and drill-down callbacks.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ProgressScreen.test.tsx</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add calm attention/improvement/search sections and bind the route</name>
<read_first>src/ui/components/RecommendationSurface.tsx, src/ui/screens/TodayScreen.tsx, app/(tabs)/calendar.tsx, app/exercise-history/[exerciseId].tsx, src/bootstrap/workoutAppRuntime.tsx</read_first>
<files>src/ui/screens/ProgressScreen.tsx, app/(tabs)/progress.tsx, src/ui/screens/RootScreens.tsx, src/ui/__tests__/ProgressScreen.test.tsx, app/__tests__/progress-route.test.tsx</files>
<action>Render Needs attention only when an actionable decision exists, explaining that it is a review item rather than a safety alert. Render Recent improvements only from comparator-backed rows. Add a searchable exercise list ordered by recent training, with calm baseline/hold/updating language and links to Exercise History. Route source sessions to existing detail routes and recommendations to the review surface. Bind the route to typed runtime loading/reload behavior and implement loading, retryable read failure, no-workout, sparse, and updating states. Preserve keyboard/D-pad order, 48dp controls, screen-reader labels, 200% text wrapping, reduced motion, adaptive layout, and existing light/dark card surfaces.</action>
<acceptance_criteria>The placeholder is removed; source reads, route navigation, search, empty/error/loading states, and non-color accessible output are covered without any SQLite import in UI code.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ProgressScreen.test.tsx app/__tests__/progress-route.test.tsx &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run Progress component/route tests, typecheck, lint, and boundary checks. Native visual review remains deferred to the shared final gate.</verification>
<success_criteria>PROG-01 through PROG-05 are visibly usable without opaque or inaccessible analytics.</success_criteria>
