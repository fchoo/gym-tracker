---
phase: 04-overall-progress-and-complete-progression
plan: 07
type: execute
wave: 4
depends_on: [04-06]
gap_closure: true
autonomous: true
requirements: [PROG-03]
files_modified:
  - src/domains/progress/contracts.ts
  - src/domains/progress/periodProjection.ts
  - src/domains/progress/periodProjection.test.ts
  - src/platform/sqlite/repositories/progressRepository.ts
  - src/ui/screens/ProgressScreen.tsx
  - src/ui/__tests__/ProgressScreen.test.tsx
must_haves:
  truths:
    - Every populated Overall Progress aggregate row exposes the source workout and exercise identifiers that contributed to that row.
    - Baseline and Hold status presentation exposes the source workout/exercise identifiers supporting the state, rather than static text with no drill-down.
    - Source references use the same selected civil-date window and effective-history authority as the displayed aggregate; stale projections still suppress both values and links.
  artifacts:
    - { path: src/domains/progress/contracts.ts, provides: typed per-summary and state source-reference contract }
    - { path: src/domains/progress/periodProjection.ts, provides: deterministic selected-window source-reference derivation }
    - { path: src/ui/screens/ProgressScreen.tsx, provides: accessible source drill-down actions beside every aggregate and status row }
  key_links:
    - { from: src/platform/sqlite/repositories/progressRepository.ts, to: src/domains/progress/periodProjection.ts, via: effective source-session identities passed into the selected-period reducer, pattern: source }
    - { from: src/ui/screens/ProgressScreen.tsx, to: app/(tabs)/progress.tsx, via: existing onOpenSession and onOpenExercise callbacks, pattern: onOpenSession|onOpenExercise }
---

<objective>
Close PROG-03 by making every Overall Progress aggregate and Baseline/Hold state traceable to its source workouts or exercises.

Purpose: a summary must remain auditable rather than becoming an unexplained number or label.
Output: typed source references, deterministic projection wiring, accessible drill-down controls, and regression coverage.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Derive selected-window source references for each summary/state row</name>
<read_first>04-VERIFICATION.md, 04-CONTEXT.md, src/domains/progress/contracts.ts, src/domains/progress/periodProjection.ts, src/platform/sqlite/repositories/progressRepository.ts, src/platform/sqlite/repositories/historyRepository.ts</read_first>
<files>src/domains/progress/contracts.ts, src/domains/progress/periodProjection.ts, src/domains/progress/periodProjection.test.ts, src/platform/sqlite/repositories/progressRepository.ts</files>
<action>RED first: add failing projection/repository assertions that each scheduled-opportunity, working-set, exercise-status, attention, and Baseline/Hold summary source collection contains stable sorted unique source session/exercise IDs from the selected civil-date window and excludes out-of-window, voided, and stale facts. Then extend the typed input/projection model and feed it from the existing effective-history source adapter after freshness passes. Do not add a second analytics authority or expose identifiers while projections are updating/unavailable.</action>
<acceptance_criteria>Every displayed aggregate/status can be traced to deterministic source IDs from the same effective-history window, and stale reads expose neither totals nor source links.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/progress/periodProjection.test.ts &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/progressRepository.test.ts</automated></verify>
<done>The selected-period projection exposes stable source references for every aggregate/status row and the focused domain/SQLite tests pass.</done>
</task>

<task type="auto" tdd="true">
<name>Task 2: Render accessible drill-down actions for every Overall row and state</name>
<read_first>04-UI-SPEC.md, src/ui/screens/ProgressScreen.tsx, src/ui/components/ProgressTrend.tsx, src/ui/__tests__/ProgressScreen.test.tsx</read_first>
<files>src/ui/screens/ProgressScreen.tsx, src/ui/__tests__/ProgressScreen.test.tsx</files>
<action>RED first: add component assertions that each populated Overall aggregate row and the Baseline/Hold state expose labelled, keyboard/D-pad-capable source workout or exercise actions and invoke the existing callbacks with the exact IDs. Implement one compact reusable source-link presentation inside the existing card, preserve text/table parity and 48dp targets, and avoid duplicate or decorative cards. Render an explicit no-source explanation only when a row genuinely has no session/exercise source.</action>
<acceptance_criteria>Every Overall aggregate and status row is auditable from the UI without adding a write path, changing the selected period, or inventing a score.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ProgressScreen.test.tsx</automated></verify>
<done>All Overall summary/status rows expose accessible source actions or an explicit no-source explanation, and component tests pass.</done>
</task>
</tasks>

<verification>Run focused reducer/repository/component tests, typecheck, lint, boundaries, and the integrity coverage gate. Native/device/attended checks remain exclusively in Phase 5 Plan 05-07.</verification>
<success_criteria>PROG-03 is satisfied by source-carrying aggregates and accessible drill-downs from the same selected-period projection.</success_criteria>
