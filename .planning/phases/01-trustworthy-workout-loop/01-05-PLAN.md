---
phase: 01-trustworthy-workout-loop
plan: 05
type: execute
wave: 3
depends_on:
  - 01-04
files_modified:
  - modules/argon2-kdf/expo-module.config.json
  - modules/argon2-kdf/package.json
  - modules/argon2-kdf/src/index.ts
  - modules/argon2-kdf/android/build.gradle
  - modules/argon2-kdf/android/src/main/java/expo/modules/argon2kdf/Argon2KdfModule.kt
  - modules/argon2-kdf/android/src/test/java/expo/modules/argon2kdf/Argon2KdfModuleTest.kt
  - src/platform/crypto/passwordKdf.ts
  - src/platform/crypto/candidateKdfDescriptor.ts
  - app/__argon2-contracts.tsx
  - scripts/run-argon2-feasibility.mjs
autonomous: true
requirements: []
must_haves:
  truths:
    - "The app-owned Argon2id path is feasibility-only and exposes no backup product UI per D-33."
    - "RFC 9106 and OWASP-floor known-answer cases pass in Kotlin and through the JS bridge."
    - "Derivation runs off the interaction thread, returns typed/redacted results, and clears native arrays where possible."
    - "CNG/autolink, API 36, minimum physical-device timing, and 16 KB APK compatibility produce a versioned pass/block descriptor."
  artifacts:
    - path: "modules/argon2-kdf/android/src/main/java/expo/modules/argon2kdf/Argon2KdfModule.kt"
      provides: "Narrow asynchronous Bouncy Castle Argon2id bridge"
    - path: "src/platform/crypto/candidateKdfDescriptor.ts"
      provides: "Versioned feasibility evidence contract"
    - path: "scripts/run-argon2-feasibility.mjs"
      provides: "KAT, responsiveness, CNG, APK, and device evidence runner"
  key_links:
    - from: "src/platform/crypto/passwordKdf.ts"
      to: "modules/argon2-kdf/src/index.ts"
      via: "narrow PasswordKdfPort adapter"
      pattern: "derive"
    - from: "scripts/run-argon2-feasibility.mjs"
      to: "src/platform/crypto/candidateKdfDescriptor.ts"
      via: "writes pass/block evidence"
      pattern: "CandidateKdfDescriptor"
---

<objective>
Deliver reviewed slice 01-05: an app-owned Argon2id feasibility spike with native/device evidence and no backup UI.

Purpose: Determine whether Phase 5 may rely on this native KDF path without prematurely designing the backup envelope.
Output: Local Expo module, narrow bridge, KAT/responsiveness/CNG/page-size tests, and a current-APK-bound candidate descriptor; final physical-device evidence is consolidated into 01-10.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trustworthy-workout-loop/01-CONTEXT.md
@.planning/phases/01-trustworthy-workout-loop/01-RESEARCH.md
@.planning/phases/01-trustworthy-workout-loop/01-PATTERNS.md
@.planning/research/STACK.md
@.planning/phases/01-trustworthy-workout-loop/01-04-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement and automate the bounded Argon2id feasibility contract</name>
  <files>modules/argon2-kdf/expo-module.config.json, modules/argon2-kdf/package.json, modules/argon2-kdf/src/index.ts, modules/argon2-kdf/android/build.gradle, modules/argon2-kdf/android/src/main/java/expo/modules/argon2kdf/Argon2KdfModule.kt, modules/argon2-kdf/android/src/test/java/expo/modules/argon2kdf/Argon2KdfModuleTest.kt, src/platform/crypto/passwordKdf.ts, src/platform/crypto/candidateKdfDescriptor.ts, app/__argon2-contracts.tsx, scripts/run-argon2-feasibility.mjs</files>
  <behavior>
    - RFC 9106 Argon2id version 19 KAT passes natively and through the bridge.
    - OWASP floor m=19456 KiB, t=2, p=1, 16-byte salt, 32-byte output passes.
    - Invalid/unbounded descriptors fail before native resource use; no secret/input/output bytes enter logs.
    - A concurrent JS interaction probe remains responsive while derivation runs on a background coroutine dispatcher.
  </behavior>
  <action>Create the local Expo module and pin Maven `org.bouncycastle:bcprov-jdk18on:1.85.2`. Expose one asynchronous, versioned, bounded binary API; run `Argon2BytesGenerator` with Argon2id/version 19 on a non-main dispatcher, caller-supplied salt/parameters, and 32-byte output. Clear native password/input/output arrays in `finally` where possible and return only typed errors plus duration metadata. Before any Gradle invocation, remove generated Android output, run `npx expo prebuild --clean --platform android`, then execute the Kotlin unit test from that generated tree. Consume the generic native build and evidence verifier created by 01-04 without modifying either script; build, hash, copy, install, and launch the exact current development-test APK before bridge KATs. Implement bridge KATs, cancellation/background behavior, redaction, two-clean-prebuild autolink, API 36 install, packaged-library inspection, and `zipalign -P 16`. Write `CandidateKdfDescriptor` with base HEAD, source-tree digest, APK SHA-256, package, algorithm/provider, parameter bounds, KAT ID, emulator timing samples, device metadata, ABI/page-size evidence, and pass/block status; verify the descriptor against the build manifest. Do not implement AES, archive formats, password screens, backup routes, restore, or export. The physical-device 10-sample calibration and logcat review run once in the consolidated 01-10 final checkpoint rather than stopping this supporting plan.</action>
  <reversibility rating="costly">Phase 5 may adopt or reject this provider after the spike; changing it then affects the security design but no Phase 1 user data format is frozen.</reversibility>
  <verify>
    <automated>npx expo prebuild --clean --platform android && ./android/gradlew --no-daemon :argon2-kdf:testDebugUnitTest && npm run android:devtest:fresh -- --suite argon2 && node scripts/run-argon2-feasibility.mjs --device=emulator --manifest artifacts/native/argon2/build.json --assert-kat --assert-responsive --assert-cng --assert-page-size && node scripts/verify-native-evidence.mjs artifacts/native/argon2/result.json</automated>
  </verify>
  <done>Automated native/bridge KAT, responsiveness, redaction, CNG, API 36, and page-size evidence pass and a provisional descriptor is produced without any backup UI or format commitment.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| JS validated bytes/descriptor → native KDF | Potentially sensitive and resource-intensive input crosses the bridge. |
| native result → diagnostics/caller | Key bytes must not leak or remain unnecessarily. |
| Maven dependency → APK | Cryptographic provider integrity affects future backups. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-05-01 | Tampering | Argon2 descriptor | mitigate | Versioned bounded schema and KATs through native and bridge paths. |
| T-01-05-02 | Information Disclosure | buffers/logs | mitigate | Binary bridge, native finally cleanup, redaction tests, and logcat inspection. |
| T-01-05-03 | Denial of Service | KDF parameters | mitigate | Allowlisted parameter bounds and background dispatcher timing gates. |
| T-01-05-04 | Tampering | Bouncy Castle artifact | mitigate | Exact Maven coordinate, Gradle lock evidence, provider/version in descriptor. |
| T-01-05-SC | Tampering | package installs | mitigate | No new npm packages; exact audited Maven primitive only. |
</threat_model>

<verification>
- Kotlin KAT and bridge KAT.
- Emulator CNG/autolink/API 36/16 KB checks.
- Current-HEAD emulator timing, packaged-library, page-size, and logcat redaction evidence; 01-10 owns the single final physical-device checkpoint.
</verification>

<success_criteria>
- D-33 feasibility is closed as pass or explicit Phase 5 block.
- No backup UI or archive contract enters Phase 1.
- The descriptor names exact device/APK/provider evidence.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-workout-loop/01-05-SUMMARY.md` with the CandidateKdfDescriptor status when done.
</output>
