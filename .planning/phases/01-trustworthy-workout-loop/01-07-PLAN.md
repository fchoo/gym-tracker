---
phase: 01-trustworthy-workout-loop
plan: 07
type: execute
wave: 4
depends_on:
  - 01-06
files_modified:
  - assets/content/full-body-foundation.v1.json
  - src/domains/content/index.ts
  - src/domains/plans/index.ts
  - src/domains/workout/index.ts
  - src/domains/plans/activateStarterPlan.ts
  - src/domains/workout/startWorkout.ts
  - src/platform/sqlite/repositories/plansWorkoutRepository.ts
  - src/ui/screens/TodayScreen.tsx
  - src/ui/components/WorkoutStartSheet.tsx
  - tests/integration/plan-workout-tracer.test.ts
  - src/ui/__tests__/TodayScreen.test.tsx
autonomous: true
requirements:
  - WORK-01
  - WORK-02
  - WORK-03
  - WORK-04
must_haves:
  truths:
    - "Owner can activate one reviewed Full Body Foundation template into user-owned copied rows without mutating bundled rows per D-11."
    - "Today shows scheduled day, estimate, next targets, comparable history, and pending suggestion status without replacing unaccepted targets per D-12."
    - "Owner can start scheduled, alternate, rest-day, or empty sessions without silent schedule advancement."
    - "Planned starts persist immutable names/order/profile/units/targets/rule snapshots."
  artifacts:
    - path: "assets/content/full-body-foundation.v1.json"
      provides: "Approved Phase 1 starter subset with IDs, targets, rests, increments, warm-ups, attribution, and rule IDs"
    - path: "src/domains/plans/activateStarterPlan.ts"
      provides: "Atomic user-owned copy activation"
    - path: "src/domains/workout/startWorkout.ts"
      provides: "All start modes and immutable session snapshots"
    - path: "src/ui/screens/TodayScreen.tsx"
      provides: "Source-backed first-use/scheduled/rest/resume Today states"
  key_links:
    - from: "src/domains/plans/activateStarterPlan.ts"
      to: "src/platform/sqlite/repositories/plansWorkoutRepository.ts"
      via: "one proven-writer transaction cloning bundled to copied ownership"
      pattern: "activate|clone"
    - from: "src/ui/screens/TodayScreen.tsx"
      to: "src/domains/workout/index.ts"
      via: "public query/command hooks only"
      pattern: "getToday|start"
---

<objective>
Deliver reviewed slice 01-07: approved seed/copy ownership and the first user-visible Today-to-workout tracer.

Purpose: Make a trustworthy source-backed session start usable only after the integrity kernel passes.
Output: Frozen starter fixture, copied activation, schedules/snapshots/start modes, Today read model/UI, and integration/component proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trustworthy-workout-loop/01-CONTEXT.md
@.planning/phases/01-trustworthy-workout-loop/01-RESEARCH.md
@.planning/phases/01-trustworthy-workout-loop/01-UI-SPEC.md
@.planning/phases/01-trustworthy-workout-loop/01-PATTERNS.md
@.planning/phases/01-trustworthy-workout-loop/01-06-SUMMARY.md
@~/.gstack/projects/gym_tracker/designs/approved-20260815/core-workout-flow.html
@~/.gstack/projects/gym_tracker/designs/system-review-20260815/app-system-reference.html

<interfaces>
`LoadRepsV1 = { version: 1; profile: "load_reps"; loadGrams: number; reps: number }`.
The Phase 1 `full-body-foundation.v1` fixture is frozen here and must be copied exactly into the asset:
- metadata: namespace `gym-tracker.original`, upstream/template ID `full-body-foundation`, source revision `1`, display name `Full Body Foundation`, audience `Beginner or returning`, schedule mode `weekday`, bindings Monday/Wednesday/Friday alternating A/B/A then B/A/B, goal `General strength, basic hypertrophy, and consistency`, estimate `48 minutes`, attribution `Original Gym Tracker program`, progression policy `load_reps.double_progression.v1`;
- exercise IDs: Back Squat `5f140001-7e35-4a6d-9100-000000000001`, Bench Press `5f140001-7e35-4a6d-9100-000000000002`, Lat Pulldown `5f140001-7e35-4a6d-9100-000000000003`, Romanian Deadlift `5f140001-7e35-4a6d-9100-000000000004`, Plank `5f140001-7e35-4a6d-9100-000000000005`, Deadlift `5f140001-7e35-4a6d-9100-000000000006`, Overhead Press `5f140001-7e35-4a6d-9100-000000000007`, Seated Cable Row `5f140001-7e35-4a6d-9100-000000000008`, Reverse Lunge `5f140001-7e35-4a6d-9100-000000000009`, Side Plank `5f140001-7e35-4a6d-9100-00000000000a`;
- Day A in order: Back Squat 3×6–8 at 60000 g, 180 s rest, 2500 g increment, warm-ups 20000 g×8 and 40000 g×5; Bench Press 3×8–10 at 42500 g, 120 s rest, 2500 g increment; Lat Pulldown 3×10–12 at 45000 g, 90 s rest, 5000 g increment; Romanian Deadlift 3×8–10 at 50000 g, 120 s rest, 5000 g increment; Plank 3×45 s, 60 s rest, `timed_hold.v1`, manual/hold policy only;
- Day B in order: Deadlift 3×5–6 at 70000 g, 180 s rest, 5000 g increment, warm-ups 30000 g×5 and 50000 g×3; Overhead Press 3×6–8 at 25000 g, 120 s rest, 2500 g increment; Seated Cable Row 3×8–10 at 40000 g, 90 s rest, 5000 g increment; Reverse Lunge 3×8–10 per side at 20000 g, 90 s rest, 2500 g increment; Side Plank 3×30 s per side, 60 s rest, `timed_hold.v1`, manual/hold policy only.
Only load/reps exercises generate the Phase 1 double-progression decision. Timed holds are logged and summarized but remain manual/hold, preserving later-phase progression breadth.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Materialize and activate the frozen user-owned starter copy</name>
  <files>assets/content/full-body-foundation.v1.json, src/domains/content/index.ts, src/domains/plans/index.ts, src/domains/plans/activateStarterPlan.ts, src/platform/sqlite/repositories/plansWorkoutRepository.ts, tests/integration/plan-workout-tracer.test.ts</files>
  <behavior>
    - Fixture validation rejects malformed version/IDs/base units/references.
    - Activation clones plan/days/order/warm-ups/targets/policies/schedule/source attribution into new copied IDs in one transaction.
    - Bundled update cannot mutate copied rows; user commands cannot mutate bundled rows.
    - Repeated activation is explicit/idempotent and never silently replaces an owned plan.
  </behavior>
  <action>Store the exact metadata, IDs, Day A/B rows, targets, rests, increments, warm-ups, timed holds, and policy assignments frozen in `<interfaces>`; validate it at the content boundary and assert a canonical JSON digest in the fixture test. Implement bundled namespace ownership and `activateStarterPlan` through the proven writer per D-11. Clone all mutable plan/schedule/target state into user-owned copied IDs and bind future revisions/sessions/recommendations to that copy. Preserve bundled immutability and attribution. Add integration tests for exact fixture values, ownership, re-import, invalid fixture, duplicate activation, and transaction rollback; do not expose the full Library/editor/catalog or automatic timed-hold progression.</action>
  <verify>
    <automated>npm run test:integration -- plan-workout-tracer --runInBand</automated>
  </verify>
  <done>The exact reviewed fixture activates atomically as a separate owned plan and bundled/user-owned write boundaries are proven.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Deliver source-backed Today and all Phase 1 start modes</name>
  <files>src/domains/workout/index.ts, src/domains/workout/startWorkout.ts, src/platform/sqlite/repositories/plansWorkoutRepository.ts, src/ui/screens/TodayScreen.tsx, src/ui/components/WorkoutStartSheet.tsx, tests/integration/plan-workout-tracer.test.ts, src/ui/__tests__/TodayScreen.test.tsx</files>
  <behavior>
    - Today covers first-use, scheduled, rest-day, comparable/first-history, pending-suggestion, resume, loading, and typed-error states.
    - Scheduled/alternate/rest-day/empty start commands create correct session kind without silently advancing schedule.
    - Planned starts snapshot name/order/profile/units/targets/rule versions.
    - Pending suggestion remains visible but does not replace current target.
  </behavior>
  <action>Implement source-backed `getTodayView` plus scheduled, alternate-day, rest-day Train anyway, and empty start commands per D-12. Start planned sessions in one transaction and persist immutable snapshots. Build Today using the 01-02 primitives: Start remains before history in compact viewport; exact labels/history/suggestion/resume/error states and accessible order follow `01-UI-SPEC.md`. The query cache is disposable and invalidated by exact command scopes only; no optimistic facts, account, network, generic offline banner, Library controls, or Progress analytics.</action>
  <verify>
    <automated>npm run test:integration -- plan-workout-tracer --runInBand && npm run test:components -- TodayScreen --runInBand</automated>
  </verify>
  <done>Owner can activate the starter, inspect trustworthy Today evidence, and enter each approved workout start mode with correct snapshots and unchanged schedule semantics.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| bundled JSON → authoritative content rows | Asset IDs/units/references enter SQLite. |
| bundled namespace → user-owned copy | Ownership must prevent template mutation and overwrite. |
| Today action → session snapshot transaction | Repeated/stale starts could create wrong or duplicate sessions. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-07-01 | Tampering | content fixture | mitigate | Human freeze, versioned Zod schema, stable IDs, base units, reference tests. |
| T-01-07-02 | Elevation of Privilege | ownership repositories | mitigate | Namespace/write-policy assertions and atomic clone into new IDs. |
| T-01-07-03 | Tampering | start commands | mitigate | Proven writer, explicit modes, revision/idempotency checks, immutable snapshots. |
| T-01-07-SC | Tampering | package installs | mitigate | No new packages. |
</threat_model>

<verification>
- Canonical fixture digest and exact row assertions.
- `npm run test:integration -- plan-workout-tracer --runInBand`
- `npm run test:components -- TodayScreen --runInBand`
</verification>

<success_criteria>
- WORK-01..04 are user-observable and transactionally proven.
- The exact missing seed input is resolved without planner invention.
- No Phase 2 catalog/editor/schedule breadth appears.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-workout-loop/01-07-SUMMARY.md` when done.
</output>
