---
phase: 01-trustworthy-workout-loop
plan: 03
type: execute
wave: 1
depends_on:
  - 01-01
files_modified:
  - src/domains/shared/index.ts
  - src/domains/shared/contracts.ts
  - src/domains/shared/errors.ts
  - src/domains/shared/clock.ts
  - src/domains/shared/diagnostics.ts
  - src/bootstrap/appContainer.ts
  - src/testing/contracts/sqliteKernel.contract.ts
  - tests/sqlite-host/sqliteKernel.host.test.ts
  - jest.config.js
  - eslint.config.js
  - scripts/check-boundaries.mjs
  - .github/workflows/pr.yml
autonomous: true
requirements:
  - FOUND-06
must_haves:
  truths:
    - "Persistence/platform inputs are Zod-validated and domain rules receive validated values per D-26."
    - "Typed errors and bounded diagnostics expose recovery metadata without SQL parameters, workout payloads, notes, passwords, keys, or plaintext per D-29."
    - "Routes/UI cannot import platform internals, execute SQL, or access raw writers per D-07 and D-27."
    - "The repository has runnable unit, component, host-SQLite, coverage, and boundary commands that 01-10 composes into the final reviewed CI order per D-30 and D-31."
  artifacts:
    - path: "src/domains/shared/contracts.ts"
      provides: "Versioned boundary schemas and validated shared values"
    - path: "src/domains/shared/errors.ts"
      provides: "Typed application error taxonomy"
    - path: "src/domains/shared/diagnostics.ts"
      provides: "Redacted bounded diagnostics"
    - path: "src/testing/contracts/sqliteKernel.contract.ts"
      provides: "Shared host/device SQLite contract interface"
    - path: "scripts/check-boundaries.mjs"
      provides: "Static architecture enforcement"
  key_links:
    - from: "src/bootstrap/appContainer.ts"
      to: "src/domains/shared/clock.ts"
      via: "explicit dependency composition"
      pattern: "Clock"
    - from: ".github/workflows/pr.yml"
      to: "package.json"
      via: "reviewed test command order"
      pattern: "test:sqlite:host|test:components"
---

<objective>
Deliver reviewed slice 01-03: schema-first contracts, explicit boundaries, diagnostics, clocks, test harnesses, and PR gate wiring.

Purpose: Give later vertical slices deterministic inward-owned interfaces and fast automated proof before native or feature implementation.
Output: Shared contracts/errors/clock/diagnostics, explicit container, Jest/RNTL/host-SQLite harness, coverage thresholds, boundary checks, and supporting CI stages.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/phases/01-trustworthy-workout-loop/01-CONTEXT.md
@.planning/phases/01-trustworthy-workout-loop/01-RESEARCH.md
@.planning/phases/01-trustworthy-workout-loop/01-PATTERNS.md
@.planning/research/ARCHITECTURE.md
@.planning/phases/01-trustworthy-workout-loop/01-01-SUMMARY.md
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Define validated contracts, clock, errors, and diagnostics</name>
  <files>src/domains/shared/index.ts, src/domains/shared/contracts.ts, src/domains/shared/errors.ts, src/domains/shared/clock.ts, src/domains/shared/diagnostics.ts, src/bootstrap/appContainer.ts</files>
  <behavior>
    - Unknown/malformed boundary versions fail as typed validation errors.
    - Application errors expose category, retryability, and bounded correlation code without raw platform messages.
    - Diagnostic serialization redacts forbidden sensitive fields and bounds entries/field lengths.
    - FakeClock deterministically drives expiry, leases, retries, and performance tests.
  </behavior>
  <action>Create versioned Zod boundary schemas, stable IDs/time values, `Clock`/`FakeClock`, application error categories with retry metadata, and bounded redacted diagnostics per D-26 and D-29. Export public contracts through one `index.ts`; pure domain code must not import Zod or platform modules. Build `appContainer.ts` as explicit construction of ports/adapters, not a DI framework. Diagnostics may record stable category, correlation, revision, and duration only; add table-driven valid/invalid/unsupported/redaction tests before implementation.</action>
  <verify>
    <automated>npm run test:unit -- contracts errors diagnostics clock --runInBand</automated>
  </verify>
  <done>Every boundary produces validated values or typed safe errors, fake time is injectable, and redaction tests prove forbidden payloads cannot enter diagnostics.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Establish host SQLite, coverage, boundary, and CI gates</name>
  <files>src/testing/contracts/sqliteKernel.contract.ts, tests/sqlite-host/sqliteKernel.host.test.ts, jest.config.js, eslint.config.js, scripts/check-boundaries.mjs, .github/workflows/pr.yml</files>
  <behavior>
    - Host tests use Node 24 built-in `node:sqlite`; mocked Expo modules are not treated as native proof.
    - Static checks fail route SQL, `src/ui -> src/platform`, raw writer leakage, internal cross-domain imports, and direct use of the prohibited Expo exclusive helper.
    - Integrity-critical modules require 100% statements/branches/functions/lines; remaining testable TypeScript requires at least 90% statements/lines/functions and 85% branches.
    - PR stages run typecheck, lint/boundaries, unit/component, host SQLite/coverage, native SQLite, then Android smoke.
  </behavior>
  <action>Configure Jest 29/jest-expo/RNTL projects and Node 24 `node:sqlite` host tests. Define the shared SQLite-kernel contract shape used by 01-04 without implementing production behavior here. Add scripts `test:unit`, `test:components`, `test:sqlite:host`, `test:sqlite:device`, `test:integration`, `test:coverage`, `test:all`, and `ci:pr`; commands whose implementation lands later must fail with an explicit missing-suite message until that slice supplies it. Enforce D-07, D-27, D-28, D-30, and D-31 through lint/static import rules. Update PR CI with full-SHA actions and the exact reviewed order; no remote API coverage matrix is required because this is an in-process/local SDK app.</action>
  <verify>
    <automated>npm run lint && npm run check:boundaries && npm run test:unit -- --runInBand && npm run test:sqlite:host -- --runInBand</automated>
  </verify>
  <done>Fast tests and static boundaries pass, coverage policy is encoded, host SQL semantics are runnable, and the final CI owner has explicit supporting commands rather than silent skips.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries
| Boundary | Description |
|----------|-------------|
| unknown persisted/platform data → domain | Untrusted shapes and versions enter trusted rules. |
| platform exceptions → user/diagnostics | Raw errors may contain sensitive payloads or implementation details. |
| route/UI imports → persistence kernel | Dependency violations could bypass transaction ownership. |

## STRIDE Threat Register
| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-01-03-01 | Tampering | boundary schemas | mitigate | Versioned Zod parsing, unsupported-version errors, and invalid fixture tests. |
| T-01-03-02 | Information Disclosure | diagnostics | mitigate | Allowlisted bounded fields and table-driven redaction tests. |
| T-01-03-03 | Elevation of Privilege | import boundaries | mitigate | CI-failing static rules for route SQL, platform imports, raw writer, and cross-domain internals. |
| T-01-03-SC | Tampering | npm installs | mitigate | Use only 01-01-approved locked packages; no additional database/test dependency. |
</threat_model>

<verification>
- `npm run lint && npm run check:boundaries`
- `npm run test:unit -- contracts errors diagnostics clock --runInBand`
- `npm run test:sqlite:host -- --runInBand`
</verification>

<success_criteria>
- FOUND-06 is fully proven and the 01-04 FOUND-03 owner has enforceable boundaries plus shared contract scaffolding.
- The 01-10 REL-02 owner has runnable early stages and explicit native/E2E commands to compose.
- D-26 through D-31 are encoded without adding a generic ORM, DI framework, event bus, or persisted query cache.
</success_criteria>

<output>
Create `.planning/phases/01-trustworthy-workout-loop/01-03-SUMMARY.md` when done.
</output>
