---
phase: 02-owned-library-and-planning
plan: 22
type: execute
wave: 13
depends_on: [02-21]
files_modified:
  - app/(tabs)/_layout.tsx
  - app/(tabs)/__tests__/_layout.test.tsx
  - src/ui/components/index.ts
  - src/ui/__tests__/foundation.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-01]
estimate: { tokens: 11000, raw_tokens: 11000, tasks: 2, confidence: high }
must_haves:
  truths:
    - "D-66 and G-02-01 close through navigator-level `tabBarPosition: left` at expanded widths; rail visuals are never painted onto a bottom-tab scene."
    - "Compact and medium layouts retain bottom navigation, while expanded and expanded-landscape layouts use one full-height rail/content row with no lower-right void."
    - "The shared 840dp breakpoint, route semantics, selected state, visible focus, keyboard/D-pad activation, 200% text behavior, and minimum 48dp targets remain consistent."
  artifacts:
    - { path: "app/(tabs)/_layout.tsx", provides: "Responsive navigator-level bottom-tab versus left-rail geometry" }
    - { path: "app/(tabs)/__tests__/_layout.test.tsx", provides: "Boundary, resize, orientation, and navigation regression coverage" }
  key_links:
    - { from: "app/(tabs)/_layout.tsx", to: "src/ui/components/index.ts", via: "one shared width classification drives navigator and AppTabs placement", pattern: "tabBarPosition|position|840" }
---

<objective>
Close the expanded-navigation geometry regression without changing the four root destinations.

Purpose: Make the expanded shell a real left rail while preserving the accepted compact and medium navigation model.
Output: Responsive navigator configuration and focused adaptive-navigation tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@app/(tabs)/_layout.tsx
@src/ui/components/index.ts
</context>

## Artifacts this phase produces

- Navigator-level adaptive tab placement.
- Focused regression coverage at compact, medium, 839dp, 840dp, expanded, resize, and landscape layouts.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-01 | Root navigation continues to expose and return to Library correctly | COVERED |
| CONTEXT | D-66 | True left rail at expanded width | COVERED |
| VERIFICATION | G-02-01 | Removes the blank lower-right region and preserves bottom tabs below the breakpoint | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Make tab placement an authoritative navigator decision</name>
  <read_first>app/(tabs)/_layout.tsx, src/ui/components/index.ts, src/ui/layout/AdaptiveScreen.tsx, src/ui/__tests__/foundation.test.tsx</read_first>
  <files>app/(tabs)/_layout.tsx, src/ui/components/index.ts</files>
  <action>Start with a failing boundary test, then drive Expo Router `Tabs.screenOptions.tabBarPosition` from the same 840dp classification used by the visual AppTabs treatment. Preserve route names, ordering, labels, safe-area behavior, selected semantics, and bottom placement below 840dp. At expanded width compose the tab bar and scene as one full-height horizontal layout; remove assumptions that reserve a bottom bar or leave unused lower-right space. Keep resize transitions deterministic and do not add a second navigation state. Commit only these task files; the commit message body must end exactly with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The navigator reports bottom placement at 839dp and left placement at 840dp, with one route state and no blank scene region.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand "app/(tabs)/__tests__/_layout.test.tsx" src/ui/__tests__/foundation.test.tsx</automated></verify>
  <done>Expanded geometry is owned by the navigator rather than decorative rail styling.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Prove adaptive navigation semantics and focus survive transitions</name>
  <read_first>app/(tabs)/_layout.tsx, app/(tabs)/__tests__/_layout.test.tsx, src/ui/components/index.ts, src/ui/__tests__/foundation.test.tsx</read_first>
  <files>app/(tabs)/__tests__/_layout.test.tsx, src/ui/__tests__/foundation.test.tsx</files>
  <action>Add focused tests for compact, medium, 839dp, 840dp, expanded portrait/landscape, and live resize. Assert one visible navigation control per destination, exact accessible names, selected state, Enter/Space and D-pad activation, visible focus restoration, 200% text reflow, safe areas, and minimum 48dp targets. Assert the expanded scene consumes remaining width and full height. Commit only these test files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Focused tests fail on the prior cosmetic-rail implementation and pass only when all width and accessibility contracts hold.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand "app/(tabs)/__tests__/_layout.test.tsx" src/ui/__tests__/foundation.test.tsx</automated></verify>
  <done>D-66 is covered at the breakpoint and across adaptive interaction modes.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Route spoofing | Tab presentation to router destination | Keep one canonical route table and assert label-to-route mapping. |
| State tampering | Width change creates duplicate navigation state | Derive both geometry and visuals from one breakpoint value. |
| Denial of use | Rail overlaps or hides scene/actions | Full-height layout, safe-area, 200% text, and orientation tests. |
| Accessibility information loss | Glyph/focus state is not exposed | Exact names, selected state, visible focus, D-pad, and 48dp assertions. |
</threat_model>

<verification>Run the two focused component suites, then typecheck and lint. No APK evidence is current after this source change; Plan 02-34 owns the exact-HEAD rebuild.</verification>
<success_criteria>G-02-01 is closed with a true expanded left rail and unchanged compact/medium navigation semantics.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-22-SUMMARY.md` when complete.</output>
