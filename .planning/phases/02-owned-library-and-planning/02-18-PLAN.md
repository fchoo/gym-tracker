---
phase: 02-owned-library-and-planning
plan: 18
type: execute
wave: 10
depends_on: [02-13, 02-15, 02-16]
files_modified:
  - app/library/plan/[planId]/day/[dayId]/remove.tsx
  - app/library/plan/[planId]/replace/[occurrenceId].tsx
  - maestro/phase2/plan-impact-replacement.yaml
  - src/domains/plans/planImpactCommands.test.ts
  - src/domains/plans/planImpactCommands.ts
  - src/platform/sqlite/repositories/planImpactRepository.ts
  - src/ui/__tests__/PlanImpactReplacement.test.tsx
  - src/ui/components/ImpactPreview.tsx
  - src/ui/screens/ExerciseReplacementScreen.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - tests/integration/plan-impact-replacement.test.ts
autonomous: true
requirements: [LIB-05, LIB-08]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 3
  confidence: low
must_haves:
  truths:
    - "D-32 removing a schedule-bound day returns a complete impact preview and requires explicit replacement, removal, or effective-date choice before Save."
    - "OwnedPlanEditorScreen opens a dedicated D-32 removal route with the current preview token and explicit replacement day, remove binding, or effective LocalDate choice."
    - "D-52 replacement lists complete metric-compatible identities first and requires explicit target, warm-up, rest, and progression review without comparability inference or history migration."
    - "D-53 offers exact `This occurrence` and `All occurrences in this plan`, previews every affected occurrence, and commits only with `Save replacement`."
    - "D-30/D-31 keep ordinary future edits available and show `Current workout is unaffected`; only schedule-restructuring impact is blocked until the workout resolves."
    - "All eight UI truths are covered for impact/replacement: Empty, Loading, Error, Populated, Partial, Overflow, Zero / one / many, and Long text."
  artifacts:
    - path: "src/domains/plans/planImpactCommands.ts"
      provides: "Schedule-bound day removal and compatible exercise replacement commands"
    - path: "src/ui/components/ImpactPreview.tsx"
      provides: "Complete before/after occurrence/schedule/target/rest/policy impact"
    - path: "src/ui/screens/ExerciseReplacementScreen.tsx"
      provides: "Exact replacement scope and Save replacement flow"
    - path: "app/library/plan/[planId]/day/[dayId]/remove.tsx"
      provides: "Dedicated D-32 schedule-bound day-removal route"
    - path: "maestro/phase2/plan-impact-replacement.yaml"
      provides: "Installed day-impact and exercise-replacement journey"
  key_links:
    - from: "src/ui/screens/ExerciseReplacementScreen.tsx"
      to: "src/domains/plans/planImpactCommands.ts"
      via: "scope, preview token, complete reviewed values, and expected revisions"
      pattern: "replacePlanExercise"
    - from: "src/domains/plans/planImpactCommands.ts"
      to: "src/platform/sqlite/repositories/planImpactRepository.ts"
      via: "one aggregate transaction spanning plan and schedule facts"
      pattern: "applyPlanImpact"
---

<objective>
Complete schedule-bound day impact and explicit exercise replacement over the core owned editor.

Purpose: Prevent LIB-05/LIB-08 structural edits from silently rebinding schedules, converting metrics, or changing history.
Output: Impact/replacement domain/repository, focused UI route/screen/component, editor integration, and rollback tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-13-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-15-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-16-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/index.ts
@src/ui/screens/OwnedPlanEditorScreen.tsx
@src/domains/metrics/registry.ts
</context>

## Artifacts this phase produces

- `src/domains/plans/planImpactCommands.ts` — `previewDayRemoval`, `removePlanDayWithImpact`, `previewExerciseReplacement`, `replacePlanExercise`.
- `src/domains/plans/planImpactCommands.test.ts` — compatibility/scope/stale preview tests.
- `src/platform/sqlite/repositories/planImpactRepository.ts` — atomic plan/schedule impact persistence.
- `tests/integration/plan-impact-replacement.test.ts` — rollback/snapshot/history/schedule proof.
- `src/ui/components/ImpactPreview.tsx` — `ImpactPreview`.
- `src/ui/screens/ExerciseReplacementScreen.tsx` — complete replacement screen.
- `app/library/plan/[planId]/day/[dayId]/remove.tsx` — thin day-removal impact route.
- `app/library/plan/[planId]/replace/[occurrenceId].tsx` — thin replacement route.
- `src/ui/__tests__/PlanImpactReplacement.test.tsx` — exact labels, state matrix, adaptive/focus coverage.
- `maestro/phase2/plan-impact-replacement.yaml` — installed D-32 day-impact and D-52/D-53 replacement flow.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Remove one schedule-bound day through the editor and explicit prospective impact route</name>
  <read_first>src/ui/screens/OwnedPlanEditorScreen.tsx, src/domains/plans/ownedPlanCommands.ts, src/domains/scheduling/scheduleCommands.ts, src/platform/sqlite/repositories/scheduleRepository.ts, src/ui/components/index.ts</read_first>
  <files>src/domains/plans/planImpactCommands.ts, src/platform/sqlite/repositories/planImpactRepository.ts, src/ui/components/ImpactPreview.tsx, app/library/plan/[planId]/day/[dayId]/remove.tsx, src/ui/__tests__/PlanImpactReplacement.test.tsx</files>
  <action>Start red with the dedicated `Remove day` route for a day bound by an active prospective schedule. `previewDayRemoval` must list every affected binding/date and return a current revision-bound preview token. Create ImpactPreview in this tracer, render the token-backed preview, and require one D-32 choice—replacement day, remove binding, or effective LocalDate—before `removePlanDayWithImpact` can atomically save the plan graph and prospective schedule facts. Existing opportunities/sessions/history remain unchanged; an active workout blocks only restructuring and the preview states `Current workout is unaffected`. Implement stale preview, empty/one/many binding, route reload/current-token refresh, commit failure, and exact invalidation tests.</action>
  <acceptance_criteria>A bound day cannot disappear without a complete current preview and explicit prospective choice; plan and schedule either both commit or both roll back.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/PlanImpactReplacement.test.tsx -t "day removal"</automated>
  </verify>
  <done>The narrowest schedule-impact path is explicit, prospective, atomic, and immutable-history-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete compatible-first occurrence/all-occurrences replacement UI and persistence</name>
  <read_first>src/domains/plans/planImpactCommands.ts, src/platform/sqlite/repositories/planImpactRepository.ts, src/ui/components/ImpactPreview.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/domains/metrics/registry.ts</read_first>
  <files>src/domains/plans/planImpactCommands.test.ts, src/ui/screens/ExerciseReplacementScreen.tsx, app/library/plan/[planId]/replace/[occurrenceId].tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, tests/integration/plan-impact-replacement.test.ts</files>
  <action>Write component/integration tests before UI. Wire OwnedPlanEditorScreen to the dedicated D-32 day-removal route and the replacement route. Implement D-52 hard compatibility on complete metric identity, with discretionary stable ranking only beneath that boundary. Require explicit reviewed target, warm-ups, rest, and progression/manual Hold; never infer conversion or move history. Offer exact D-53 scope labels `This occurrence` and `All occurrences in this plan`, preview all affected days/occurrences, and commit only with `Save replacement` plus expected revisions/current preview token. Use the tracer-created ImpactPreview for both replacement and day removal, preserve `Current workout is unaffected`, and block only schedule restructuring during active workout. Cover all eight UI truths, long plans/many occurrences, 200% text, focus/keyboard/D-pad, non-color status, reduced motion, save error retention, and 48dp.</action>
  <acceptance_criteria>Replacement cannot submit an incompatible or incomplete target, preview cannot go stale, scope is exact, and existing snapshots/observations are byte-identical.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/PlanImpactReplacement.test.tsx</automated>
  </verify>
  <done>Owned-plan structural impacts and exercise replacement are complete without silent schedule or metric mutation.</done>
</task>

<task type="auto">
  <name>Task 3: Author the installed day-impact and exercise-replacement journey</name>
  <read_first>src/ui/__tests__/PlanImpactReplacement.test.tsx, app/library/plan/[planId]/day/[dayId]/remove.tsx, app/library/plan/[planId]/replace/[occurrenceId].tsx, maestro/phase2/owned-plan-editor.yaml, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md</read_first>
  <files>maestro/phase2/plan-impact-replacement.yaml</files>
  <action>Author one public semantic installed flow that enters D-32 day removal from the owned editor, verifies current impact, exercises replacement-day, remove-binding, and effective-date choices before Save, then exercises D-52/D-53 compatible-first replacement with exact `This occurrence`, `All occurrences in this plan`, reviewed target/warm-up/rest/policy, and `Save replacement`. Include stale-preview refresh/retry and active-workout restructuring block without private controls.</action>
  <acceptance_criteria>The flow proves the complete D-32 choice surface and D-52/D-53 replacement scope against public labels, including current preview behavior.</acceptance_criteria>
  <verify>
    <automated>node -e "const fs=require('fs'); const s=fs.readFileSync('maestro/phase2/plan-impact-replacement.yaml','utf8'); for(const x of ['Remove day','This occurrence','All occurrences in this plan','Save replacement']) if(!s.includes(x)) process.exit(1)"</automated>
  </verify>
  <done>The installed plan-impact flow is complete and ready for unchanged-APK execution in Plan 02-21.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Impact preview → confirmed aggregate command | Preview facts may become stale before Save. |
| Replacement candidate → target/history semantics | Similar names cannot imply metric compatibility or history transfer. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | owned-plan/custom exercise authority | high | mitigate | Owned/custom predicates and no bundled/history mutation. |
| T-02-04 | Tampering | preview/plan/schedule revisions | high | mitigate | Revision-bound previews and one aggregate transaction. |
| T-02-05 | Tampering | metric/schedule interpretation | high | mitigate | Complete identity compatibility and prospective explicit choices only. |
| T-02-07 | Information Disclosure | impact diagnostics | medium | mitigate | UI shows intentional plan facts; logs retain codes/counts only. |
</threat_model>

<verification>
Run focused integration day-removal sample and complete component replacement suite; final editor Maestro includes public replacement coverage.
</verification>

<success_criteria>
Structural plan changes and replacements require complete current impact review and never infer schedule, metric, or history changes.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-18-SUMMARY.md` when done.
</output>
