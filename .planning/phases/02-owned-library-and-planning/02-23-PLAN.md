---
phase: 02-owned-library-and-planning
plan: 23
type: execute
wave: 13
depends_on: [02-21]
files_modified:
  - src/ui/components/CalendarField.tsx
  - src/ui/components/CalendarField.test.tsx
  - src/ui/components/index.ts
  - src/ui/screens/StarterActivationScreen.tsx
  - src/ui/screens/ScheduleEditorScreen.tsx
  - src/ui/screens/PlanDayRemovalScreen.tsx
  - src/ui/__tests__/StarterPlans.test.tsx
  - src/ui/__tests__/ScheduleEditor.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-06, LIB-07, LIB-08, LIB-09]
estimate: { tokens: 16000, raw_tokens: 16000, tasks: 3, confidence: high }
must_haves:
  truths:
    - "The date half of D-57 and G-02-02 closes: every Phase 2 date-entry flow uses an accessible in-app calendar rather than free-form date typing."
    - "The calendar emits the existing canonical `YYYY-MM-DD` LocalDate value and preserves start-date, effective-date, timezone, DST, and prospective-only domain rules."
    - "No package is installed; month navigation, selection, dismissal, validation, 200% text, keyboard/D-pad operation, focus restoration, and 48dp targets are covered."
  artifacts:
    - { path: "src/ui/components/CalendarField.tsx", provides: "Reusable dependency-free LocalDate calendar selector" }
    - { path: "src/ui/components/CalendarField.test.tsx", provides: "Calendar boundary, semantics, focus, and accessibility proof" }
    - { path: "src/ui/screens/ScheduleEditorScreen.tsx", provides: "Calendar-backed effective-date editing" }
  key_links:
    - { from: "src/ui/components/CalendarField.tsx", to: "src/domains/scheduling/localDate.ts", via: "canonical LocalDate strings remain parsed and validated by the domain", pattern: "YYYY-MM-DD|LocalDate|parseLocalDate" }
---

<objective>
Replace all Phase 2 free-form date entry with one accessible in-app calendar control.

Purpose: Make date selection unambiguous without weakening the established LocalDate and schedule contracts.
Output: A reusable calendar field and migrated starter/schedule/day-removal date flows.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@src/domains/scheduling/localDate.ts
@src/ui/screens/StarterActivationScreen.tsx
@src/ui/screens/ScheduleEditorScreen.tsx
@src/ui/screens/PlanDayRemovalScreen.tsx
</context>

## Artifacts this phase produces

- A shared in-app calendar selector built from current React Native/project dependencies.
- Calendar-backed starter activation, schedule editing, and effective-date removal flows.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-06, LIB-07 | Starter schedule start date | COVERED |
| REQ | LIB-08, LIB-09 | Owned-plan effective dates and prospective scheduling | COVERED |
| CONTEXT | D-57 | All date input uses a calendar | COVERED |
| VERIFICATION | G-02-02 date scope | Raw date fields and accessibility gaps | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build the dependency-free LocalDate calendar primitive</name>
  <read_first>src/domains/scheduling/localDate.ts, src/ui/components/index.ts, src/ui/theme/index.ts, src/ui/__tests__/foundation.test.tsx</read_first>
  <files>src/ui/components/CalendarField.tsx, src/ui/components/CalendarField.test.tsx, src/ui/components/index.ts</files>
  <action>Start with failing tests, then implement an in-app calendar field using only installed dependencies and React Native primitives. Preserve canonical `YYYY-MM-DD` values; support bounded previous/next month navigation, today/default selection, disabled minimum/maximum dates, explicit confirm/cancel, semantic selected/disabled states, modal focus containment/restoration, Enter/Space and D-pad navigation, 200% text reflow, reduced motion, and 48dp controls. Keep validation in the existing LocalDate domain instead of accepting JavaScript Date coercion. Commit only these task files, ending the commit message body exactly with `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Tests cover leap years, month/year boundaries, invalid canonical input, date bounds, selection, dismissal, accessible names/states, focus restoration, and no new dependency.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/components/CalendarField.test.tsx</automated></verify>
  <done>A reusable calendar returns only canonical domain-compatible LocalDate values.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate starter and schedule date entry</name>
  <read_first>src/ui/components/CalendarField.tsx, src/ui/screens/StarterActivationScreen.tsx, src/ui/screens/ScheduleEditorScreen.tsx, src/ui/__tests__/StarterPlans.test.tsx, src/ui/__tests__/ScheduleEditor.test.tsx</read_first>
  <files>src/ui/screens/StarterActivationScreen.tsx, src/ui/screens/ScheduleEditorScreen.tsx, src/ui/__tests__/StarterPlans.test.tsx, src/ui/__tests__/ScheduleEditor.test.tsx</files>
  <action>Replace raw starter start-date and schedule effective-date text entry with CalendarField. Preserve exact defaults, preview copy, timezone identity, DST handling, validation, disabled/save state, and command payloads. Update tests to select dates through the calendar and prove cancel does not mutate drafts. Commit only these files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Neither screen permits free-form date typing and both submit the same canonical values under all existing schedule rules.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/StarterPlans.test.tsx src/ui/__tests__/ScheduleEditor.test.tsx</automated></verify>
  <done>Starter activation and schedule editing use calendar input end to end.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Migrate prospective day-removal date entry and run the date gate</name>
  <read_first>src/ui/components/CalendarField.tsx, src/ui/screens/PlanDayRemovalScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/ui/__tests__/PlanImpactReplacement.test.tsx</read_first>
  <files>src/ui/screens/PlanDayRemovalScreen.tsx, src/ui/__tests__/ScheduleEditor.test.tsx</files>
  <action>Use CalendarField for the effective-date branch of day removal, preserving the earliest allowed date, explicit choice semantics, preview, and typed error mapping. Add focused coverage to the existing schedule suite for boundary selection, cancel, and save. Audit the Phase 2 screens for any remaining editable `YYYY-MM-DD` TextInput and fail the task if one remains. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Every in-scope editable date is calendar-backed and a source audit finds no free-form date input.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/components/CalendarField.test.tsx src/ui/__tests__/StarterPlans.test.tsx src/ui/__tests__/ScheduleEditor.test.tsx</automated></verify>
  <done>The date portion of G-02-02 is closed across all identified consumers.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Input tampering | Calendar selection to LocalDate command | Canonical domain parsing and bounded dates; no locale-string trust. |
| State confusion | Dismissed picker changes a draft | Commit selection only on explicit confirmation and test cancellation. |
| Schedule integrity loss | Date/timezone conversion shifts intent | Keep LocalDate text and stored timezone authoritative; no implicit UTC conversion. |
| Accessibility denial | Calendar cannot be operated without touch | Semantic grid/actions, D-pad/keyboard support, focus restoration, and 48dp targets. |
</threat_model>

<verification>Run focused component tests, typecheck, lint, and a source audit for raw editable date fields. Plan 02-34 must rebuild the APK after this source change.</verification>
<success_criteria>The date portion of D-57/G-02-02 is fully calendar-backed with unchanged scheduling semantics.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-23-SUMMARY.md` when complete.</output>
