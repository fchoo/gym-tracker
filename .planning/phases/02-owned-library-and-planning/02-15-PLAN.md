---
phase: 02-owned-library-and-planning
plan: 15
type: execute
wave: 9
depends_on: [02-11, 02-12]
files_modified:
  - app/library/plan/[planId]/edit.tsx
  - app/library/plan/create.tsx
  - maestro/phase2/owned-plan-editor.yaml
  - src/bootstrap/ownedPlanRuntime.tsx
  - src/ui/__tests__/OwnedPlanEditor.test.tsx
  - src/ui/components/PlanEditorFields.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
autonomous: true
requirements: [LIB-08]
estimate:
  tokens: 36000
  raw_tokens: 36000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-21/D-22 `Create my own` asks for plan and first day names, creates an inactive Draft, shows exact missing requirement, and disables Schedule/Activate until valid."
    - "D-23/D-24 use exact noun-specific Save labels and dirty-leave `Save changes`, `Discard`, `Keep editing`; no blur/debounce persistence."
    - "D-25 reorder provides drag plus `Move up`/`Move down`, modifies only the draft, and persists with containing Save."
    - "D-26/D-28 duplicate full graph to fresh inactive IDs and archive/restore without deletion."
    - "D-30/D-31 show `Current workout is unaffected`, preserve immutable current snapshots, and defer schedule-restructuring impact to Plan 02-18."
    - "All eight UI truths are covered for owned editor routes: Empty, Loading, Error, Populated, Partial, Overflow, Zero / one / many, and Long text."
  artifacts:
    - path: "src/ui/screens/OwnedPlanEditorScreen.tsx"
      provides: "Complete create/rename/edit/duplicate/reorder/archive/restore user-facing plan editor"
    - path: "maestro/phase2/owned-plan-editor.yaml"
      provides: "Installed create/draft/save/reorder/duplicate/archive/restore journey"
  key_links:
    - from: "src/ui/screens/OwnedPlanEditorScreen.tsx"
      to: "src/bootstrap/ownedPlanRuntime.tsx"
      via: "complete draft and expected revisions submitted only by noun-specific Save"
      pattern: "saveOwnedPlan"
    - from: "app/library/plan/[planId]/edit.tsx"
      to: "src/ui/screens/OwnedPlanEditorScreen.tsx"
      via: "thin route adapter"
      pattern: "OwnedPlanEditorScreen"
---

<objective>
Deliver the complete core owned-plan editor UI, routes, runtime wiring, tests, and installed journey.

Purpose: Make LIB-08 directly usable while preserving aggregate Save, draft validity, fresh duplication, archive/restore, and snapshot safety.
Output: Editor routes/screen/fields/runtime/tests and `maestro/phase2/owned-plan-editor.yaml`.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-11-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-12-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@src/ui/components/index.ts
@src/ui/screens/LibraryScreen.tsx
@src/bootstrap/workoutAppRuntime.tsx
</context>

## Artifacts this phase produces

- `src/ui/components/PlanEditorFields.tsx` — plan/day/target draft fields and accessible reorder controls.
- `src/ui/screens/OwnedPlanEditorScreen.tsx` — complete core editor and lifecycle actions.
- `app/library/plan/create.tsx` — thin create route.
- `app/library/plan/[planId]/edit.tsx` — thin edit route.
- `src/ui/__tests__/OwnedPlanEditor.test.tsx` — exact Save/dirty/draft/reorder/lifecycle/adaptive state tests.
- `maestro/phase2/owned-plan-editor.yaml` — public installed editor journey.
- `src/bootstrap/ownedPlanRuntime.tsx` — owned-plan editor runtime adapter over the base app runtime.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Create one named draft, edit its first day, and commit with Save plan</name>
  <read_first>src/ui/components/index.ts, src/ui/screens/LibraryScreen.tsx, src/bootstrap/workoutAppRuntime.tsx, .planning/phases/02-owned-library-and-planning/02-UI-SPEC.md, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/ui/components/PlanEditorFields.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, app/library/plan/create.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/bootstrap/ownedPlanRuntime.tsx</files>
  <action>Start red with `Create my own`: collect plan name and first day name, call the runtime draft command, open the day editor, add one valid targeted exercise, and commit the complete aggregate only with exact `Save plan`. Build `PlanEditorFields` over existing inputs/actions and runtime plan port. Implement D-21/D-22 draft labels, exact missing requirement, disabled Schedule/Activate reason, and inactive status. Implement D-23 noun-specific `Save plan`, `Save day`, `Save target`; blur/debounce/reorder stays in-memory. On save validation failure preserve values, focus error summary, and expose Retry-safe copy. Cover empty/loading/error/populated/partial and zero/one/many graph states.</action>
  <acceptance_criteria>A named empty draft is visible and unschedulable; a valid complete draft commits atomically via Save plan and returns to authoritative plan detail without intermediate writes.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx -t "create|save plan"</automated>
  </verify>
  <done>The narrowest user-facing create/edit/save path is wired through routes, runtime, aggregate command, and authoritative readback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete dirty leave, reorder, duplicate, archive/restore, active editing, and Maestro</name>
  <read_first>src/ui/components/PlanEditorFields.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/bootstrap/workoutAppRuntime.tsx, src/ui/components/index.ts</read_first>
  <files>src/ui/screens/OwnedPlanEditorScreen.tsx, app/library/plan/[planId]/edit.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, maestro/phase2/owned-plan-editor.yaml</files>
  <action>Add D-24 dirty-leave ConfirmationSheet with exact `Save changes`, `Discard`, `Keep editing`; use the existing exported ConfirmationSheet. Add D-25 drag handle plus accessibility actions `Move up`/`Move down`, draft-only until Save. Wire full-graph duplicate and archive/restore per D-26/D-28 with commit-gated result and focus restoration. Permit active/in-progress plan future edits, show exact `Current workout is unaffected`, and disable only changes that return schedule-impact requirement per D-30/D-31. Cover overflow/long text, compact/medium/expanded, landscape, 200% stacking, keyboard/D-pad, visible/restored focus, reduced motion, non-color state, and 48dp. Author public Maestro create/save/reorder/duplicate/archive/restore flow using exact labels.</action>
  <acceptance_criteria>Dirty leave and lifecycle copy are exact; duplicate is fresh/inactive; archive is reversible; schedule-impact actions do not silently save; all eight UI truths pass.</acceptance_criteria>
  <verify>
    <automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx</automated>
  </verify>
  <done>The core LIB-08 editor is complete and its public installed journey is owned for final execution.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Process-local draft → aggregate command | Partial fields and reorder gestures must not become source writes. |
| Editor action → active schedule/current workout | Future edits cannot silently restructure schedule or change immutable snapshots. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | owned-plan UI authority | high | mitigate | Runtime exposes owned commands only; no template/platform imports. |
| T-02-04 | Tampering | dirty/stale editor revisions | high | mitigate | Complete drafts, expected revisions, validation focus, commit-gated readback. |
| T-02-05 | Tampering | schedule/snapshot interpretation | high | mitigate | Impact-required result, immutable current workout, exact explanatory copy. |
| T-02-07 | Information Disclosure | editor errors | medium | mitigate | Preserve values locally but emit only safe messages/codes. |
</threat_model>

<verification>
Run focused and full editor component tests; final exact-APK Maestro execution is owned by Plan 02-21.
</verification>

<success_criteria>
The owner can create, rename, edit, duplicate, reorder, archive, and restore plans through explicit atomic UI actions.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-15-SUMMARY.md` when done.
</output>
