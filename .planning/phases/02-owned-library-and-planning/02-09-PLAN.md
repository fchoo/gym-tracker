---
phase: 02-owned-library-and-planning
plan: 09
type: execute
wave: 4
depends_on: [02-06]
files_modified:
  - src/domains/library/search.test.ts
  - src/domains/library/search.ts
  - src/platform/sqlite/repositories/librarySearchRepository.ts
  - tests/integration/exercise-search.test.ts
autonomous: true
requirements: [LIB-03, LIB-04]
estimate:
  tokens: 30000
  raw_tokens: 30000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "E-14 through E-27 are executable search/repository tests with requirement/category traceability."
    - "D-11 ranking is exact canonical, canonical prefix, alias exact/prefix, then remaining normalized partial, with canonical alphabetical key and stable ID ties; Favorite/Recent never outrank text."
    - "D-14 filters use OR within type/muscle/equipment/origin/visibility/recent/favorite and AND across non-empty groups; D-10 unavailable/hidden/archived rows stay excluded by default."
    - "Queries of one/two code points use bounded relational matching; three or more use packaged trigram candidates; punctuation and operator words remain bound data."
    - "Thirty-row opaque keyset pages are bound to normalized query, filters, catalog revision, and normalization version; stale cursors return typed restart instead of duplicates/reordering."
  artifacts:
    - path: "src/domains/library/search.ts"
      provides: "Normalization, filters, ranking tuple, alias cause, and cursor contracts"
    - path: "src/platform/sqlite/repositories/librarySearchRepository.ts"
      provides: "Prepared relational/FTS candidate search with authoritative ranking and keyset hydration"
  key_links:
    - from: "src/domains/library/search.ts"
      to: "src/platform/sqlite/repositories/librarySearchRepository.ts"
      via: "validated SearchRequest and opaque cursor fingerprint"
      pattern: "SearchRequest|SearchCursor"
    - from: "src/platform/sqlite/repositories/librarySearchRepository.ts"
      to: "exercise_search_fts"
      via: "bound candidate selection followed by relational rank/filter calculation"
      pattern: "MATCH|rank"
---

<objective>
Implement the product search contract over the proven packaged FTS prerequisite.

Purpose: Satisfy LIB-03/LIB-04 while keeping relational rows, not FTS/BM25 or engagement signals, authoritative.
Output: Pure search rules, prepared keyset repository, and focused unit/integration edge proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-06-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/testing/contracts/phase2Fts.contract.ts
@src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts
@src/platform/sqlite/sqliteKernel.ts
</context>

## Artifacts this phase produces

- `src/domains/library/search.ts` — `normalizeSearchText`, `rankExerciseMatch`, filter canonicalization, and cursor codec.
- `src/domains/library/search.test.ts` — D-10..D-16 and E-14..E-27 pure tables.
- `src/platform/sqlite/repositories/librarySearchRepository.ts` — `searchExercises` and `listRecentExercises`.
- `tests/integration/exercise-search.test.ts` — authoritative search/filter/page/parity behavior.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Return one alias-caused partial result with the locked rank tuple</name>
  <read_first>src/testing/contracts/phase2Fts.contract.ts, src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md, .planning/phases/02-owned-library-and-planning/02-RESEARCH.md</read_first>
  <files>src/domains/library/search.ts, src/domains/library/search.test.ts</files>
  <action>Start red with a query whose alias prefix causes a match and assert canonical name plus `Matched alias: …`, rank tier, canonical alphabetical key, and stable ID. Implement versioned Unicode-code-point normalization, punctuation/operator treatment as data, bounded query length, D-11 rank tiers, D-14 filter canonicalization, and a signed-by-validation opaque cursor payload bound to query/filter/catalog/normalization fingerprints. Cover E-14 through E-27 pure cases: empty/single inputs, one/two/three code points, diacritics/punctuation, equal rank adjacency, 29/30/31 boundaries, stable ties, cursor idempotency, and precision-independent ordering.</action>
  <acceptance_criteria>Alias cause is explicit, ranking exactly follows D-11, filter groups are canonical, and invalid/stale cursors are typed restart results rather than accepted offsets.</acceptance_criteria>
  <verify>
    <automated>npm run test:unit -- --runInBand src/domains/library/search.test.ts</automated>
  </verify>
  <done>The narrowest alias search path establishes the complete deterministic query/rank/cursor contract.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Execute prepared candidate search, relational filters/ranking, Recent, and stable keyset pages</name>
  <read_first>src/domains/library/search.ts, src/domains/library/search.test.ts, src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts, src/platform/sqlite/sqliteKernel.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/platform/sqlite/repositories/librarySearchRepository.ts, tests/integration/exercise-search.test.ts</files>
  <action>Write integration cases before repository code. Use fixed prepared SQL: bounded relational LIKE for one/two code points and bound trigram MATCH candidates for three or more, then authoritative joins/EXISTS predicates and D-11 rank calculation. Hydrate exactly 30 rows by keyset, return next cursor from the last complete rank tuple, and detect catalog/normalization/filter mismatch. Implement D-12 Recent as latest completed working-set exposure in completed/partial sessions, capped at 10 unique exercises, and D-13 Favorite as explicit persistent filter/row fact without relevance boost. Ensure D-10 visibility defaults and D-15/D-16 attribution/alias read fields. Test punctuation, rollback/parity, page failure preserving prior results, and no raw query text in diagnostics.</action>
  <acceptance_criteria>Search and all filters return stable nonduplicated pages; Recent/Favorite semantics are exact; FTS failure can be repaired without changing relational results.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/exercise-search.test.ts</automated>
  </verify>
  <done>LIB-03/LIB-04 search is complete as a prepared, bounded, relationally authoritative repository.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Owner text/filters → SQL/FTS | Untrusted punctuation and operators cross query-language boundaries. |
| FTS candidates → product results | Derivative candidates cannot determine rank, visibility, or source truth. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-02 | Tampering / Denial | search grammar/pages | high | mitigate | Versioned normalization, bounds, fixed prepared templates, and punctuation tables. |
| T-02-04 | Tampering | mutable cursor/catalog | medium | mitigate | Cursor fingerprints and typed restart on revision mismatch. |
| T-02-06 | Tampering | FTS/result parity | high | mitigate | Relational ranking/filtering and derivative parity/rebuild prerequisite. |
| T-02-07 | Information Disclosure | search diagnostics | medium | mitigate | Codes/counts/revisions only; no raw query text or SQL parameters. |
</threat_model>

<verification>
Run the focused pure and integration suites; confirm E-14..E-27, punctuation, 29/30/31 pages, and D-10..D-16.
</verification>

<success_criteria>
Exercise search is safe, paginated, deterministic, filter-complete, and independent of FTS scoring or engagement signals.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-09-SUMMARY.md` when done.
</output>
