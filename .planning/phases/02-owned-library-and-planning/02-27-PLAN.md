---
phase: 02-owned-library-and-planning
plan: 27
type: execute
wave: 13
depends_on: [02-21]
files_modified:
  - src/domains/workout/activeWorkout.ts
  - src/domains/workout/setCommands.ts
  - src/domains/workout/setCommands.test.ts
  - src/domains/workout/index.ts
  - src/platform/sqlite/repositories/workoutRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/bootstrap/workoutAppRuntime.test.tsx
  - tests/integration/complete-set.test.ts
autonomous: true
gap_closure: true
requirements: [LIB-07, LIB-08, LIB-11, LIB-12]
estimate: { tokens: 21000, raw_tokens: 21000, tasks: 3, confidence: high }
must_haves:
  truths:
    - "D-64 and G-02-05 close against the current full migration manifest: Add warm-up, Copy warm-up, and Add working set persist complete metric identity and return the inserted row."
    - "Every inserted `session_sets` row carries `metric_profile`, `metric_contract_version`, and `exercise_metric_generation`, plus compatible owned target/observation identity; Add Working selects every value it consumes."
    - "D-63's domain half closes through a narrow revision-checked command that edits one completed working set only while the authoritative workout remains `in_progress`."
    - "Completed-set correction preserves later set/exercise progress, the active pointer, current rest state/revision, immutable session snapshot identity, and unrelated rows; stale/finalized/invalid writes fail atomically."
    - "The eight-second whole-session Undo API may remain for backward compatibility, but it is not extended or used as the new correction mechanism; finalized-session correction/audit/void/restore remains Phase 3."
  artifacts:
    - { path: "src/platform/sqlite/repositories/workoutRepository.ts", provides: "Current-schema add/copy writes and narrow completed-set correction transaction" }
    - { path: "src/domains/workout/setCommands.ts", provides: "Typed revision-checked completed-set correction command" }
    - { path: "tests/integration/complete-set.test.ts", provides: "Full-manifest persistence, conflict, rollback, and invariance proof" }
  key_links:
    - { from: "src/domains/workout/setCommands.ts", to: "src/platform/sqlite/repositories/workoutRepository.ts", via: "typed current-schema insertion and completed-set correction port", pattern: "addWarmup|copyPreviousWarmup|addWorkingSet|reviseCompleted" }
    - { from: "src/bootstrap/workoutAppRuntime.tsx", to: "src/domains/workout/setCommands.ts", via: "one authoritative runtime command for later UI use", pattern: "reviseCompleted|correctCompleted" }
---

<objective>
Repair active-workout set insertion on the current schema and add safe in-progress completed-set correction.

Purpose: Make the owner’s add/copy actions persist reliably and permit corrections without rewinding later workout state.
Output: Current-schema repository transactions, typed command/runtime wiring, and latest-manifest tests.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@src/platform/sqlite/migrations/0006_metric_profiles.ts
@src/platform/sqlite/repositories/workoutRepository.ts
@src/domains/workout/activeWorkout.ts
@src/domains/workout/setCommands.ts
@src/bootstrap/workoutAppRuntime.tsx
@tests/integration/complete-set.test.ts
</context>

## Artifacts this phase produces

- Current-schema Add warm-up, Copy warm-up, and Add working set repository paths.
- A selected-set, revision-checked correction command for completed working sets in active workouts.
- Full-manifest integration coverage that replaces the migration-0001-only false proof.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-07, LIB-08 | Active execution of cloned/user-owned plan targets | COVERED |
| REQ | LIB-11, LIB-12 | Complete versioned metric identity for inserted/corrected observations | COVERED |
| CONTEXT | D-63, D-64 | In-progress correction and current-schema add/copy | COVERED |
| VERIFICATION | G-02-05 and domain half of G-02-08 | Confirmed schema bug and missing correction command | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Reproduce and repair all current-schema set insertion paths</name>
  <read_first>src/platform/sqlite/migrations/index.ts, src/platform/sqlite/migrations/0006_metric_profiles.ts, src/platform/sqlite/repositories/workoutRepository.ts, tests/integration/complete-set.test.ts</read_first>
  <files>src/platform/sqlite/repositories/workoutRepository.ts, tests/integration/complete-set.test.ts</files>
  <action>First update the integration fixture to migrate through the current manifest and prove Add warm-up, Copy warm-up, and Add working set fail under the existing INSERTs. Repair each transaction so the inserted row carries the exercise snapshot's exact metric profile, contract version, generation, and compatible owned target/observation fields. Make Add Working's SELECT return every identity field it consumes. Preserve serialized `BEGIN IMMEDIATE`, deterministic ordinal assignment, idempotency, active-session/exercise status checks, rollback, and commit-before-return. Test all supported profile families, missing/incompatible source, stale revisions, duplicate requests, and injected SQL failure. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>All three operations persist and reload an inserted row under the latest schema with complete identity; every failure rolls back without partial rows.</acceptance_criteria>
  <verify><automated>npm run test:integration -- --runInBand tests/integration/complete-set.test.ts</automated></verify>
  <done>The physical-device add/copy regression has a current-schema fix and proof.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Add a narrow revision-checked completed-set correction transaction</name>
  <read_first>src/domains/workout/activeWorkout.ts, src/domains/workout/setCommands.ts, src/domains/workout/setCommands.test.ts, src/platform/sqlite/repositories/workoutRepository.ts, tests/integration/complete-set.test.ts</read_first>
  <files>src/domains/workout/activeWorkout.ts, src/domains/workout/setCommands.ts, src/domains/workout/setCommands.test.ts, src/domains/workout/index.ts, src/platform/sqlite/repositories/workoutRepository.ts, tests/integration/complete-set.test.ts</files>
  <action>Define a typed command for revising one completed working set, carrying session ID/revision, set ID/revision, validated replacement observation, and deterministic request identity. Inside one serialized transaction require session status `in_progress`, set kind working, set status completed, exact session/set revisions, and exact metric identity. Update only that set plus narrowly required record/progression invalidation derivatives; do not restore a snapshot, change completion order, reopen later sets/exercises, move active pointers, or replace current rest state/revision. Return committed view/conflict outcome only after commit. Cover stale session/set revisions, replay, finalized/partial/discarded status, warm-up/skipped/active targets, invalid/incompatible observations, rollback, and preservation of later progress/pointer/rest. Keep historical correction, audit ledger, void/remove/restore, and broad projection rebuild in Phase 3. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>A completed working set can be revised at any later time during the same active workout, while every unrelated source fact remains byte-for-byte or value-equivalent unchanged.</acceptance_criteria>
  <verify><automated>npm run test:unit -- --runInBand src/domains/workout/setCommands.test.ts &amp;&amp; npm run test:integration -- --runInBand tests/integration/complete-set.test.ts</automated></verify>
  <done>The backend supports unlimited in-progress completed-set correction without whole-session rollback.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Expose committed add/copy/correction outcomes through the runtime</name>
  <read_first>src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutAppRuntime.test.tsx, src/domains/workout/setCommands.ts, src/domains/workout/index.ts</read_first>
  <files>src/bootstrap/workoutAppRuntime.tsx, src/bootstrap/workoutAppRuntime.test.tsx</files>
  <action>Expose the new correction command and preserve inserted-row identity/results for Add warm-up, Copy warm-up, and Add working set so Plan 02-28 can focus/reveal the committed row. Propagate typed validation/conflict/status failures instead of swallowing them. Keep this task headless: do not edit ActiveWorkoutScreen or SetRow. Add runtime tests for forwarding, committed result refresh, retryable versus non-retryable mapping, and rejection propagation. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The UI has one authoritative runtime path for all four mutations and enough committed identity to render retry/focus behavior.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:unit -- --runInBand src/domains/workout/setCommands.test.ts src/bootstrap/workoutAppRuntime.test.tsx &amp;&amp; npm run test:integration -- --runInBand tests/integration/complete-set.test.ts</automated></verify>
  <done>Backend/runtime contracts are ready for the Active Workout UI convergence plan.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Lost-update tampering | UI revision to SQLite row | Compare session and set revisions inside the write transaction. |
| Metric identity corruption | Snapshot identity to inserted/corrected set | Persist all three identity columns and validate observation compatibility. |
| Privilege/state bypass | Correction command to finalized session | Authoritative in-transaction `in_progress` check. |
| Excessive mutation | Selected-set edit to whole workout | Narrow SQL update and invariance tests for later progress, pointer, and rest. |
| Information disclosure | Mutation error/logging | Typed bounded codes only; no raw observation values in logs. |
</threat_model>

<verification>Run unit/runtime tests, current-manifest integration tests, typecheck, and lint. Plan 02-34 owns fresh APK evidence after all source changes.</verification>
<success_criteria>G-02-05 and the domain portion of G-02-08 close with complete identity and no time-limited or whole-session correction mechanism.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-27-SUMMARY.md` when complete.</output>
