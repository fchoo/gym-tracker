---
phase: 02-owned-library-and-planning
plan: 24
type: execute
wave: 14
depends_on: [02-23]
files_modified:
  - src/ui/components/SemanticNumberField.tsx
  - src/ui/components/TimeDurationField.tsx
  - src/ui/components/SemanticInputFields.test.tsx
  - src/ui/components/PlanEditorFields.tsx
  - src/ui/components/SetRow.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - src/ui/screens/MetricMigrationScreen.tsx
  - src/ui/screens/ExerciseEditorScreen.tsx
  - src/ui/__tests__/CustomExerciseScreens.test.tsx
  - src/ui/__tests__/OwnedPlanEditor.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-05, LIB-08, LIB-11, LIB-12]
estimate: { tokens: 19000, raw_tokens: 19000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "The time/number half of D-57 and G-02-02 closes: durations and time-like values use an accessible time-style control, while number-only values request integer or decimal keypads appropriate to their metric."
    - "Canonical target/observation values, blank-versus-zero meaning, precision, metric-profile validation, and save payloads remain unchanged."
    - "The controls remain usable at 200% text with keyboard/D-pad activation, visible validation, deterministic focus restoration, and minimum 48dp targets."
    - "No package is installed and no date-control work from Plan 02-23 is duplicated."
  artifacts:
    - { path: "src/ui/components/TimeDurationField.tsx", provides: "Reusable time-style duration selector" }
    - { path: "src/ui/components/SemanticNumberField.tsx", provides: "Integer/decimal semantic numeric field" }
    - { path: "src/ui/components/SetRow.tsx", provides: "Metric-appropriate active-workout numeric input behavior" }
  key_links:
    - { from: "src/ui/components/TimeDurationField.tsx", to: "src/ui/screens/OwnedPlanEditorScreen.tsx", via: "canonical seconds/milliseconds values without raw duration typing", pattern: "duration|seconds|milliseconds" }
    - { from: "src/ui/components/SemanticNumberField.tsx", to: "src/ui/components/SetRow.tsx", via: "profile-specific integer/decimal keypad and validation semantics", pattern: "number-pad|decimal-pad|keyboardType" }
---

<objective>
Finish D-57 by replacing free-form duration entry and standardizing all number-only fields.

Purpose: Give planning and workout metrics controls that match their data type without changing domain meaning.
Output: Shared time/duration and semantic numeric primitives adopted by all remaining Phase 2 consumers.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@src/ui/components/PlanEditorFields.tsx
@src/ui/components/SetRow.tsx
@src/ui/screens/OwnedPlanEditorScreen.tsx
@src/ui/screens/MetricMigrationScreen.tsx
@src/ui/screens/ExerciseEditorScreen.tsx
</context>

## Artifacts this phase produces

- Shared time/duration and integer/decimal input primitives.
- Migrated owned-plan, metric-migration, custom-exercise, and active-set fields.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-05 | Custom-exercise default rest | COVERED |
| REQ | LIB-08 | Owned-plan target and rest editing | COVERED |
| REQ | LIB-11, LIB-12 | Metric-aware precision and observation shapes | COVERED |
| CONTEXT | D-57 | Time-style and number-only input semantics | COVERED |
| VERIFICATION | G-02-02 time/number scope | Raw duration and inconsistent keypad findings | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Build reusable time-style and semantic numeric primitives</name>
  <read_first>src/ui/components/PlanEditorFields.tsx, src/ui/components/SetRow.tsx, src/ui/theme/index.ts, src/ui/__tests__/foundation.test.tsx</read_first>
  <files>src/ui/components/SemanticNumberField.tsx, src/ui/components/TimeDurationField.tsx, src/ui/components/SemanticInputFields.test.tsx, src/ui/components/PlanEditorFields.tsx</files>
  <action>Start with failing tests. Implement a time-style duration selector with explicit hour/minute/second segments or equivalent bounded controls, plus a numeric field that declares integer versus decimal semantics and the matching keyboard type. Preserve canonical string values for drafts, explicit blank/zero distinctions, locale-independent decimal parsing, min/max/precision validation, accessibility labels, visible errors, keyboard/D-pad operation, modal focus restoration, 200% text reflow, and 48dp targets. Use installed dependencies only. Integrate through PlanEditorFields without importing domain policy into presentation. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Primitive tests cover zero, blank, max, invalid text, decimal precision, confirm/cancel, accessibility state, focus, and keyboard type.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/components/SemanticInputFields.test.tsx</automated></verify>
  <done>The shared controls express duration, integer, and decimal intent without changing stored contracts.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Migrate planning and exercise consumers</name>
  <read_first>src/ui/components/TimeDurationField.tsx, src/ui/components/SemanticNumberField.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/screens/MetricMigrationScreen.tsx, src/ui/screens/ExerciseEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/ui/__tests__/CustomExerciseScreens.test.tsx</read_first>
  <files>src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/screens/MetricMigrationScreen.tsx, src/ui/screens/ExerciseEditorScreen.tsx, src/ui/__tests__/CustomExerciseScreens.test.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</files>
  <action>Replace duration/rest fields with the time-style control and all count/load/distance/round fields with the integer or decimal primitive dictated by the metric contract. Preserve existing labels, defaults, units, target conversion, blank/zero behavior, migration preview, validation, and command payloads. Add consumer tests for every metric-profile field family and custom default rest. Commit only these files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Planning screens no longer use free-form duration input, and every number-only input exposes the correct keypad and unchanged canonical payload.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx src/ui/__tests__/CustomExerciseScreens.test.tsx</automated></verify>
  <done>Owned-plan, migration, and custom-exercise values use semantic controls.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 3: Normalize active-set number entry and close the source audit</name>
  <read_first>src/ui/components/SemanticNumberField.tsx, src/ui/components/TimeDurationField.tsx, src/ui/components/SetRow.tsx, src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx</read_first>
  <files>src/ui/components/SetRow.tsx, src/ui/components/SemanticInputFields.test.tsx</files>
  <action>Adopt semantic numeric fields for all SetRow load, reps, assistance, distance, duration, and rounds observations. Use the time-style control only where the value is a duration and doing so preserves the one-tap active-workout path; keep immediate numeric editing where the existing metric interaction requires it, but enforce the correct numeric/decimal keypad. Preserve target seeding, autosave, validation, and profile-specific observation shape. Audit all in-scope number-only TextInputs and document any intentional text field. Commit task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>Every active-set numeric field uses appropriate input semantics, and the audit finds no generic keyboard on number-only values.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/components/SemanticInputFields.test.tsx src/ui/__tests__/ActiveWorkoutMetricProfiles.test.tsx src/ui/__tests__/OwnedPlanEditor.test.tsx src/ui/__tests__/CustomExerciseScreens.test.tsx</automated></verify>
  <done>The time/number portion of G-02-02 is closed across planning and active-workout input.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Numeric input tampering | Text/control value to metric parser | Bounded locale-independent parsing and existing domain validation. |
| Precision corruption | Decimal display to integer storage | Explicit unit conversion and profile-specific round-trip tests. |
| State loss | Picker dismissal or keyboard transition | Draft-owned values and deterministic focus restoration. |
| Accessibility denial | Compact segmented controls cannot be operated | Semantic labels/states, keyboard/D-pad support, 200% text, and 48dp targets. |
</threat_model>

<verification>Run focused primitive/consumer suites, typecheck, lint, and a source audit for generic keyboards and free-form durations. Plan 02-34 owns fresh APK evidence.</verification>
<success_criteria>D-57/G-02-02 is closed for time, duration, and numeric inputs without package or domain-contract changes.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-24-SUMMARY.md` when complete.</output>
