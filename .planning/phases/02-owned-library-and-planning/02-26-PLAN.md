---
phase: 02-owned-library-and-planning
plan: 26
type: execute
wave: 14
depends_on: [02-25]
files_modified:
  - src/ui/screens/RootScreens.tsx
  - src/ui/screens/TodayScreen.tsx
  - src/ui/screens/ActiveWorkoutScreen.tsx
  - src/ui/screens/ExerciseDetailScreen.tsx
  - src/ui/screens/StarterPlanDetailScreen.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - src/ui/screens/WorkoutCompletionScreen.tsx
  - src/ui/__tests__/TodayScreen.test.tsx
  - src/ui/__tests__/ActiveWorkoutScreen.test.tsx
  - src/ui/__tests__/WorkoutCompletionScreen.test.tsx
autonomous: true
gap_closure: true
requirements: [LIB-05, LIB-06, LIB-07, LIB-08, LIB-10, LIB-11, LIB-12]
estimate: { tokens: 18000, raw_tokens: 18000, tasks: 3, confidence: medium }
must_haves:
  truths:
    - "D-56 is app-wide rather than Library-only: root, Today, Active Workout, detail, editor, and completion content groups use the shared grey-canvas/near-black flat-card system."
    - "Cards remain flat and independently scannable; no card is nested inside another card, and inputs, sheets, dialogs, navigation chrome, and transient notices retain their appropriate semantic surfaces."
    - "Existing behavior, copy, accessibility roles, hierarchy, selection, state restoration, compact/medium/expanded layouts, 200% text, and System/Light/Dark semantics do not change."
    - "This plan changes visual containment only; Plans 02-28 and 02-29 own workout actions, status glyphs, sticky identity, Today's plan navigation, and rest behavior."
  artifacts:
    - { path: "src/ui/screens/RootScreens.tsx", provides: "Global root-surface adoption without black navigation or form fields" }
    - { path: "src/ui/screens/ActiveWorkoutScreen.tsx", provides: "Shared content-card presentation ready for later workout behavior changes" }
    - { path: "src/ui/screens/WorkoutCompletionScreen.tsx", provides: "High-contrast flat completion summaries" }
  key_links:
    - { from: "src/ui/components/index.ts", to: "src/ui/screens/TodayScreen.tsx", via: "shared ContentCard and semantic canvas roles", pattern: "ContentCard|canvas|card" }
    - { from: "src/ui/theme/index.ts", to: "src/ui/screens/ActiveWorkoutScreen.tsx", via: "appearance-resolved high-contrast card tokens", pattern: "card|textOnCard" }
---

<objective>
Complete app-wide adoption of the approved neutral-grey canvas and near-black content cards.

Purpose: Ensure D-56 applies consistently beyond Library while preserving the distinction between cards and interactive chrome.
Output: Migrated root, Today, workout, detail, editor, and completion content surfaces with focused regressions.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@AGENTS.md
@.trae/rules/rules.md
@DESIGN.md
@.planning/REQUIREMENTS.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-UI-SPEC.md
@.planning/phases/02-owned-library-and-planning/02-VERIFICATION.md
@.planning/phases/02-owned-library-and-planning/02-25-SUMMARY.md
@src/ui/components/index.ts
@src/ui/theme/index.ts
</context>

## Artifacts this phase produces

- Global card-system adoption on every owner-facing content surface identified by Plan 02-25.
- Structural tests that reject nested cards and inappropriate black treatment on inputs/sheets/navigation.

## Source Coverage Audit

| Source | IDs / feature | Plan coverage | Status |
|---|---|---|---|
| REQ | LIB-05..08, LIB-10..12 | Custom/starter/owned-plan/metric content surfaces outside Library | COVERED |
| CONTEXT | D-56 | App-wide neutral canvas and near-black flat cards | COVERED |
| VERIFICATION | G-02-03 | Closes the checker finding that Library-only scope was insufficient | COVERED |

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Adopt global cards in root, Today, and detail surfaces</name>
  <read_first>src/ui/screens/RootScreens.tsx, src/ui/screens/TodayScreen.tsx, src/ui/screens/ExerciseDetailScreen.tsx, src/ui/screens/StarterPlanDetailScreen.tsx, src/ui/components/index.ts, src/ui/theme/index.ts, src/ui/__tests__/TodayScreen.test.tsx</read_first>
  <files>src/ui/screens/RootScreens.tsx, src/ui/screens/TodayScreen.tsx, src/ui/screens/ExerciseDetailScreen.tsx, src/ui/screens/StarterPlanDetailScreen.tsx, src/ui/__tests__/TodayScreen.test.tsx</files>
  <action>Use the shared ContentCard for bounded content groups on root, Today, exercise detail, and starter-plan detail. Keep the screen canvas grey; keep navigation, search, form fields, sheets, dialogs, and notices on their designated semantic surfaces. Do not nest ContentCard or duplicate a card boundary around an existing card row. Preserve all copy, query state, actions, data ordering, route semantics, empty/loading/error/partial/long-text behavior, and adaptive layout. Add focused assertions for canvas/card roles, non-nesting, high-contrast content, and 200% text. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>All bounded content groups on these routes use the shared card role, while non-card controls remain visually and semantically distinct.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/TodayScreen.test.tsx src/ui/__tests__/foundation.test.tsx</automated></verify>
  <done>Root, Today, and detail surfaces follow the app-wide card system.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Adopt global cards in workout, editor, and completion surfaces</name>
  <read_first>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/screens/WorkoutCompletionScreen.tsx, src/ui/components/index.ts, src/ui/theme/index.ts, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/WorkoutCompletionScreen.test.tsx</read_first>
  <files>src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/screens/WorkoutCompletionScreen.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/WorkoutCompletionScreen.test.tsx</files>
  <action>Migrate existing bounded workout, plan-editor, and completion summaries to the shared flat ContentCard while preserving current behavior. Do not implement sticky headers, Today's plan, completed-set editing, new glyph actions, or RestDock changes here. Keep editable fields and sheets visually distinct and never put a card around another card. Add representative tests for populated/error/partial/long-content states, appearance modes, and layout scaling. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The three convergence surfaces use the global card roles without behavior changes or card nesting.</acceptance_criteria>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ActiveWorkoutScreen.test.tsx src/ui/__tests__/WorkoutCompletionScreen.test.tsx src/ui/__tests__/OwnedPlanEditor.test.tsx</automated></verify>
  <done>Workout, editor, and completion content matches the global surface system.</done>
</task>

<task type="auto">
  <name>Task 3: Close the app-wide surface inventory</name>
  <read_first>DESIGN.md, src/ui/theme/index.ts, src/ui/components/index.ts, src/ui/screens/RootScreens.tsx, src/ui/screens/TodayScreen.tsx, src/ui/screens/ActiveWorkoutScreen.tsx, src/ui/screens/ExerciseDetailScreen.tsx, src/ui/screens/StarterPlanDetailScreen.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/screens/WorkoutCompletionScreen.tsx</read_first>
  <files>src/ui/__tests__/TodayScreen.test.tsx, src/ui/__tests__/ActiveWorkoutScreen.test.tsx, src/ui/__tests__/WorkoutCompletionScreen.test.tsx</files>
  <action>Audit all owner-facing screens against the Plan 02-25 inventory. Add assertions that every intended content-card surface resolves through the global token/primitive and that inputs, sheets, dialogs, navigation, and notices do not. Verify high-contrast text, non-color state semantics, visible focus, 48dp actions, compact/medium/expanded/landscape behavior, 200% text, and reduced motion. Record any intentionally non-card surface in the summary with its semantic reason; do not silently omit a screen. Commit only task files with the exact trailer `Co-authored-by: TRAE CLI <noreply@users.noreply.github.com>`.</action>
  <acceptance_criteria>The inventory is complete, all intended screens are migrated, and every exception is semantically justified rather than accidental.</acceptance_criteria>
  <verify><automated>npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:components -- --runInBand src/ui/__tests__/foundation.test.tsx src/ui/__tests__/LibraryScreen.test.tsx src/ui/__tests__/TodayScreen.test.tsx src/ui/__tests__/ActiveWorkoutScreen.test.tsx src/ui/__tests__/WorkoutCompletionScreen.test.tsx</automated></verify>
  <done>D-56 is implemented as an app-wide default rather than a Library-only theme.</done>
</task>

</tasks>

<threat_model>
## OWASP ASVS L1 / STRIDE

| Threat | Boundary | Mitigation |
|---|---|---|
| Authority spoofing | Card appearance implies false status/ownership | Preserve explicit textual source and state labels. |
| Interaction tampering | Visual migration rewires commands | Keep callbacks/test IDs and behavior assertions unchanged. |
| Information hiding | Near-black card loses contrast or state | Appearance-mode token tests plus non-color semantics. |
| Denial of use | Nested cards or fixed height clips content | No-nesting assertions and adaptive/200% text coverage. |
</threat_model>

<verification>Run focused component suites, typecheck, lint, and the explicit app-wide inventory. Plan 02-34 owns the new exact-HEAD APK and device evidence.</verification>
<success_criteria>D-56/G-02-03 is visibly consistent across all app content surfaces without converting controls/chrome into cards.</success_criteria>
<output>Create `.planning/phases/02-owned-library-and-planning/02-26-SUMMARY.md` when complete.</output>
