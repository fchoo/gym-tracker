---
phase: 02-owned-library-and-planning
plan: 16
type: execute
wave: 7
depends_on: [02-03, 02-08, 02-11]
files_modified:
  - src/domains/scheduling/scheduleCommands.test.ts
  - src/domains/scheduling/scheduleCommands.ts
  - src/platform/sqlite/repositories/scheduleRepository.ts
  - tests/integration/schedule-commands.test.ts
autonomous: true
requirements: [LIB-09]
estimate:
  tokens: 32000
  raw_tokens: 32000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "D-40/D-41 initial and later schedules commit explicit complete versions with before/after/effective LocalDate and never rewrite earlier opportunities/sessions/history."
    - "D-42/D-43 Repeat, Skip, Advance, scheduled completion, override/rest/empty/alternate training, and explicit `Advance rotation after this workout` persist exact events/pointer effects."
    - "D-44 through D-49 pending/consumed overrides, Weekday skip/missed, start LocalDate, DST/stored zone, and prospective timezone choice are immutable/revision-safe."
    - "E-53 through E-58 integration cases prove unique dates/bindings, empty state, Unicode zone validation, stable ordering, request idempotency, and concurrent expected-revision conflict."
  artifacts:
    - path: "src/domains/scheduling/scheduleCommands.ts"
      provides: "Complete schedule version/override/pointer/opportunity command orchestration"
    - path: "src/platform/sqlite/repositories/scheduleRepository.ts"
      provides: "Atomic effective-dated schedule persistence and effective opportunity reads"
  key_links:
    - from: "src/domains/scheduling/scheduleCommands.ts"
      to: "src/domains/scheduling/scheduleState.ts"
      via: "pure transition result becomes one repository transaction"
      pattern: "transition"
    - from: "src/platform/sqlite/repositories/scheduleRepository.ts"
      to: "src/platform/sqlite/migrations/0008_schedule_activation.ts"
      via: "versions, bindings, overrides, opportunities, pointers, and events"
      pattern: "schedule_versions|schedule_events"
---

<objective>
Persist the complete schedule command contract over the shared 0008 source schema.

Purpose: Close LIB-09 domain/repository behavior before Today and schedule UI consume it.
Output: Command orchestration, schedule repository, pure command tests, and integration rollback/concurrency proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-03-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-08-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-11-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@src/domains/scheduling/scheduleState.ts
@src/platform/sqlite/migrations/0008_schedule_activation.ts
@src/platform/sqlite/serializedWriter.ts
</context>

## Artifacts this phase produces

- `src/domains/scheduling/scheduleCommands.ts` — `saveScheduleVersion`, `setDateOverride`, `repeatRotation`, `skipOpportunity`, `advanceRotation`, `changeScheduleTimeZone`.
- `src/domains/scheduling/scheduleCommands.test.ts` — D-40..D-49 orchestration and E-53..E-58 tables.
- `src/platform/sqlite/repositories/scheduleRepository.ts` — effective opportunity reads and atomic event/version commands.
- `tests/integration/schedule-commands.test.ts` — rollback, idempotency, concurrency, and immutable-history proof.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Save one prospective schedule version and read its effective opportunity</name>
  <read_first>src/domains/scheduling/scheduleState.ts, src/domains/scheduling/localDate.ts, src/platform/sqlite/migrations/0008_schedule_activation.ts, src/platform/sqlite/serializedWriter.ts, .planning/phases/02-owned-library-and-planning/02-CONTEXT.md</read_first>
  <files>src/domains/scheduling/scheduleCommands.ts, src/domains/scheduling/scheduleCommands.test.ts, src/platform/sqlite/repositories/scheduleRepository.ts, tests/integration/schedule-commands.test.ts</files>
  <action>Start red with a valid owned plan saving one complete Weekday or Rotation version effective today, then read the effective opportunity from stored timezone and bindings. Implement D-40/D-41 input validation, before/after preview token, expected schedule/plan revisions, complete binding replacement in a new version, and exact invalidations after commit. Never update an existing schedule version in place. Cover empty/one binding, Unicode IANA zone, stable ordinal/date ordering, repeated request identity, stale concurrent save, rollback, and preservation of prior opportunities/sessions.</action>
  <acceptance_criteria>A prospective version becomes effective on its LocalDate, prior versions remain readable/unchanged, and stale or failed saves create no partial version/bindings/events.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/schedule-commands.test.ts -t "schedule version"</automated>
  </verify>
  <done>The narrowest edit-to-effective-opportunity persistence path is complete and immutable-history-safe.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Complete override, opportunity, rotation, missed-day, and timezone commands</name>
  <read_first>src/domains/scheduling/scheduleCommands.ts, src/domains/scheduling/scheduleCommands.test.ts, src/platform/sqlite/repositories/scheduleRepository.ts, tests/integration/schedule-commands.test.ts, src/domains/scheduling/scheduleState.ts</read_first>
  <files>src/domains/scheduling/scheduleCommands.ts, src/domains/scheduling/scheduleCommands.test.ts, src/platform/sqlite/repositories/scheduleRepository.ts, tests/integration/schedule-commands.test.ts</files>
  <action>Add D-42..D-49 command tables and persistence: scheduled Rotation completion advances; Repeat holds; Skip records skipped opportunity and advances; Advance emits event/pointer only; alternate/rest/empty uses explicit advance intent only; pending override can be confirmed-replaced; consumed override is immutable; Weekday skip is date-only; missed is `Planned but not completed`; start LocalDate remains session fact; stored timezone controls intent; timezone change records prospective version after exact owner choice. Persist event plus opportunity/override/pointer/version changes in one FIFO transaction with expected revisions and idempotent request IDs. Return typed conflicts and exact invalidations, never broad refresh.</action>
  <acceptance_criteria>Every transition persists exactly the pure state result, repeated/stale commands cannot duplicate facts, and no command rewrites consumed overrides, prior opportunities, sessions, or history.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/schedule-commands.test.ts</automated>
  </verify>
  <done>The full schedule repository contract is complete and ready for Today/editor integration.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Owner schedule command → effective-dated facts | Stale intent must not rewrite or duplicate calendar history. |
| Workout outcome → rotation pointer | Only eligible scheduled completion or explicit intent may advance. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-03 | Tampering | owned plan/schedule authority | high | mitigate | Owned-plan predicate and shared source schema constraints. |
| T-02-04 | Tampering | expected revisions/request IDs | high | mitigate | One serialized event+state transaction and stale conflicts. |
| T-02-05 | Tampering | silent advancement/timezone history | high | mitigate | Pure transition tables, append-only facts, prospective versions. |
| T-02-07 | Information Disclosure | schedule diagnostics | medium | mitigate | Safe codes/dates/revisions only. |
</threat_model>

<verification>
Run focused tracer and complete integration suites; verify every D-40..D-49 and E-53..E-58 case.
</verification>

<success_criteria>
All schedule mutations are explicit, prospective, append-only where historical, atomic, idempotent, and timezone-safe.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-16-SUMMARY.md` when done.
</output>
