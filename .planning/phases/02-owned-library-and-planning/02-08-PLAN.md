---
phase: 02-owned-library-and-planning
plan: 08
type: execute
wave: 5
depends_on: [02-03, 02-04, 02-05, 02-07]
files_modified:
  - src/domains/plans/activateStarterPlan.test.ts
  - src/domains/plans/activateStarterPlan.ts
  - src/domains/scheduling/activation.ts
  - src/platform/sqlite/migrations/0008_schedule_activation.ts
  - src/platform/sqlite/repositories/starterPlanRepository.ts
  - tests/integration/starter-activation-repository.test.ts
  - tests/migrations/fixtures/v6-metric-profiles.sql
  - tests/sqlite-host/schedule-activation.test.ts
autonomous: true
requirements: [LIB-07, LIB-09]
estimate:
  tokens: 38000
  raw_tokens: 38000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Migration 0008 creates the shared authoritative activation/schedule source schema consumed by starter activation, owned plans, and later schedule commands."
    - "D-01 through D-05 and D-40 are domain/repository prerequisites: one active schedule, explicit existing-copy choice, preserved inactive prior schedule, active-workout block, editable start date/mode/bindings, and confirmation-before-commit."
    - "Activation clones the complete accepted template graph into fresh user-owned IDs with source attribution and immutable independence; repeated identity/revision commands are idempotent or typed conflicts."
    - "D-55 `Gym Body-Part Split` clones all five ordered weekday days and all 20 weighted equipment-first occurrences through the same complete graph path, with no bodyweight occurrence, substitution, template mutation, or silent omission."
    - "E-42 through E-46 and E-53 through E-58 are persisted with source authority, FIFO writes, expected revisions, and no pre-commit acknowledgement."
  artifacts:
    - path: "src/platform/sqlite/migrations/0008_schedule_activation.ts"
      provides: "Shared schedule/version/binding/override/opportunity/event and starter-copy schema"
    - path: "src/domains/plans/activateStarterPlan.ts"
      provides: "Explicit clone/reactivate/switch activation command"
    - path: "src/platform/sqlite/repositories/starterPlanRepository.ts"
      provides: "Accepted starter reads and atomic independent-copy activation"
  key_links:
    - from: "assets/content/starter-plans.v2.json"
      to: "src/platform/sqlite/repositories/starterPlanRepository.ts"
      via: "accepted starter hash validation and full graph clone"
      pattern: "starterSourceRevision"
    - from: "src/domains/plans/activateStarterPlan.ts"
      to: "src/domains/scheduling/activation.ts"
      via: "validated existing-copy choice and initial schedule preview"
      pattern: "activate|reactivate"
---

<objective>
Create the shared activation/schedule source schema and repository prerequisite for accepted starters.

Purpose: Give LIB-07/LIB-09 one authoritative transaction model before any activation or schedule UI exists.
Output: Migration 0008, activation/schedule contracts, clone/reactivate repository, retained fixture, and atomic integration proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-03-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-04-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-05-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@assets/content/starter-plans.v2.json
@artifacts/review/phase2/starter-plans-acceptance.json
@src/domains/plans/activateStarterPlan.ts
@src/platform/sqlite/migrations/index.ts
</context>

## Artifacts this phase produces

- `src/domains/scheduling/activation.ts` — initial schedule/copy-choice/active-workout validation contracts.
- `src/platform/sqlite/migrations/0008_schedule_activation.ts` — shared activation and schedule source schema.
- `tests/migrations/fixtures/v6-metric-profiles.sql` — immutable post-0006 fixture; version 7 is intentionally unused and migration ordering remains explicit.
- `tests/sqlite-host/schedule-activation.test.ts` — schema, uniqueness, retained migration, and source-authority proof.
- `src/domains/plans/activateStarterPlan.ts` — `activateStarterPlan` orchestration contract.
- `src/domains/plans/activateStarterPlan.test.ts` — choice/preview/block validation.
- `src/platform/sqlite/repositories/starterPlanRepository.ts` — full graph clone/reactivation.
- `tests/integration/starter-activation-repository.test.ts` — rollback/idempotency/prior-schedule preservation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Clone one accepted starter day into an inactive-owned graph with one active schedule</name>
  <read_first>assets/content/starter-plans.v2.json, artifacts/review/phase2/starter-plans-acceptance.json, src/domains/scheduling/scheduleState.ts, src/domains/plans/activateStarterPlan.ts, src/platform/sqlite/migrations/index.ts</read_first>
  <files>src/domains/scheduling/activation.ts, src/platform/sqlite/migrations/0008_schedule_activation.ts, tests/migrations/fixtures/v6-metric-profiles.sql, tests/sqlite-host/schedule-activation.test.ts</files>
  <action>Start with a retained-v6 host test that creates source tables for one accepted starter, copies one day/target/policy into fresh owned IDs, and enforces one globally active schedule. Export forward migration 0008 as the direct successor of 0006 (version 7 intentionally unused) for host/native contract setup; Plan 02-11 owns the final ordered runtime manifest after every Phase 2 migration module exists. Create effective-dated schedule versions, Weekday/Rotation bindings, active/inactive lifecycle, rotation pointer, overrides, opportunities, and append-only events plus source-template attribution. Add partial unique enforcement for exactly one active schedule and constraints for pending/consumed facts. Define validation for D-01/D-03/D-05/D-40 initial activation input and E-42..E-46/E-53..E-58 empty/ordering/idempotency/concurrency cases.</action>
  <acceptance_criteria>Retained databases migrate; one accepted day can be copied under fresh owned IDs; no database state permits two active schedules or mutable consumed facts.</acceptance_criteria>
  <verify>
    <automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/schedule-activation.test.ts</automated>
  </verify>
  <done>The thinnest accepted-template-to-owned-schedule source path is proven with the shared schema.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Implement complete clone/reactivate/switch activation repository behavior</name>
  <read_first>src/domains/scheduling/activation.ts, src/platform/sqlite/migrations/0008_schedule_activation.ts, tests/sqlite-host/schedule-activation.test.ts, src/domains/plans/activateStarterPlan.ts, src/platform/sqlite/serializedWriter.ts</read_first>
  <files>src/domains/plans/activateStarterPlan.ts, src/domains/plans/activateStarterPlan.test.ts, src/platform/sqlite/repositories/starterPlanRepository.ts, tests/integration/starter-activation-repository.test.ts</files>
  <action>Write orchestration tests first for D-01 through D-05, D-40, and D-55. Require exact accepted starter revision, editable start LocalDate, explicit Weekday/Rotation bindings, confirmation token, and when copies exist an explicit `reactivate_existing` or `create_another` choice. Reject switching while an active workout exists. In one FIFO transaction clone all days/exercises/targets/warm-ups/rest/policies/schedule defaults/source attribution into fresh IDs or reactivate the chosen copy; deactivate but preserve the prior owned plan/schedule. Return committed read model and exact invalidations only after commit. Test every one of the six accepted templates, including all five ordered `Gym Body-Part Split` days and 20 weighted occurrences, plus repeated request identity, stale revisions, fresh IDs, no partial graph, and prior schedule preservation.</action>
  <acceptance_criteria>Activation never silently chooses, resets, archives, omits a D-55 day/occurrence, or mutates templates; all graph/schedule changes commit atomically and active-workout switching is blocked.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/starter-activation-repository.test.ts</automated>
  </verify>
  <done>Accepted starter activation is a complete independent-copy repository capability with no UI dependency.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted template asset → owned graph | Bundled source must clone completely without remaining mutable linkage. |
| Activation intent → global active schedule | Stale/repeated intent could create multiple active schedules or partial copies. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | accepted starter input | high | mitigate | Verify acceptance hash and all references before transaction. |
| T-02-03 | Tampering | bundled/owned boundaries | high | mitigate | Fresh owned IDs, source attribution, origin predicates, no template writes. |
| T-02-04 | Tampering | activation/schedule revisions | high | mitigate | Expected revisions, request identity, partial unique index, one transaction. |
| T-02-05 | Tampering | silent schedule mutation | high | mitigate | Explicit copy choice/mode/bindings and immutable events/opportunities. |
</threat_model>

<verification>
Run the focused migration and integration suites; verify fresh IDs, exact-one-active enforcement, and complete rollback.
</verification>

<success_criteria>
Starter activation and schedule source facts are authoritative, independent, revision-safe, and UI-ready.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-08-SUMMARY.md` when done.
</output>
