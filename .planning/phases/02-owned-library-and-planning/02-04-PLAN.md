---
phase: 02-owned-library-and-planning
plan: 04
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - src/domains/content/catalog.ts
  - src/domains/content/catalog.test.ts
  - src/platform/sqlite/migrations/0004_content_library.ts
  - src/platform/sqlite/repositories/contentRepository.ts
  - tests/integration/content-import.test.ts
  - tests/migrations/fixtures/v2-phase1.sql
  - tests/migrations/fixtures/v3-phase1.sql
  - tests/sqlite-host/content-library.test.ts
autonomous: true
requirements: [LIB-02, LIB-10]
estimate:
  tokens: 39000
  raw_tokens: 39000
  tasks: 2
  confidence: low
must_haves:
  truths:
    - "Only the hash-accepted 02-01 catalog can import, and the narrowest path creates one bundled exercise plus aliases/taxonomy in one explicit transaction."
    - "Migration 0004 preserves all v2/v3 Phase 1 IDs and bytes while adding authoritative catalog, attribution, availability, taxonomy, and search-term source tables."
    - "D-50 validates the complete pack before atomic origin-scoped upsert; D-51 converts absent upstream bundled rows to Unavailable without deleting identity, attribution, plans, or snapshots."
    - "E-06 through E-13 and E-59 through E-63 remain enforced at the runtime schema/repository boundary."
  artifacts:
    - path: "src/domains/content/catalog.ts"
      provides: "Strict runtime catalog and content-update result contracts"
    - path: "src/platform/sqlite/migrations/0004_content_library.ts"
      provides: "Forward catalog/attribution/taxonomy/availability source schema"
    - path: "src/platform/sqlite/repositories/contentRepository.ts"
      provides: "Atomic accepted-pack import and bundled-only update repository"
  key_links:
    - from: "assets/content/exercise-library.v1.json"
      to: "src/domains/content/catalog.ts"
      via: "strict runtime parse plus manifest/acceptance hash verification"
      pattern: "parseExerciseCatalog"
    - from: "src/platform/sqlite/repositories/contentRepository.ts"
      to: "src/platform/sqlite/sqliteKernel.ts"
      via: "one private serialized write with explicit transaction ownership"
      pattern: "kernel.write"
---

<objective>
Import the accepted catalog into authoritative relational source tables without changing Phase 1 identities.

Purpose: Turn the accepted build-time artifact into LIB-02/LIB-10 runtime truth while preserving user-owned and historical rows.
Output: Runtime schemas, migration 0004, retained fixtures, repository import/update behavior, and host/integration proof.
</objective>

<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/02-owned-library-and-planning/02-01-SUMMARY.md
@.planning/phases/02-owned-library-and-planning/02-CONTEXT.md
@.planning/phases/02-owned-library-and-planning/02-RESEARCH.md
@src/domains/content/index.ts
@src/platform/sqlite/migrations/index.ts
@src/platform/sqlite/serializedWriter.ts
@tests/migrations/fixtures/v1-phase1.sql
</context>

## Artifacts this phase produces

- `src/domains/content/catalog.ts` — accepted catalog parser and `ContentUpdateResult`.
- `src/domains/content/catalog.test.ts` — runtime boundary and acceptance-hash tests.
- `src/platform/sqlite/migrations/0004_content_library.ts` — catalog source schema.
- `tests/migrations/fixtures/v2-phase1.sql` — retained user-version 2 database.
- `tests/migrations/fixtures/v3-phase1.sql` — retained user-version 3 database.
- `src/platform/sqlite/repositories/contentRepository.ts` — `importAcceptedCatalog` and update result.
- `tests/sqlite-host/content-library.test.ts` — migration and origin-authority matrix.
- `tests/integration/content-import.test.ts` — commit/rollback/idempotency/snapshot preservation.

<tasks>

<task type="tracer" tdd="true">
  <name>Task 1: Migrate and import one accepted bundled exercise end-to-end</name>
  <read_first>assets/content/exercise-library.v1.json, assets/content/exercise-library.v1.manifest.json, artifacts/review/phase2/exercise-library-acceptance.json, src/domains/content/index.ts, src/platform/sqlite/migrations/index.ts</read_first>
  <files>src/domains/content/catalog.ts, src/domains/content/catalog.test.ts, src/platform/sqlite/migrations/0004_content_library.ts, tests/sqlite-host/content-library.test.ts</files>
  <action>Start with a host test that migrates a retained Phase 1 database, parses the accepted asset/manifest/acceptance hashes, and inserts one bundled exercise with one alias and taxonomy link. Add and export forward-only migration 0004 for direct host and integration setup after 0003; never edit 0001-0003 and do not register 0004 in the runtime manifest here. Plan 02-11 is the sole writer of `src/platform/sqlite/migrations/index.ts` after every Phase 2 migration module exists. Create authoritative relational tables for source packs/revisions, exercises, aliases, taxonomy, attribution, availability, owner preferences, and normalized search terms while retaining existing rows and IDs. The parser must reject hash mismatch, unresolved review, unknown versions, invalid relations, and all E-06..E-13 runtime boundary cases before a write begins.</action>
  <acceptance_criteria>v0/v1/v2/v3 databases migrate to 0004, existing IDs and historical JSON bytes remain unchanged, and exactly one accepted bundled row is queryable with attribution and availability.</acceptance_criteria>
  <verify>
    <automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/content-library.test.ts -t "tracer"</automated>
  </verify>
  <done>One accepted catalog row travels through parse, migration, serialized import, and readback without identity drift.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Close atomic full import, bundled-only upgrades, retained fixtures, and runtime repository tests</name>
  <read_first>src/domains/content/catalog.ts, src/platform/sqlite/migrations/0004_content_library.ts, src/platform/sqlite/migrations/index.ts, src/platform/sqlite/serializedWriter.ts, tests/sqlite-host/content-library.test.ts</read_first>
  <files>src/platform/sqlite/repositories/contentRepository.ts, tests/integration/content-import.test.ts, tests/migrations/fixtures/v2-phase1.sql, tests/migrations/fixtures/v3-phase1.sql</files>
  <action>Implement a narrow repository port over the private kernel using direct 0004 setup in host/integration tests; runtime manifest registration remains exclusively owned by Plan 02-11. Per D-50, stage/validate every row and reference before one origin-scoped `BEGIN IMMEDIATE` write; return committed added/updated/newlyUnavailable counts and exact invalidation scopes only after commit. Per D-51, mark missing bundled rows Unavailable, preserve stable identity/attribution/references, and never auto-replace. Repeated same-revision import is idempotent; expected revision/hash mismatch is typed conflict; injected failures roll back source and derived search-term rows. Assert custom/copied rows, plan references, active snapshots, observations, and Phase 1 IDs are byte-stable, covering E-59..E-63.</action>
  <acceptance_criteria>The full accepted pack imports atomically, reruns without duplicates, update failure leaves all source rows unchanged, and bundled updates cannot mutate custom/copied/history facts.</acceptance_criteria>
  <verify>
    <automated>npm run test:integration -- --runInBand tests/integration/content-import.test.ts</automated>
  </verify>
  <done>Runtime content import and upgrades preserve authority, ownership, identity, and historical immutability.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|---|---|
| Accepted asset → runtime parser | Committed files still require hash/version validation before source mutation. |
| Bundled update → user-owned/history rows | Origin authority must prevent cross-owner changes. |

## STRIDE Threat Register

| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-02-01 | Tampering | catalog asset/import | high | mitigate | Acceptance hash gate, strict complete parse, and rollback injection tests. |
| T-02-03 | Tampering | ownership predicates | high | mitigate | Origin-scoped SQL and byte-preservation tests for custom/copied/history rows. |
| T-02-04 | Tampering | import revision | high | mitigate | Expected source revision/hash and idempotent typed conflict semantics. |
| T-02-07 | Information Disclosure | import diagnostics | medium | mitigate | Report codes/counts/hashes only, never raw instructions or workout data. |
</threat_model>

<verification>
Run the focused host tracer and integration import suite; inspect migration order and retained v2/v3 fixture assertions.
</verification>

<success_criteria>
The accepted catalog is authoritative, transactional, forward-migrated, and incapable of mutating user-owned or historical facts.
</success_criteria>

<output>
Create `.planning/phases/02-owned-library-and-planning/02-04-SUMMARY.md` when done.
</output>
