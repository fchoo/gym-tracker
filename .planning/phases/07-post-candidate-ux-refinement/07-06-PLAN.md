---
phase: 07-post-candidate-ux-refinement
plan: 06
type: execute
wave: 2
depends_on: [07-05]
files_modified:
  - src/ui/components/ScheduleBindingEditor.tsx
  - src/ui/screens/StarterActivationScreen.tsx
  - src/ui/__tests__/ScheduleEditor.test.tsx
  - src/ui/__tests__/StarterPlans.test.tsx
autonomous: true
requirements: [UX-20]
estimate: {tokens: 36000, raw_tokens: 36000, tasks: 2, confidence: low}
ui_considerations:
  - {id: UI-E03-SCHEDULE, category: error, element: save failure preserves schedule draft, verification: explicit, owner: 07-06 tests}
  - {id: UI-E04-SCHEDULE, category: populated, element: Weekday and Rotation lists, verification: explicit, owner: 07-06 tests}
  - {id: UI-E06-SCHEDULE, category: overflow, element: responsive schedule rows, verification: explicit, owner: 07-06 tests}
  - {id: UI-E07-SCHEDULE, category: zero-one-many, element: schedule bindings, verification: explicit, owner: 07-06 tests}
  - {id: UI-B01-DRAG, category: loading, element: held drag feedback, verification: backstop, owner: 07-10 native evidence}
must_haves:
  truths:
    - Per D-05, Weekday and Rotation use the 07-05 drag/accessibility contract in owned editing and starter activation.
    - Weekday radio meaning and Rotation semantics remain unchanged; movement affects draft order only.
    - Failed save/activation retains the draft and exact recovery copy; no visible Up/Down/Position returns.
  artifacts:
    - {path: src/ui/components/ScheduleBindingEditor.tsx, provides: owned Weekday and Rotation parity}
    - {path: src/ui/screens/StarterActivationScreen.tsx, provides: activation Weekday and Rotation parity}
  key_links:
    - {from: shared reorder row, to: existing schedule move, via: bounded draft ordinal normalization}
---
<objective>Complete D-05 schedule drag parity in owned-plan editing and starter activation.</objective>
<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-post-candidate-ux-refinement/07-CONTEXT.md
@.planning/phases/07-post-candidate-ux-refinement/07-UI-SPEC.md
@.planning/phases/07-post-candidate-ux-refinement/07-05-SUMMARY.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Apply drag parity to owned Weekday and Rotation schedules</name>
  <files>src/ui/components/ScheduleBindingEditor.tsx, src/ui/__tests__/ScheduleEditor.test.tsx</files>
  <read_first>src/ui/components/ScheduleBindingEditor.tsx, src/ui/__tests__/ScheduleEditor.test.tsx, src/ui/components/PlanEditorFields.tsx</read_first>
  <behavior>
    - Both modes share long-press, displacement, reduced motion, TalkBack, keyboard/D-pad, bounds, and normalization.
    - Weekday radios remain independent; failure preserves draft and exact retry state.
  </behavior>
  <action>RED first. Per D-05, route both modes through the 07-05 primitive. Preserve weekday choice, schedule copy, explicit save, and draft on exact failure. The locked UI-SPEC overrides the sub-audit that suggested leaving owned Weekday unchanged. Do not persist on movement or restore visible directional controls.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/ScheduleEditor.test.tsx &amp;&amp; npm run typecheck</automated></verify>
  <acceptance_criteria>Owned Weekday/Rotation parity, 200% reflow, alternatives, and failure retention pass.</acceptance_criteria>
  <done>Owned schedule UX-20 is complete.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Apply the same contract before starter activation</name>
  <files>src/ui/screens/StarterActivationScreen.tsx, src/ui/__tests__/StarterPlans.test.tsx</files>
  <read_first>src/ui/screens/StarterActivationScreen.tsx, src/ui/__tests__/StarterPlans.test.tsx, src/ui/components/PlanEditorFields.tsx</read_first>
  <behavior>
    - Both activation modes share the same movement routes and retain draft until confirmation.
    - Cancellation/failure preserves configuration; retry submits normalized draft once.
  </behavior>
  <action>RED first. Replace local directional presentation with the shared primitive for both modes, preserving move/ordinal/radio/confirmation behavior. Test bounds, rapid input, failed activation, retry, and 200% text; never write schedule facts before command success.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/StarterPlans.test.tsx src/ui/__tests__/ScheduleEditor.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
  <acceptance_criteria>Starter Weekday/Rotation parity and explicit confirmation pass.</acceptance_criteria>
  <done>Full schedule UX-20 is complete.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
| Artifact | Contract |
|---|---|
| ScheduleBindingEditor modes | Shared drag and assistive ordering |
| StarterActivation schedule rows | Draft-only reorder before confirmation |
</artifacts_this_phase_produces>
<commit_protocol>Atomic task commits must use disabled hooks helper and end exactly with `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`; verify via `git log -1 --format=%B`.</commit_protocol>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Input to schedule draft/save | Reorder must remain bounded and non-authoritative until save. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-SCHEDULE-ORDER | Tampering | binding order | high | mitigate | Stable IDs, normalized draft, explicit save/activation. |
| T-07-SCHEDULE-A11Y | Denial of Service | reorder controls | medium | mitigate | Shared touch/TalkBack/keyboard/D-pad routes. |
</threat_model>
<verification>Run both focused suites, typecheck, lint, boundaries, and git diff --check.</verification>
<success_criteria>Weekday and Rotation schedule ordering is accessible, responsive, and explicit-save only in both contexts.</success_criteria>
<output>Create `.planning/phases/07-post-candidate-ux-refinement/07-06-SUMMARY.md` when done.</output>
