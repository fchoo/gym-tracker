---
phase: 07-post-candidate-ux-refinement
plan: 05
type: execute
wave: 1
depends_on: []
files_modified:
  - src/ui/components/PlanEditorFields.tsx
  - src/ui/components/PlanDaySwitcher.tsx
  - src/ui/screens/OwnedPlanEditorScreen.tsx
  - src/ui/__tests__/PlanEditorFields.test.tsx
  - src/ui/__tests__/OwnedPlanEditor.test.tsx
autonomous: true
requirements: [UX-20]
estimate: {tokens: 40000, raw_tokens: 40000, tasks: 2, confidence: low}
ui_considerations:
  - {id: UI-E04-PLAN, category: populated, element: day switcher and plan rows, verification: explicit, owner: 07-05 editor tests}
  - {id: UI-E05-DAY, category: partial, element: one stable-ID selected day, verification: explicit, owner: 07-05 day-switcher tests}
  - {id: UI-E06-PLAN, category: overflow, element: plan rows at 200 percent, verification: explicit, owner: 07-05 component tests}
  - {id: UI-E07-PLAN, category: zero-one-many, element: plan days and exercises, verification: explicit, owner: 07-05 editor tests}
  - {id: UI-E08-PLAN, category: long-text, element: day and exercise names, verification: explicit, owner: 07-05 component tests}
  - {id: UI-B05-OWNED, category: long-text, element: assistive reorder without visible directional controls, verification: backstop, owner: 07-10 native evidence}
must_haves:
  truths:
    - Per D-04, one stable-ID day is edited at a time and every day is a visible labelled selector.
    - Shared rows keep 48dp drag, displacement, reduced motion, TalkBack, keyboard/D-pad alternatives while removing visible Up/Down/Position.
    - Replace is a right-aligned labelled glyph; all changes remain drafts until explicit save.
    - Save failure preserves draft and exact retry copy while authoritative plan stays unchanged.
  artifacts:
    - {path: src/ui/components/PlanEditorFields.tsx, provides: shared drag and non-visible alternatives}
    - {path: src/ui/components/PlanDaySwitcher.tsx, provides: stable-ID single-day selection}
  key_links:
    - {from: reorder handle, to: draft move, via: drag TalkBack and Shift+Arrow}
    - {from: day selector, to: selectedDayId, via: stable identity and nearest survivor}
---

<objective>Deliver the owned-plan/day half of UX-20 with drag-first accessible editing and explicit-save authority.</objective>
<execution_context>
@$HOME/.trae/gsd-core/workflows/execute-plan.md
@$HOME/.trae/gsd-core/templates/summary.md
</execution_context>
<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/07-post-candidate-ux-refinement/07-CONTEXT.md
@.planning/phases/07-post-candidate-ux-refinement/07-UI-SPEC.md
@.planning/phases/07-post-candidate-ux-refinement/07-PATTERNS.md
</context>
<tasks>
<task type="auto" tdd="true">
  <name>Task 1: Clean the shared reorder primitive</name>
  <files>src/ui/components/PlanEditorFields.tsx, src/ui/__tests__/PlanEditorFields.test.tsx</files>
  <read_first>src/ui/components/PlanEditorFields.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/ui/components/index.ts</read_first>
  <behavior>
    - Visual row is handle, copy, trailing action without visible directional controls or position sentence.
    - Long press/displacement, reduced motion, TalkBack increment/decrement, Enter/Space, and Shift+Arrow move one bounded draft.
  </behavior>
  <action>RED first. Refine PlanEditorReorderableRow once, preserving gesture and accessibility behavior while adding shared keyboard/D-pad movement and a wrapping trailing slot. Use exact label/hint, 48dp target, and draft callback. Do not persist or make gesture the only route.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/PlanEditorFields.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run check:boundaries</automated></verify>
  <acceptance_criteria>No visible ordinal/directional control remains; every non-visual movement mode passes.</acceptance_criteria>
  <done>The shared UX-20 reorder primitive is ready for all consumers.</done>
</task>
<task type="auto" tdd="true">
  <name>Task 2: Add the single-active-day switcher and trailing Replace glyph</name>
  <files>src/ui/components/PlanDaySwitcher.tsx, src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx</files>
  <read_first>src/ui/screens/OwnedPlanEditorScreen.tsx, src/ui/__tests__/OwnedPlanEditor.test.tsx, src/ui/components/PlanEditorFields.tsx</read_first>
  <behavior>
    - Every day is a 48dp row labelled `{day name}. {n} exercises`; selection changes one editor below.
    - Stable-ID selection survives reorder; removed selection chooses nearest survivor; zero/one/many and long names remain reachable.
    - Replace is right-aligned glyph; failed save retains draft and exact recovery action.
  </behavior>
  <action>RED first. Per D-04, extract PlanDaySwitcher without expanding all day forms. Key selection by ID, define nearest survivor, use shared reorder rows, and place Replace in the trailing slot. Preserve draft on exact save-failure state; never persist on selection, movement, or replacement.</action>
  <verify><automated>npm run test:components -- --runInBand src/ui/__tests__/OwnedPlanEditor.test.tsx src/ui/__tests__/PlanEditorFields.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
  <acceptance_criteria>Every day is reachable, selection is stable, Replace is responsive, and failed save preserves the draft.</acceptance_criteria>
  <done>The owned-plan UX-20 contract is complete.</done>
</task>
</tasks>
<artifacts_this_phase_produces>
| Artifact | Contract |
|---|---|
| PlanEditorReorderableRow | Gesture plus invisible assistive/keyboard alternatives |
| PlanDaySwitcher | Every day reachable, one active editor |
</artifacts_this_phase_produces>
<commit_protocol>After each task passes, create one atomic commit with `TRAE_GIT_ATTRIBUTION_HELPER= git -c core.hooksPath=/dev/null commit`; final trailer exactly `Co-authored-by: TRAE CLI &lt;noreply@users.noreply.github.com&gt;`.</commit_protocol>
<threat_model>
## Trust Boundaries
| Boundary | Description |
|---|---|
| Input to draft order | Touch and assistive modes must share bounded movement. |
| Draft to save | UI must not claim persistence before commit. |
## STRIDE Threat Register
| Threat ID | Category | Component | Severity | Disposition | Mitigation Plan |
|---|---|---|---|---|---|
| T-07-PLAN | Tampering | draft order | high | mitigate | Stable IDs, bounds, normalized draft, explicit save. |
| T-07-PLAN-A11Y | Denial of Service | reorder | medium | mitigate | Labelled 48dp handle and non-gesture alternatives. |
</threat_model>
<verification>Run focused primitive/editor suites, typecheck, lint, boundaries, and git diff --check.</verification>
<success_criteria>Owned-plan UX-20 is reachable, responsive, accessible, and draft-only until save.</success_criteria>
<output>Create `.planning/phases/07-post-candidate-ux-refinement/07-05-SUMMARY.md` when done.</output>
