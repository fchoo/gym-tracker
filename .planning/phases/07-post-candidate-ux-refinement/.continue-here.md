---
context: phase
phase: 07-post-candidate-ux-refinement
task: 1
total_tasks: 2
status: in_progress
last_updated: 2026-09-11T01:56:11.410Z
---

# BLOCKING CONSTRAINTS — Read Before Anything Else

> These are not suggestions. Each constraint below was discovered through failure or is an explicit release boundary.
> Acknowledge each one explicitly before proceeding.

- [ ] CONSTRAINT: Authoritative checkout isolation — Work only in `/private/tmp/gym-tracker-phase7-final-integration.xNQHWn/worktree`; the dirty primary checkout at `/Users/bytedance/Documents/personal/gym_tracker` is user-owned and must remain untouched.
- [ ] CONSTRAINT: Review gate is incomplete — The two final exact-diff reviewers were interrupted before verdict. Restart both against the current nine-file diff and require CLEAR before staging.
- [ ] CONSTRAINT: Current APK is stale — `artifacts/native/phase2/build.json` and APK bind committed HEAD `a31fe43`, not the dirty source. Commit the reviewed patch first, then build once at exact HEAD and regenerate every native evidence artifact.
- [ ] CONSTRAINT: Viewport evidence must target visible actionable state — Hidden live-region announcements, clipped child text, and off-screen controls caused real Maestro failures. Preserve the exact surviving-row/cardinality checks, relative selectors, and bounded target-gated swipes.
- [ ] CONSTRAINT: Protected release order — PR #26 must pass all six checks and be squash-merged before exactly one candidate is dispatched from `main`; derive the date live with `TZ=Asia/Singapore`. Never force-push or dispatch from the PR branch.
- [ ] CONSTRAINT: Evidence identity arguments — Local Phase 7 Maestro and N4 prepare/record/verify calls must include the exact candidate `--manifest-sha256`; do not copy the incomplete command in `07-10-PLAN.md`.
- [ ] CONSTRAINT: Release authority boundary — Samsung N4 is observation-only. Do not record owner approval, promote, publish, tag, or run Terminal Seal.

**Do not proceed until all boxes are checked.**

## Critical Anti-Patterns

| Pattern | Description | Severity | Prevention Mechanism |
|---------|-------------|----------|----------------------|
| Stale-byte proof | Direct native probes passed on APK `5fa10741…ccb79`, but tracked Maestro/tests changed afterward, so those bytes cannot close the gate. | blocking | Rebuild once only after a clean exact-HEAD commit; verify manifest source digest, installed APK, reports, screenshots, and roundtrip bytes together. |
| Invisible announcement assertion | Maestro cannot satisfy visibility waits on visually hidden live-region text such as `Warm-up W3 removed`. | blocking | Assert exact surviving row/cardinality and absence of the removed row/action instead. |
| Blind or center swipe | Center swipes were intercepted by full-width inputs and unconditional swipes could hide already-visible targets. | blocking | Use right-edge, bounded, `while: notVisible` traversal and assert the target before acting. |
| Ambiguous label tap | Visible labels such as `Effective date` were not necessarily the actionable control. | blocking | Reveal the section and use a relative selector tied to the field help text. |
| Candidate before merge | A candidate from a PR or stale commit would break main-bound evidence identity and consume the one allowed dispatch. | blocking | Verify six protected checks, squash merge, fetch/verify `origin/main`, then derive and dispatch one candidate. |
| Incomplete CLI identity | Plan prose omits required `--manifest-sha256` on local Phase 7 and N4 commands. | blocking | Read each script's actual CLI contract and pass manifest SHA, package, serial, output, and report/evidence paths explicitly. |
| Historical migration rewrite | Migration 17 was previously widened in place, which would fail databases that had already registered its original schema. | advisory | Keep migration 17 immutable at 128 and use committed migration 18 (`a713707`) for the forward 256-character entity-ID widening. |

<current_state>
Phase 7 Plan 10 Task 1 is in progress. The authoritative branch is `fix/phase7-final-reconciliation`; implementation parent `a31fe43e74843c4ba5322e97ddc501fef890aedc` is followed by one metadata-only pause commit containing this file and `.planning/HANDOFF.json`. Exactly nine implementation/evidence files remain modified and unstaged. Focused source suites and all previously failing long native journeys passed against unchanged pre-patch APK bytes, but the two final reviewers were interrupted and no exact-current-HEAD build exists. PR #26 remains open and blocked on remote head `48d2909974dc87fb7be3f130ea26c49a37327aaf`; `origin/main` is `9cdecb86dedeb9edd7bf5bc4d1c85d08bdc81660`. No Phase 7 candidate has been dispatched.
</current_state>

<completed_work>

Completed Tasks:
- Phase 7 Plans 07-01 through 07-09: implementation complete and committed.
- Retained native-flow investigation: completed across responsive 839/840dp, workout removal/retry, schedule, starter activation, Phase 1 full loop, denied/late notifications, and three-repeat airplane flow.
- Dirty-slice source verification: Phase 2 evidence 101/101, Phase 7 evidence 17/17, shared tooling 25/25, and all six modified Maestro YAML files parsed.
- Migration compatibility correction: historical migration 17 restored and forward migration 18 committed with preservation coverage.
- Task 1: in progress. Nine-file evidence hardening is implemented but not staged or committed.
</completed_work>

<remaining_work>

- Task 1a: Restart `final_retained_diff_review` and `phase7_durable_state_review` against the exact current diff; require both CLEAR.
- Task 1b: Stage only the nine files, inspect staged diff and secret scan, then commit with hooks disabled and exactly one required TRAE co-author trailer.
- Task 1c: At the new clean HEAD, use Node 24.19.0, npm 11.17.0, JDK 17.0.20+8, and Android SDK `/opt/homebrew/share/android-commandlinetools`; build Phase 2 exactly once.
- Task 1d: Pass native SQLite 64/64, canonical 20-execution Phase 2 Maestro, benchmark, temporary roundtrip, and automated-only exact-byte verifier.
- Task 1e: Fetch and push without force to PR #26's branch, wait for all six protected checks, squash-merge, fetch, and verify `origin/main`.
- Task 1f: Derive one live Singapore-date candidate ID from merged main, dispatch workflow ID `343430127` exactly once, resolve immutable run/artifact IDs, download, hash-verify, install, and run exact-byte Phase 7 evidence without rebuilding.
- Task 2: Connect Samsung SM-S916B and complete N4-01 through N4-05 observation-only evidence with `approval_status: evidence_pending`.
- Closure: Create `07-EVIDENCE.md` and `07-10-SUMMARY.md`, run Phase 7 verification, and synchronize roadmap/state/requirements without any release approval or Terminal Seal.
</remaining_work>

<decisions_made>

- Hard-delete only non-completed sets because completed-set correction, undo, and history semantics must remain intact.
- Replace hidden announcement waits with exact durable survivor/cardinality proof because Maestro visibility is a rendered-state contract.
- Use bounded right-edge target-gated swipes and relative selectors because full-width controls intercepted center gestures and labels alone were ambiguous.
- Rebuild only after the current evidence patch is committed because tracked test/flow bytes participate in source identity.
- Keep one fresh candidate main-bound and immutable from workflow build through emulator and Samsung evidence.
- Keep Samsung N4 observation separate from Phase 5 owner approval and all promotion/publication actions.
- Keep Concept G, blue tri-tone ascending bars, as the locked icon direction.
</decisions_made>

<blockers>
- Final exact-diff review: interrupted by the user's stop request; restart two fresh read-only reviewers.
- Exact-current-HEAD native evidence: cannot start until review and commit are complete.
- PR #26: current remote checks are stale and blocked; native-and-smoke failed and artifact roundtrip was skipped.
- Samsung N4: device was not connected at pause; defer until the one verified candidate exists.
</blockers>

## Required Reading (in order)

1. `.planning/HANDOFF.json` — machine-readable exact state, identities, blockers, and next action.
2. This file — failure-derived constraints and release boundaries.
3. `.planning/phases/07-post-candidate-ux-refinement/07-10-PLAN.md` — active plan; correct its execution commands mentally by adding explicit `--manifest-sha256`.
4. `.planning/phases/07-post-candidate-ux-refinement/07-VALIDATION.md` — Phase 7 verification contract.
5. `.planning/phases/07-post-candidate-ux-refinement/07-09-SUMMARY.md` — last completed plan.
6. `.github/workflows/pr.yml` and `.github/workflows/release-candidate.yml` — protected PR chain and main-bound build-once workflow.
7. `scripts/run-phase2-maestro.mjs`, `scripts/run-phase7-maestro.mjs`, `scripts/generate-phase7-attended-checklist.mjs`, and `scripts/verify-release-candidate-manifest.mjs` — actual CLI and exact-byte contracts.
8. `.planning/METHODOLOGY.md` if present — apply project analytical lenses before assumptions.

## Critical Anti-Patterns (do NOT repeat these)

- Do not infer success from hidden accessibility announcements. Prove visible authoritative state plus absence.
- Do not use blind swipes or gestures through input centers. Gate every bounded right-edge traversal on target visibility.
- Do not accept the retained pre-patch APK as exact-current-HEAD evidence.
- Do not touch the dirty primary checkout or overwrite user-owned changes.
- Do not force-push, bypass required PR checks, or dispatch a candidate before verified squash merge.
- Do not dispatch more than one Phase 7 candidate.
- Do not omit `--manifest-sha256`, rebuild downloaded candidate bytes, or mix evidence identities.
- Do not write approval, promote, publish, tag, or invoke Terminal Seal.

## Infrastructure State

- Authoritative worktree: `/private/tmp/gym-tracker-phase7-final-integration.xNQHWn/worktree`.
- Branch/implementation parent: `fix/phase7-final-reconciliation` / `a31fe43e74843c4ba5322e97ddc501fef890aedc`; expect one metadata-only pause commit on top.
- Dirty state: exactly nine unstaged files listed in `.planning/HANDOFF.json`; nothing staged before this handoff.
- Retained stale probe artifact: source-tree SHA-256 `47abceaffa1c8c81b941b5222d6a87908fe24450fb0135c5629d2af7a3619b38`; APK SHA-256 `5fa10741a74e398900fe2a31f83529e3a9d3891f9c9f95ba7a8975df0ecccb79`; size `123066171`.
- Emulator: `emulator-5554`, ready; airplane mode `0`; physical display `1080x2400`, density `420`. Emulator process remains running; no Gradle, Metro, Maestro, or evidence runner was active at pause.
- Runtime pins: Node `24.19.0`, npm `11.17.0`, JDK `/Users/bytedance/Library/Java/JavaVirtualMachines/temurin-17.0.20+8.jdk/Contents/Home`, Android SDK `/opt/homebrew/share/android-commandlinetools`.
- PR #26: `https://github.com/fchoo/gym-tracker/pull/26`, OPEN/BLOCKED, remote head `48d2909974dc87fb7be3f130ea26c49a37327aaf`. Latest run `34093743293`: first four gates passed, native-and-smoke failed, artifact roundtrip skipped.
- Candidate workflow ID: `343430127`. Candidate count for Phase 7: zero.
- Review agents `final_retained_diff_review` and `phase7_durable_state_review` were explicitly interrupted and have no accepted final verdict.
- Unified exec process count was above the host warning threshold; avoid creating persistent sessions and reuse existing processes where possible.
- No outstanding async external job manifest was found.

<context>
The implementation is close, but the evidence chain is intentionally fail-closed. The dirty changes are limited to retained Maestro behavior and matching regression contracts. The correct next move is not another ad hoc native run: first obtain fresh review verdicts on these exact bytes, commit atomically, then regenerate the canonical exact-HEAD artifact and run the whole protected path in order. Treat any source edit after that build as invalidating the artifact.
</context>

<next_action>
Start with: verify the pause commit's parent is `a31fe43e74843c4ba5322e97ddc501fef890aedc`, verify that commit changed only `.planning/HANDOFF.json` and this `.continue-here.md`, and confirm the same nine implementation files remain unstaged. Then launch two fresh independent read-only reviews of the complete implementation diff and require both to return CLEAR before staging.
</next_action>
