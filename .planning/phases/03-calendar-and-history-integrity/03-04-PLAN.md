---
phase: 03-calendar-and-history-integrity
plan: 04
type: execute
wave: 4
depends_on: [03-03]
files_modified:
  - src/domains/history/correctionCommands.ts
  - src/domains/history/correctionContracts.ts
  - src/platform/sqlite/repositories/historyRepository.ts
  - src/platform/sqlite/repositories/historyCommandRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/ui/screens/SessionDetailScreen.tsx
  - src/ui/screens/SessionCorrectionScreen.tsx
  - app/session/[sessionId].tsx
  - app/session/[sessionId]/correct.tsx
  - src/ui/__tests__/SessionCorrectionScreen.test.tsx
  - src/platform/sqlite/repositories/historyCommandRepository.test.ts
autonomous: true
requirements: [HIST-05, HIST-06]
must_haves:
  truths:
    - Completed and partial sessions support the approved correction matrix without mutating their original snapshots.
    - Each discrete correction writes append-only field/set audit facts with identity, before/after values, timestamp, and provenance.
    - A successful correction atomically persists the complete effective snapshot, audit records, subject revisions, recommendation invalidation, and durable rebuild work before UI acknowledgement.
  artifacts:
    - { path: src/domains/history/correctionCommands.ts, provides: validated correction command and conflict contracts }
    - { path: src/platform/sqlite/repositories/historyCommandRepository.ts, provides: serialized source transaction for correction plus audit/fan-out }
    - { path: src/ui/screens/SessionCorrectionScreen.tsx, provides: explicit editor with save/conflict/cancel behavior }
  key_links:
    - { from: src/ui/screens/SessionCorrectionScreen.tsx, to: src/bootstrap/workoutAppRuntime.tsx, via: typed correction command and exact reload, pattern: correctSession }
    - { from: src/platform/sqlite/repositories/historyCommandRepository.ts, to: src/domains/history/historySubjects.ts, via: before/after effective snapshot fan-out, pattern: collectHistorySubjects }
---

<objective>
Implement finalized session correction without turning immutable workout facts into editable rows.

Purpose: let the owner repair a historical entry while preserving exact original facts, transparent audit history, and deterministic dependent-state invalidation.
Output: correction command/editor, append-only audit ledger, conflict recovery, and atomic source/effect verification.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Freeze correction contracts and validation for complete effective snapshots</name>
<read_first>03-CONTEXT.md, 03-UI-SPEC.md, src/domains/workout/sessionDetail.ts, src/domains/metrics/contracts.ts, src/domains/scheduling/localDate.ts, src/ui/components/CalendarField.tsx</read_first>
<files>src/domains/history/correctionContracts.ts, src/domains/history/correctionCommands.ts, src/domains/history/correctionCommands.test.ts</files>
<action>Start with an exhaustive matrix of valid and invalid edits. The command accepts a base effective revision and produces a complete validated effective snapshot, never a partial SQL patch. Support value edits, warm-up/working set kind, add/remove set, exercise replacement only with compatible valid identity, local date/time, effort, note, and valid plan/day association. Preserve immutable original IDs and assign stable synthetic identities only for effective added structures. Reject unsupported statuses, incompatible metric identity, invalid time/date, unsafe association, missing required working-set semantics, stale revision, and any direct source-row rewrite. Calculate deliberate discrete audit deltas from the canonical before/after snapshots.</action>
<acceptance_criteria>The correction contract is deterministic, rejects ambiguity, covers all approved fields, and makes a no-op correction impossible to acknowledge as a change.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/history/correctionCommands.test.ts</automated></verify>
</task>

<task type="tracer" tdd="true">
<name>Task 2: Persist overlays, audit deltas, invalidation, and effects in one writer transaction</name>
<read_first>src/platform/sqlite/repositories/historyRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/effects/historyProjectionEffects.ts, src/platform/sqlite/sqliteKernel.ts</read_first>
<files>src/platform/sqlite/repositories/historyCommandRepository.ts, src/platform/sqlite/repositories/historyRepository.ts, src/platform/sqlite/repositories/historyCommandRepository.test.ts, src/bootstrap/workoutAppRuntime.tsx</files>
<action>Write rollback and success fixtures first. Under one kernel.write transaction, read the effective base snapshot/revision, validate the command, write a new full overlay revision, append immutable audit event rows, increment every old/new subject revision, invalidate only matching pending recommendations, supersede obsolete rebuild effects, enqueue canonical rebuild effects, and commit. The original session/snapshot bytes must remain unchanged. Inject failures after every source step and assert no partial overlay, audit, revision, invalidation, or effect survives. Return authoritative effective detail only after commit; runtime then requests best-effort effect draining and reloads exact Calendar/session/exercise scopes.</action>
<acceptance_criteria>A correction either commits every source fact/effect dependency or nothing, a stale command returns a recoverable conflict, and audit mutation/deletion fails at SQLite level.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyCommandRepository.test.ts &amp;&amp; npm run test:unit -- --runInBand src/domains/history/correctionCommands.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 3: Deliver the accessible correction and audit surface from Session detail</name>
<read_first>03-UI-SPEC.md, src/ui/screens/SessionDetailScreen.tsx, src/ui/components/CalendarField.tsx, src/ui/components/SemanticNumberField.tsx, src/ui/layout/AdaptiveScreen.tsx</read_first>
<files>src/ui/screens/SessionDetailScreen.tsx, src/ui/screens/SessionCorrectionScreen.tsx, app/session/[sessionId].tsx, app/session/[sessionId]/correct.tsx, src/ui/__tests__/SessionCorrectionScreen.test.tsx</files>
<action>Begin with component tests for loading, disabled, populated, validation, conflict, save failure, success, audit, compact, expanded, keyboard/D-pad, and 200 percent text states. Add a secondary Correct session action for completed/partial history only. The editor uses existing civil-date and numeric primitives, explicit set-kind and add/remove controls, Cancel, and Save correction. Do not optimistically rewrite the detail screen. On conflict, retain user input, explain the stale view, and offer reload. Show a discreet Audit history disclosure with field/set identity, human-readable before/after values, timestamp, and no raw payload dump. Preserve warm-up separation and neutral/caution semantics that do not rely on color.</action>
<acceptance_criteria>The user can correct every approved field safely, understand what changed, recover from conflict/failure without data loss, and reach all controls with assistive input.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/SessionCorrectionScreen.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run correction-domain and host SQLite atomicity suites, Calendar/session route components, migration retention, typecheck, lint, and boundaries. Correction UI must not import SQLite and source facts must remain readable after injected derivative failures.</verification>
<success_criteria>HIST-05 and HIST-06 are delivered through an immutable-original/effective-overlay model with auditable atomic repair that uses the shared HIST-08 rebuild contract from Plan 03.</success_criteria>
