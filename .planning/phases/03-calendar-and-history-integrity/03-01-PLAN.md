---
phase: 03-calendar-and-history-integrity
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - src/platform/sqlite/migrations/0013_history_integrity.ts
  - src/platform/sqlite/migrations/index.ts
  - src/domains/history/contracts.ts
  - src/domains/history/calendar.ts
  - src/platform/sqlite/repositories/historyRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/ui/screens/CalendarScreen.tsx
  - app/(tabs)/calendar.tsx
  - src/ui/__tests__/CalendarScreen.test.tsx
  - src/platform/sqlite/repositories/historyRepository.test.ts
autonomous: true
requirements: [HIST-01, HIST-02, HIST-03]
must_haves:
  truths:
    - Calendar reads civil LocalDate facts and distinguishes completed, partial, manual, planned-not-completed, and current-day states without color-only meaning.
    - Selecting a date lists factual sessions with effective local date/time and actual exercise and working-set N/N percentage counts.
    - Original local date, stored timezone, source timestamps, and creation offset survive device timezone changes and remain readable after correction.
  artifacts:
    - { path: src/platform/sqlite/migrations/0013_history_integrity.ts, provides: immutable effective-history boundary and creation-offset retention }
    - { path: src/platform/sqlite/repositories/historyRepository.ts, provides: typed Calendar/session factual reads through effective history }
    - { path: src/ui/screens/CalendarScreen.tsx, provides: civil month grid and selected-date factual session surface }
  key_links:
    - { from: app/(tabs)/calendar.tsx, to: src/bootstrap/workoutAppRuntime.tsx, via: typed Calendar capability, pattern: loadCalendar }
    - { from: src/platform/sqlite/repositories/historyRepository.ts, to: owned_plan_schedule_opportunities, via: persisted planned-not-completed state, pattern: planned_not_completed }
---

<objective>
Establish the immutable/effective history boundary and deliver the Calendar read tracer.

Purpose: make chronological history source-backed before adding mutable correction commands.
Output: migration, typed history read port, Calendar root, and component/integration coverage.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Add the additive history-integrity schema and effective-fact contract</name>
<read_first>03-CONTEXT.md, 03-RESEARCH.md, src/platform/sqlite/migrations/0001_initial.ts, src/platform/sqlite/migrations/index.ts, src/platform/sqlite/migrationRunner.ts, src/domains/workout/sessionDetail.ts</read_first>
<files>src/platform/sqlite/migrations/0013_history_integrity.ts, src/platform/sqlite/migrations/index.ts, src/domains/history/contracts.ts, src/platform/sqlite/repositories/historyRepository.test.ts</files>
<action>Start with migration fixtures from version 12. Add forward/additive tables and columns for original creation offset, canonical history overlay, immutable audit/lifecycle events, history subject revisions, projection freshness rows, and the dedicated `history_rebuild_effects` revision-fenced rebuild queue. Preserve original session rows and the existing constrained `pending_effects` contract; create triggers rejecting audit mutation/deletion and invalid lifecycle/overlay states. Define validated history contracts that distinguish original/effective facts and canonicalize JSON deterministically. Backfill offset from each source started instant and stored timezone without changing local date. Add retained migration and rollback fixtures proving foreign keys, original row bytes, explicit voided compatibility, dedicated queue invariants, and civil-date retention.</action>
<acceptance_criteria>Version 13 applies from retained schemas, preserves source rows, records stable creation offsets, and fails closed on invalid or rewritten audit facts.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Implement effective Calendar and selected-date repository reads</name>
<read_first>src/platform/sqlite/repositories/scheduleRepository.ts, src/bootstrap/scheduleRuntime.tsx, src/platform/sqlite/repositories/workoutOutcomeRepository.ts, src/domains/scheduling/localDate.ts, src/domains/history/contracts.ts</read_first>
<files>src/domains/history/calendar.ts, src/platform/sqlite/repositories/historyRepository.ts, src/bootstrap/workoutAppRuntime.tsx, src/platform/sqlite/repositories/historyRepository.test.ts</files>
<action>Write failing source-backed tests then implement a single history repository read path. Resolve original session facts plus overlay when present; default filter excludes voided rows. Join owned schedule opportunities for planned-not-completed state without inventing dates. Month/selected-date reads accept validated LocalDate only, use stored/effective civil facts, preserve timestamps/offset metadata, and calculate actual exercise/working-set counts from effective data. Expose typed runtime methods; routes/screens must not import repositories or SQLite. Do not calculate Phase 4 period metrics here.</action>
<acceptance_criteria>Calendar data shows every required date state, selected-date factual counts, stable sorting, and zero/one/many states with no instant/date-only timezone drift.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyRepository.test.ts &amp;&amp; npm run typecheck</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 3: Replace the Calendar placeholder with the civil month tracer</name>
<read_first>03-UI-SPEC.md, src/ui/components/CalendarField.tsx, src/ui/screens/RootScreens.tsx, src/ui/layout/AdaptiveScreen.tsx, src/ui/components/index.ts, app/(tabs)/calendar.tsx</read_first>
<files>src/ui/screens/CalendarScreen.tsx, app/(tabs)/calendar.tsx, src/ui/__tests__/CalendarScreen.test.tsx</files>
<action>Start with failing component tests. Implement labelled civil month navigation, weekday headers, non-color state labels/glyphs, selected-date factual session cards, and empty/loading/error states. Use current theme/primitives and no third-party calendar package. At medium/expanded widths optionally use AdaptiveScreen secondary pane; compact stacks content. Session cards open the existing detail route and all controls retain keyboard/D-pad/focus/200% semantics.</action>
<acceptance_criteria>Calendar is a real root destination with civil month navigation, all state semantics, factual sessions/counts, usable loading/error/empty states, and no fake analytics.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/CalendarScreen.test.tsx &amp;&amp; npm run lint</automated></verify>
</task>
</tasks>

<verification>Run typecheck, lint, Calendar component tests, and retained migration/SQLite fixtures. Source changes invalidate native evidence; defer serialized native candidate generation until Phase 3 source stabilizes.</verification>
<success_criteria>HIST-01 through HIST-03 are source-backed and visible in Calendar without altering original historical facts.</success_criteria>
