---
phase: 03-calendar-and-history-integrity
plan: 05
type: execute
wave: 5
depends_on: [03-03, 03-04]
files_modified:
  - src/domains/history/historyLifecycleCommands.ts
  - src/platform/sqlite/repositories/historyCommandRepository.ts
  - src/platform/sqlite/repositories/historyRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/ui/screens/SessionDetailScreen.tsx
  - src/ui/screens/MoreScreen.tsx
  - src/ui/screens/RemovedSessionsScreen.tsx
  - src/ui/screens/TodayScreen.tsx
  - app/more/index.tsx
  - app/more/removed-sessions.tsx
  - app/_layout.tsx
  - src/ui/__tests__/RemovedSessionsScreen.test.tsx
  - src/platform/sqlite/repositories/historyCommandRepository.test.ts
  - scripts/run-phase3-maestro.mjs
  - scripts/benchmark-phase3.mjs
  - scripts/verify-phase3-native-evidence.mjs
  - package.json
autonomous: true
requirements: [HIST-07]
must_haves:
  truths:
    - Completed sessions can be reversibly removed from ordinary history and derived metrics, while original facts and audit/lifecycle data survive.
    - Removed sessions are discoverable in one focused surface and restoration uses an explicit confirmation with no permanent-delete control.
    - Void/restore produces the same source/effect/rebuild guarantees as correction and Phase 3 automated/native evidence is exact-HEAD only.
  artifacts:
    - { path: src/domains/history/historyLifecycleCommands.ts, provides: void/restore validation and explicit lifecycle transitions }
    - { path: src/ui/screens/RemovedSessionsScreen.tsx, provides: restored-history management surface }
    - { path: scripts/verify-phase3-native-evidence.mjs, provides: evidence identity and automated-only contract verifier }
  key_links:
    - { from: src/ui/screens/RemovedSessionsScreen.tsx, to: src/bootstrap/workoutAppRuntime.tsx, via: typed removed-session reads and restore command, pattern: restoreSession }
    - { from: src/platform/sqlite/repositories/historyCommandRepository.ts, to: src/platform/sqlite/effects/historyProjectionEffects.ts, via: lifecycle subject union plus durable rebuild effects, pattern: rebuild_history_subject }
---

<objective>
Close Phase 3 with reversible removal/restoration and serial exact-HEAD automated proof.

Purpose: preserve owner control without permanent history deletion, then prove every mutation converges through the same history source and projection pathway.
Output: Remove from history, Removed sessions, restore, Phase 3 verification commands, and no attended evidence.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Implement reversible void and restore as audited lifecycle source mutations</name>
<read_first>03-CONTEXT.md, src/domains/history/correctionCommands.ts, src/platform/sqlite/repositories/historyCommandRepository.ts, src/domains/history/historySubjects.ts, src/domains/history/projectionReducer.ts</read_first>
<files>src/domains/history/historyLifecycleCommands.ts, src/platform/sqlite/repositories/historyCommandRepository.ts, src/platform/sqlite/repositories/historyCommandRepository.test.ts</files>
<action>Write mutation and rollback fixtures before implementation. Permit Remove from history only for completed sessions and require the current effective revision plus an explicit lifecycle confirmation token. A void appends an immutable lifecycle/audit event and writes effective lifecycle voided without deleting source rows or original snapshots. Restore only accepts an existing voided session and appends an immutable restore event. Both transitions collect the full old/new subject union, transactionally invalidate dependent pending recommendations before acknowledgement, advance revisions, supersede obsolete work, and queue canonical targeted rebuilds. Ordinary Calendar, exercise history, and metrics exclude voided sessions by default; Removed-session reads include only voided effective facts and preserve enough factual context for safe restoration.</action>
<acceptance_criteria>Completed-only void, restore-only-after-void, idempotency/conflict/rollback, recommendation invalidation, and target/full rebuild equivalence all have retained source-backed coverage.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand src/platform/sqlite/repositories/historyCommandRepository.test.ts src/platform/sqlite/repositories/historyProjectionRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Add Remove from history and Removed sessions UI with accessible destructive safeguards</name>
<read_first>03-UI-SPEC.md, src/ui/screens/SessionDetailScreen.tsx, src/ui/screens/TodayScreen.tsx, src/ui/components/index.ts, src/ui/layout/AdaptiveScreen.tsx</read_first>
<files>src/ui/screens/SessionDetailScreen.tsx, src/ui/screens/MoreScreen.tsx, src/ui/screens/RemovedSessionsScreen.tsx, src/ui/screens/TodayScreen.tsx, app/more/index.tsx, app/more/removed-sessions.tsx, app/_layout.tsx, src/bootstrap/workoutAppRuntime.tsx, src/ui/__tests__/RemovedSessionsScreen.test.tsx</files>
<action>Start with route/component tests. Add a secondary Remove from history action only for eligible completed details, followed by a clear confirmation that says the session disappears from normal history and metrics but can be restored later. Add the minimal existing-system More route and a Today entry, then place Removed sessions there with loading, error, empty, populated, and restore-confirmation states. Each row shows date, plan/source, working-set count, and removal timestamp. Restore reloads exact affected views after committed success. No permanent delete, hidden swipe destructive action, optimistic removal, color-only warning, or silently changed recommendation state is allowed. Preserve 48dp targets, focus return, keyboard/D-pad, screen-reader labels, reduced motion, and adaptive/200 percent text behavior.</action>
<acceptance_criteria>The lifecycle is understandable and reversible on-device, default history excludes voided facts, and all destructive/recovery controls are explicit and accessible.</acceptance_criteria>
<verify><automated>npm run test:components -- --runInBand src/ui/__tests__/RemovedSessionsScreen.test.tsx &amp;&amp; npm run typecheck &amp;&amp; npm run lint</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 3: Add Phase 3 exact-HEAD automated verification without attended approval</name>
<read_first>scripts/run-phase2-maestro.mjs, scripts/benchmark-phase2.mjs, scripts/verify-phase2-native-evidence.mjs, scripts/phase2-evidence-identity.mjs, package.json, .planning/phases/02-owned-library-and-planning/02-VERIFICATION.md</read_first>
<files>scripts/run-phase3-maestro.mjs, scripts/benchmark-phase3.mjs, scripts/verify-phase3-native-evidence.mjs, package.json</files>
<action>Extend the existing exact-candidate evidence pattern rather than copying generic test commands. Define source-owned native contract/maestro flows for Calendar state, correction, audit, void, restore, stale rebuild rejection, and full-rebuild equivalence. Add a bounded Phase 3 benchmark only for factual history/projection work with a documented threshold derived from current behavior. The evidence verifier must bind implementation head, source-tree digest, package identity, APK hash, and all automated producer results to one unchanged APK. Run serialized native generation and automated checks once only after all Phase 3 source changes stabilize. Do not generate approval records, terminal-seal material, or attended-device claims; mark required physical review as deferred for the final single gate.</action>
<acceptance_criteria>Phase 3 has a repeatable automated-only verifier that fails closed on source/APK identity drift and produces no false attended approval.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand scripts &amp;&amp; npm run typecheck &amp;&amp; npm run lint &amp;&amp; npm run test:all -- --runInBand</automated></verify>
</task>
</tasks>

<verification>Run the complete Phase 3 source suite, exact-HEAD native/maestro/benchmark path serialized after source stabilization, and the automated-only evidence verifier. Do not run or record the deferred attended verification; it remains part of the single final physical review after Phase 5.</verification>
<success_criteria>HIST-07 closes with reversible lifecycle facts and exact automated evidence, using the shared HIST-08/HIST-09 deterministic rebuild guarantees from Plan 03 while attended verification remains intentionally deferred.</success_criteria>
