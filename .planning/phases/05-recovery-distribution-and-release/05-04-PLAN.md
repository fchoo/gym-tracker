---
phase: 05-recovery-distribution-and-release
plan: 04
type: execute
wave: 2
depends_on: [05-03]
files_modified:
  - src/platform/sqlite/migrations/0016_portability_restore_state.ts
  - src/platform/sqlite/migrations/index.ts
  - src/platform/sqlite/repositories/logicalRestoreRepository.ts
  - src/domains/portability/restoreCommands.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - app/more/data-and-recovery.tsx
  - src/domains/portability/restoreCommands.test.ts
  - tests/sqlite-host/logicalRestoreRepository.test.ts
  - tests/sqlite-host/portabilityMigration.test.ts
autonomous: true
requirements: [DATA-04, DATA-05]
must_haves:
  truths:
    - Restore authenticates/decrypts before parsing and validates all shape/ID/reference/limit rules before any writer transaction starts.
    - Preview provides only bounded replacement facts and final replacement is protected by typed `REPLACE` confirmation.
    - Any precommit/cancel/insert error leaves the exact prior logical state unchanged.
  artifacts:
    - { path: src/platform/sqlite/repositories/logicalRestoreRepository.ts, provides: one-transaction user-owned source replacement }
    - { path: src/platform/sqlite/migrations/0016_portability_restore_state.ts, provides: durable rebuild-pending restore state }
    - { path: src/domains/portability/restoreCommands.ts, provides: staged preflight/preview/commit command boundary }
  key_links:
    - { from: src/domains/portability/restoreCommands.ts, to: src/domains/portability/backupFormat.ts, via: authenticate/decrypt before parse, pattern: decrypt }
    - { from: src/platform/sqlite/repositories/logicalRestoreRepository.ts, to: src/platform/sqlite/sqliteKernel.ts, via: one serialized writer transaction, pattern: kernel.write }
---

<objective>
Implement restore preflight, preview, and all-or-nothing user-owned source replacement.

Purpose: recover data safely without raw database replacement or partial writes.
Output: migration-backed rebuild state, strict restore validator, one-transaction replacement repository, and destructive-confirmation runtime/UI flow.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Add restore state and strict no-write preflight/preview</name>
<read_first>05-CONTEXT.md, src/domains/portability/backupContracts.ts, src/domains/portability/backupFormat.ts, src/platform/sqlite/migrations/index.ts, src/domains/shared/errors.ts</read_first>
<files>src/platform/sqlite/migrations/0016_portability_restore_state.ts, src/platform/sqlite/migrations/index.ts, src/domains/portability/restoreCommands.ts, src/domains/portability/restoreCommands.test.ts, tests/sqlite-host/portabilityMigration.test.ts</files>
<action>Start with retained migration and parser tests. Add an additive singleton restore state that records `ready` or `rebuild_pending` without storing archive payload/password/key. Implement staged read-only preflight: bounded selected-file read, GTBK authentication/decryption, strict manifest/schema/count/ID/reference validation, and preview counts/reference availability. Model cancellation only before the source commit begins. Reject wrong password and every tamper/unsupported/oversize/unknown/malformed/duplicate/missing-reference case with safe typed results and prove no repository writer call occurs.</action>
<acceptance_criteria>Preflight produces a summary only for an authenticated fully validated snapshot; all rejection/cancellation paths reveal no sensitive payload and leave the database untouched.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/domains/portability/restoreCommands.test.ts &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/portabilityMigration.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Replace user-owned source tables in one serialized transaction and wire the restore UX</name>
<read_first>src/platform/sqlite/sqliteKernel.ts, src/platform/sqlite/repositories/logicalBackupRepository.ts, src/platform/sqlite/migrations/0016_portability_restore_state.ts, src/bootstrap/workoutAppRuntime.tsx, 05-UI-SPEC.md</read_first>
<files>src/platform/sqlite/repositories/logicalRestoreRepository.ts, src/domains/portability/restoreCommands.ts, src/bootstrap/workoutAppRuntime.tsx, app/more/data-and-recovery.tsx, tests/sqlite-host/logicalRestoreRepository.test.ts, app/more/__tests__/data-and-recovery.test.tsx</files>
<action>Use host-SQLite foreign-key fixtures to establish the exact delete/insert sequence. Implement a repository method that receives only a previously validated snapshot and, within one `kernel.write()`, clears/reinserts only the audited user-owned table graph in FK-safe order, retains catalog reference data, sets `rebuild_pending`, and checks affected counts/invariants before commit. Add fault injection at each replacement stage and compare canonical pre-state dumps to post-failure state. Runtime accepts only a preflight token/validated snapshot that is still valid; UI imports through a read-only picker, focuses Review backup, requires exact `REPLACE`, prevents duplicate presses, renders safe errors, and does not optimistically update data.</action>
<acceptance_criteria>Every precommit failure—including a deliberate insert failure—rolls back the entire logical replacement; valid confirmation performs exactly one source replacement transaction and records deterministic rebuild work.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/logicalRestoreRepository.test.ts &amp;&amp; npm run test:components -- --runInBand app/more/__tests__/data-and-recovery.test.tsx</automated></verify>
</task>
</tasks>

<verification>Run restore/backup host fixture suite, focused unit/component tests, full migration tests, typecheck, lint/boundaries, and integrity coverage. Confirm no raw database file is imported/exported by production code.</verification>
<success_criteria>DATA-04 and DATA-05 are met for preflight and source replacement: only a fully authenticated/validated/confirmed archive can commit, and every other outcome preserves existing logical facts.</success_criteria>
