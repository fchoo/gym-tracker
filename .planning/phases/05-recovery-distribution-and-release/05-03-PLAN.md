---
phase: 05-recovery-distribution-and-release
plan: 03
type: execute
wave: 2
depends_on: [05-01, 05-02]
files_modified:
  - package.json
  - src/platform/sqlite/repositories/logicalBackupRepository.ts
  - src/platform/files/expoBackupFilePort.ts
  - src/domains/portability/backupCommands.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - app/more/index.tsx
  - app/more/data-and-recovery.tsx
  - app/more/__tests__/data-and-recovery.test.tsx
  - tests/sqlite-host/logicalBackupRepository.test.ts
  - src/platform/files/expoBackupFilePort.test.ts
autonomous: true
requirements: [DATA-01, DATA-02, DATA-03]
must_haves:
  truths:
    - Owner can create a bounded encrypted logical backup that includes all user-owned source categories and no raw SQLite/database artifacts.
    - Password/key/plaintext buffers and temporary files are wiped/deleted on success, error, and cancellation before sharing.
    - UI invokes only runtime capabilities and explicitly shares a completed archive through the OS.
  artifacts:
    - { path: src/platform/sqlite/repositories/logicalBackupRepository.ts, provides: stable user-owned logical collector }
    - { path: src/platform/files/expoBackupFilePort.ts, provides: bounded temporary archive and explicit share adapter }
    - { path: app/more/data-and-recovery.tsx, provides: accessible backup entry and password flow }
  key_links:
    - { from: src/bootstrap/workoutAppRuntime.tsx, to: src/platform/sqlite/repositories/logicalBackupRepository.ts, via: typed backup capability, pattern: createLogicalBackup }
    - { from: src/domains/portability/backupCommands.ts, to: src/platform/files/expoBackupFilePort.ts, via: wipe-before-share archive lifecycle, pattern: share }
---

<objective>
Deliver the first production tracer: create and explicitly share a password-protected logical backup of the owner’s data.

Purpose: prove the envelope against real user-owned SQLite data while preserving runtime/UI boundaries.
Output: bounded collector, encrypted export command/file adapter, accessible More entry, and data-completeness/cleanup proof.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Collect a stable user-owned logical snapshot from SQLite</name>
<read_first>05-01-PLAN.md, src/platform/sqlite/migrations/0008_schedule_activation.ts, src/platform/sqlite/migrations/0013_history_integrity.ts, src/platform/sqlite/migrations/0015_progression_evidence.ts, tests/sqlite-host/historyProjectionRepository.test.ts</read_first>
<files>src/platform/sqlite/repositories/logicalBackupRepository.ts, tests/sqlite-host/logicalBackupRepository.test.ts</files>
<action>Write host-SQLite fixtures first containing custom exercise data, copied/owned plan/schedule/target/policy graph, sessions/sets, correction/void/restore history, settings, references, and both recommendation graphs. Implement the explicit table registry from 05-01 in FK-safe stable primary-key order. Snapshot inside a consistent read boundary appropriate to the kernel; include schema/manifest counts and source references, exclude bundled rows/FTS/projections/effects/recovery files, and reject incomplete/unknown tables. Test empty data, deterministic repeated output, all required source categories, no raw `.db` field, and a failure that leaves source untouched.</action>
<acceptance_criteria>A logical snapshot faithfully captures every specified user-owned source category with deterministic ordering and never leaks derivative or bundled catalog authority into its payload.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/logicalBackupRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Encrypt, clean up, share, and expose the backup tracer through More</name>
<read_first>src/domains/portability/backupFormat.ts, src/platform/crypto/passwordKdf.ts, src/bootstrap/workoutAppRuntime.tsx, app/more/index.tsx, 05-UI-SPEC.md, node_modules/expo-file-system/src/File.ts</read_first>
<files>package.json, src/platform/files/expoBackupFilePort.ts, src/domains/portability/backupCommands.ts, src/bootstrap/workoutAppRuntime.tsx, app/more/index.tsx, app/more/data-and-recovery.tsx, app/more/__tests__/data-and-recovery.test.tsx, src/platform/files/expoBackupFilePort.test.ts</files>
<action>Add the Expo-managed sharing dependency, then test the command with fake KDF/crypto/file/share ports before using device APIs. Convert password text to an owned temporary byte array, collect/encode/encrypt to a cache archive, wipe all sensitive owned arrays in `finally`, delete incomplete/failed/cancelled temporary files, and invoke OS share only after a complete valid archive exists. Add typed runtime capability and the More/Data and recovery backup flow from the UI contract: labelled password+confirmation, visible-state controls, non-optimistic busy/error/success/share states, safe diagnostics, and retry. Do not persist a password, key, archive payload, or file path in route state/logs.</action>
<acceptance_criteria>The owner can reach Create secure backup from More, produce/share a valid encrypted logical archive, and every cancellation/failure leaves no plaintext/temp residue and no source mutation.</acceptance_criteria>
<verify><automated>npm run test:unit -- --runInBand src/platform/files/expoBackupFilePort.test.ts &amp;&amp; npm run test:components -- --runInBand app/more/__tests__/data-and-recovery.test.tsx &amp;&amp; npm run test:sqlite:host -- --runInBand tests/sqlite-host/logicalBackupRepository.test.ts</automated></verify>
</task>
</tasks>

<verification>Run focused host/unit/component tests, typecheck, lint, boundaries, and the integrity coverage gate. Verify package-lock changes are Expo-managed and no generated native directories are staged.</verification>
<success_criteria>DATA-01 through DATA-03 have a usable source-tested secure-export tracer with complete logical coverage and explicit OS sharing.</success_criteria>
