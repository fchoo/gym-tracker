---
phase: 05-recovery-distribution-and-release
plan: 05
type: execute
wave: 2
depends_on: [05-04]
files_modified:
  - src/platform/sqlite/repositories/restoreReconciliationRepository.ts
  - src/platform/sqlite/repositories/logicalRestoreRepository.ts
  - src/bootstrap/workoutAppRuntime.tsx
  - src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts
  - src/platform/sqlite/repositories/historyProjectionRepository.ts
  - tests/sqlite-host/restoreReconciliationRepository.test.ts
  - tests/sqlite-host/cleanInstallRestore.test.ts
  - app/more/data-and-recovery.tsx
autonomous: true
requirements: [DATA-05, DATA-06]
must_haves:
  truths:
    - Bundled references reconcile against locally approved catalog data without importing/replacing bundled sources.
    - After committed restore, FTS and all history/progress/recommendation derivatives rebuild deterministically or remain explicitly pending/retryable.
    - A clean-install restore achieves the same usable source/derivative state without Auto Backup contamination.
  artifacts:
    - { path: src/platform/sqlite/repositories/restoreReconciliationRepository.ts, provides: local catalog-reference reconciliation and pending rebuild coordinator }
    - { path: tests/sqlite-host/cleanInstallRestore.test.ts, provides: source and rebuild parity fixture }
  key_links:
    - { from: src/platform/sqlite/repositories/restoreReconciliationRepository.ts, to: src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts, via: post-commit FTS rebuild, pattern: rebuildSearchIndex }
    - { from: src/platform/sqlite/repositories/restoreReconciliationRepository.ts, to: src/platform/sqlite/repositories/historyProjectionRepository.ts, via: post-commit projection rebuild, pattern: rebuildAll }
---

<objective>
Complete restoration by reconciling local catalog references and rebuilding all derivatives deterministically after source facts commit.

Purpose: ensure a portable snapshot becomes a usable local app state without smuggling bundled content or presenting stale search/progress results.
Output: restore recovery coordinator, startup retry integration, clean-install fixture, and rebuild/reconciliation proof.
</objective>

<tasks>
<task type="tracer" tdd="true">
<name>Task 1: Reconcile catalog references and coordinate retryable rebuild state</name>
<read_first>src/platform/sqlite/repositories/exerciseSearchIndexRepository.ts, src/platform/sqlite/repositories/historyProjectionRepository.ts, src/platform/sqlite/repositories/contentRepository.ts, src/platform/sqlite/migrations/0016_portability_restore_state.ts, 05-CONTEXT.md</read_first>
<files>src/platform/sqlite/repositories/restoreReconciliationRepository.ts, src/platform/sqlite/repositories/logicalRestoreRepository.ts, tests/sqlite-host/restoreReconciliationRepository.test.ts</files>
<action>Begin with fixtures covering current, revised, and unavailable bundled references plus custom/copied identities. Implement local-only reconciliation that never inserts archive-bundled rows and leaves history/source references traceable when local content is unavailable. Read `rebuild_pending` after a committed source replacement, invoke existing FTS and history/projection rebuild APIs sequentially outside the replacement writer, perform freshness/parity checks, then atomically mark `ready`. On rebuild error/interruption retain pending state and return a safe retryable result. Do not nest `kernel.write()` or promise one cross-repository transaction.</action>
<acceptance_criteria>Catalog changes cannot overwrite user facts, unavailable references remain safe/readable, and post-commit rebuild error leaves a deterministic pending state instead of stale derivative success.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/restoreReconciliationRepository.test.ts</automated></verify>
</task>

<task type="auto" tdd="true">
<name>Task 2: Integrate startup retry and clean-install restore/rebuild parity</name>
<read_first>src/bootstrap/workoutAppRuntime.tsx, src/platform/sqlite/repositories/logicalRestoreRepository.ts, tests/sqlite-host/historyProjectionRepository.test.ts, app.config.ts</read_first>
<files>src/bootstrap/workoutAppRuntime.tsx, src/platform/sqlite/repositories/restoreReconciliationRepository.ts, tests/sqlite-host/cleanInstallRestore.test.ts, app/more/data-and-recovery.tsx</files>
<action>Wire the coordinator into runtime initialization after migrations/catalog installation and into successful restore handling. Gate derivative-dependent success presentation while pending/rebuild retry is active; add recovery UI wording from the UI contract. Build a clean-database fixture that imports a valid archive, runs local catalog installation/reconciliation/rebuild, then compares canonical user facts, FTS parity, record/comparable-exposure/metric/period/recommendation projections, and availability states with the original fixture. Include an Auto Backup/D2D-contamination control in test configuration/evidence contract rather than relying on existing device data. Prove retry after an injected rebuild failure reaches the same final state as an uninterrupted clean restore.</action>
<acceptance_criteria>A clean installation can restore a valid archive to equivalent user-owned facts and usable rebuilt derivatives; startup/retry never silently treats a pending rebuild as current.</acceptance_criteria>
<verify><automated>npm run test:sqlite:host -- --runInBand tests/sqlite-host/restoreReconciliationRepository.test.ts tests/sqlite-host/cleanInstallRestore.test.ts</automated></verify>
</task>
</tasks>

<verification>Run all portability host fixtures, migration/history/FTS parity suites, typecheck, lint/boundaries, and integrity coverage. Validate no product code depends on a backup of the raw database file.</verification>
<success_criteria>DATA-06 is met with local bundled-reference reconciliation and deterministic, observable derivative recovery from a clean installation.</success_criteria>
